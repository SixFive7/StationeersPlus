# KeypadMod Fix Playtests

Implemented changes awaiting in-game confirmation. An agent records a playtest here, not in `TODO.md`, when it has changed code whose behavior can only be confirmed by running the game: single-player, a hosted multiplayer session, or the dedicated server under `DedicatedServer/`. This keeps `TODO.md` a list of work still to do, and gives one place to check whether a change already has a pending test before adding another.

Rules:
- Add an entry when code is implemented but its in-game behavior is unconfirmed. Write down everything a tester needs: what changed (commit if there is one), single-player vs multiplayer / dedicated-server, the save or world to set up, the exact in-game steps, what to watch (InspectorPlus request files, specific log lines, on-screen behavior, IC10 reads), and the expected result. Point at any staged `.work/<date>-<slug>/` request files or playbook.
- Check first. Before adding, scan the entries below so a change already covered by a pending test is not duplicated; extend the existing entry instead.
- Remove an entry when one of these happens: a run confirms it works; a run shows it broken (then add a fresh `TODO.md` item for the fix, or keep working on it now); or the player says the playtest is done. Entries are plain bullets, not checkboxes: like `TODO.md`, finished items are removed, not ticked off. Outcomes live in git history.

Load + patch verification PASSED on the dedicated server (game 0.2.6228.27061, 2026-05-31): deployed alongside the full mod set, KeypadModFix logged `KeypadMod Fix active: 2 of 2 patch(es) applied to keypadmod.Keypad.`, KeypadMod's keypad prefab registered (`patch Keypad: StructureKeypad`, `StructureKeypad added to WorldManager`), and the whole stack loaded with zero `MissingMethodException` and zero errors. What remains is the in-client behavioral confirmation below; a dedicated server has no player to operate its own keypad, so these need someone at a game client.

Server-side vs client-side, which matters for where the mod must be installed:
- Fix 1 (keypress crash) runs on the simulation owner. In single-player that is the host; on a dedicated server it is the SERVER. So the machine running the simulation needs KeypadModFix. A multiplayer client pressing the button does not need it for fix 1.
- Fix 2 (screen input) runs on the CLIENT that types the value (it builds the SetLogicFromClient message). So the CLIENT needs KeypadModFix for fix 2. Without it, the client sends the old message and the value still will not apply.

On load, the BepInEx log shows `KeypadModFix v1.0.0 loaded; patches deferred until all mods are loaded.` then `KeypadMod Fix active: 2 of 2 patch(es) applied to keypadmod.Keypad.`. If it logs `KeypadMod ... is not installed` or `1 of 2`, the deploy or KeypadMod is wrong; stop and report that line.

- **Fix 1 behavioral (keypress crash), single-player or dedicated server.** Build or creative-spawn an `ItemKitKeypad`, place and power it, press several number buttons. Expected: the keypress pulse runs and the log shows NO `MissingMethodException: ... UniTask.Delay`. Before this mod, the first keypress threw and froze the keypad. Single-player: watch the Player.log / BepInEx log. Dedicated server: a client presses the buttons; watch `DedicatedServer/data/server.log` (the SERVER runs PulseMode). Optional: after a keypress, copy `.work/2026-05-31-keypadmodfix/inspector-requests/screen_after.json` into the running machine's `BepInEx\inspector\requests\` and confirm in the snapshot that `keypadmod.Keypad._interactionLocked` settles back to false.

- **Fix 2 behavioral (screen input never applied), dedicated-server client.** This is the bug a dedicated-server player reported ("shows 0, can't set a value"). The CLIENT must have KeypadModFix installed for this test (the fix runs client-side). With the server up, a client (running KeypadModFix) opens the keypad screen, types a number, submits. Expected: the value applies and the screen shows the new number. Before the fix, a client submit did nothing because the message went out with no `LogicType` and the server dropped it. Verify with InspectorPlus on the SERVER: drop `.work/2026-05-31-keypadmodfix/inspector-requests/screen_before.json` into the server's `install\BepInEx\inspector\requests\` before the client submits, then `screen_after.json` right after; confirm `keypadmod.Keypad` `Setting` changed from its old value to the typed number. Single-player / host was never affected (it sets `Setting` directly).
