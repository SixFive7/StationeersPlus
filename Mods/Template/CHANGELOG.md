# Changelog

Full version history for {{Mod Display Name}}. The newest entry also appears in `About.xml` `<ChangeLog>` and as the latest note on the Steam Workshop Change Notes tab. Newest version on top: every release prepends a new `## v<X.Y.Z>` entry and never removes a shipped one. Each entry uses the shared changelog format (see `Mods/Template/LAYOUT.md`): a `v<X.Y.Z>: summary` heading plus `- ` bullets that each end in a period, with wording identical to the `About.xml` `<ChangeLog>` body.

## v0.1.0: Initial release
- Describe the first public release here as one or more complete statements, each ending with a period.
