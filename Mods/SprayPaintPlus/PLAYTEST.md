# Spray Paint Plus Playtests

Implemented changes awaiting in-game confirmation. An agent records a playtest here, not in `TODO.md`, when it has changed code whose behavior can only be confirmed by running the game: single-player, a hosted multiplayer session, or the dedicated server under `TestRig/DedicatedServer/`. This keeps `TODO.md` a list of work still to do, and gives one place to check whether a change already has a pending test before adding another.

Rules:
- Add an entry when code is implemented but its in-game behavior is unconfirmed. Write down everything a tester needs: what changed (commit if there is one), single-player vs multiplayer / dedicated-server, the save or world to set up, the exact in-game steps, what to watch (InspectorPlus request files, specific log lines, on-screen behavior, IC10 reads), and the expected result. Point at any staged `.work/<date>-<slug>/` request files or playbook.
- Check first. Before adding, scan the entries below so a change already covered by a pending test is not duplicated; extend the existing entry instead.
- Remove an entry when one of these happens: a run confirms it works; a run shows it broken (then add a fresh `TODO.md` item for the fix, or keep working on it now); or the player says the playtest is done. Entries are plain bullets, not checkboxes: like `TODO.md`, finished items are removed, not ticked off. Outcomes live in git history.

The v1.11.0 paired-settings work is verified headlessly as far as it can be: 104 assertions passed on the dedicated server covering the merge truth table, the cycling ladder in all nine client-by-server combinations, authority resolution in all three network roles, paint-family grouping across the full 16x16 matrix, and the effective-settings log line. Re-run any time with `Scenario = spp-settings-merge-verify` in `install/BepInEx/config/net.scenariorunner.cfg`. The entitlement half re-runs with `spp-dlc-gate-verify`.

Four agent-driven end-to-end runs have happened, all recorded in `.work/2026-07-27-spraypaintplus-settings-split/TEST-RESULTS.md`. The 2026-07-27 run cleared Session 1 entirely and most of Sessions 2 and 3. The 2026-07-28 run cleared most of Session E and the reachable half of Session D against the post-removal build. The 2026-07-29 runs cleared Sessions D and C outright after fixing the two tooling blockers that had stopped the previous run, then closed Session E's last two checks. Read that file's "Notes for a future session" before the next run; the tooling notes in it are the difference between a productive session and a wasted one.

## The 2026-08-12 harness run: seven of eight green

The eight automated checks stood at **7 passed, 1 failed, 0 inconclusive** against game 0.2.6420.27780. Full narrative, per-check detail and every measurement: `.work/2026-08-11-spraypaintplus-playtests/CAMPAIGN-LOG.md`; evidence bundle: `.work/2026-08-12-playtest-spraypaintplus/`.

That run was made with the PowerShell harness. The checks are now C# at `Mods/SprayPaintPlus/playtests/`, compiled into `TestRig/testrig.exe` and run with `testrig playtest` (`--only "<name pattern>"` selects; check names are the `CheckSpec` names, not file names). It has since been reproduced and completed through the ported engine on 0.2.6428.27798; see "Notes for the next run" below.

The 2026-08-11 result (0 passed, 2 failed, 6 inconclusive) was **not** evidence against the mod. Every one of those outcomes traced to the harness or to how it read the world, and the three most expensive are worth stating once so nobody re-derives them:

- **`Thing.CustomColor` cannot detect paint.** It is a `ColorSwatch` reference whose rendering is its own type name, so `/thing` reports `matchesPrefab: true` and `isNull: false` on every object in the world, painted or not. Three checks read it and concluded nothing had been painted when every stroke had in fact landed. The readable signal is the row-level `customColorIndex`.
- **A cable spawns at `customColorIndex` 4, which is exactly what `ItemSprayCanRed` applies.** Before and after were identical on a working stroke and on a stroke that never happened. The checks now spawn ColorGray (1) and spray red (4). That also explains the old `baseline-not-matte`: a same-colour repaint never runs `SetCustomColor`, so `EmissionColor` stayed at its `Color.white` initial value.
- **The four `joiner-not-in-roster` results were not join failures.** A `/connect` issued from inside a check body, after that body disconnected the joiner, had its own copy of the bring-up logic and did not confirm-and-retry. That is now one shared `Connect-RigJoiner`.

The mod itself needed no change for any of it.

**Verified by hand on both game builds, before and after the update**: with `Server - Network Painting / Network Paint Cables` off and the client half on, three strokes produce exactly three console notices, the third ending "No more notices about this one until you rejoin.", and a fourth stroke produces silence. The seed always paints (the prefix on `OnServer.SetCustomColor` is `void` and cannot suppress it) and the network flood is correctly withheld.

## Notes for the next run

`testrig playtest` on 2026-08-16 against game 0.2.6428.27798 stood at 8 passed, 0 failed, 0 inconclusive, host plus joiner. Three things that run settled, all of which cost time to re-derive:

- **`KeyManager.GetMouseDown("Secondary")` is not an input-plumbing gap.** It bottoms out in `Input.GetKeyDown(KeyCode.Mouse1)` and injected input reaches it: one `/input/mouse button=1` on a driven listen host reported `consumed: true`, `observed.getKeyDown: 4`, `gate.normalModeRan: 4`, console closed and cursor gate open. The three-way probe this file used to recommend has nothing left to find.
- **The eyedropper prints on both streams, once per click, and the two carry different prefixes.** The console gets the display name and the BepInEx log gets the code name, so no filter literal works on both: `[Spray Paint Plus] Color cycling is limited to one paint family here: ...` against `[SprayPaintPlus] Color cycling is ...`. A filter that concatenates a prefix onto a mid-sentence fragment matches neither.
- **A stale control plugin in an instance ends every check before the mod is reached.** It is copied at `create` time, and picking up a new plugin build is now `testrig deploy TestRig --target clients --as <id>`, which rewrites `BepInEx/plugins/TestRig/` in place and sweeps any predecessor out of both load paths. `create --force` still works and rebuilds the tree with it, but it is no longer the only route, and after either one the mod under test has to be deployed again.

## Still pending

- **Regression guard, headless (`spp-settings-merge-verify`).** Still not obtained, now after three attempts each blocked by something different. The dedicated server IS now on 0.2.6420.27780 and carries the correct 96,768-byte build, so the groundwork is done; what is left is the run itself.

  What the next attempt needs to know:
  - **Most of what follows describes the pre-merge world and is kept for the parts that are still true.** The probe now lives in the merged `TestRig` plugin, which the dedicated server already runs as `data/mods/Local_TestRig/`, and `spp-settings-merge-verify` is one of its dispatcher scenarios. So the first thing to try is one HTTP call rather than a config file: `testrig call --target server --as <id> --path "/scenario/run?id=spp-settings-merge-verify&ticks=<n>"`, which runs it for N simulation ticks and returns every line it wrote plus a pass / fail / inconclusive verdict, with no log file to pick. `GET /scenarios` lists what is there. Arming through a config value is the fallback, not the route.
  - **`update-game --target server` deletes a server-only dev-plugin** (it mirrors `BepInEx/` from the client install, which has none), and it did exactly that to `ScenarioRunner`. The merged plugin takes the StationeersLaunchPad path under `data/mods/` instead, which that mirror does not touch, but check `testrig status` for staleness after any `update-game` and re-deploy if it went.
  - **If you do fall back to arming by config**, the setting is `Scenario` in `install/BepInEx/config/net.sixfive7.testrig.cfg` (the predecessor's `net.scenariorunner.cfg` is still recognised). That file does not exist after a fresh install until the plugin has run once, so it has to be written by hand, and **the session state reset blanks that value at every session boundary**, so arm it after taking the lock and before starting the server. Prove it armed before trusting the run, and note that `[ScenarioRunner]` output lines land in `install/BepInEx/LogOutput.log`, not in `data/server.log`, which carries Unity output.
  - **Use `--load <SaveName> --map <Map>`, not `--new`.** The 2026-08-11 attempt spent 17 minutes generating a Lunar world and never reached the dispatcher. `LunaSppTest` and other saves are in `TestRig/DedicatedServer/data/saves/`; stage anything you need to survive BEFORE the session's first mutating command, because a world not in the recorded set is deleted at the session boundary.
  - **The world never ticks with nobody connected.** `GameManager.DelayedStartupPause` parks it about five seconds after start, and measured on 0.2.6428.27798 the tick count stayed at zero for a full 287 s run. A scenario armed at boot then emits one line and nothing else, which looks exactly like a typo in its id. Either connect a client or set `Force Unpause Without Client`.
  - **The 2026-08-12 attempt died on a file-sharing violation**, not on the scenario: `IOException: Sharing violation on path ...\data\setting.xml` followed by `[Global]: An error occurred during loading. Exiting`, after all 62 mods had loaded. Two things held that file at once. Note that `start` reported a healthy pid and `status` afterwards just said `server: stopped`, so an exit during load is indistinguishable from a finished run unless the log is read.

  Check 04 is NOT this guard, and its own header says so: the tally lives in the dedicated server's log and no reader in the harness can reach it.
