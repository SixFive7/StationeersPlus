# Power Grid Plus Playtests

Implemented changes awaiting in-game confirmation. An agent records a playtest here, not in `TODO.md`, when it has changed code whose behavior can only be confirmed by running the game: single-player, a hosted multiplayer session, or the dedicated server under `DedicatedServer/`. This keeps `TODO.md` a list of work still to do, and gives one place to check whether a change already has a pending test before adding another.

Rules:
- Add an entry when code is implemented but its in-game behavior is unconfirmed. Write down everything a tester needs: what changed (commit if there is one), single-player vs multiplayer / dedicated-server, the save or world to set up, the exact in-game steps, what to watch (InspectorPlus request files, specific log lines, on-screen behavior, IC10 reads), and the expected result. Point at any staged `.work/<date>-<slug>/` request files or playbook.
- Check first. Before adding, scan the entries below so a change already covered by a pending test is not duplicated; extend the existing entry instead.
- Remove an entry when one of these happens: a run confirms it works; a run shows it broken (then add a fresh `TODO.md` item for the fix, or keep working on it now); or the player says the playtest is done. Entries are plain bullets, not checkboxes: like `TODO.md`, finished items are removed, not ticked off. Outcomes live in git history.

This mod has never been released (now at 0.2.0 after the full power rework). It compiles and loads cleanly on the dedicated server (all Harmony patches apply, no errors), and the data/logic half of the rework is now headless-verified. What remains below is the irreducible client residue: things a headless server physically cannot show (pixel rendering, Update-driven animation, cursor / settings-panel / dropdown UI, a real second network peer, or an allocator scenario that needs built topology).

## Headless campaign 2026-06-16: what is already verified (do NOT re-test)

Driven by new `pgp-pt-*` ScenarioRunner scenarios against the clean Downloads `Luna.save`. Full log: `.work/2026-06-16-pgp-playtest/results.md`. These passed and are removed from the queue:

- Fault hover line CONTENT + exact hex colors + precedence (CYCLE > VVF > OVERLOAD > SHED): shed `#ffa500`, overload/cycle/vvf `#ff2626`, dead-input `#9aa0a6`, throttle `#d9a441`. (`pgp-pt-hover-all`, 15/15.)
- P4 per-device OVERLOAD clause text on Transformer / Battery / APC / RocketPowerUmbilical Male+Female / PowerTransmitter+Receiver.
- P7 dead-input cue text + a real fault taking precedence over the cue.
- P13 throttle hover text on a saved Setting=0 transformer + stacking below a fault line.
- P9 APC `DischargeSpeed` is cell-only (reads `ApcCellDischargeCache`, not the bundled `SoftSupplyShareCache`).
- P10 `VariableVoltageFault` readable on producers (solar, solid generator) + reads 1 when faulted; NOT readable on a transformer or PowerConnector.
- P12 `HasOnOffState` table: gas / solid / stirling / DynamicGenerator true; solar / wind / large wind / RTG / connector false. Active-source gate: solar always active, buttoned generator active iff ON, empty connector not delivering. OFF-as-reset clears a fault within the sweep after OnOff=false.
- P11 producer-isolation: 108 of 1024 solar isolated (direct-wired), 916 behind transformers do NOT fault (the "transformer clears isolation" rule, across the live base).
- P3 `DeviceOutputSanitizer.Sanitize` clamps NaN / +Inf / -Inf to 0 and passes finite through.
- P6.1 cross-mod: `PowerTransmitterPlusInterop.SourceDrawMultiplier` reads PowerTransmitterPlus's distance multiplier end-to-end (m up to 2.545 on long links); degrades to 1 when PowerTransmitterPlus is absent.
- Custom LogicType reads (Priority / Shedding / Overloaded / CycleFault read 0 baseline and 1 when forced; battery + APC charge/discharge speed reads).
- Flash component attach + the `OrangeFlashColor` (1, 165/255, 0 = #ffa500) / `RedFlashColor` (1,0.15,0.15 = #ff2626) / `FlashHz` 2 constants. (The orange was corrected from a drifted #ff8c00 to the decided #ffa500; the exact on-screen shade is a Group A re-eyeball.)
- Clean-Luna baseline: 207 segmenters, shed=0 overload=0 cycle=0 vvf=108, no false faults, no atomic-tick exceptions, PGP loads with no fatal.

Two bugs the campaign found were FIXED + re-verified headlessly on 2026-06-16:
- **Flash render on Battery / APC / nuclear battery -- FIXED.** `DiscoverRenderers` now falls back (when no on/off-button mesh resolves) to the APC `MasterLever`, the nuclear battery `Indic0NoShadow`, and the stationary battery body; ScenarioRunner `pgp-pt-flash-all` confirms renderers>0 on all three. The on-screen pulse is still a client visual confirm (Group A).
- **Burn-reason hover attach -- FIXED.** `BurnReasonPatches` was missing its class-level `[HarmonyPatch]`, so `PatchAll` silently skipped all three of its patches (the OnRegistered consume, the `Structure.GetPassiveTooltip` "Burned:" line, the Tooltip re-apply). Added it. ScenarioRunner `pgp-pt-burnreason` confirms the load-time tier burn now attaches the reason (`attachedCount=1`, `consumeHit=True`, `GetAttached` returns "Wrong voltage..."). The on-screen wreckage hover is still a client visual confirm (Group B).

Finding still open:
- The historical "Transformer Priority + Shedding 56/56" probe suite is STALE after the v0.2.0 rework: its probes reference the removed `TransformerAllocator`, the renamed flash-color field, the old "Shedding (Priority N)" wording, and the removed `ShedStateMessage`. The current behaviors are re-verified by the `pgp-pt-*` probes; the knob (8/8), labeller (3/3), and save/load-hook (4/4) sub-probes still pass.

## Single-allocator rework 2026-06-18 (overload-first; Legacy allocator + toggle removed)

The allocator is now a single implementation: the old Legacy allocator, the `EnableSweepAllocator` toggle, and `SweepAllocatorSync` are removed. Overload is evaluated BEFORE shed and is sticky (grow-only within a tick, reset only by the 60 s timeout or a player turn-off); only shed is re-decidable. Verified HEADLESS on the dedicated server this session (do NOT re-test):

- Core no-regression: nets 492209 and 503288 report Required == Actual == Potential every tick, `_isPowerMet=T`, `_powerRatio=1`, no oscillation, no atomic-tick exceptions or non-convergence (`pgp-reversed-transformer-probe`).
- 60-second-freeze fix: in a contended fanout (a low-priority non-step-up consumer X plus an overloading sibling Y, tight feeder), X is NEVER shed and Y is OVERLOADED across the whole trace; a grow-only / shed-first allocator would have frozen X here (`pgp-2cycle-freeze` PASS, F=429369, X=180624, Y=420940).
- OFF-as-reset clears every fault registry (shed / overload / cycle / VVF) for a switched-off device (code-verified, `OffAsResetSweep`).

Live residue (a headless server cannot show the pixels or a real second peer):

- **(Group A) Overload-before-shed label.** Throttle a transformer on a contended input net so its downstream out-demands its cap (e.g. transformer 360223 `s d0 Setting 10` while its input net is itself short): it must show the RED OVERLOAD flash + "(Overloaded: ...)" hover and IC10 `l rX d0 Overloaded` = 1, NOT the orange SHED. A transformer that is only input-starved (downstream within its cap) must still show SHED. The registry decision (overload wins, the CYCLE > VVF > OVERLOAD > SHED precedence) is dedi-verified; this is the on-screen label confirm.
- **(Group A) Settings panel no longer shows the allocator toggle.** Open the StationeersLaunchPad mod settings: there is no "Enable Sweep Allocator" entry under `Server - Power`. The allocator is always on.
- **(Group B) 60s-freeze, eyes-on.** Build a fanout: a step-down feeder into net F, F feeding two transformers X and Y, with X (low priority) feeding a real load and Y (high priority) feeding a load bigger than Y's throttled cap so Y overloads. Throttle the feeder so F cannot supply both. Y must flash RED (overload) and X must stay powered (NOT go dark for 60 s). Dedi-proven on a constructed topology; this is the live confirm that the low-priority consumer is not frozen.
- **(Group D) Join handshake after the suffix change.** The join suffix no longer carries the allocator flag (removed with the toggle), so its wire format changed by one field. Re-confirm a client still joins cleanly and host-set transformer priorities plus live fault state replicate correctly (re-run the existing "MP fault sync + join handshake" check against the new build).

## Logic passthrough all-port fix 2026-06-18 (bridge every network a mode-1 device touches)

`PassthroughTopology` now bridges EVERY cable network a mode-1 device connects -- power input, power output, AND a dedicated `Data`-port network (plus a linked dish's far power+data networks) -- not just the opposite power side. (`GetOtherSide` removed; replaced by `IsEnabledBridge` + an all-port `GetBridgeNetworks` that now yields `DataCableNetwork`, consumed by the set-based `GatherReachable`.) This fixes `LogicPassthroughMode 1` on devices whose logic sits on a SEPARATE data port: the rocket transformer (`StructureRocketTransformerSmall`), the large transformer (`StructureTransformer`), the Station / Large battery, and the wireless dishes. The connector dump (`Research/GameClasses/RocketPowerUmbilical.md` + the Transformer/Battery pages) confirms which prefabs have that shape.

Verified HEADLESS this session (do NOT re-test the load/no-regression part): the fixed build loads on the live 60-mod `Luna_pgp_power` save with no errors, passthrough actively bridges transitive components, and every combined-port device still bridges correctly (ScenarioRunner `pgp-passthrough-port-probe`, no crash, no Harmony failure). Now CONFIRMED at runtime (2026-06-19) on the developer-provided Luna rocket save (`Luna_pgp_rocket`): the rocket transformer `StructureRocketTransformerSmall` ref 524504 has a dedicated data port (net 524537) distinct from its power in (524348) / out (524665), and `dataReachableFromPower=True` -- a reader on its data port now reaches both power sides. 16 of 16 separate-data-port devices bridged (`pgp-passthrough-port-probe END ... dataPortBridgedFromPower=16`), and the rocket transformer also reports `modePreForce=1` (defaults transparent). The only remaining check is the client-side UI / IC10 read-across below: the headless probe proves the topology + reachability (GatherReachable now folds in the data-port network), not the on-screen motherboard dropdown / live IC10 `l` read.

Live confirm (single-player or dedi; needs a built topology the headless saves lacked):
- **Rocket transformer (the original report).** In a rocket, place a Transformer Rocket (Small); wire its power in/out into the rocket power run and its Data port onto the rocket data bus an IC10 / the avionics reads. `s d0 LogicPassthroughMode 1`. From an IC10 on the data bus, confirm a device on the transformer's power-output network becomes readable (batch-read a ref on the far power side, or the motherboard dropdown lists it). Pre-fix this returned nothing; post-fix it should resolve. Confirm the reverse too (a reader on a power side sees the data-bus devices).
- **Station Battery variant (no rocket needed, minimal repro).** Place a Station Battery (`StructureBattery`, dedicated Data port); wire power in/out on two networks and run a SEPARATE data cable from its Data port to an IC10 housing's network. `s db LogicPassthroughMode 1`. The IC10 on the battery's data network must now see the power-in and power-out networks' devices (and vice versa).
- Expected: with the fix, a reader on ANY of the device's ports (power in, power out, data) sees devices on all the others. Combined-port devices (small transformer, APC) are unchanged.

Default mode (done 2026-06-19): `StructureRocketTransformerSmall` is now in `PassthroughModeStore.GetDefaultMode`'s transparent list (with the station small transformer + reversed), so the rocket transformer defaults to `LogicPassthroughMode 1` -- transparent out of the box. The confirm steps above no longer need the explicit `s d0 LogicPassthroughMode 1` on a fresh rocket transformer (it is already 1); to also exercise the write path, set it to 0 then back to 1.

## Client residue, grouped to minimize reloads

Setup save: `DedicatedServer/data/saves/Luna_pgp_residue/` (a clean copy of the Downloads `Luna.save`). It already shows, on load with no building: the 108 producer-isolation solar (VVF hover, stopped generating) and six Setting=0 transformers (amber throttle hover, dark subnets, refs 343183 / 343182 / 511092 / 511047 / 511050 / 511091). Load it once in single-player (copy to `Documents/My Games/Stationeers/saves/`) or join the dedicated server with it loaded. Key device refs from the campaign: transformer 360223 (small, loaded ~50 W downstream), PowerTransmitter 121753, solid generator 36369, power connector 36384, vanilla batteries 69371 / 36581 / 36261.

### Group A: one save, no building (single-player, hover + IC10 triggers)
- **Fault flash colors + countdown.** Place an IC10 housing + chip on transformer 360223's data port. `s d0 Setting 10` (below its ~50 W downstream): RED overload flash on the on/off button + hover "(Overloaded: Downstream demand exceeds this transformer's limit! Ns)". Restore `s d0 Setting 5000`. The countdown must tick DOWN smoothly (not in 0.5 s steps) and use the system decimal separator. Hover content and colors are already verified; this is the visible-pulse + smoothness check.
- **Flash visible on each device class (incl. Battery/APC after the fix).** Force a fault on one of each (IC10 Setting throttle on a transformer; for the others the campaign confirmed the registry path) and watch the device. Confirm the orange/red pulse renders on Transformer, PowerTransmitter, PowerReceiver, RocketPowerUmbilicalMale, the gas/solid/stirling generators, AND (post-fix) on the stationary Battery (body), nuclear battery (the Indic indicator), and APC (the MasterLever). Headless confirmed the flash component + a renderer is now present on Battery/APC/nuclear; this is the eyes-on confirm that the pulse is visible. Solar / wind / RTG / umbilical-Female are hover-only (hover the body while faulted).
- **Producer-isolation visuals.** Hover several of the 108 solar: red VVF hover "(Variable Voltage Fault: connected to ... without transformer. Ns)", and they are not generating. A solar behind a transformer (not among the 108) shows no fault.
- **Throttle hover.** Hover the six Setting=0 transformers: amber "(Throttled to 0 W of {max} W ... The dial sets priority.)", subnets dark. Raise one via IC10 `s d0 Setting 50000` and confirm the line and the darkness clear.

### Group B: same save, a few built structures (single-player)
- **Cycle-fault flash.** Build an anti-parallel transformer pair (T1 input N1 / output N2, T2 input N2 / output N1) on a powered grid; also try a transformer+battery+APC ring. Every member flashes RED, no cable burns, downstream goes dark; after 60 s it retries. Detection produced zero false positives on the baseline, so this confirms the flash + no-burn behavior.
- **Battery-overload + two-battery synced recovery (P5).** A battery-fed room with no other supply: drain below the rigid load, expect a RED overload flash and a dark subnet (not partial power), re-firing each 60 s. Then two batteries on one output net, each discharge-capped too low alone but jointly enough: they share and do NOT flash; raise the load above their combined cap so both flash, drop it back, they must recover TOGETHER in one re-arm; with the load still high, toggling one OFF/ON must NOT recover and must not strand the net on a drifting timer. InspectorPlus: `OverloadRegistry` entries on the cohort must share one expiry tick.
- **Dead-input recovery timing (P7).** A transformer (also an APC, and a PT/PR pair) feeding a load with its input cable on a network with NO source: grey "(No upstream supply)" cue, no flash, no countdown; connect a generator to the input and it must start delivering THE SAME tick (no 60 s lockout). Cue content already verified; this is the same-tick recovery + no-pulse check.
- **Producer-rich coverage (P10 / P11 / P12 instance gaps).** The campaign save had only solar + a solid generator + a connector, so VVF logic-read was instance-confirmed on solar and the solid generator only. Build one of each remaining producer (gas generator, stirling engine, small wall turbine, small + large wind turbine) wired straight to a consumer with no transformer, and confirm: each enters VVF; a Logic Reader / IC10 `l rX d0 VariableVoltageFault` returns 1 (solar / wind / turbine / gas / stirling), while the connector and RTG stay hover-only; the gas + stirling generators show the RED button flash (the solid one was confirmed to render, 2 renderers). Then the multi-producer network-retry: put a buttonless producer (solar) and a buttoned one (gas/solid) on the SAME no-transformer net, confirm their VVF countdowns tick down TOGETHER (one synced timer), add a transformer and toggle the buttoned producer OFF, and confirm BOTH clear that tick (the buttonless one has no button of its own). The single-device OFF-as-reset and active-source gate are already verified headless; this confirms the multi-producer cohort firing + the per-instance flash render.
- **Live wrong-tier burn + cursor rejects (P1 / NEW-3).** Build a normal cable bridging into a powered heavy network: the boundary cable bursts to wreckage within a frame or two, the network splits, no cascade on a multi-junction build. Hover the wreckage: "Burned: Wrong voltage ...". Hold a normal-cable coil next to a heavy cable / an RTG: red ghost + the wrong-voltage reject message. Device kits are never cursor-blocked (green ghost; the cable burns next tick). The burn-reason attach is fixed and verified headlessly on the load-time tier burn (the reason now attaches to the wreckage); this is the eyes-on confirm that the wreckage hover renders "Burned:" for a live in-play burn.

### Group C: PowerTransmitter links (single-player; allocator decisions are server-side)
- **P6 input-limited link SHEDS, link-rated link OVERLOADS.** Feed a transmitter's input from a small source whose PotentialLoad is below both the link cap and downstream demand: the dish shows SHED (orange, "(Shedding: Insufficient upstream supply!)"), receiver subnet dark; adding input supply clears it. Make the input rich but keep downstream above the link's rated cap (vanilla 5000 W, or a finite PowerTransmitterPlus Max Transfer Capacity): the pair shows OVERLOAD ("This power link cannot carry the downstream demand!"). Routing logic + both hovers are verified; this confirms the firing.
- **P6.1 PowerTransmitterPlus distance draw (needs PowerTransmitterPlus).** A LONG link (m well above 1) feeding a receiver-side load, with the transmitter's input network sized for `delivered` but NOT `delivered*m` (a small generator + local rigid lights on the input side). The input-side lights must NOT dim: the link SHEDS cleanly instead of browning the source's rigid devices. Shorten the link or set k=0 (m=1) and confirm normal pass-through. The interop read is verified (m up to 2.545); this confirms the no-partial-power firing on the source side.
- **P8 parallel-supplier proportional split.** Two transformers of different capacity at the SAME Priority on one loaded output net: read each one's delivered power via IC10 `l rX d0 PowerActual` (the campaign confirmed PowerActual reflects per-transformer throughput). They share in proportion to capacity, not lowest-RefId-takes-all. Set different priorities: the higher one fills to cap first. Raise the load above combined cap: both trip OVERLOAD together.

### Group D: two peers (host + one connected client)
- **MP fault sync + join handshake.** Trigger a shed/overload on the host; the client sees the same flash + countdown within a tick. Join mid-fault: the joining client shows the correct remaining countdown. Toggle the faulted device OFF from the client: visuals clear on both peers. (In-process serializer/state-machine paths are verified; only the live LaunchPadBooster transport is unconfirmed.)
- **Deterministic merge sort + late-join CableNetworkId.** Two networks of different sizes; client cuts to split then bridges; both peers must show the SAME surviving `CableNetworkId` (lowest of the two). Repeat with the host bridging. Late-join: client reads the same `CableNetworkId` per cable as the host.
- **Logic-passthrough client replication.** Open a wall Computer screen on the output side of an APC / transformer; flip its `LogicPassthroughMode` via IC10 from either peer; the dropdown re-lists on BOTH peers within a tick with no replug. Server toggles under `Server - Passthrough` re-list all motherboards on both peers. Dish relink refreshes both cable sides. (Setup save with refs: copy `Luna_pgp_residue`; the cascade drains off a MonoBehaviour.Update that does not fire on a headless server, so this is client-only.)

### Group E: UI + mod-set variations
- **Settings panel.** Open the StationeersLaunchPad mod settings; confirm the PowerGridPlus groups render and a live toggle (e.g. a passthrough master switch) takes effect.
- **Stationpedia footers.** Open a producer, a transformer, an APC, a battery in the in-game Stationpedia; confirm the producer-isolation / rate-cap / priority footers render as distinct trailing sections.
- **Emergency lights (uninstall the third-party Battery Backup Light mod first).** With Battery Backup Light present, PGP's emergency-light patches stay inactive (logged at load). Remove that mod, load: battery wall lights park off on grid power and come on when the grid fails; per-light Mode 1 opts out; the `Emergency Light Prefabs` list accepts a modded prefab name.
- **Broken-device in-game console (P3).** With a deliberately-broken modded device (NaN power math), confirm the in-game console prints `[Power Grid Plus] Broken device: "<name>" (ref N) ...` once per device and the grid stays sane. The clamp itself is verified; this is the console-naming half.
- **§5.7 generator cable-burn, live sole-trigger.** Build a generator bank exceeding the 100 kW heavy-cable cap on a PURE-heavy network: ~10 s of sustained overload burns one cable at the top generator's output, a brief spike does not, transformer/battery overflow trips RED instead of burning. The deterministic 20-tick window decision is verified 5/5 (`pgp-cable-burn-window-probe`); this is the live integration.
- **Heavy-cable device draw sweep.** InspectorPlus sweep of each `VoltageTier.IsHighDrawMachine` whitelist machine (CarbonSequester, Furnace family, ArcFurnace, Centrifuge, Recycler, IceCrusher, HydraulicPipeBender, DeepMiner) capturing `UsedPower`; drop or extend the whitelist based on which actually exceed 5 kW. Request files: `.work/2026-05-28-pgp-heavy-cable-sweep/`.
