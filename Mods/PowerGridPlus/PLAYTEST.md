# Power Grid Plus Playtests

Implemented changes awaiting in-game confirmation. An agent records a playtest here, not in `TODO.md`, when it has changed code whose behavior can only be confirmed by running the game: single-player, a hosted multiplayer session, or the dedicated server under `DedicatedServer/`. This keeps `TODO.md` a list of work still to do, and gives one place to check whether a change already has a pending test before adding another.

Rules:
- Add an entry when code is implemented but its in-game behavior is unconfirmed. Write down everything a tester needs: what changed (commit if there is one), single-player vs multiplayer / dedicated-server, the save or world to set up, the exact in-game steps, what to watch (InspectorPlus request files, specific log lines, on-screen behavior, IC10 reads), and the expected result. Point at any staged `.work/<date>-<slug>/` request files or playbook.
- Check first. Before adding, scan the entries below so a change already covered by a pending test is not duplicated; extend the existing entry instead.
- Remove an entry when one of these happens: a run confirms it works; a run shows it broken (then add a fresh `TODO.md` item for the fix, or keep working on it now); or the player says the playtest is done. Entries are plain bullets, not checkboxes: like `TODO.md`, finished items are removed, not ticked off. Outcomes live in git history.

This mod has never been released (now at 0.2.0 after the full power rework). It compiles and loads cleanly on the dedicated server (all Harmony patches apply, no errors), but the client-facing half of the rework is unplaytested. The entries below must pass before the first release cut.

- **Full power rework (2026-06-10, v0.2.0): atomic tick on vanilla PowerTick, joint shed/overload allocator over every segmenting device, elastic storage, fault visuals everywhere, full-snapshot MP sync, side-car burn reasons, rocket umbilical, emergency-light list.** See `POWER_DEVIATIONS.md` at the repo root for the deviations and judgment calls of this pass.

  **Already headless-verified on the dedi (Luna save):** clean load, no atomic-tick exceptions across 108+ ticks; zero false shed / overload / cycle faults on the 168-wired-segmenter grid; producer-isolation faults the expected 108 solar panels (4 networks, WallLightBattery violators) -- always-on per the locked decision; the two saved Setting=0 transformers (refs 343183 / 343182) are logged at load and deliver 0 as designed. Synthetic forced-overload scenario (save `Luna_pgp_overload`, transformer 19777 Setting=100 against ~3130 W downstream): per-device hit-max OVERLOAD fired within a tick, enforcement zeroed it, the subnet went dark cleanly (req=3130, pot=0 -- no partial power), probes show `faults=[overload]` on exactly that device.

  **Client items to verify (need a rendering client; headless has no LateUpdate / GetPassiveTooltip):**
  - **Fault visuals on every device class.** Shed -> ORANGE pulsing button; Overload / Cycle Fault / Variable Voltage Fault -> RED. Now attached to Transformer, Battery, APC, PowerTransmitter, PowerReceiver, RocketPowerUmbilicalMale, and the button-bearing producers (coal / gas / stirling). Hover the device BODY or its on/off BUTTON: one fault line with a live two-decimal countdown (e.g. "(Shedding: Insufficient upstream supply! 42.17s)"), highest-precedence fault only (CYCLE > VVF > OVERLOAD > SHED). Solar / wind / RTG / umbilical-Female are hover-only (no button): hover the body while faulted.
  - **Overload hover wording is device-specific (deviation P4).** When a device enters OVERLOAD the hover names that device's own constraint, not a generic transformer line. Confirm each overload-capable class reads correctly: a transformer `Downstream demand exceeds this transformer's limit!`, a stationary battery `...this battery's discharge rate!`, an APC `...this APC's output!`, a rocket umbilical `...this umbilical's discharge rate!`, and a Microwave Power Transmitter / Receiver pair `This power link cannot carry the downstream demand!` (hovering either half shows the link line). Easiest repro is the transformer (throttle its Setting below downstream demand via IC10); the storage cases ride the P5 battery-overload test below. The clause is picked locally from the hovered instance, so a 2-peer check should confirm the client sees the same per-device wording as the host.
  - **Countdown smoothness + locale.** The seconds tick down continuously (not in 0.5 s steps) and use the system decimal separator.
  - **OFF-as-reset on toggleable devices.** Toggle a faulted device that has an on/off button (transformer, battery, APC, PT, the fuel generators) OFF: flash stops and the fault clears within a tick (both peers). ON re-evaluates; a persisting cause re-fires. Buttonless producers and the Power Connector dock have their own behaviour, covered in the deviation P12 entry below.
  - **Producer-isolation visuals.** Coal generator + light, no transformer: coal flashes RED, hover names the violator ("connected to Light without transformer"). Solar + light: solar stops generating, hover-only line. Solar -> transformer -> light: no fault. On the Luna base, the 108 known solar panels show the hover line.
  - **Cycle-fault visuals.** Anti-parallel transformer pair (N1->N2 + N2->N1, powered): both flash RED, no cable burns, downstream dark. Also try a transformer+battery+APC ring; ALL members flash now (flash-attach covers every segmenter class since this pass).
  - **Multiplayer sync (2-peer).** Trigger a shed/overload on the host: the client sees the same flash + countdown within a tick. Join mid-fault: the joining client immediately shows the correct remaining countdown (join handshake). Toggle the faulted device OFF from the client: visuals clear on both peers.
  - **Burn-reason persistence.** Burn a cable (wrong-tier junction is easiest), hover the wreckage ("Burned: ..."), save, reload: the line is still there. Then disable the mod once, load, confirm the world loads clean (vanilla hover, no errors).
  - **Elastic storage feel.** A battery-backed room: batteries should sit idle while generators cover demand (DischargeSpeed 0 via IC10), discharge exactly the shortfall during a deficit, and split charge proportionally when several charge from one surplus. `MaxChargeSpeed` / `MaxDischargeSpeed` / `ChargeSpeed` / `DischargeSpeed` readable on batteries, APCs, and umbilicals (umbilical needs a docked rocket).
  - **Battery-overload edge (deviation P5).** Drain a battery-fed room's battery with no other supply: when it cannot cover the rigid load it should trip a RED overload flash (subnet dark) rather than partial-powering, re-firing each 60 s until supply returns. Confirm this feels acceptable in play.
  - **Two-battery network-synced recovery (deviation P5 re-arm sync, 2026-06-14).** Put two batteries on one output network, each with a discharge-rate cap too low to carry the load alone but together enough (e.g. two 5 kW-discharge batteries under an ~8 kW load). With both running they share the load and do NOT flash (combined supply covers demand). Force the recovery path: raise the load above their combined cap so both flash RED (subnet dark), then drop it back under their combined cap; they must come back on TOGETHER within one re-arm cycle and the room relights, not take turns failing. Then test the network-level retry: while the pair is overloaded, drop the load below their combined capacity and toggle ONE battery OFF then ON; the whole bank must retry together and the room relight THAT moment (not wait out a 60 s timer, not reset the countdown). With the load still above their combined capacity, the same toggle must NOT recover (it eats a fresh synced 60 s) and must not strand the network on a permanently-drifting timer. Mode: single-player covers it; a 2-peer pass confirms the client sees the same flashes and recovery.
  - **Generator cable burn (§5.7), deterministic 20-tick rule (2026-06-13 P2 rework).** The burn is now deterministic (no randomness, no `CableBurnFactor` setting): a cable burns when the 20-tick (10 s) running average of direct generator power on its network exceeds the weakest cable's cap, and the victim is the cable at the output of the top-producing generator over that window. Transformer/battery-driven overflow must still NOT burn the cable (it trips those devices RED via the allocator). The window resets on a burn and the network is marked burn-pending (`SplitPendingRegistry`), so the allocator defers durable shed/overload lockouts on it during the in-flight window. **Headless-verified:** the decision math (grace floor, sustained-overload arms + top-producer ranking, single-spike-averaged-out, reset) passed 5/5 via the ScenarioRunner `pgp-cable-burn-window-probe`, and clean Luna showed zero spurious burns. Client/live checks that remain: build a generator bank exceeding the 100 kW heavy-cable cap on a **pure-heavy** network (the clean sole-trigger case; a generator overflowing a *normal* cable is also a misplaced-device tier violation, and the tier burn preempts it) and confirm: it takes ~10 s of sustained overload to burn (not instantly, not never), the wreckage appears at the top generator's output cable, only one cable burns per cycle (no cascade from a single spike), and a brief spike that subsides within 10 s does not burn.
  - **Emergency lights.** With the third-party Battery Backup Light mod uninstalled: battery wall lights park off on grid power, come on when the grid fails; per-light Mode 1 opts out; the `Emergency Light Prefabs` list accepts a modded prefab name.
  - **Setting=0 legacy transformers.** On Luna: the two dark subnets behind refs 343183 / 343182 are the saved Setting=0 transformers. Either raise their Setting via IC10 (only lever in-game) or confirm they were deliberately off; review deviation P13 if this UX feels wrong.

- **Wrong-tier burn timing + no-cascade (2026-06-13, P1 Option B+C rework).** The tier-burn path was reworked: detection runs on the worker thread (per-tick backstop) plus a main-thread `CableNetwork.OnNetworkChanged` subscription (immediate), the burn executes on the main thread so the split lands before the next tick, and a state-based `SplitPendingRegistry` (not a fixed cooldown) prevents re-burning until the split lands. Headless-verified on the dedi (save `Luna_pgp_tierburn`, kept under `DedicatedServer/data/saves/`: cable 360235 on network 429366 flipped heavy->normal): exactly one main-thread burn, picked the normal cable, the split resolved the mismatch, no re-burn, fault registry stayed `shed=0 overload=0 cycle=0`, no exceptions; clean-Luna baseline unchanged. Client checks that still need a rendering client:
  - (a) **Immediate burn on build.** In a live game, build a normal cable bridging into a powered heavy network (or vice versa). The boundary cable should burst to wreckage within a frame or two of placement, not after a visible powered pause. The network visibly splits.
  - (b) **No cascade / no double-burn.** Build a junction with several wrong-tier boundaries at once. They resolve about one burn per tick with no runaway cascade and no repeated bursts on the same cable (the pending gate).
  - (c) **Wreckage hover** still reads "Burned: Wrong voltage -- ...".
  - (d) **Multiplayer:** both peers converge on the same post-split `CableNetworkId` (vanilla authoritative split); the host's burn replicates.
  - (e) **Performance under heavy build churn.** `OnNetworkChanged` fires on every cable/device membership change and coalesces a re-check that scans all networks. Rapidly build and deconstruct large cable runs (or paste a big blueprint) and watch for any frame-time hitch or log spam from the re-check on a large base; it should stay imperceptible (cached-tier reads over a few hundred networks, at most once per frame).
  - (f) **Pending gate does not stick.** After a tier burn, the network must become re-checkable once its split lands (the headless test showed exactly one burn then a clean network). In live play with ongoing building near a recently-burned junction, confirm no network gets stuck permanently undecided (no device on it left without power-distribution updates after the split settles).
  - Mode: single-player covers (a)-(c), (e), (f); (d) needs host + one client.

- **Broken-device in-game alert + NaN sanitization (2026-06-13 P3 rework).** A device reporting NaN / Infinity power is clamped to 0: the known classes (producers, Battery, AreaPowerControl, rocket umbilical) at their source `GetGeneratedPower` / `GetUsedPower` postfix, so the network stays powered; an unknown / modded class is caught by the per-network `CalculateState` guard, which zeroes that network's sums for one tick and scans the network to name the culprit. Every occurrence goes to the BepInEx file log (`LogOutput.log`); each broken device is named ONCE per world session in the in-game console (`ConsoleWindow.PrintError`). Headless-verified: clean Luna sanitizes nothing and is unchanged (no NaN sources, no regression). Client/live check (needs a deliberately-broken device -- a modded device with NaN power math, or a synthetic one): confirm the in-game console shows `[Power Grid Plus] Broken device: "<name>" (ref N) ...` once for that device, the file log records every occurrence, and the grid stays sane (no NaN cascade into battery charge or power ratios). If you want the in-game console to fire literally every tick instead of once-per-device, that is a one-line change. Mode: single-player (the host sees the console error); on a dedicated server it lands in the server console/log.

- **PowerTransmitterPlus distance-draw modelling (cross-mod, deviation P6.1, 2026-06-14).** Needs BOTH PowerGridPlus and PowerTransmitterPlus loaded. Build a microwave Power Transmitter / Receiver link with a LONG distance, so PowerTransmitterPlus's source-draw multiplier m = 1 + k*km is well above 1 (default k = 5, so ~1 km already roughly 6x the source draw), feeding a receiver-side load. Size the TRANSMITTER's input network for the delivered amount but NOT for delivered*m: a small generator plus some local rigid lights on the input side. Expected with the fix: when the input network cannot afford delivered*m, the link SHEDS cleanly (the receiver subnet goes dark; the transmitter's input-side rigid lights stay FULLY powered) instead of browning the source network's rigid devices to a fraction. The key check: the input-side local lights must NOT dim. Then shorten the link (or set k = 0) so m = 1 and confirm normal pass-through returns. Also confirm a long link cannot silently overdraw its INPUT cable (build it on a normal input cable, watch the input-cable protection apply against delivered*m). Regression: with PowerTransmitterPlus NOT loaded, microwave links behave exactly as before (m = 1). Mode: single-player (allocator decisions are server-side). InspectorPlus: snapshot the PT input network's PotentialLoad / RequiredLoad and the pair's shed state before and after raising the load past the input budget; the input net must not show RequiredLoad > PotentialLoad with rigid devices left partially powered.

- **Dead-input "no upstream supply" cue (deviation P7, 2026-06-14).** Build a transformer (also test an APC and a microwave Power Transmitter / Receiver pair) feeding a small load on its OUTPUT side, with its INPUT cable on a network that has NO power source (no generator, no battery, no upstream transformer). The downstream load stays dark and the contributor idles. Hover the contributor's body AND its on/off button: a steady grey `(No upstream supply)` line with NO `{n}s` countdown and NO orange/red flash (it must not pulse at all). Then connect a generator to the input network: the contributor must start delivering THE SAME tick (no 60 s lockout to wait out) and the cue must vanish. A real fault on the same device (overload / cycle / shed) must take precedence over the cue (you see the fault line + flash, not the grey cue). Multiplayer: a connected client hovering the dead-input contributor sees the same grey cue (synced via the `KindDeadInput` snapshot) and it clears on both peers when the input is powered. Confirm batteries/umbilicals (pure suppliers) never show the cue. Mode: single-player covers it; one 2-peer pass for the client sync.

- **Parallel-supplier load split (deviation P8, 2026-06-14).** Put two transformers of DIFFERENT capacity (e.g. a large and a small) at the SAME Priority on one output network, feeding a load below their combined cap. Read each transformer's delivered power (the Power Actual IC10 value, or InspectorPlus on the seg throughputs): they must share the load in PROPORTION to capacity (the large one carries proportionally more), not the lower-RefId one carrying it all. Then set them to DIFFERENT priorities: the higher-priority one must carry the load to its cap first, the lower-priority one only picking up the remainder (primary/backup). Raise the load above their combined cap: both must trip OVERLOAD together (a fully-engaged tier overloads all-or-nothing). Multiplayer: both peers must compute the same split (the per-supplier delivered values match on host and client). Mode: single-player covers the split; one 2-peer pass for determinism.

- **APC DischargeSpeed is cell-only (deviation P9, 2026-06-14).** On an APC that is BOTH passing power through (input -> output with a downstream load) AND discharging its cell to cover a shortfall, read `DischargeSpeed` via IC10: it must report ONLY the cell's discharge rate (matching what a battery's `DischargeSpeed` shows for the same drain), NOT passthrough + cell. Cross-checks: `DischargeSpeed <= MaxDischargeSpeed` (the cell cap) at all times, and when the cell is idle (downstream covered by passthrough or generators alone) `DischargeSpeed` reads 0 even though power is flowing THROUGH the APC. Battery and umbilical `DischargeSpeed` are unchanged (already cell-only). Mode: single-player.

- **VVF logic exposure on all producers (deviation P10, 2026-06-14).** Wire each producer type directly to a rigid consumer (a light) with NO transformer between so it enters Variable Voltage Fault, and read `VariableVoltageFault` via a Logic Reader / IC10: it must return 1 while faulted and 0 once a transformer is inserted. Cover the newly-exposed producers, solar panel, wind turbine (small + large), and the small wall turbine, in addition to the gas / solid-fuel / stirling generators that already exposed it. Negative checks: a NON-producer device (a light, a transformer, a battery) must still NOT read `VariableVoltageFault` (the instance filter must not leak it onto shared base methods), and `PowerConnector` and `RadioscopicThermalGenerator` (RTG) are intentionally not exposed (no logic surface, though both still enter VVF and show it hover-only / via zeroed output). Multiplayer: the read matches on host and client (synced via the KindVariableVoltage snapshot). Mode: single-player covers it.

- **Portable generator / connector / small turbine are producers (deviation P11, 2026-06-14).** Confirm producer-isolation covers the newly-classified producers. Plug a portable generator into a power connector and wire it straight to a machine (a light, a furnace) with NO transformer: the connector must enter Variable Voltage Fault, stop generating, and show the fault hover (buttonless, so hover-only); the machine goes dark. Insert a transformer between the connector and the machine: the fault clears and power flows. Repeat for the small wall turbine wired straight to a consumer. Negative: an EMPTY power connector (nothing docked, generating 0) must NOT fault. Also open the power connector / small turbine / a fuel generator in the in-game Stationpedia and confirm the new POWER GRID PLUS producer-isolation footer appears (auto-attached to every producer prefab). Mode: single-player.

- **Producer-isolation OFF-as-reset: active-source gate + network-level retry (deviation P12, 2026-06-15).** The producer-isolation detector now faults only ACTIVE producers (a buttoned producer while ON, a Power Connector while its docked generator delivers, a buttonless producer always), and VVF is committed at the NETWORK level with retry-before-reset on one shared synced timer (mirrors the elastic-overload commit §8.4.1). Things to confirm in-game:
  - **Buttonless producer VVF countdown no longer freezes.** Wire a buttonless producer (solar panel, wind turbine, the small wall turbine) straight to a rigid consumer with NO transformer so it enters Variable Voltage Fault. Hover it: the countdown must COUNT DOWN from ~60 s (then re-fire on expiry), NOT sit frozen at ~60.00 s.
  - **Buttoned producer OFF-as-reset works.** A gas / solid / stirling generator wired straight to a machine (no transformer) → VVF, counting down. Switch the generator OFF: its VVF clears within a tick and STAYS clear (before this rework the detector re-noted it by class, so it re-faulted immediately). Switch it back ON with the wiring still wrong → it re-faults.
  - **Empty / off Power Connector does not fault (deviation P11 expectation, now enforced).** A power connector with NOTHING docked, on a network with a rigid consumer and no transformer, must NOT show VVF (it delivers nothing, so it is not a producer). Dock a generator and switch it ON → the connector enters VVF (counting down), the machine is dark. Switch the generator OFF, or pull it off the dock → the connector's fault clears within a tick (it must NOT linger ~60 s).
  - **Network-level retry via a buttoned producer (the core of part 3).** Put a buttonless producer (solar) AND a buttoned producer (solid generator) on the SAME network with a rigid consumer and no transformer → both VVF, and the two countdowns must tick DOWN TOGETHER (one shared synced timer, not two drifting ones). Add a transformer to fix the wiring, then toggle the solid generator OFF: BOTH the generator and the solar clear within a tick (the toggle retries the whole producer cohort, so the buttonless solar clears even though it has no button of its own). Repeat WITHOUT adding the transformer: toggling the generator off must re-fault the whole cohort on a fresh synced 60 s ("resolve, or immediately fault again"). With no toggle at all, a fix clears at the 60 s expiry, there is no instant auto-recovery, by design (strict overload-pattern).
  - **InspectorPlus probe (confirms the inferred OnOff table).** Request staged at `.work/2026-06-15-pgp-producer-onoff/onoff-probe.json` (gitignored; regenerate from the `types`/`fields` listed there). With one of each producer placed plus a generator docked on a connector, drop it into `<StationeersInstall>/BepInEx/inspector/requests/` and read the snapshot: `GasFuelGenerator`, `SolidFuelGenerator`, `StirlingEngine`, and `DynamicGenerator` must report `HasOnOffState = true`; `SolarPanel`, `WindTurbineGenerator`, `LargeWindTurbineGenerator`, `TurbineGenerator`, `RadioscopicThermalGenerator`, and `PowerConnector` must report `false`. This confirms the decompile-inferred table (only the gas generator was decompile-proven). The connector active-gate itself reads the docked generator's `PowerGenerated`, so it does not depend on this table; the probe is the cross-check.
  - Mode: single-player covers all; one 2-peer pass to confirm the client sees the same counting-down / synced VVF and the same clears (KindVariableVoltage snapshot). Refs: `Mods/PowerGridPlus/PowerGridPlus/OffAsResetSweep.cs`, `VariableVoltageFaultDetector.cs`, `ProducerClassifier.cs`, POWER.md §8.5 + §10.3, `Research/GameSystems/PowerProducerOnOffState.md`.

- **Transformer throttle hover warning (deviation P13, 2026-06-15).** A transformer with `Setting != OutputMaximum` (a custom IC10 throttle, or a legacy / vanilla save dialed below max) shows a neutral amber info line on BOTH the case hover and the on/off button hover: `Throttled to {set} W of {max} W by a custom IC10 "Setting" value. The dial sets priority.` Checks:
  - **Default build shows nothing.** Place a fresh transformer; hover the case and the on/off button, NO throttle line (it defaults to `Setting = OutputMaximum`). Turn the in-world dial, it changes Priority, not Setting, so the line must NOT appear.
  - **Throttled transformer shows the line on both surfaces.** Via IC10 `s d0 Setting 2000` on a 5000 W transformer. Hover the case: the amber line appears with the live values ("Throttled to 2000 W of 5000 W ..."). Hover the on/off button: the same line appends to the button's contextual name (e.g. "Turn Off (Throttled to 2000 W ...)"). Set it back to 5000 (or OutputMaximum): the line disappears.
  - **Setting=0 case (the original P13 trigger).** Load the Luna save; the two Setting=0 transformers (343183, 343182) show "Throttled to 0 W of {max} W ..." and their subnets are dark. Raise Setting via IC10 (or deconstruct + rebuild, which defaults to OutputMaximum): the darkness and the line both clear.
  - **Stacks below a fault.** Throttle a transformer below its downstream demand so it OVERLOADS: the hover must show BOTH the red Overloaded line AND the amber throttle line (the throttle explains the overload).
  - **Live values track.** Change Setting via IC10 while hovering; the {set} value in the line follows the new Setting (the hover re-polls each frame).
  - Mode: single-player covers it (hover text is client-rendered). One 2-peer pass confirms the client sees the same line (Setting is synced vanilla state). Refs: `Mods/PowerGridPlus/PowerGridPlus/TransformerThrottleHover.cs`, `Patches/FaultHoverPatches.cs`, `Patches/TransformerHoverErrorPatches.cs`, POWER.md §5.3.

- **PT/PR input-limited link SHEDS instead of OVERLOADS (deviation P6, 2026-06-15).** A microwave Power Transmitter / Receiver link whose deliverable is held down by its INPUT supply (not its own rated cap) now SHEDS ("insufficient upstream supply") rather than OVERLOADS ("this link cannot carry the demand"). The allocator sets `Seg.InputLimited` when `liveCap >= InputNetwork.PotentialLoad - Eps` and routes the §8.4 shortfall to `BrownoutRegistry` instead of `OverloadRegistry`. Checks (build a transmitter + receiver + a downstream load):
  - **Input-limited -> SHED.** Feed the transmitter's INPUT network from a small source (one solar panel / a low generator) whose `PotentialLoad` is below both the link's rated cap and the downstream demand, with a receiver-side load exceeding what the input can supply. The dish hover must show SHED (orange flash + "(Shedding: Insufficient upstream supply!)"), NOT OVERLOAD; the receiver subnet goes dark cleanly (no partial power). Adding input supply (a generator) clears it.
  - **Link-rated-limited -> OVERLOAD.** Make the input RICH (`PotentialLoad` above the link's rated cap) but keep downstream demand above the link's rated transfer cap. The pair must show OVERLOAD ("This power link cannot carry the downstream demand!"), the link itself is the bottleneck. (Vanilla rated cap is `MaxPowerTransmission` 5000 W.)
  - **PowerTransmitterPlus unlimited -> always SHED.** With PowerTransmitterPlus at its default unlimited `Max Transfer Capacity`, any receiver-side shortfall must read SHED (the link is never the rating bottleneck). Set a finite `Max Transfer Capacity` below the downstream demand with a rich input and it must read OVERLOAD.
  - **Long vanilla link.** A long vanilla (no PowerTransmitterPlus) link with significant distance loss and unmet downstream reads OVERLOAD (the loss is a link limitation); acceptable.
  - Mode: single-player covers the allocator decisions (server-side); one 2-peer pass confirms the client sees the same SHED/OVERLOAD flash + hover. Refs: `Mods/PowerGridPlus/PowerGridPlus/PowerAllocator.cs` (the `InputLimited` flag + the §8.4 hit-max pass), POWER.md §8.4 / §8.4.2.

- **Transformer Priority + Shedding -- comprehensive headless verification (third session, 2026-06-03). One real PGP bug was found and fixed by this round.**

  **Total headless coverage: 56 / 56 PASS, 0 FAIL** across the original PSP probe (23 phases, prior session), the NBP per-network breakdown (53 networks logged), and 7 new probes added this session (33 phases). The new probes exercise the surfaces that the original PSP could not reach: knob-step computation, BrownoutFlashBehaviour attach, hover error message, labeller redirect, multiplayer-sync code paths, save/load Harmony attachment, and global allocator topology cases.

  **Real bug discovered and fixed by these tests: `TransformerHoverErrorPatches` was missing a class-level `[HarmonyPatch]` attribute, AND was targeting `Thing.GetPassiveTooltip` instead of the actually-overriding `ElectricalInputOutput.GetPassiveTooltip`.** Combined, the Harmony postfix never attached to anything, so the shed hover error was completely invisible in-game. Headless probe HP P0b confirmed `PGP patch attached on EIO.GetPassiveTooltip=False` before the fix; after switching to `[HarmonyPatch(typeof(ElectricalInputOutput), nameof(Thing.GetPassiveTooltip))]` at the class level plus `[HarmonyPostfix]` on the method, HP P0b reports `True` and HP P3 PASSes with the expected `Shedding (Priority N): insufficient upstream supply` text appearing in `PassiveTooltip.Extended`.

  **New scenario catalogue (all gated behind the `pgp-priority-shedding-all` aggregator):**
  - `pgp-priority-shedding-knob-probe` (C-a + C-b) -- 8/8 PASS. NeedleFullScale=200, step constants 1/10, and the screwdriver Button1/Button2 invocation path: Button2 no-Alt +10, Button2 Alt +1, Button1 no-Alt -10, Button1 Alt -1, clamping at 0 from priority 5, and `doAction=false` no-write. Exercises the real `TransformerInteractWithPatches.InteractWithPatch` static method directly with `SetKnobMethod` and `HandleButtonSettingMethod` nulled out so the visual update is skipped on the worker thread.
  - `pgp-priority-shedding-flash-probe` (C-c) -- 4/4 PASS. `FlashColor = (1.00, 0.55, 0.00)` (orange band), `FlashHz = 2`, `BrownoutFlashBehaviour` attached to all 107 transformers on Luna, every transformer's `_renderers[]` is non-empty (DiscoverRenderers found at least one on/off button MeshRenderer per prefab variant).
  - `pgp-priority-shedding-hover-probe` (C-d) -- 5/5 PASS after the bug fix. Diagnostic P0b confirms patch attached on EIO.GetPassiveTooltip. Forces a shed via 2x NoteShortfall, calls `Thing.GetPassiveTooltip(null)` via reflection on a Transformer instance, verifies returned `Extended` contains the expected color + priority + insufficient-supply text. P3b confirms the postfix logic itself appends correctly when invoked directly.
  - `pgp-priority-shedding-labeller-probe` (C-e) -- 3/3 PASS (P4 SKIP, no non-Transformer ISetable in this save). Invokes `TransformerLabellerPatches.Set_Prefix` and `InputSetting_Prefix` with synthetic `(ISetable, ref LogicType)` arguments; verifies Setting -> Priority swap for Transformer and no-op for non-Setting LogicTypes.
  - `pgp-priority-shedding-mp-probe` (C-f) -- 5/5 PASS. Tests `BrownoutRegistry.SetClientShedding` / `ClientIsShedding` state machine, `ShedStateMessage.Process` and `PriorityMessage.Process` host short-circuits via IsServer gate, and a full join-suffix round-trip: seeds 3 synthetic priorities, calls `Plugin.SerializeJoinSuffix` into a 64KB `RocketBinaryWriter`, extracts the buffer via reflection, wraps in a MemoryStream, calls `Plugin.DeserializeJoinSuffix` via a `RocketBinaryReader(Stream)`, verifies all 3 priorities restored byte-identical after a mutation pass. Payload was 545 bytes on Luna's transformer count. Cross-process multiplayer delivery cannot be tested from a single dedi (handler / serializer / state machine ARE verified end-to-end here; only the network-layer transport itself is the remaining unverified piece, and that's the same LaunchPadBooster transport every other PGP message uses).
  - `pgp-priority-shedding-saveload-probe` (C-g) -- 4/4 PASS. Uses `Harmony.GetPatchInfo` to confirm `PowerGridPlus`-owned patches are attached on `Assets.Scripts.Serialization.SaveHelper.Save(DirectoryInfo, string, bool, CancellationToken)`, `Assets.Scripts.Serialization.XmlSaveLoad.LoadWorld`, and `Thing.OnFinishedLoad`. P4 NOTE: `PrioritySideCar.LoadedPriorities` is null on this Luna load because the source save has no `pwrgridplus-priority.xml` yet (the source was the user's vanilla save); on a -Load run with an existing side-car this is a non-null Count > 0.
  - `pgp-priority-shedding-topology-probe` -- 4/4 PASS, **including the 418422/418423 cascade case the user reported in the previous session**. P1: every parallel transformer pair respects priority order. P2: all 59 output networks respect the demand cap (`totalAlloc <= RequiredLoad`). P3: all 15 low-demand transformers allocate within their downstream `RequiredLoad` (no greedy `OutputMaximum` claim). P4: 418422 and 418423 are NOT in lockout under default priorities (`shedding=False` for both, with `alloc=0 req=260 pot=20 OutMax=50000`); the cascade fix (per-output-network demand tracking + global allocator) holds.

  **R-1 PASS verified live on dedi + connected client, 2026-06-04.** Orange flash + hover both behave correctly. Took four rounds of fix to land:
  - Round 1: `BrownoutFlashBehaviour.material.SetColor(_EmissionColor, ...)` -- silent no-op because the Stationeers `StandardInstanced` shader uses per-instance properties.
  - Round 2: switched to `MaterialPropertyBlock` on the renderer -- still wrong colour because the SwitchOnOff state-coloured materials carry a baked `_EmissionMap` that overrides per-property writes.
  - Round 3: hover text was originally on `Thing.GetPassiveTooltip` (transformer-body hover). User correctly pointed out the right place is the on/off button's contextual hover; reworked to postfix `Thing.GetContextualName(Interactable)` and filter for `InteractableType.OnOff` on a shedding Transformer.
  - Round 4: full material swap. `BrownoutFlashBehaviour` now clones the original `sharedMaterial` at first shed (`EnsureFlashMaterials`), strips `_EmissionMap` from the clone, enables `_EMISSION`, and swaps `r.sharedMaterial = orangeClone` on shed-enter; restores the original `sharedMaterial` on shed-exit. Companion `SwitchOnOffShedPatches` Harmony prefix on `Objects.SwitchOnOff.RefreshColorState` returns false when the parent is a shedding Transformer, so vanilla state transitions during shed can't swap the material back. Documented in `Research/GameClasses/SwitchOnOff.md`.

  Also fixed during R-1 verification (unrelated to flash):
  - **Knob axis bug**: `TransformerInteractWithPatches.SetKnobPatch` was rotating on local Z and skipping the `_needleBaseRotation` reset; vanilla rotates on local Y (per the `[Tooltip("Minimum degrees rotation on local Y")]` attribute, verified via decompile L403307-403309 and the actual SetKnob body L403587-403597). Fixed: mirror vanilla exactly -- read `_needleBaseRotation`, set `localRotation = baseRotation`, then `Rotate(0, angle, 0, Space.Self)`. Knob also explicitly called from `Thing.OnFinishedLoad` postfix so it lands at the right rotation on save load. Documented in `Research/GameClasses/Transformer.md` (verification history correction).

  **The two items that still require a live connected client + friend**:
  - **(R-2) Real 2-peer multiplayer cross-process delivery.** Host + one connected client. Host writes a Priority via IC10; client's `l rX d<n> Priority` returns the same value within one tick. Trigger a shed on the host; client sees both the orange flash and the shed hover. Late-join: host first with a save containing per-Transformer priority overrides; fresh client joins, first IC10 read of any non-default-priority transformer returns the host's value. All in-process handlers / serializer paths are verified by `pgp-priority-shedding-mp-probe`; the network transport itself (LaunchPadBooster carrier) is shared with every other PGP message that has been release-tested.
  - **(R-3) Real save/load round-trip via the game's save UI.** Set Priority on three transformers to non-default values via IC10, save via the in-game menu, quit to main menu, reload, confirm IC10 reads return the same values. The headless `pgp-priority-shedding-probe` P10 exercised the `WriteSideCar -> ReadSideCarFromDir` code path; `pgp-priority-shedding-saveload-probe` confirmed the Harmony hooks are attached on `SaveHelper.Save` / `XmlSaveLoad.LoadWorld` / `Thing.OnFinishedLoad`. What remains is just verifying the game's UI save flow exits cleanly and the reload picks up `pwrgridplus-priority.xml`.

  Original PSP probe (still PASS; verified again this session via the aggregator):

  - P1 PASS -- inventory: 44 transformers on Luna, every one reports `PriorityStore.DefaultPriority = 100` (clean baseline; no priority writes leaked from prior sessions). All 5 transformers feeding the contested trunk `InputNet=429366` (potential 80 kW, sum-of-OutputMaximum 90 kW) are visible with their `OutputMaximum`, `Allocated`, `Shedding` fields.
  - P2 PASS -- `SetLogicValue(LogicType.Setting=12, 175)` redirects to Priority: `PriorityStore.GetPriority(360223)` reads 175 after the write. `GetLogicValue(Setting)` returns OutputMaximum (5000) post-write. `GetLogicValue(Ratio=24)` returns 1.0 (hardcoded). `GetLogicValue(Maximum=23)` returns OutputMaximum (5000).
  - P3 PASS -- LogicType surface: `CanLogicRead(Priority=6578)=true`, `CanLogicWrite(Priority=6578)=true`, `CanLogicRead(Shedding=6579)=true`, `CanLogicWrite(Shedding=6579)=false` (read-only).
  - P4 PASS -- strict-priority allocation: with priorities {500, 250, 100, 50, 25} assigned to 5 transformers (OutputMaximums 50/25/5/5/5 kW) on a 80 kW input net, `TransformerAllocator.GetAllocatedSupply` returns exactly {50000, 25000, 5000, 0, 0}. Predicted shed count = 2; matches actual. (Fix landed in this session: `PriorityStore.Version` counter is bumped on every write, and `TransformerAllocator`'s per-network cache includes the version in its key. Without this, intra-tick priority writes hit a stale cache.)
  - P4b PASS -- lockout precedence: `BrownoutRegistry.NoteShortfall` returns false on the first call and true on the second consecutive-tick call (matches ShortfallTolerance=2). A locked-out transformer promoted to priority 9999 still allocates 0 W (the 10-second hold survives priority changes, as specified).
  - P5 PASS -- constants: `BrownoutRegistry.ShortfallTolerance = 2`, `BrownoutRegistry.LockoutDurationTicks = 20` (= 10 s at 2 Hz tick rate).
  - P6 PASS -- master toggle wiring: `Settings.EnableTransformerShedding.Value == ShedSettingsSync.Effective` on the host (both `true`).
  - P7 PASS -- Stationpedia footer present for all 4 transformer prefab names (`StructureTransformer`, `StructureTransformerSmall`, `StructureTransformerSmallReversed`, `StructureTransformerLarge`) and ABSENT for a non-transformer prefab (`ItemWrench` negative case).
  - P8 PASS -- `PriorityMessage.Process(0)` on the host short-circuits via `IsServer` gate; Priority unchanged afterwards.
  - P9 PASS -- `PrioritySideCar.Snapshot()` reflects every PriorityStore write (5 entries after phases P2 + P4).
  - P10 PASS -- in-memory + on-disk side-car round-trip: `WriteSideCar(tempZip, snapshot)` followed by `ReadSideCarFromDir(extractDir)` preserves 5 entries with byte-identical (ReferenceId, Priority) pairs. This is the same code path PrioritySaveLoadPatches drives during a real save / load; verifying it here decouples the test from the batch-mode-stdin no-op on `-Save -Name <X>` (documented in `DedicatedServer/CLAUDE.md`).
  - P11 PASS -- master toggle OFF fallback: flipping `Settings.EnableTransformerShedding.Value = false` flips `ShedSettingsSync.Effective` to false; `GetAllocatedSupply` returns the vanilla `Setting` value (5000 for Luna's small transformers); Stationpedia footer text contains "Behaviour: vanilla". Toggle restored to ON before phase 12 + END.
  - P12 PASS -- `NoteSupplyOk` resets the shortfall counter: a single shortfall followed by a supply-ok followed by another shortfall on a non-consecutive tick does NOT enter lockout (sticky-counter regression check).

  Files changed this session: `Mods/PowerGridPlus/PowerGridPlus/Patches/TransformerHoverErrorPatches.cs` (class-level `[HarmonyPatch(typeof(ElectricalInputOutput), nameof(Thing.GetPassiveTooltip))]` + method-level `[HarmonyPostfix]` -- the bug fix), `DedicatedServer/dev-plugins/ScenarioRunner/ScenarioRunner/Dispatcher.Shedding.cs` (new partial class with 8 scenarios), `DedicatedServer/dev-plugins/ScenarioRunner/ScenarioRunner/Dispatcher.cs` (made partial + 8 new switch cases), `DedicatedServer/dev-plugins/ScenarioRunner/ScenarioRunner/ScenarioRunner.csproj` (added the partial file), `DedicatedServer/dev-plugins/ScenarioRunner/README.md` (catalogue entry for each new scenario), `Research/GameClasses/Interactable.md` (added `Interaction` readonly struct documentation needed to construct synthetic `Interaction` for the knob probe).

- **APC and Battery rate caps + cable-headroom cap -- VERIFIED HEADLESSLY (no pending human checks).** Verification completed 2026-05-28 via `ScenarioRunner` scenario `pgp-rate-cap-probe` against `APC-Luna.save` on the dedicated server. Toggle-ON results: 8/8 wired APCs pass charge-cap + output-cap formulas, 20/20 wired batteries pass charge / discharge / logic-value cap checks (including 2 `StructureBatteryMedium` third-party prefabs correctly returning `Infinity` for the per-prefab default), Stationpedia footer 5/5 positives + 3/3 negatives. Toggle-OFF results (re-ran with `EnableAreaPowerControlFix = false` and `EnableBatteryLimits = false`): 7/8 APCs fail output cap (vanilla `AvailablePower` returned uncapped, confirming the postfix exits early when the master is off), 20/20 batteries fail `usedPower` cap (vanilla full headroom returned, confirming the postfix exits early). Stationpedia footer render correctness was verified by static analysis of the en.resx -> `Localization.GetThingDescription` -> `Localization.ParseHelpText` -> `StationpediaPage.Description` -> `TextMeshProUGUI.text` pipeline (curated under `Research/Patterns/StationpediaDescriptionRendering.md`): vanilla descriptions use literal `\n` paragraph separators with no `<br>` markup, ParseHelpText preserves `\n` and only expands `{TOKEN:...}` macros, our footer contains zero `{TOKEN:...}` patterns so nothing accidentally expands, and TMP's default `paragraphSpacing` matches the vanilla render. The footer renders as a visually distinct trailing section. No in-game eyeball check required.

- **Power simulation baseline (single-player).** A world with batteries, transformers, APCs, and a multi-provider / multi-consumer network. Confirm power distributes, cable burns fire on overload and mixed-tier, voltage-tier enforcement works, burn-reason tooltips appear on wreckage.
  - Mode: single-player.

- **Deterministic merge sort, single-player.** Cut + place a cable in a 50+ cable network. No console errors; the surviving network is the lowest-`ReferenceId` of the two merged (the sort runs unconditionally, so single-player always picks the lower id).
  - Mode: single-player.

- **Deterministic merge sort, multiplayer (the load-bearing test).** Host + one client, two cable networks of different sizes. Client cuts to split, then places a cable bridging them. Both peers' tablet must show the SAME surviving `CableNetworkId`. Repeat with the host bridging. This is the direct verification that the class-B fix lands.
  - Mode: multiplayer, host + one client.
  - Refs: commit 14946c5; `Research/Patterns/MultiplayerStateMutation.md`.

- **Late-join client.** Join a save with cable networks; client reads the same `CableNetworkId` per cable as the host.
  - Mode: multiplayer, late-join.

- **Logic-passthrough device-list refresh + client sync.** Implementation: commits c98842c (PGP cascade engine + `PassthroughModeMessage` / `PassthroughSettingsMessage` / `IJoinSuffixSerializer` join restore + `DishLinkPatches`), 29381ca (shared `MainThreadDispatcher`), 53e4bc1 (PowerTransmitterPlus TODO for the dish back-ref mirror).
  - **Verified 2026-05-28 on dedicated server, save copy of `C:\Users\jori\Downloads\APC-Luna.save` (static-load half only).** BepInEx LogOutput.log shows `Loading [Power Grid Plus 0.1.0]` -> `Power Grid Plus v0.1.0 loaded; patches deferred to prefab load` -> `Power Grid Plus patches applied`, with no `same key already added` duplicate-key fatal on `PassthroughModeMessage` or `PassthroughSettingsMessage` registration. Side-car `pwrgridplus-passthrough.xml` (10 transformer mode overrides, including the Mode=0 outlier ReferenceId 309109) and `pwrxmplus-autoaim.xml` (10 dish links) are present in the save. An InspectorPlus baseline snapshot reached all 43 transformers, 22 ProgrammableChipMotherboards, 16 APCs, 10 PowerTransmitter / 10 PowerReceiver pairs, 2 `PowerTransmitterOmni`, and 5 vanilla `Battery` instances on this save; CableNetwork 357516 (transformer 416366 input side) and 417400 (output side) both report `DataDeviceListDirty: False` (silent rebuild ran). The cascade re-fire and dropdown refresh themselves are NOT verifiable headless: the cascade is drained from `MainThreadDispatcher.Update`, and per `DedicatedServer/CLAUDE.md` "`MonoBehaviour.Update` does not fire after world load" on a headless dedicated server. Every (a)-(f) below needs a rendering game client.
  - **Setup for the rest.** For dedi + client: copy `C:\Users\jori\Downloads\APC-Luna.save` to `DedicatedServer/data/saves/APC-Luna/APC-Luna.save`, then `dedicated-server.ps1 -Start -Load APC-Luna -Map Lunar`, then the developer joins via Direct Connect to `127.0.0.1:28016`. For solo / hosted-MP: copy the same source into `Documents/My Games/Stationeers/saves/APC-Luna/APC-Luna.save` and load it from the main menu. Key devices: Computer ReferenceId 397675 (`StructureComputerBigScreenWallMounted`, the playbook target), Transformer 416366 (`StructureTransformerSmallReversed`, side-car Mode=1), Transformer 309109 (side-car Mode=0 outlier), one auto-aim dish pair such as 121753 (`StructurePowerTransmitter`) -> 289145 (`StructurePowerTransmitterReceiver`) or the bidirectional pair 289143 / 378380.
  - **(a) Dropdown refresh on flip, host side (solo-testable).** Single-player or hosted-MP host. Load the save, open Computer 397675's screen, note the device dropdown contents. Flip Transformer 416366's `LogicPassthroughMode` via tablet or IC10 (`s d<n> LogicPassthroughMode 0`, then back to `1`). The dropdown re-lists without replugging the motherboard. Cross-passthrough devices appear or disappear with the flip. Pre-fix behaviour: the dropdown was stale until the motherboard was removed and reinserted. The developer can do this without the friend.
  - **(b) Client mode replication on flip (2-peer).** Friend connected. Repeat (a). The friend's view of Computer 397675's dropdown must also update live with no replug, within roughly one network tick. Exercises `PassthroughModeMessage.SendAll(0L)`.
  - **(c) Client-originated mode write (2-peer).** Friend connected. The FRIEND flips Transformer 416366 from their tablet. The write is routed through the host, and both peers' dropdowns refresh. Exercises the client -> host write + host rebroadcast round-trip.
  - **(d) Server-setting live toggle (2-peer).** Both peers connected. Host opens the PGP settings panel and toggles one of `Enable Transformer Logic Passthrough` / `Enable Battery Logic Passthrough` / `Enable Apc Logic Passthrough` / `Enable Power Transmitter Logic Passthrough` (all under `Server - Passthrough`). All motherboards on both peers re-list immediately; the client adopts the host's value. Exercises `PassthroughSettingsMessage.SendAll`.
  - **(e) Join-suffix restore on late-join.** Host first, with the APC-Luna save running (its 10 transformer mode overrides plus whatever the four global toggles are set to). A fresh client then joins. The client's first view of each motherboard on a network touching a non-default transformer already shows the merged dropdown without a replug or further round-trip. Exercises `IJoinSuffixSerializer.DeserializeJoinSuffix` for both the per-device mode entries and the four global settings booleans.
  - **(f) Dish-link refresh on link / relink (2-peer).** Both peers connected. Pick a dish pair from the auto-aim side-car (e.g. TX 121753 -> RX 289145, or move TX 286124 so it re-targets a different RX). Force a re-link by toggling auto-aim or repositioning. The motherboard dropdowns on both cable sides should refresh on both peers. On the CLIENT specifically, the RX-cable-side motherboard must see the TX-cable-side devices, which exercises `DishLinkPatches.RefreshLink`'s client back-reference mirror (`rx.LinkedPowerTransmitter = tx`, guarded by `!NetworkManager.IsServer`, idempotent if PowerTransmitterPlus also runs and writes the same back-reference).

- **In-game behaviour test (NEW-1 + NEW-3 simulation half).** Remaining sub-checks for the three-tier voltage feature; all test already-implemented behavior (the NEW-2 multiplier sweep and the NEW-3 device-tier rule are already in `verified.md`).
  - NEW-1: an all-super-heavy-cable network at >>>50 kW throughput doesn't burn cables (off via config -> super-heavy starts burning at its rating). Carve-out at `EnableUnlimitedSuperHeavyCables=true` already verified via ScenarioRunner's `pgp-cable-burn-probe` (see `verified.md`: `TestBurnCable(10000, 10000)` returns null for every super-heavy network). The off-toggle half remains: invert the config, re-run `pgp-cable-burn-probe` (or add a `pgp-superheavy-carveout-probe` scenario that calls `TestBurnCable(2000000, 2000000)` against every network so burnChance=3 always fires), confirm super-heavy networks now return a Cable.
  - NEW-3 cable-tier rule: place a normal cable adjacent to an existing heavy-cable network containing a turned-on generator + load. The boundary normal cable burns on the next tick. Wreckage tooltip: "Burned: Wrong voltage -- normal cable was bridging into a different cable tier". Force the scenario via `tools/save-edit/` (drop a single normal cable adjacent to an active heavy network in a Luna copy), load it, confirm the burn log line + a fresh `CableRuptured` via InspectorPlus.
  - NEW-3 cursor reject (cable-on-cable): hold a normal-cable coil, hover next to an existing heavy cable -> red ghost + "Wrong voltage -- this cable's tier doesn't match the adjacent cable network. Use a transformer." Client UI; needs a developer with the game client (playbook at `.work/2026-05-22-pgp-verify-task2/playbook.md`).
  - NEW-3 cursor reject (cable-on-device): hold a normal-cable coil, hover next to an existing turret/RTG -> red ghost + "Wrong voltage -- [device] doesn't accept normal cable." Client UI; see playbook.
  - Device placement is never cursor-blocked: hold a solar-panel kit, hover over a normal cable -> green ghost; place; cable burns next tick. Client UI; see playbook.
  - Burn-reason tooltips: hover wreckages from each burn category (sustained overload, mixed-tier bridge, adjacent misplaced device) and confirm the "Burned: ..." text in the wreckage's extended hover tooltip. Client UI; see playbook. (Server-side `BurnReasonRegistry.Attach` path is exercised by the device-tier burn already in `verified.md`.)
  - Power-flow gate: a network with a misplaced device but ALL devices off (`OnOff = false`) destroys no cables; turning a source + a load on fires the burn on the next tick. Needs a constructed scenario via `tools/save-edit/`; not observable on the Luna load alone.
  - Loaded save with pre-existing tier mix: construct the scenario via `tools/save-edit/` (single normal cable bridging two heavy networks with a running generator + load) and re-load to verify the boundary burn (D11 retroactive enforcement only fires on a mixed-tier network with active power flow; a clean Luna load produces zero burns).
  - InspectorPlus request files staged at `.work/2026-05-22-pgp-verify-task2/requests/pgp-task2-*.json` (7 files); developer playbook for client-side parts at `.work/2026-05-22-pgp-verify-task2/playbook.md`.

- **`Cable.ConnectedDevices()` on the cursor ghost.** The Round-3 cable-on-device cursor reject calls `__instance.ConnectedDevices()` on a placement-preview cable. The Research page (`Research/Patterns/CursorAdjacencyLookup.md`) says this should work the same way `ConnectedCables(NetworkType.Power)` does on a cursor; verify in-game by hovering a normal cable next to a generator and seeing the red message. The headless half is done (code review at `.work/2026-05-22-pgp-verify-task4/code-review.md`, developer playbook at `.work/2026-05-22-pgp-verify-task4/playbook.md`, mod built Release + deployed to the dedicated server). Cursor logic is client-side, so the in-game red-message confirmation is developer-driven; follow the playbook.
  - Mode: client UI (single-player or any session with the game client).

- **Heavy-cable device-list draw sweep.** The high-draw machine whitelist in `VoltageTier.IsHighDrawMachine` (`CarbonSequester`, `FurnaceBase`, `ArcFurnace`, `Centrifuge`, `Recycler`, `IceCrusher`, `HydraulicPipeBender`, `DeepMiner`) was assembled from community consensus; only the first three are verified-by-decompile to draw >5 kW. Confirm which of the rest actually exceed 5 kW.
  - Mode: single-player, high-load save.
  - Observe: InspectorPlus sweep covering each whitelist machine, capturing `UsedPower` / `MaxUsedPower` / `OperatingTemperature`. Request file + playbook staged at `.work/2026-05-28-pgp-heavy-cable-sweep/` (the `.work/` tree is gitignored, so regenerate on a fresh clone).
  - Follow-up (stays in `TODO.md`): based on the measured draws, drop or extend the whitelist in `VoltageTier.IsHighDrawMachine`.

- **APC per-device `LogicPassthroughMode` field.** The `AreaPowerControl` prefab now exposes the same writable `LogicPassthroughMode` logic field that transformers, batteries, and TX/RX dishes already had. Default mode is 1 (transparent) for every APC, matching today's behaviour. The server toggle `Enable APC Logic Passthrough` becomes a master kill-switch that gates the per-device mode (same shape as the other three families). New file: `Patches/AreaPowerControlPassthroughLogicPatches.cs`. Changes in `PassthroughTopology.cs` (GetOtherSide consults per-device mode + APC added to GetBridgeNetworks), `PassthroughSaveLoadPatches.cs` (APC case in the OnFinishedLoad switch so the side-car restores APC mode), `PassthroughModeStore.cs` (APC default 1), `LogicTypeRegistry.cs` (description string lists APCs), `Settings.cs` (server-toggle description normalised to the master-kill-switch wording).
  - **Verified 2026-05-28 on dedicated server, fresh copy of `C:\Users\jori\Downloads\APC-Luna.save` (static-load half only).** Build clean (`dotnet build -c Release` 0 warnings, 0 errors). BepInEx LogOutput.log shows `Loading [Power Grid Plus 0.1.0]` -> `Power Grid Plus v0.1.0 loaded; patches deferred to prefab load` -> `Registered 1 IC10 constants (9 -> 10)` -> `Power Grid Plus patches applied` -> `Injected 1 entries into Logicable arrays`, with no `same key already added` fatal on the new `AreaPowerControlPassthroughLogicPatches` registration and no `AreaPowerControl|Passthrough` exception or warning anywhere in the log. InspectorPlus baseline (`.work/2026-05-28-pgp-apc-passthrough/01-apc-baseline.snapshot.json`) reached all 16 APCs; APC ref 360395 (input CableNetwork 357516, output CableNetwork 360409) shows the same 28-entry `DataDeviceList` on both sides, confirming the bridge mechanic still merges device lists across the APC under the new gate. Autosave side-car `pwrgridplus-passthrough.xml` carries the 10 transformer overrides from the source save (9 mode=1 plus the mode=0 outlier ReferenceId 309109); no APC entries appear because nothing has written a per-device APC override yet, which is the expected baseline. Headless verification cannot exercise (a)-(e) below: IC10 chips, Computer dropdowns, the StationeersLaunchPad settings panel, and a 2-peer setup all need a rendering game client.
  - Mode: single-player covers most; one 2-peer subcheck for client replication.
  - Setup: reuse `C:\Users\jori\Downloads\APC-Luna.save` (16 APCs already in the save per the 2026-05-28 InspectorPlus baseline above). Pick one APC by ReferenceId from an InspectorPlus snapshot before starting; an APC near a Computer screen makes (b) easier to observe.
  - (a) **IC10 read default.** Load the save solo. From an IC10 chip wired to the APC's cable network, `l rX d<n> LogicPassthroughMode` returns `1` for the selected APC. Confirms `GetLogicValuePatch` is wired and `PassthroughModeStore.GetDefaultMode` returns 1 for APCs.
  - (b) **IC10 write + dropdown refresh.** Open a wall-mounted Computer screen on the OUTPUT side of the selected APC; note that devices from the INPUT side are listed in the dropdown (this is the existing passthrough behaviour preserved). From the IC10 chip, `s d<n> LogicPassthroughMode 0`. Reopen the screen: input-side devices are gone from the dropdown. `s d<n> LogicPassthroughMode 1` returns them. Confirms `SetLogicValuePatch` stores the mode, `PassthroughTopology.GetOtherSide` honours it, and `ScheduleCascadeForDevice` refreshes the dropdown.
  - (c) **Save / load persistence.** With the APC at mode 0 from (b), save the world and quit to main menu. Reload the save. The IC10 `l rX d<n> LogicPassthroughMode` still returns `0`. Confirms the side-car XML carries the APC entry and `ThingOnFinishedLoadPassthroughPatch` restores it.
  - (d) **Server toggle master-kill behaviour.** With the APC at mode 1 (default), open the StationeersLaunchPad settings panel and switch `Enable APC Logic Passthrough` off. The output-side Computer dropdown no longer shows input-side devices, even though the per-device mode is still 1. Switch it back on; the devices return. Confirms the server toggle gates per-device mode as the master kill-switch.
  - (e) **Client mode replication (2-peer).** Host running the save, one client connected. Client opens the output-side Computer screen and sees the input-side devices. Host (or client) flips the APC's `LogicPassthroughMode` via IC10. The CLIENT's dropdown updates within roughly one network tick with no replug. Exercises `PassthroughModeMessage.SendAll(0L)` for the APC ReferenceId. The other multiplayer subchecks (join-suffix restore, server-setting live toggle) are already covered by entries (d) and (e) of the 2026-05-28 cascade test above, since the APC change reuses the same plumbing.
