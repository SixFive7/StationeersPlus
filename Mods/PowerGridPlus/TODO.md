# Power Grid Plus TODO

This file tracks open issues only. Entries are plain bullets, not `- [ ]` checkboxes; when an item is done, remove it rather than ticking it off. Completed work lives in git history.

Implemented changes still awaiting an in-game or dedicated-server test do not belong here; record those in `PLAYTEST.md` (same folder).

## Allocator follow-ups

- **Capability-delta review after the 2026-07-12 atomic-pipeline rebuild (flagged for user review; collected while re-syncing the docs).** Old behaviours the greenfield rebuild dropped or changed; each needs a "fine as built" or "rebuild it" verdict: (1) no vanilla power-ratio fallback exists for anything the snapshot cannot classify; unclassified demand is treated as rigid. (2) The recursive-provider backstop prefix is gone; loop handling is the cycle-fault detector alone. (3) Fuse selection on an over-capacity net is now deterministic (lowest-rated fuse blows) and the trigger condition differs from vanilla's per-fuse roll. (4) Wireless carrier nets read ~0: the cable net between a transmitter and its receiver carries no modelled load, so IC10 `PowerActual` on the transmitter and load reads on that net report ~0. Candidate fix: WriteBack fills carrier nets with the pair's settled totals. (5) Partial-power sentinel monitoring is gone (nothing to monitor: partial delivery is impossible by construction). (6) Per-delivery Error/OnOff re-gating at apply time is gone; the tick is one-instant coherent from the snapshot (exception: the APC cell is re-fetched at credit time). (7) The APC deferred cell-cover path is replaced by same-tick elastic share. (8) `AllowSetPower` and the SetPowerFromThread block are dead surfaces; quarantine plus exemptions carry third-party protection now. (9) Load fields on nets that lose their last device may hold stale values (low confidence; verify with a snapshot on an emptied net). (10) POWER.md 17.41 `lastTopologyChangeTick` (skip work when topology is unchanged) is designed but unimplemented. (11) The APC exposes no writable `LogicPassthroughMode` logic port; user verdict 2026-07-13: wanted someday, not now (see the dedicated bullet below). (12) Plain-consumer `ReceivePower` delivery side effects never run; see the dedicated bullet below (five vanilla classes broken, including the omni transmitter and suit storage).
- **APC LogicPassthroughMode IC10 field (user-wanted, someday; not now).** The port never existed in any runnable build (the 2026-05-28 attempt in commit 83e2310d never compiled because the file was missing from the old explicit-include csproj; the 2026-06-22 SDK-glob migration auto-included it, its two unresolvable Harmony targets killed ALL mod patches for a day, and the 2026-06-23 fix deleted the file with an incorrect "superseded" claim). The fix when built: one line (`|| l is AreaPowerControl` in the `IsBridge` predicate; decompile-verified the APC's CanLogicRead/GetLogicValue overrides fall through to the patched base, CanLogicWrite/SetLogicValue are inherited from Device; do NOT resurrect the direct four-method APC patch class, two of its targets cannot resolve) plus doc sync (POWER.md section 12 and the LogicTypeRegistry Stationpedia text still overclaim the port; README/RESEARCH wrongly say the APC's seeded mode "persists with the save" when it re-seeds from config each launch) plus a first-ever playtest.
- **Delivery-shim follow-ups (the shim itself shipped 2026-07-13: five-class allow-list, census log, Extra Delivery Devices setting).** Remaining: watch the load-time `ReceivePower`-override census log for third-party candidates and decide enrollment per mod; MorePowerMod's large omni transmitter is the first candidate and its charging mechanism needs a one-time DLL decompile (the local Workshop copy) plus curation to confirm it follows the vanilla omni's ReceivePower pattern before enrolling it.
- **Mixed-tier residuals (prevention + self-healing shipped 2026-07-14, POWER.md decision 31; fixture pgp-mixedwire-fixture 10/10).** Remaining: (a) multi-cell straight variants (Straight3/5/10) carry only their LAST cell's SmallCell back-pointer, so a mid-span overlap of a long piece can escape both the theft capture and the stack repair; decide whether to enumerate GridBounds cells when long pieces matter. (b) Same-tier cell theft is still not refused at registration (vanilla merge-replace flows re-occupy same-tier cells, so the guard cannot distinguish a legitimate replace from a foreign print at the SmallCell.Add moment); since the 2026-07-15 decision-32 extension, persisted same-tier duplicates are collapsed at world load, so the residual is only a stack created DURING play, which self-resolves when its twin is destroyed (the orphan re-seat restores the survivor) or at the next load. (Idle-mixed posture, burn only when power touches it, user-confirmed 2026-07-15. Full-cascade heal re-verified 2026-07-14 late: five stack repairs at all five live stacked cells, zero burns needed, survey flaggedNets=0, zero errors.)
- **Research curation queue from the 2026-07-15 fresh-device trace (the bug itself is fixed, decision-33 phase 1, 2026-07-17; see PLAYTEST.md).** Still to curate: device port-geometry probe data (the mutual-facing FillConnected law with measured port cells per prefab, captured in the FDT PROBE log lines and re-emitted by every pgp-fresh-device-trace run); Device.GetUsedPower gates onto Research/GameClasses/Device.md (currently only on Structure.md); and an RTG wattage conflict (live 4000 W at 0.2.6403.27689 vs DevicePowerDraw.md's 50 kW sourced at 0.2.6228; fresh-validator protocol required before touching that page).
- **Doc sync deferred from the 2026-07-14 fixes.** POWER.md carries no decision entry for the Powered-ownership carve-outs (umbilical halves owned on their input-network row, the ElevatorShaft/ElevatorLevel exemption removal, the umbilical Female enrolling in the allocator despite OnOff); the rationale lives in code comments (PoweredOwnership.cs, PowerAllocator.cs). Consolidate into a decision entry on the next POWER.md touch. README and About.xml do not yet describe the mixed-tier registration guard, the stack repair, or the load-time wreckage sweep (POWER.md decisions 31 and 32); sync them before the next release.
- **Power fault redesign (decision-33 candidate): overload split by cause, Undersupplied, sheddable step-ups, hold fixes. Phases 1 (hold fixes) and 2 (1a/1b overload split + capacity-sum fix; build 34DBD844, pgp-overload-split-fixture 14/14, pgp-chain-fixture regression 14/14) SHIPPED 2026-07-17, see PLAYTEST.md; the selector, step-up, and Undersupplied phases remain.** User decisions so far: the 1a/1b overload hybrid (1a = a net's OWN rigid demand alone exceeds the deliverable cap, immediate grow-only Device overloaded, shedding cannot help; 1b = the post-shedding unsheddable residual against the cap at loop settle); the starvation end-state gets the player-facing name Undersupplied (a face on the existing DEAD_UNMET machinery, hover with needs-vs-delivers numbers plus an UPSTREAM POINTER naming the feeding segmenter, else the room cue recommends generators when one Priority dial was the fix; name approved 2026-07-15); step-up transformers become sheddable (drop the categorical section-5.2 victim exclusion at PowerAllocator.cs:1506, FeedsActiveSeg stays as the hop protection) so terminal heavy machine nets behind small step-ups join leaf-first deprioritization. Six-agent adversarial review 2026-07-15: base direction sound (the exclusion was a 2026-06-16 spec axiom with no incident behind it and both original supports are dead; nothing in the allocator assumes tree-ness, multi-source topologies are first-class Kahn multi-root; today's protected trunk verifiably strands the whole budget into a verdict-dead net while innocent leaf siblings take lockouts). REQUIRED modification package before building: (a) watt-aware victim selection within a priority tier (best-fit single-cover sheds the smallest single claim covering the deficit, which decapitates a 50 kW trunk for an 8 kW deficit once trunks are in the pool); (b) SHIPPED in phase 2 (fixture P2d): the capacity sum counts Deprioritized-but-not-Locked suppliers at EffCap in rule 1a AND 1b, so a same-tick shed cannot stamp false sticky Device-overloaded on surviving co-suppliers; (c) NO 'served direct rigid demand' protection extension (killed by three agents: circular for the terminal case it exists to shed, convergence-unsafe if keyed on round-varying served state; intent is expressed via Priority, which becomes meaningful on step-ups for the first time); (d) per-round deep-first victim re-decision (the deciding pass clears flags then walks source-to-leaf, so children always look active at parent-decision time: escalation currently runs one level per TICK and every escalation tick darkens the source net for a full 60 s hold); (e) RESOLVED (user 2026-07-15, purity over comfort): NO trickle floor; a trickle-fed pocket shedding its export every 120 ticks is the designed, honest rhythm; document it, do not soften it; (f) the battery rate-capped=Overloaded vs energy-drained=Undersupplied split is a prerequisite (a store behind a shed trunk silently carries the branch, drains by policy because inactive segs propagate zero soft desire, then rule 2 mislabels the drained bank); (g) GROUNDWORK SHIPPED in phase 2 (fixture P2c): rule 1b runs at loop settle on the unsheddable residual, with an outer re-decide iteration so an overload that frees budget still un-sheds its victim the same tick (the 2c guarantee preserved); the trunk-shed-versus-1b ordering interplay completes with the step-up phase; (h) PARTIALLY SHIPPED 2026-07-17 as phase 1 (build C60F2089, dedi-verified via pgp-fresh-device-trace: stub classifies DEAD_NOSUPPLY with no hold, merged net LIVE on the first post-bridge tick, neighbors never flicker): the verdict-formula reorder (supply-absence tested first) and the merge-boundary hold clear (main-thread enqueue in MergeDeterminismPatches, worker drain at the tick head) are in; REMAINING decision: held-net demand presentation (during a hold tail the net's claim can shed a live sibling to fund flow that publishes zero, and nothing flashes anywhere); (i) fixture lockstep in the SAME commit: pgp-chain-fixture re-implements the protection expression locally with null-safe mirror writes (silently stays green while modeling the retired policy) and P7 'hops never victims' needs redefinition, plus new cases for terminal step-ups and trunk nets with direct machines; (j) priority-default migration guidance (every existing trunk sits at default 100, the first victim tier; players who raised leaf priorities pushed their own backbone to the front of the firing line); (k) doc wave: POWER.md dated decision entry amending sections 5.2 / 8.3 / 8.0.3 and decisions 20 / 24, RESEARCH.md 3.4, CHANGELOG/About leaf-first phrasing, Stationpedia priority guidance, census re-baselining; wireless step-up dish pairs become sheddable too (document; receiver base goes lockout+DEAD_NOSUPPLY instead of hold). (l) hover/reason lockstep with the 2026-07-15 fault-hover overhaul (in tree, other session): DeprioritizeReason (LowerPriority / EqualBestFit / EqualLargest) and the per-victim decision recording in SelectDeprioritizationVictimsDetailed (ShortfallW at the instant of the cut, VictimPriority, sticky Seg payload reused by the 2-cycle union) encode the CURRENT tier-major best-fit-decreasing walk, so the watt-minimizing selector of (a) must regenerate the reason vocabulary (equal-priority victims become members of the minimal-watt cover set; EqualBestFit / EqualLargest retire), redefine ShortfallW under set-selection (the deficit the chosen set covers, or a per-victim marginal; decide at build), move the recording site together with the deep-first re-decision of (d), keep the wire byte and join-suffix shape, and update the FaultHover reason strings, the reason-case fixtures, and the PLAYTEST reason asserts in the SAME commit; OverloadRegistry.StorageW's upstream-derivation (available = cap - storage) must be re-derived from the corrected cap of (b). Starvation communication after (b) narrows Device overloaded (user question 2026-07-15, answered): NO fourth fault kind is needed; the shed branch's Deprioritized hover already names the cause ('Upstream demand of X exceeded the upstream supply of Y' plus the shortfall and reason lines) and Undersupplied covers the victimless case (a generator-only network with machines and nothing sheddable has no device to flash, so the network-level cue is the only possible surface). Also surfaced, tracked as its own work: the availability-blind within-tier desire split with synchronized lockout/hold timers can hold a physically servable multi-source net permanently dark with zero live ticks (re-split desires excluding shed suppliers on settle rounds, or de-synchronize the timers).
- **Dead-net gauges lie: CurrentLoad / PowerActual report model flow on verdict-dark nets (found 2026-07-15; probably the source of the 'partially powered' impression in the step-up shape).** WriteBack fills RequiredLoad / CurrentLoad / PotentialLoad from model values for ALL nets including dead ones (PowerAllocator.cs:1079-1086 -> WriteBack.cs:137-140) while actual delivery on a dead net is zero, so the network analyser and IC10 PowerActual show partial watts flowing in a dark room. Either dead-zero Current (and decide what Potential should honestly read) or document the field as model flow; until then it will contradict every Undersupplied cue the redesign adds.
- **BatteryCellCharger: elastic demand + charging-speed hover (user feature request 2026-07-13).** Today the cell charger is a rigid consumer served through the delivery shim (its vanilla ReceivePower charges the slotted cells at full requested rate whenever the net is live). Future: model it like a storage device with elastic demand, so it charges its cells only from surplus (never competes with rigid machine load, same posture as batteries), and show the actual allocated charging speed on its hover tooltip (the ChargeSpeed pattern the stationary batteries already expose).
- **Battery losses become heat (user-approved 2026-07-08).** Convert the configured charge loss (`Battery Charge Efficiency`, since 2026-07-13 a cost multiplier defaulting to 1.5: stored = credit / cost, sub-500 W post-loss trickles stored in full) into heat dumped on the battery instead of vanishing, via the existing `EnergyToHeatRatio` hook (Battery inherits it from `ElectricalInputOutput`); today the loss is plain energy destruction, applied in `Core/WriteBack.ApplyCredit`, so that is where the heat dump would hook in. When building this, also consider renaming the setting (user 2026-07-13): "Efficiency" no longer matches the cost-multiplier semantics; a rename changes the BepInEx key and reseeds the value, which is free while the mod is unpublished. Optionally extend the same mechanism to a configurable fraction of raw (dis)charge throughput (the original shape of this bullet; percentage TBD, default 0 keeps current behaviour). Applies to vanilla Battery, BatteryLarge, and the third-party Nuclear variant via the shared base class.
- **Network-wide battery charge balancing.** Every power tick, batteries on the same cable network redistribute stored charge so all sit at the same `PowerRatio` (an extra pass at the end of ALLOCATE walking each network's battery roster, averaging stored energy proportionally to capacity). Removes the "this one battery is empty while that other is full" failure mode players hit with mixed bank ages. Relation to the elastic-to-soft transfer rung (shipped): the transfer already moves energy between banks whenever a store still has charge headroom (a donor's leftover discharge fills any charging store, cross-network included), so mixed banks now converge while any of them is below full; what it does NOT do is equalize `PowerRatio` between stores that raise no charge request (a full-rate-capped store, or same-net stores whose requests outpace donors), which is the residual gap this bullet would close. Re-evaluate against soak data before building it.
- **Surface network status on the network analyser tablet.** Show the per-net Powered-ownership verdict (LIVE, DEAD_UNMET with its hold countdown, DEAD_NOSUPPLY), the shortfall classification (Served / Dry / Throttled / Deadlock), and any active fault lockouts on the in-game network analyser cartridge view, so a dark subnet is diagnosable in-world. Today DEAD_UNMET has no player-facing cue at all (no flash, no hover, no countdown; POWER.md section 0 decision 19 notes the gap): the cartridge is the natural surface since it already renders per-network data, and it avoids inventing a new device. Consider a hover line on dark devices and promoting the step-up / cable-limited partial-delivery carve-outs to real OVERLOAD lockouts as cheaper interim cues.
- **Per-tier extra-device lists, and transformer output caps synced to the output wire tier (user 2026-07-13).** Add two more settings shaped like Extra Heavy-Cable Devices, one for each remaining tier (extra normal-cable devices, extra super-heavy-cable devices), so any modded device can be assigned to any tier. Additionally, sync the per-tier Watts cap values to the maximum transformer output for transformers outputting to a wire of that tier: a transformer whose output side connects to normal cable caps its output at Normal Cable Max Watts, and likewise per tier, so a transformer can never be asked to push more than its output wire is rated for.
- **Settings 34 (Enable Wall Light Battery Emergency Mode) and 25 (APC Battery Discharge Rate) flagged for future review (user 2026-07-13).** No change now; revisit both in a later settings pass.
- **Absorb Omni Transmitter Settings.** Fold the [Omni Transmitter Settings](https://steamcommunity.com/sharedfiles/filedetails/?id=3643534844) mod's behaviour into Power Grid Plus: configurable max transmit range, distance-based charge-power falloff, and a minimum-power-per-battery floor for the vanilla Omni (Microwave) Transmitter (`PowerTransmitterOmni`, a plain single-network consumer per `Research/GameClasses/PowerTransmitterOmni.md`, not a bridge). Adds a `Server - Wireless` config section; still a pure patch. Check the source mod's license first; adapt with attribution or reimplement. Does not overlap Power Transmitter Plus (which handles the dish pair).

- **Full-strict producer isolation stands: no zero-draw carve-out (user decision 2026-07-13).** The Luna_debug load faults 390 solar panels across 15 networks; the foreign devices include the sun-tracker sensors (6 proximity, 5 daylight) along with nuclear batteries, wall-light batteries, and APCs. The user confirmed strict is intended: sensors belong on a data-only net or behind a transformer, and bases (including Luna) must be rewired. Residual work: the pgp-rearch-suite charge phase fails on Luna_debug until its buses are legalized; either rewire the dedi save or make the suite synthesize its own supply the way pgp-chain-fixture does.
- **Post-rearchitecture cleanup residuals (the 2026-07-12 cleanup pass applied everything else: dead code deleted, snapshot-row seams closed, per-net guards added, stale docs and settings descriptions rewritten).** Remaining: the `*EnforceTail` method-name family vs the driver's "TAIL" phase wording (deliberately kept, rename only if it keeps confusing); `LedgerAdoption._worstSiteTo` is write-only after the bracket-layer removal (trim with the next touch); `PowerAllocator.RunAtomic` itself carries no per-net guard (pure math on snapshot data, deliberate; revisit only if a soak ever shows a throw from inside the solve); stale code-header comments still cite deleted files (`CableBurnWindow.cs` and `CableMax.cs` cite PowerTickPatches, `DeviceOutputSanitizer.cs` cites PowerTickPatches.CalculateState_NanGuard, `ChargeDeliveryAudit.cs` / `DischargeDeliveryAudit.cs` cite the deleted observation-bracket patch files, `CableNetworkPatches.cs` claims "the vanilla PowerTick now runs unmodified", `Settings.cs` cites "PowerTickPatches / CableBurnWindow"), sweep them in one comment-cleanup pass.
- **Round-3 audit fix package (four-agent adversarial audit, 2026-07-12; none implemented yet).** UPDATE after the same-day B + D1 rearchitecture: tier 1 (a) world-load arm-and-drain SHIPPED (Core/LoadBoundary), (c) `_tierCache` ConcurrentDictionary SHIPPED; the PowerDeviceList root lock is UNNECESSARY (the snapshot derives membership from lock(DeviceList) + the PowerCables predicate and never touches the lazy getter); tier 2 mostly retired (single boundary read, no ENFORCE, no vanilla delivery), tier 3 retired (no vanilla-list iteration in the solve; the sentinel and observation brackets are deleted), tier 4 partially retired (ThreadStatic ModWriteWindow moot, latch TryAdd moot). Residual live items: quarantine pruning for destroyed ids, EmergencyLightPrefabs SettingChanged rebuild, the fixed-point round cap tied to the watchdog, and the Research curation queue at the end of this bullet. Findings against commit 646aa8b3, full reports summarized in `.work/2026-07-12-architecture-redesign/SESSION-HANDOFF.md`; many may be subsumed by the architecture redesign, re-triage there before building piecemeal. Tier 1 (confirmed): (a) world-load Clear() race: `XmlSaveLoad.LoadWorld` is async so the FaultRegistryLoadPatches postfix fires at load START on the main thread while a tick can be in flight; the new plain containers (NetLiveness hold table, PoweredOwnership._state) can be corrupted and an in-flight publish can restore the OLD world's verdict map after the clear; fix = arm-and-drain at the tick boundary (the UnknownBridgeCensus.Arm pattern). (b) ownership + mitigation/APC-fix config split-brain: dead-net zeroing lives only in the caches nothing reads when those toggles are off (moot once the 2026-07-12 decision to hardcode ownership + transformer exploit mitigation + APC fix always-on lands, but verify battery/umbilical/wireless clamps stay unconditional). (c) `VoltageTierEnforcer._tierCache` is a plain static Dictionary written from both threads (worker PROTECT/ALLOCATE/ENFORCE + main-thread RunRecheckAll on every topology edit); worst case corrupts and hangs the power worker; fix = ConcurrentDictionary + snapshot the weakest-cable cap into the Net model at GATHER. Tier 2: enrollment reads live Error/IsOperable while ENFORCE gates read the snapshot, and the five supply caches accept last-tick entries (freshness window CurrentTick-1), so a mid-tick Error flip serves one tick of unmodeled stale totals (extend SegControlSnapshot.Entry with Error/operability, feed the adapters from it, narrow cache freshness to same-tick during ENFORCE); `Battery.UsePower` is unpatched and its live drain gates can deliver without debiting the store for one tick (mirror the ReceivePower snapshot gate); the seg adapters re-read terminal networks several times per TryDescribe and `LinkedReceiver` twice (read into locals once); PROTECT (current-mismatch / cycle / tier / OffAsResetSweep) decides from live single-instant reads BEFORE the tick's control snapshot exists, and the current-mismatch unknown-producer cable burn fires from the worker with no main-thread re-detect (move the control snapshot to the tick boundary; route the current-mismatch burn through the VoltageTierEnforcer reserve-then-re-detect pattern); the ownership layer amplifies unlatched third-party demand tears from one device-flicker to 60 s whole-net darkness (load-time census of third-party GetUsedPower overrides + require two consecutive DeadUnmet ticks before arming the hold); PowerConnector latch checks the dock reference live per call so a mid-tick dock/undock bypasses it (store the dock verdict in the latch entry); deprioritization/overload masters OFF + ownership ON latches unfundable nets dark permanently while the setting descriptions promise vanilla behavior (fix descriptions or skip liveness darkness there). Tier 3 (torn iteration): `PowerDeviceList` is iterated in 8-9 windows per net per tick and every `lock(PowerDeviceList)` is ineffective because the vanilla lazy rebuild runs unlocked on the reader thread and vanilla writers lock `DeviceList` (a different object); ROOT FIX = one Harmony wrapper making `CableNetwork.RefreshPowerAndDataDeviceLists` run under lock(DeviceList) + lock(_powerDeviceList) (lock order verified deadlock-free), then the retained-GATHER-snapshot rework for the ENFORCE-tail sweep (iterate GATHER's per-net snapshots, delete the unclassified streak machinery, decided 2026-07-12) as the simplification on top, then per-net try/catch in OBSERVE/EnforceNet and per-device in the device tick so residual races cost one device instead of the whole tick; `BatteryCellCharger.ReceivePower` iterates its cell list unlatched inside ApplyState and `Bench.GetUsedPower`'s computing read iterates Appliances (copy-before-iterate prefixes); unlocked `FuseList[0]` read in the recursive-provider prefix (take the lock); SatelliteDish has two vanilla-own list races (tolerate; the per-device catch contains them). Tier 4 (hardenings): `[ThreadStatic]` ModWriteWindow; local try/catch in the sweep (a throwing third-party DisplayName override currently aborts the whole tick); AssessPower suppression needs a verdict-freshness clause (PublishedTick >= CurrentTick - 1) so it cannot suppress while nothing owns the edges; latch postfixes should TryAdd (true first-read-wins under a cross-thread double-compute); prune `_quarantined` for destroyed ids in the 600-tick sweep; EmergencyLightPrefabs caches forever across config edits (SettingChanged rebuild); the exemption reflection should also cover PoweredValue-getter overrides; duplicated section comment in Settings.cs; stale 0.2.6228 citation in ProducerFaultEnforcementPatches; consider a fixed-point round cap tied to the watchdog (a non-converging solve can burn ~200 rounds in one tick). Research curation queue (agent claims NOT yet fresh-validated; validate against the decompile before curating): LoadWorld async-postfix timing (268507-268510), the PowerDeviceList rebuild + per-list writer lock census (270690-270787 etc.), DensePool vs ConcurrentDensePool ForEach semantics (228779-229222), Thing.OnOff/PoweredValue IsThread cached-read branches (318249-318300), OnServer.Interact destroyed-parent no-op (39710-39722), vanilla Transformer power surfaces (424598+), ElectricityTick client early-out (272091), the SetPowerFromThread caller census.

## Verification tasks

- **Cross-mod compatibility pass: unverified pairings remaining.** The 2026-05-22 compat pass (`Research/Unsorted/PowerGridPlusCrossModCompat.md`) live-tested MorePowerMod and PowerOverhaul against Power Grid Plus; both clean. Not yet live-testable (not subscribed): MoreCables, CableTypeSwitcher, EL Switche, Deadly Electricity, 3.6 Megawatt Battery, EGC, Super XL Wireless Battery (EGC Edit), BuffWirelessBatteries, Jigawatt Battery, RRI - Boost Da Powa, Better Power Mod, Custom Power DeepMiner, Super Structures Mod, 2x Solar Power, Realistic Solar Constants. The compat page records the code-analytical assessment and per-mod action; re-run live tests as each is subscribed. The unknown-bridge census now surfaces any third-party two-port power device at load, which is the first thing to check in each pairing.
- **Heavy-cable device whitelist: drop or extend after the draw sweep.** The hardcoded high-draw machine list in `VoltageTier.IsHighDrawMachine` (CarbonSequester, FurnaceBase, ArcFurnace, Centrifuge, Recycler, IceCrusher, HydraulicPipeBender, DeepMiner) was assembled from community consensus; only the first three are verified-by-decompile to draw over 5 kW. The InspectorPlus draw sweep is queued in `PLAYTEST.md`; adjust the whitelist when it reports.

## Pointers

- `RESEARCH.md` -- architecture, file walkthrough, patch catalog, decision log.
- Repo-root `POWER.md` -- the power-system algorithm spec, revised 2026-07-12 for the atomic snapshot pipeline.
- `Research/GameClasses/Cable.md`, `PowerTick.md`, `Transformer.md`, `AreaPowerControl.md`, `Battery.md`, `RocketPowerUmbilical.md`, `PowerTransmitterOmni.md`; `Research/GameSystems/StructurePlacementValidation.md`, `DevicePowerDraw.md`, `RecipeDataLoading.md`, `PowerTickThreading.md` -- the curated game internals this mod relies on.
- `Mods/PowerTransmitterPlus/` -- the sibling mod that owns the wireless dish pair (billing model, ModApi, beam visuals). Wireless-dish changes go there, not here.
- `.work/revolt-source/` -- read-only clone of Re-Volt (the upstream the device fixes derive from).
