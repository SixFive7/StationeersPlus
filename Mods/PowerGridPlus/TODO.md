# Power Grid Plus -- TODO

Pending work for the in-progress mod. The design brief and decision log (D1-D13) live in `RESEARCH.md`; game internals live in the central `Research/` knowledge base.

## Decisions and deviations from the 2026-05-12 / 05-13 build sessions

This is the record of choices made and corners cut while building the mod (the user asked for it to be carried as far as possible, with every deviation logged here for review). The mod **compiles** (`dotnet build -c Release` is green) and **loads cleanly on the dedicated server** (all Harmony patches apply, no errors) but has **not had its runtime behaviour play-tested** -- the actual power sim, cable burns, voltage-tier enforcement, and the battery/transformer/APC fixes are unverified.

### Round 3 (2026-05-13): build-time-device-rejection -> reactive cable-burn, plus burn-reason tooltips

After Round 2 the only NEW-3 gap was "build-time cursor reject for misplaced devices". On reflection the user preferred to not gate device placement at all and instead let the cable that the misplaced device is on burn -- symmetric with the existing cable-tier rule. Done:

- **`PowerGridTick.CalculateState_New` no longer zeroes a misplaced device's power.** Instead it records the first misplaced device of the tick (`_misplacedDeviceForBurn`); `ApplyState_New` burns the cable adjacent to it (via `device.PowerCables` filtered by `CableNetwork`). The device itself is never destroyed.
- **Both tier-burns moved to `ApplyState_New`, gated on `powerFlow > 0`.** An idle/off network destroys nothing, even if mixed-tier or with a misplaced device. As soon as actual power flows (Required > 0 *and* Potential > 0), the burn fires. Mixed-tier burns take precedence over device-tier burns (root cause first). The existing `_tierResolutionPending` flag still scopes one burn per tick instance.
- **`Cable_OnRegistered_Postfix` removed.** Cable registration triggers `DirtyPowerAndDataDeviceLists` -> `IsDirty = true` -> `Initialize_New` re-detects mixed-tier on the next tick -> `ApplyState_New` burns when power flows. The dirty-flag path is the same as for loaded saves; one code path covers both.
- **`Cable_CanConstruct_Postfix` extended** with a second pass over `Cable.ConnectedDevices()` -- placing a cable next to an existing device on a tier it doesn't accept is now rejected at the cursor with "Wrong voltage -- {device} doesn't accept {tier} cable". (Cable-to-cable cursor reject is unchanged.) Confirmed valid on the cursor ghost per `Research/Patterns/CursorAdjacencyLookup.md`.
- **Burn-reason tooltips.** Each call site that invokes `Cable.Break()` registers a reason string in `BurnReasonRegistry` keyed by the dying cable's `LocalGrid`. A postfix on `CableRuptured.OnRegistered(Cell)` consumes that entry and attaches it to the wreckage via a `ConditionalWeakTable<Thing, string>` sidecar. A postfix on `Thing.GetPassiveTooltip` filtered to `__instance is CableRuptured` appends "Burned: <reason>" to the tooltip's `Extended` field. Four reason categories so far: overload (vanilla-style sustained throughput), wrong voltage (mixed-tier bridge), wrong voltage (adjacent misplaced device), and the un-tagged fallback (some future game path we didn't author). Threading: `BurnReasonRegistry._pendingByCell` is a `ConcurrentDictionary<Grid3, string>` -- multiple power-tick worker threads can register concurrently safely. See `Research/GameClasses/Cable.md` (Wreckage section, 2026-05-13) for the curated game-internals.

Net result: build-time gating is cable-side only; device placement is never blocked. The "you put it on the wrong cable" feedback is the cable burning, with a tooltip on the wreckage explaining why. The runtime checks are bounded by network size and only do work on dirty rebuilds (cable tier) + the existing per-device tick loop (device tier) -- no extra dirty-flag plumbing was needed.

### Round 2 (2026-05-13): the previously-stubbed/deferred items, now resolved

After the first pass the user asked for everything stubbed/deferred/needing-a-call to be finished. Their answers (and what was done):

- **Retroactive enforcement on load**: keep the D11 design -- the first power tick after load burns the lowest-tier boundary cable of each mixed-tier network, splitting it. No grace period, no opt-out toggle. (Already implemented; unchanged. Still a sharp edge on first install -- see the NEW-3 note below -- but it's what was asked for.)
- **APCs (and transformers) in the tier scheme**: no per-device tier restriction and no per-prefab tier mapping -- they bridge whatever they're wired to. The single universal rule is the cable one (two cable tiers in one network -> burn the smaller); a transformer's / APC's input and output are always separate networks, so they never trigger it. This **supersedes the old D9/D10** (Small=heavy<->normal, Medium=heavy<->heavy, Large=superHeavy<->heavy, bidirectional rewrite) -- none of that was implemented and it isn't going to be; the universal rule does the job. Implemented: `Transformer`, `AreaPowerControl`, `WirelessPower`/`PowerReceiver`/`PowerTransmitter`, `PowerTransmitterOmni` are all tier-exempt in `VoltageTier.IsAllowedOnTier`.
- **Heavy-cable device list (D8)**: a built-in whitelist of the community-known high-draw machines -- `CarbonSequester`, `FurnaceBase` (covers `Furnace` + `AdvancedFurnace`), `ArcFurnace`, `Centrifuge`, `Recycler`, `IceCrusher`, `HydraulicPipeBender`, `DeepMiner` (covers `CombustionDeepMiner`) -- plus a `Extra Heavy-Cable Devices` config string (comma-separated PrefabNames) for modded machines. These may use heavy *or* normal cable, not super-heavy. (I did not exhaustively verify each one's draw exceeds 5 kW against the game data this session; `CarbonSequester`/`AdvancedFurnace`/`ArcFurnace` are verified from the decompile, the rest are the well-known set. If one shouldn't be there, drop it from `VoltageTier.IsHighDrawMachine`.)
- **Ordinary device on heavy/super-heavy = hard reject + no power if it slips through**: the "no power if it slips through" half is implemented (`PowerGridTick.CalculateState_New` zeroes a device's used and generated power when it's on a tier `VoltageTier.IsAllowedOnTier` rejects). The "hard reject at build time" half is **NOT done** -- it needs a device-to-adjacent-cable lookup at placement-preview time (`Device.CanConstruct`), which wasn't worked out. So today: an ordinary device placed on heavy/super-heavy cable places fine but receives no power; same for a generator/battery on the wrong tier. The build-time rejection (red cursor + message) for devices is the one remaining NEW-3 gap. (Cable-on-cable build-time rejection IS in, best-effort, via `Cable.CanConstruct` + `CableNetwork.ConnectedNetworks`.)
- Also dropped: the `Generators Require Heavy Cable` sub-toggle. There's now one toggle, `Enable Voltage Tiers`, for the whole NEW-3 feature. Added: the `Extra Heavy-Cable Devices` config string. Both under `Server - Voltage Tiers`.

### First-pass record (2026-05-12)

### Structure / scaffolding

- **Graduated from `Plans/` to `Mods/`** by `git mv`. `PLAN.md` was renamed to `RESEARCH.md` in place; its content (decision log, requirements, config surface, work phases, the Re-Volt feature matrix, the mod-landscape appendices) was kept as-is. It still reads partly like a forward-looking plan rather than a finished `RESEARCH.md`; a proper rewrite into the `RESEARCH.md` shape (architecture / file walkthrough / patch catalog with formulas / game internals / pitfalls / design decisions) is still owed. The "work phases" section in particular is now stale.
- **Project layout**: `Mods/PowerGridPlus/PowerGridPlus.sln` + `Mods/PowerGridPlus/PowerGridPlus/PowerGridPlus.csproj`, seeded from `Mods/Template/` conventions. Fresh `<ProjectGuid>` generated. References add `UniTask` (the ported tick uses `.Forget()` on `Device.SetPowerFromThread`) on top of the Template baseline. Plugin class is `PowerGridPlus.Plugin` (the Template's `Plugin` name, not `PowerGridPlusPlugin`).
- **Preview images are placeholders.** `Preview.source.png` (1280x720), `About/Preview.png` (1280x720), `About/thumb.png` (640x360) are solid-dark PNGs with the mod name drawn on them, generated so the build's `Content` items resolve. Real preview art is needed before release.
- **`Plugin.Awake` defers Harmony patching to `Prefab.OnPrefabsLoaded`** (the released-mod pattern), sets `MOD.Networking.Required = true`, then `harmony.PatchAll()` then `CableCostPatches.ApplyRecipeCost()`, all in a try/catch that logs fatal on failure. No custom save-data type (Re-Volt's `CircuitBreakerSaveData` was dropped with the breakers).

### What was ported from Re-Volt, and what was dropped

- **Ported** (renamed, MIT notice retained in file headers): the power-tick rewrite (`RevoltTick` -> `PowerGridPlus.Power.PowerGridTick`), `CableNetworkPatches` (swap the `PowerTick` per network), `PowerTickPatches` (route `Initialise`/`CalculateState`/`ApplyState` to the new tick + reverse-patch `CacheState` and `CheckForRecursiveProviders`), `DevicePatches` (`Device.AssessPower` re-implementation), `StationaryBatteryPatches`, `BatteryLogicPatches`, `TransformerExploitPatches`, `TransformerLogicPatches`, `AreaPowerControlPatches`.
- **Dropped**: all circuit-breaker / smart-breaker / heavy-breaker classes, the Load Center, the item kits and their `electronics.xml` recipes, `PrefabPatcher`, `ThingPatches`, the visual/animator components, `CircuitBreakerSaveData`, the `IBreaker`/`IPatchable`/`ISwitchgearComponent`/`ILoadCenter` interfaces, the `MultiConstructor`/`SingleConstructor` subclasses, and the `Heavy Breaker Maximum Trip Setting` / `Enable Custom Objects` config entries. This drops the LibConstruct/StationeersMods dependency -- the mod is pure BepInEx + StationeersLaunchPad.
- **`PowerGridTick` simplifications vs `RevoltTick`**: the `PowerClass` enum, the `PowerStates[]` array, `GetPowerState`, `ClassifyDevice`, and the load-center wiring were all removed (they only existed to support the Load Center). `DevicePatches.AssessPower` was simplified to match (no `GetPowerState` gate). `RevoltTick`'s `float.IsNormal(...)` NaN guard was replaced with `float.IsNaN(x) || float.IsInfinity(x)` (`float.IsNormal` is .NET Core 3.0+ / not in .NET Framework 4.7.2; the replacement is behaviourally equivalent for "is this a usable finite watt count"). `MathF.Min` (Re-Volt's `StationaryBatteryPatches`) was replaced with `UnityEngine.Mathf.Min` for the same portability reason.
- **APC patch -- fixed an apparent Re-Volt bug**: Re-Volt's `AreaPowerControllerPatches` declares two `[HarmonyPrefix]` patches both targeting `AreaPowerControl.GetUsedPower`, the first of which takes a `powerUsed` parameter that only matches `AreaPowerControl.UsePower`. Power Grid Plus targets `UsePower` for that one (which is what the body implements). If the in-game load reveals the original was right after all, revert.
- **Risk -- reverse patches**: `PowerTickPatches.CacheState`/`CheckForRecursiveProviders` (both confirmed to exist as private methods on `PowerTick` in the decompile), `DevicePatches.SetPower` (`Device.SetPower`, assumed from Re-Volt), `StationaryBatteryPatches.GetIsOperable` (`Battery.get_IsOperable`, assumed from Re-Volt). If any target doesn't resolve, `harmony.PatchAll()` throws and the try/catch in `Plugin` logs it as fatal -- check the BepInEx log first thing when testing.
- **Risk -- patch signatures assumed from Re-Volt source** (not all re-verified against the 0.2.6228.27061 decompile): `Device.AssessPower(CableNetwork, bool isOn)`, the `Transformer`/`Battery`/`AreaPowerControl` `GetGeneratedPower`/`GetUsedPower`/`ReceivePower`/`CanLogicRead`/`GetLogicValue` overrides, the `____powerProvided` private-field name. Re-Volt 1.4.0 targets a similar game version, so these should hold, but the load log is the first place to look.

### NEW-1 -- unlimited super-heavy cable

- Implemented in `PowerGridTick.TestBurnCable`: if `EnableUnlimitedSuperHeavyCables` and the network's weakest cable is `Cable.Type.superHeavy` (so the whole network is super-heavy), return no burn. Normal and heavy networks keep the vanilla rolling-average probabilistic burnout. No deviation from the plan.

### NEW-2 -- super-heavy cable cost multiplier

- **IMPLEMENTED (2026-05-13).** Two-layer cost path. The committed `GameData/cable-recipes.xml` overlay patch-replaces the vanilla `ItemCableCoilSuperHeavy` Electronics Printer recipe with Time 8, Energy 800, Constantan 1.0, Electrum 1.0 (the 2.0x default, vanilla was 0.5 / 0.5). `CableCostPatches.ApplyRecipeCost()` runs at plugin load and, if the configured `Super-Heavy Cable Cost Multiplier` differs from 2.0, re-applies the recipe at runtime via `WorldManager.RecipeData` -> `ElectronicsPrinter.RecipeComparable.AddRecipe(...)` -> `GenerateRecipieList()`, so the config value wins over the overlay. Prefab name (`ItemCableCoilSuperHeavy`) and the recipe loader hook are documented in `Research/GameSystems/RecipeDataLoading.md`. The plan (D12) intent is preserved: only the coil-crafting recipe is modified; nothing touches per-cable `BuildStates[0].Tool.EntryQuantity`.
- **Caveat -- dedicated-server deploy.** `DedicatedServer/dedicated-server.ps1 -DeployMods` copies only the built DLL, NOT the `GameData/` folder. On that test path the overlay is absent and only the runtime patch (when the multiplier is non-default) applies. A real Workshop install ships the whole mod folder including `GameData/`, so the overlay is present. Not a bug, just a deploy-tool limitation.
- **What remains: in-game verification.** Covered in the round-2 in-game behaviour test below ("NEW-2": Electronics Printer recipe display at multipliers 1.0 / 2.0 / 3.0). No code work pending.

### NEW-3 -- three-tier transmission voltages

- **Cable-tier incompatibility (the headline) -- working, two enforcement paths:**
  - `VoltageTierPatches.Cable_OnRegistered_Postfix`: after a cable registers and merges, if its network now holds more than one `CableType`, `VoltageTier.ResolveMixedTierNetwork` burns one lowest-tier cable (preferring the cable that just registered, since that's the bridge the player placed). The re-flood splits the network; the next rebuild re-checks; it converges over a few ticks.
  - `PowerGridTick.Initialize_New` detects a mixed-tier network on rebuild and requests the same resolution (covers loaded saves with pre-existing junctions, and anything `OnRegistered` missed). Guarded by a per-tick `_tierResolutionPending` flag so it doesn't queue a second burn before the first split takes effect.
  - **Deviation -- coarse "boundary" heuristic**: only the just-registered cable is treated as the preferred victim; otherwise *some* lowest-tier cable in the network is burned, not necessarily the one geometrically at the junction. In a line like `heavy-heavy-normal-heavy-heavy` this is fine (one burn). In `normal-normal-heavy` it may burn two normals instead of one. Converges either way. Refining this needs the cable grid-adjacency API.
  - **Sharp edge -- retroactive enforcement on load**: with `EnableVoltageTiers` defaulting on, loading *any* existing save that has a normal cable touching a heavy cable (legal in vanilla, possibly common in real bases) will start burning cables on first power tick. The plan (D11) specifies exactly this ("a one-time boundary-cable cascade on first tick"), but it is destructive on first install. Worth reconsidering: on first load, log a warning listing the mixed networks and give the player a grace period, or require a config opt-in for retroactive enforcement. Left as the plan specifies for now.
- **Generators-on-heavy (D13) -- working at the tick level:** `PowerGridTick.CalculateState_New` zeroes a device's generated power if `EnableVoltageTiers` + `Generators Require Heavy Cable` are on, the device is a generator, and the network is not all-heavy. So a solar panel / wind turbine / fuel generator / RTG / Stirling engine / gas-pipe turbine on a normal or super-heavy network produces nothing.
  - **Deviation -- no build-time rejection for generators yet.** A player can still *place* a generator on a normal cable; it just won't generate. The plan wanted a `Device.CanConstruct` postfix rejecting it with a message; that needs the device-to-adjacent-cable lookup at preview time, which wasn't worked out tonight. Add it. (The tick-level enforcement is the real teeth either way.)
  - The "is this a generator?" discriminator (`VoltageTier.IsGenerator`) is a type whitelist: `IPowerGenerator` (covers `SolarPanel`, `WindTurbineGenerator`, `LargeWindTurbineGenerator`) plus `TurbineGenerator`, `StirlingEngine`, `PowerGeneratorSlot` (covers `SolidFuelGenerator`), `PowerGeneratorPipe` (covers `GasFuelGenerator`), `RadioscopicThermalGenerator`. Wireless `PowerReceiver` is **not** in the list (it relays, doesn't generate) -- revisit whether a wireless relay's large output should also be heavy-only.
- **Build-time cable-tier rejection -- best effort only:** `VoltageTierPatches.Cable_CanConstruct_Postfix` tries `CableNetwork.ConnectedNetworks(__instance)` at placement-preview time and rejects with "Cannot connect: different transmission voltage -- use a transformer." if a connected network already carries a different tier. Wrapped in try/catch -- if the preview cable isn't gridded yet (or the API shape differs), it does nothing and the reactive burn-on-join handles it post-placement. Needs in-game verification that `ConnectedNetworks` works on a preview cable.
- **Device-tier policy beyond generators -- NOT implemented.** The plan (D8) also wanted: ordinary devices restricted to normal cable, stationary batteries restricted to heavy, super-heavy as a backbone with no devices at all, and a configurable high-draw-machine whitelist (Carbon Sequester / Advanced Furnace / Arc Furnace) allowed on heavy. None of that placement gating exists yet -- only the cable-tier-incompat rule and the generator rule. Without it, you *can* hang anything off heavy/super-heavy cable; it just isn't lore-correct.
- **Transformer tier mapping (D9/D10) -- NOT implemented.** The plan wanted Small = heavy<->normal only, Medium = heavy<->heavy flow-limiter only, Large = super-heavy<->heavy only, all bidirectional, re-implementing `Transformer.GetGeneratedPower`/`GetUsedPower`/`ReceivePower` symmetrically. Tonight only Re-Volt's transformer *exploit fix* is ported (one-directional, vanilla flow direction). The cable-tier rule already makes a transformer the only thing that can bridge tiers (its input and output sit on separate networks, so no burn fires) -- but any transformer prefab can bridge any tier pair, and transformers are still one-directional. Distinguishing the prefabs needs their prefab names; the bidirectional rewrite is a separate chunk.
- **In-game tier names / rejection message**: the plan (D12) wants "Normal / Heavy / Super-Heavy cable" and the message "Cannot connect: different transmission voltage -- use a transformer". The rejection message string is in the code; no in-game name changes were made.

### Game-internals facts pinned down this session (also curated into `Research/`)

- `Cable`, `CableFuse`, `Transformer`, `Battery`, `AreaPowerControl` are in namespace `Assets.Scripts.Objects.Electrical`. `Device` is in `Assets.Scripts.Objects.Pipes`. `CableNetwork`, `PowerTick`, `PowerProvider` are in `Assets.Scripts.Networks`. `IPowerGenerator` (and `WindTurbineGenerator`) are in the top-level `Objects` namespace. The `Pick<T>(this List<T>)` / `Pick<T>(this List<T>, System.Random)` extension is in `Assets.Scripts.Util` (decompile ~line 214049).
- `PowerTick.CacheState()` and `PowerTick.CheckForRecursiveProviders()` are private instance methods (decompile lines 254656 and 254613; `CheckForRecursiveProviders` is called from `CalculateState` at 254610).
- `Cable.OnRegistered(Cell cell)` is the registration override; it does the network merge only when `GameManager.GameState != GameState.Loading && GameManager.RunSimulation`.

## Implementation backlog

- [ ] **Stationpedia entry for `LogicPassthroughMode`.** The newly-added writable logic slot (6577) on `Transformer` has every UI integration except Stationpedia. PowerTransmitterPlus ships `StationpediaPatches.cs` that postfixes `Stationpedia.PopulateLogicVariables` and calls `Stationpedia.Register(...)` for each entry in its `LogicTypeRegistry.All`. Mirror that file as `Mods/PowerGridPlus/PowerGridPlus/StationpediaPatches.cs` so the in-game wiki shows a page for `LogicPassthroughMode` with the description already on the `CustomLogicType` record. ~40 lines; idempotent via best-effort reflection per the PowerTransmitterPlus pattern.

- [x] **Per-port tier rules for two-port devices.** Implemented 2026-05-14, revised 2026-05-15 to make the transformer rule bidirectional + actively-bridging-gated:
  - **APC**: both `InputConnection.GetCable()` and `OutputConnection.GetCable()` must be on the same tier. If they differ, the lower-tier side's adjacent cable burns via `VoltageTier.BurnPortMismatchCable` when the relevant network has `powerFlow > 0`. (`WirelessPower` subclasses inherit the same shape but only one port is cable-wired in practice; rule vacuous on them.)
  - **Transformer**: per-variant UNORDERED tier-pair via `VoltageTier.GetTransformerTierMap`. Small `{heavy, normal}`, Medium `{heavy, heavy}`, Large `{superHeavy, heavy}`. Direction is interchangeable (e.g. a Small wired heavy-in/normal-out OR normal-in/heavy-out both validate -- the latter is the step-up / generation case).
  - **Transformer burn trigger** (stricter than the per-network powerFlow gate): the burn fires ONLY when (a) the tier-pair rule is violated AND (b) `Transformer.OnOff == true` AND (c) `Transformer._powerProvided > 0` (actively bridging on both sides). When triggered, BOTH adjacent cables burn via `VoltageTier.BurnTransformerBothCables`. The transformer itself is never destroyed (expensive to build); the wreckage tooltips on both cables are the player signal.
  - **Cursor reject for cables** (scope confirmed: cables only, never transformers/devices):
    - Cable's tier must be in the variant's pair. Otherwise rejected.
    - Asymmetric variants (Small, Large): when exactly ONE side is already wired, the new cable's tier must DIFFER from that side's tier; otherwise rejected. (Mirror of the APC rule, which requires SAME tier.)
    - Symmetric variant (Medium): only the pair-membership check applies.
    - Both sides already wired: cable-to-cable network-tier check covers extension cases.
  - **PowerGridTick** tracks `_portMismatchTransformerForBurn` (Transformer) and `_portMismatchCableForBurn` (Cable + APC owner) separately; `ApplyState_New` priority is mixed-tier > transformer-active-violation (both cables) > APC mismatch (single cable) > misplaced single-port device (single cable). Each network burns at most one resolution per tick via `_tierResolutionPending`. Both-cable burn is idempotent: the other network's tick instance running the same check is a no-op because Unity's destroyed-object null overload returns true after Break().

- [x] **Boundary cable pick refinement.** Implemented 2026-05-14: `VoltageTier.ResolveMixedTierNetwork` now does a first pass picking only lowest-tier cables that have at least one grid-adjacent higher-tier cable (via `Cable.ConnectedCables(NetworkType.Power)`). Falls back to the previous "any lowest-tier" rule when the grid-adjacency pass returns nothing (defensive for torn networks mid-rebuild). The transformer / APC port-mismatch burns already pick the adjacent cable directly via `Connection.GetCable()` so they are boundary-correct by construction.

- [x] **Retroactive tier enforcement.** Decision (2026-05-14): D11 stays, no grace period, no opt-in. A loaded save with pre-existing mixed-tier networks burns its boundary cables on the first power-flowing tick after load. The earlier Option-C proposal (default-off opt-in for retroactive enforcement) is rejected. Rationale per the user: "Just burn everything that needs burning in the new rules. No old save grace."

- [x] **Device-side server-hardening (Direction C).** Removed (2026-05-14): the user trusts players ("It is not a competitive game"). The reactive cable-burn deterrent is sufficient.

## Considered features (not yet decided)

These would all sit on top of the v1 design. None needs a new prefab, so they stay within the pure-patch stance, but each expands the feature surface and needs an explicit go/no-go.

- [ ] **Cable-heat dissipation (from Deadly Electricity).** Convert a small fraction of a cable network's throughput into heat dumped into the cable run's local atmosphere each tick -- "wires get warm under load". Config-toggled, default off (flavour, not a fix). Idea source: the [Deadly Electricity](https://steamcommunity.com/sharedfiles/filedetails/?id=3575959825) mod's "cable efficiency loss -> heat with network size" mechanic. Open questions: how much heat (a small % of `Consumed`?), where to inject it (the cable's `WorldGrid` atmosphere, like `WorldManager.Spark` does on rupture), whether it scales with network size or throughput, and whether it interacts badly with the Re-Volt-derived proportional-sharing tick. Decompile the relevant `AtmosphericEventInstance` / `MoleEnergy` injection path before committing.
- [ ] **Absorb Omni Transmitter Settings.** Fold the [Omni Transmitter Settings](https://steamcommunity.com/sharedfiles/filedetails/?id=3643534844) mod's behaviour into Power Grid Plus: configurable max transmit range, distance-based charge-power falloff, and a minimum-power-per-battery floor for the vanilla Omni (Microwave) Transmitter (`PowerTransmitterOmni`). Adds a `Server - Wireless` config section. Expands the mod from "wired power sim" to "wired sim + omni-transmitter tuning" (a deliberate scope widening that overrides the earlier "wireless tuning out of scope"). Still a pure patch (patch `PowerTransmitterOmni`, no new prefab). Check the Omni Transmitter Settings source/license first -- if available and permissive, adapt with attribution; otherwise reimplement from scratch. Don't conflict with this repo's separate Power Transmitter Plus mod, which handles the Microwave Power Transmitter dish (a different device).
- [ ] **Craftable RTG (nuclear recipe).** Add a survival crafting recipe for the RTG (`RadioscopicThermalGenerator`; the prefab exists and currently only deconstructs as a "Creative RTG"). Use vanilla nuclear materials (uranium-line ores / refined products -- check what's craftable without a third-party "Processed Uranium" dependency). Recipe-only (no new prefab), so it fits the pure-patch stance; mechanically the same kind of `RecipeData` / gamedata patch as NEW-2's cost bump. Precedents: [CraftableRTG](https://steamcommunity.com/sharedfiles/filedetails/?id=3026880031), [Survival RTG](https://steamcommunity.com/sharedfiles/filedetails/?id=2464235697), [Nuclear Recipes](https://steamcommunity.com/sharedfiles/filedetails/?id=3004097828). Under D13 a crafted RTG goes on `heavy` cable like every generator. Config-toggled. Decide which machine prints it, the recipe, and whether it stays "Creative RTG" on deconstruction or returns the kit.

## Verification tasks

- [x] **Load test (done 2026-05-12 + re-verified 2026-05-13 across rounds 2 and 3).** Deployed to the dedicated server alongside the user's full ~50-mod set (Re-Volt was *not* in that set, so no PowerTick conflict), started a fresh `-New Moon` world. Result: BepInEx loaded the plugin, `Plugin.Awake` ran (config bound), `Prefab.OnPrefabsLoaded` fired, and `harmony.PatchAll()` applied **all** patch classes -- including the four reverse patches (`PowerTick.CacheState`/`CheckForRecursiveProviders`, `Device.SetPower`, `Battery.get_IsOperable`), every assumed-from-Re-Volt signature, the new universal-base `Thing.GetPassiveTooltip` postfix (filtered to `CableRuptured`), and the `CableRuptured.OnRegistered` postfix -- with **no exceptions** across all three rounds. The mod is structurally sound and patches apply; what's *not* yet verified is runtime behaviour.

- [ ] **In-game behaviour test (round 1 -- ported Re-Volt simulation).** On a populated save (the developer provides one), confirm:
  - Power flows on a normal-cable network with loads + a small generator.
  - A normal cable carrying sustained throughput above its 5 kW rating eventually burns (gradual / probabilistic via the rolling-window `TestBurnCable`); a fuse on the same network blows before the cable.
  - The weakest fuse / weakest cable fails first (a normal cable next to a heavy cable burns first).
  - Recursive / looped network (`Enable Recursive Network Limits` off by default) doesn't force-burn cables.
  - Stationary battery respects `Max Battery Charge Rate` (0.002) and `Max Battery Discharge Rate` (0.007) -- the battery's logic `ImportQuantity` / `ExportQuantity` outputs match the configured maxima.
  - `Battery Charge Efficiency` < 1.0 actually loses energy (set 0.5 and check the stored vs incoming delta).
  - Transformer doesn't generate free power (the input-side draw equals the output-side throughput + `UsedPower`); transformer's `PowerActual` logic value reads the current throughput.
  - APC: no idle power drain when nothing's downstream; no slow battery drain when the input is energised but the output is idle.
  - Prepare InspectorPlus request files for before/after states of the battery + transformer scenarios.

- [ ] **In-game behaviour test (round 2 -- NEW-1, NEW-2, NEW-3 simulation half).**
  - **NEW-1**: an all-super-heavy-cable network at >>>50 kW throughput doesn't burn cables (off via config -> super-heavy starts burning at its rating).
  - **NEW-2**: open the Electronics Printer; the super-heavy cable coil recipe shows 1.0 Constantan + 1.0 Electrum (the GameData overlay's 2x). With `Super-Heavy Cable Cost Multiplier` set to 3.0, restart server, check the recipe shows 1.5 / 1.5 (the runtime override). With multiplier 1.0, the recipe shows the vanilla 0.5 / 0.5.
  - **NEW-3 device-tier rule (the heart of round 3)**: place a solar panel on a normal cable next to a base that has loads -> the cable burns on the next tick that has actual power flow. Hover the wreckage -> tooltip ends with "Burned: Wrong voltage -- the adjacent Solar Panel doesn't accept normal cable". An *off* / unloaded network with a misplaced device destroys nothing (turn the base off; the misplacement persists harmlessly).
  - **NEW-3 cable-tier rule**: place a normal cable adjacent to an existing heavy-cable network containing a turned-on generator + load. The boundary normal cable burns on the next tick. Wreckage tooltip: "Burned: Wrong voltage -- normal cable was bridging into a different cable tier".
  - **NEW-3 cursor reject (cable-on-cable)**: hold a normal-cable coil, hover next to an existing heavy cable -> red ghost + "Wrong voltage -- this cable's tier doesn't match the adjacent cable network. Use a transformer."
  - **NEW-3 cursor reject (cable-on-device)**: hold a normal-cable coil, hover next to an existing turret/RTG -> red ghost + "Wrong voltage -- [device] doesn't accept normal cable."
  - **Device placement is never cursor-blocked**: hold a solar-panel kit, hover over a normal cable -> green ghost; place; cable burns next tick.
  - **Burn-reason tooltips**: deliberately trigger each category (sustained overload, mixed-tier bridge, adjacent misplaced device) and confirm the "Burned: ..." text appears in the wreckage's extended hover tooltip with the right wording.
  - **Power-flow gate**: a network with a misplaced device but ALL devices off (`OnOff = false`) destroys no cables. As soon as one device draws power AND a source generates, the tier-burn fires on the next tick.
  - **Loaded save with pre-existing tier mix**: load a save that already has a normal-cable-touching-heavy-cable junction. First power tick with any load + source flow burns the boundary cable. (The D11 design -- destructive on first install; reconsider if play-testing shows it's too aggressive on legacy bases.)
  - Prepare InspectorPlus request files for: `types=[Cable], fields=[CableType, MaxVoltage, CableNetwork.ReferenceId, PowerCable.CableNetwork.ReferenceId, IsCursor]`; `types=[CableRuptured], fields=[LocalGrid, transform]`; `types=[Battery, Transformer, AreaPowerControl], fields=[InputNetwork.ReferenceId, OutputNetwork.ReferenceId, PowerStored, PowerMaximum, UsedPower, _powerProvided, Setting]`.

- [ ] **Multi-port-device convergence sanity-check.** A misplaced device with multiple `OpenEnds` touching multiple cables in the same network: confirm that successive tier burns isolate it within a reasonable number of ticks rather than chain-reacting through far-away cables. Place an Advanced Furnace on super-heavy cable and watch how many cables burn before it's orphaned (Advanced Furnace is allowed on heavy/normal, *not* super-heavy). Acceptable as long as the burns are local to the device. Cap it (one-burn-per-device) if it turns out pathological.

- [ ] **`Cable.ConnectedDevices()` on the cursor ghost.** The Round-3 cable-on-device cursor reject calls `__instance.ConnectedDevices()` on a placement-preview cable. The Research page (`Research/Patterns/CursorAdjacencyLookup.md`) says this should work the same way `ConnectedCables(NetworkType.Power)` does on a cursor; verify in-game by hovering a normal cable next to a generator and seeing the red message.

- [ ] **`Thing.GetPassiveTooltip` universal-base postfix sanity-check.** `BurnReasonPatches` patches `Thing.GetPassiveTooltip` and filters to `__instance is CableRuptured`. Every tooltip in the game runs through this postfix. Confirm no measurable per-frame cost when hovering arbitrary things, and no broken-looking tooltips on non-wreckage things (the postfix only acts when the filter matches; verify the negative case).

- [ ] **APC patch -- verify whether the Re-Volt-source "bug fix" is right.** Re-Volt 1.4.0's `AreaPowerControllerPatches.UsePowerPatch` declares `[HarmonyPatch(nameof(AreaPowerControl.GetUsedPower))]` but takes a `powerUsed` parameter that only matches `AreaPowerControl.UsePower`. Power Grid Plus patches `UsePower` for that one (which is what the body actually implements). If in-game testing reveals the APC stops draining its battery into a downstream network correctly, this is the first suspect -- revert to Re-Volt 1:1.

- [ ] **Retroactive tier enforcement -- decide whether it's too harsh.** The D11 design (kept per round-2 answer) burns the boundary cable on the first power-flowing tick of any pre-existing mixed-tier network. On a 50-cable legacy normal-touching-heavy junction this is one burn (good). On a base where the player has many small such junctions, this could chew through a lot of cable on first install. Reconsider if play-testing shows this is too aggressive; options live in `TODO.md` (config toggle default-off, grace period, warn-then-burn).

- [ ] **Cross-mod compatibility pass (once Power Grid Plus has a tested build).** Install alongside each power-related mod from `RESEARCH.md` Appendix B / D and smoke-test, prioritising the compat-flagged cases:
  - [Re-Volt](https://steamcommunity.com/sharedfiles/filedetails/?id=3587239682) -- it swaps the `PowerTick` instance per network the same way this mod does, so the two fight. Decide: detect Re-Volt and refuse to load / patch `RevoltTick` instead / document as incompatible.
  - [MoreCables](https://steamcommunity.com/sharedfiles/filedetails/?id=3555588082) -- adds cable tiers above vanilla. The three-tier gating must classify them (treat as `superHeavy`-equivalent? reject? configurable?).
  - [CableTypeSwitcher](https://steamcommunity.com/sharedfiles/filedetails/?id=3470776044) and [EL Switche](https://steamcommunity.com/sharedfiles/filedetails/?id=3287183705) -- they create/convert cables at runtime, bypassing the build-time gate; verify the reactive burn-on-join backstop catches the illegal junctions they can produce.
  - [MorePowerMod](https://steamcommunity.com/sharedfiles/filedetails/?id=3243132734) and the big-battery stat mods ([3.6 Megawatt Battery](https://steamcommunity.com/sharedfiles/filedetails/?id=3004087671), [EGC](https://steamcommunity.com/sharedfiles/filedetails/?id=3007388410), [Super XL Wireless Battery (EGC Edit)](https://steamcommunity.com/sharedfiles/filedetails/?id=3706534108), [BuffWirelessBatteries](https://steamcommunity.com/sharedfiles/filedetails/?id=3355912171), [Mod: Jigawatt Battery](https://steamcommunity.com/sharedfiles/filedetails/?id=1785747072), [RRI - Boost Da Powa](https://steamcommunity.com/sharedfiles/filedetails/?id=2359999429)) -- their batteries inherit the discharge-rate limits; check the numbers stay sane (a 230 MW / 1.21 GW battery x 0.7%/tick is a huge wattage; may need a special case or a config).
  - [Deadly Electricity](https://steamcommunity.com/sharedfiles/filedetails/?id=3575959825) -- patches the same vanilla electrical classes / tick; likely incompatible; confirm and document.
  - [Better Power Mod](https://steamcommunity.com/sharedfiles/filedetails/?id=3234916147), [PowerOverhaul](https://steamcommunity.com/sharedfiles/filedetails/?id=3579306377), [Custom Power DeepMiner](https://steamcommunity.com/sharedfiles/filedetails/?id=3491001740), [Super Structures Mod](https://steamcommunity.com/sharedfiles/filedetails/?id=3159841144), [2x Solar Power](https://steamcommunity.com/sharedfiles/filedetails/?id=1669621266), [Realistic Solar Constants](https://steamcommunity.com/sharedfiles/filedetails/?id=1475742702) -- generation-buff / per-device-tweak mods; expected to coexist, but a buffed solar/wind output just makes the heavy-tier farm bigger; sanity-check.
  - The rest (recipe/content/economy/QoL mods, IC10 scripts) -- expected fully orthogonal; spot-check a couple.
  - Record results in this repo's `Research/` (a compat page) or this mod's `RESEARCH.md`.

## Open decisions and housekeeping

- [ ] **Push or keep `master` local.** Master is well ahead of `origin/master` with the in-development commits. The mod isn't on the Workshop yet, no public consumer; the only question is whether to mirror the local history up to origin so it isn't at risk of an accidental hard reset. Decide: push, or sit on the local branch until first Workshop publish.
- [x] **`Mods/PowerGridPlus/RESEARCH.md` rewrite.** Done in 2026-05-14: the renamed PLAN.md was converted into the proper RESEARCH.md shape (overview / architecture / file walkthrough / patch catalog / decision log with round-2 + round-3 supersedes notes / pitfalls / open questions). The forward-looking plan content is preserved as the decision log.
- [x] **In-chat burn message (declined).** Confirmed declined 2026-05-14: the burned-cable wreckage hover tooltip is the only player-facing feedback for tier mismatches. No chat hook. Mod has zero in-chat messages.
- [ ] **Considered features -- pick one (or none) and start scoping.** Cable-heat dissipation (flavour), Absorb Omni Transmitter Settings (scope widening), Craftable RTG (recipe-only). Details in the "Considered features" section below.
- [ ] **`Settings.cs` Heavy-Cable Device List vs draw verification.** The hardcoded high-draw machine whitelist (`CarbonSequester`, `FurnaceBase`, `ArcFurnace`, `Centrifuge`, `Recycler`, `IceCrusher`, `HydraulicPipeBender`, `DeepMiner`) was assembled from community consensus; only the first three are verified-by-decompile to draw >5 kW. Drop or extend by playing through with an InspectorPlus `types=[Device], fields=[DisplayName, UsedPower, MaxUsedPower, GetUsedPower(network)]` sweep on a high-load save.
- [ ] **Real `MaxVoltage` of heavy / superHeavy cable** -- recorded as an Open Question in `Research/GameClasses/Cable.md`. Needed for any "burn at exactly the right wattage" tuning; also informs how aggressive the gradual-burn rate is on heavy vs normal.
- [ ] **Heavy and insulated cable coil prefab names** -- Open Question in `Research/GameClasses/Cable.md`. Super-heavy resolved (`ItemCableCoilSuperHeavy`); the heavy and insulated names are needed if we ever extend the NEW-2 cost rule beyond super-heavy.

## Pointers

- `RESEARCH.md` -- the design brief and decision log D1-D13 (was `PLAN.md`; needs the proper `RESEARCH.md` rewrite).
- `Research/GameClasses/Cable.md`, `PowerTick.md`, `Transformer.md`; `Research/GameSystems/StructurePlacementValidation.md`, `DevicePowerDraw.md`, `RecipeDataLoading.md` -- the curated game internals this mod relies on.
- `.work/revolt-source/` -- read-only clone of Re-Volt (the upstream this mod derives from).
