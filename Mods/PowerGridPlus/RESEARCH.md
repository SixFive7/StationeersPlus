# Power Grid Plus -- RESEARCH

Project-scoped internals: how the mod is built, every patch site and what it does, the design decisions behind the choices, and the game-internals knowledge the mod relies on. Forward-looking work and pending decisions live in `TODO.md`, not here.

The mod is a derivative of Sukasa's [Re-Volt](https://github.com/sukasa/revolt) (MIT, Copyright (c) 2025 Sukasa) trimmed to a pure-patch subset, plus original features on top. A read-only clone of the upstream lives at `.work/revolt-source/`.

---

## 1. Overview

Power Grid Plus rewrites the cable-network power tick and adds a three-tier transmission-voltage policy on top of the vanilla cable types. The device-fix half (transformer / battery / APC exploit and leak fixes) is inherited from Re-Volt; the atomic tick, the global allocator with its three flow classes (rigid demand, soft storage charge, elastic storage discharge), the fault system, and the segment adapter contract are original. The voltage-tier half (`normal`, `heavy`, `superHeavy` cables as three mutually exclusive voltages, bridged only by transformers / APCs; per-device tier rules; reactive cable-burn enforcement; burn-reason tooltips on wreckages) is original. Two further original additions round out the mod: a writable per-device `LogicPassthroughMode` that makes power-network bridging devices (transformers, stationary batteries, and linked power transmitter / receiver dishes) logic-transparent, and a deterministic `CableNetwork` / `StructureNetwork` merge fix that keeps network `ReferenceId`s identical across multiplayer peers.

It is a **pure-patch mod**: no asset bundle, no new prefabs, no recipes for new items. The only added content is a `GameData/cable-recipes.xml` overlay that doubles the super-heavy cable coil's crafting cost. Removing the mod from a save reverts behaviour to vanilla; the side-car XML the mod writes into the save ZIP for per-device `LogicPassthroughMode` overrides is skipped silently by the vanilla loader. (Burn-reason wreckage state is runtime-only and is not persisted; see section 6.)

---

## 2. Mod identity

| Field | Value |
|---|---|
| Display name | Power Grid Plus |
| Code name | PowerGridPlus |
| Folder / `.sln` / `.csproj` | `Mods/PowerGridPlus/PowerGridPlus.sln` + `Mods/PowerGridPlus/PowerGridPlus/PowerGridPlus.csproj` |
| `RootNamespace` / `AssemblyName` / DLL | `PowerGridPlus` |
| Plugin GUID / `ModID` | `net.powergridplus` |
| C# namespace root | `PowerGridPlus` |
| Workshop ID | none yet (assigned at first publish; goes in `About.xml` `<WorkshopHandle>`) |
| Dependencies | BepInEx + StationeersLaunchPad (+ LaunchPadBooster). No LibConstruct, no StationeersMods asset-bundle infrastructure. |
| Multiplayer | `MOD.Networking.Required = true` -- the power-tick rewrite runs host-side and every client needs the matching DLL. |
| License | Apache 2.0 for original code (repo standard); Re-Volt-derived files retain their MIT notice (see section 11). |

The mod was seeded from `Mods/Template/` and graduated from `Plans/` to `Mods/` during the 2026-05-12 / 05-13 build sessions.

---

## 3. Architecture

> **Revision history for this section.** The 2026-06-10 rework (v0.2.0) deleted the Re-Volt-derived `PowerGridTick` subclass, its routing prefixes, and `DevicePatches.AssessPower`; the unmodified vanilla `PowerTick` runs inside the atomic phases and every behaviour is delivered through device-method patches plus the global allocator. The 2026-07-02 rearchitecture then unified the flow classes (storage charge became a first-class Soft flow riding the same backward/forward sweep as rigid demand, replacing the single-pass surplus walk), made every routed segment publish its full presentation totals, formalized bridge devices behind a segment adapter contract, added the PowerTransmitterPlus billing-ownership handshake, and adopted the vanilla `_powerProvided` ledger plus the Powered flag (load sweep, per-tick settle, healthy-set presentation). The repo-root `POWER.md` was revised in step (2026-07-03) and is again the authoritative algorithm spec end to end; this section is its prose companion with file-level anchors.

### 3.1 Plugin bootstrap

The plugin is a `BaseUnityPlugin` (`PowerGridPlus.Plugin`) whose `Awake` binds the BepInEx config and registers `Prefab.OnPrefabsLoaded` as the deferred patch site. Inside that callback the plugin sets `MOD.Networking.Required = true`, registers the LaunchPadBooster network messages and the join-suffix serializer, runs `Harmony.PatchAll()`, calls `CableCostPatches.ApplyRecipeCost()` (runtime override of the cable-recipes XML overlay), and calls `Ic10ConstantsPatcher.Apply()` (extends `ProgrammableChip.AllConstants` and the syntax-highlighting `ScriptEnum` arrays). The whole block is wrapped in try/catch -- a failure logs fatal but does not crash the game.

### 3.2 The three-authority problem and the two shared channels

Three authorities compute wired power for the same watts: the vanilla per-network relaxation (each `CableNetwork` settles independently, coupled to its neighbours only by a one-tick-lagged `PotentialLoad` mirror), the PowerGridPlus atomic allocator (one global solve per tick), and, when PowerTransmitterPlus is installed, its wireless transfer-debt billing. They share exactly two narrow channels: the `GetGeneratedPower` advertise and the per-device `_powerProvided` debt ledger. The pre-2026-07-02 code let each authority assume it owned those channels, which produced four recurring gap classes: (a) incomplete presentation (an allocator-granted flow with no carrier on both terminals of a segment does not exist in-game), (b) ledger / advertise ownership ambiguity between the two mods, (c) vanilla settle semantics leaking through (the Powered flag, the strict power-met test, unbounded battery demand, the never-zeroed ledger), and (d) unmodelled bridges needing bespoke patch clusters. The rearchitecture answers each in turn: one solver with one flow representation (3.4), complete per-segment presentation (3.5) behind an explicit adapter contract (3.6), cross-mod billing ownership (3.7), and vanilla-semantics adoption (3.8 / 3.9).

The motivating defect: on the Luna save, two 25 kW nuclear-battery chargers idled at 0 W forever with no fault anywhere. Battery charge was granted by a separate surplus walk whose result was published only for APC segments; transformer and wireless segments advertised and billed rigid figures only, so the granted 50 kW of charge had no carrier on either terminal, vanilla computed a zero power ratio on the battery network, and the state was a permanent fault-free fixed point. The fix is structural, not a patch: every granted flow now publishes on both terminals of every segment kind.

### 3.3 The atomic tick

The atomic tick (`Patches/AtomicElectricityTickPatch.cs`) owns `ElectricityManager.ElectricityTick` via prefix-return-false (host-only, `GameManager.RunSimulation` gate) and runs these phases per tick:

1. **Load hooks** (one-shot, before anything reads state): the unknown-bridge census (3.11) and the ledger-adoption sweep (3.9). Both are armed at plugin load and re-armed by `FaultRegistryLoadPatches` on every world load, so a stale saved ledger can never bill through the first tick.
2. **OBSERVE**: vanilla `PowerTick.Initialise + CalculateState` per network (fresh `Required` / `Potential`); `BreakableFuses` / `BreakableCables` are cleared once per tick (vanilla never clears them).
3. **PROTECT**: server-side OFF-as-reset sweep (`OffAsResetSweep.cs`), wrong-tier cable burns (`VoltageTierEnforcer.cs`), CYCLE_FAULT detection (`CycleGraphBuilder.cs`, directed-SCC over `SegmentingDeviceRegistry.cs`), producer-isolation VARIABLE_VOLTAGE_FAULT (`VariableVoltageFaultDetector.cs` + `ProducerClassifier.cs`, with a cable-burn fallback for unclassified producer-like devices), and a re-observe when anything newly faulted.
4. **ALLOCATE**: `PowerAllocator.RunAtomic` (3.4), then the per-tick full fault snapshots to clients (`FaultRegistrySnapshotMessage.cs`).
5. **ENFORCE**: vanilla `Initialise + CalculateState + ApplyState` per network, iterated in the shallow-first topological order ALLOCATE published (`PowerAllocator.ShallowFirstNetworks`; processing a contributor's input network before its output network kills the one-tick supply lag that made multi-stage transformer chains oscillate). The device-method patches make every allocator decision visible (lockout zeroes via `Patches/CycleFaultEnforcementPatches.cs` + `Patches/TransformerExploitPatches.cs` + `Patches/ProducerFaultEnforcementPatches.cs`; presentation totals and share caps via the transformer / wireless / battery / APC / umbilical patches). Vanilla `_powerRatio` partial scaling stays as the safety net for anything unclassified.
6. **ENFORCE tail**: after all ApplyState mutations have settled, `PoweredPresentation.ReconcileEnforceTail()` re-asserts Powered=True on idle healthy segmenters (3.8) and `LedgerAdoption.SettleEnforceTail()` settles every owned segmenter ledger (3.9). Runs before the device tick, so the IC10 LOGIC phase reads the settled ledger (transformer `PowerActual` equals actual current throughput).
7. **DEVICE + LOGIC**: vanilla copies (`AllPoweredThings.OnPowerTick`, then `CircuitHolders.Execute`).

### 3.4 The allocator: one demand vector, three flow classes

`PowerAllocator.RunAtomic` builds a per-tick model (GATHER), computes a true topological order over conducting contributor edges (ORDER, Kahn; cycle members are already locked so the live graph is a DAG), iterates a backward-desire / forward-supply fixed point with re-decidable shed and grow-only overload (DECIDE, max `2N+4` rounds), computes net-local elastic shares, publishes the presentation caches and snapshots, and audits itself (conservation check). Every ordering is integer-keyed (depth, priority, ReferenceId; float claims quantised to whole Watts) for multiplayer determinism.

Three flow classes ride the same sweep:

- **Rigid** is ordinary consumer demand plus the contributor pulls that serve it, and it alone drives every fault decision (shed, structural overload, supply overload). The backward pass splits each network's residual rigid need (demand minus generators) over its suppliers priority-tier-first and EffCap-proportional within a tier, capped per contributor at its effective capacity; `DesiredPull = DesiredThroughput * max(m, 1) + quiescent`. The forward pass grants in topo order and re-decides shedding against settled upstream supply each round.
- **Soft** is storage charge (battery, APC cell, umbilical cell). It propagates leaf-to-source in the SAME backward walk with the same tier-first proportional splitter, but each contributor's soft capacity is `EffCap - rigid desired throughput`: soft never displaces rigid. Forward, soft is granted per network out of the FIRM residual only (generators plus contributor inflow left after rigid loads and granted rigid pulls; elastic discharge never funds charging), proportionally over local charge requests and consumer soft pulls, capped by the weakest cable's remaining headroom. Unmet soft desire is silently clamped: never a shed, never an overload, never a lockout, never a dead-input cue. The proportional split kills the deleted surplus walk's double-count by construction (parallel contributors divide a downstream request instead of each propagating it whole).
- **Elastic** is storage discharge. It stays net-local: it covers only the rigid shortfall left after generators and contributor inflow on its own output network, split proportionally against effective discharge caps, and never propagates across contributors.

The contributor's quiescent draw is carried exactly once: on the rigid pull whenever the contributor carries any rigid flow, else on the soft pull. Because soft rides the same priority tiers as rigid, a high-priority transformer's storage charges before a low-priority one's (a documented behaviour change vs the priority-blind surplus walk).

### 3.5 Per-segment presentation totals (the Stage 1 fix)

The publish tail computes, for every routed contributor, `TotalThrough = Throughput + SoftThrough` (output-side delivery) and `TotalPull = TotalThrough * max(m, 1) + quiescent` (input-side bill, exact by construction whenever the seg conducts), and writes `TransformerSupplyCache.Set(RefId, TotalThrough, TotalPull)` for EVERY routed seg kind: Transformer, wireless pair, and APC alike. The APC additionally publishes `ApcPassthroughCache = TotalThrough` (its fresh input-side passthrough bill), `ApcCellDischargeCache` (the cell-only share, so `DischargeSpeed` means the cell rate), and `SoftSupplyShareCache = TotalThrough + cellShare` (vanilla `GetGeneratedPower` bundles passthrough and cell). Per-device storage charge shares go to `SoftDemandShareCache`, and the storage patches min-clamp against them so each store charges exactly its grant. Inactive contributors (shed / overloaded / cycle-faulted) carry all-zero totals. Publishing totals for every kind is what guarantees a granted soft flow has a carrier on both terminals of its segment: a battery charging behind a transformer or wireless pair sees the charge advertised downstream AND billed upstream in the same tick. Deleted with this change: the surplus walk, `GrantThrough`, `SoftReqTotal`, and the computed-but-never-consumed `Net.ElasticDelivered`.

One user-visible consequence: network `PotentialLoad` / `RequiredLoad` mirrors, hover tooltips, and IC10 reads of transformer / wireless throughput now include storage-charge flow, not just running machines.

### 3.6 The segment adapter contract (`SegAdapters.cs`, Stage 2b)

GATHER no longer open-codes each bridge class; it consults one `ISegAdapter` per modelled class. An adapter answers `Describes(device)` (pure type membership) and `TryDescribe(device, out SegSpec)` (the device's per-tick PHYSICAL description: flow kind, terminal networks, capacities, distance multiplier, quiescent draw). GATHER attaches allocator POLICY on top (priority, lockout state, shed bookkeeping). Two flow models:

- **Routed**: power crosses the device within the tick it is granted; the device becomes one allocator Seg. The output side delivers `TotalThrough`; the input side bills `TotalThrough * max(m, 1) + quiescent` the same tick.
- **Buffered**: power stops in an internal cell; nothing crosses within the tick. The device contributes storage roster entries instead of a Seg (cell charge = Soft demander, cell discharge = Elastic supplier); the physical crossing happens outside the allocator.

| Adapter | Device class | Kind | Capacity | m | Notes |
|---|---|---|---|---|---|
| `TransformerAdapter` | `Transformer` | Routed | `min(Setting, cable caps) - quiescent` | 1 | Step-up classified (never sheds, section 5.2 of `POWER.md`). |
| `WirelessPairAdapter` | linked `PowerTransmitter` + `PowerReceiver`, ONE seg anchored on the transmitter | Routed | static link rating (PowerTransmitterPlus effective Max Transfer Capacity; 0 = unlimited maps to a finite 1e9 sentinel, never `float.MaxValue`; without PowerTransmitterPlus, vanilla `MaxPowerTransmission` minus the distance-delivery loss), bounded by `min(inputCableCap / m, outputCableCap)`, minus both halves' quiescent | interop `m >= 1` | The rating is STATIC, never the live `InputNetwork.PotentialLoad` (a live read collapses to a cross-tick zero fixed point on transformer-fed sources). The receiver half never yields its own description; a cycle-faulted partner locks the pair. |
| `ApcAdapter` | `AreaPowerControl` | Routed | cable caps only (`CapacitySetting = float.MaxValue`: no hit-max rule) | 1 | The internal cell is NOT part of the routed description; GATHER enrolls it separately as storage. |
| `UmbilicalAdapter` | `RocketPowerUmbilical` (Male + Female; shared abstract base since game 0.2.6403) | Buffered | soft = `min(charge rate, input cable cap, cell headroom)`; elastic = `min(discharge rate, output cable cap, cell store)` | n/a | Formalizes the store-and-forward model: grid to near cell this tick, cell to partner cell in vanilla phase 2, partner cell to far grid on a later tick. Since 0.2.6403 the phase-2 crossing is BIDIRECTIONAL (the Male pushes station to rocket; the Female runs its own TransferProgress-gated rocket-to-station transfer). The allocator never models the crossing itself; it only sees each cell's level change between ticks. See `Research/GameClasses/RocketPowerUmbilical.md`. |

`Battery` and `PowerReceiver` carry no adapter of their own: the battery is pure storage enrolled directly by GATHER, and the receiver is described through its linked transmitter. The unknown-bridge census (3.11) uses `SegAdapters.AnyDescribes` to tell modelled classes from unmodelled ones.

### 3.7 PowerTransmitterPlus interop tiers and the billing ownership handshake

`PowerTransmitterPlusInterop.cs` resolves once at first use (no per-tick reflection; PowerGridPlus never hard-references PowerTransmitterPlus) and degrades through three tiers:

1. **ModApi** (PowerTransmitterPlus 1.9.0+, `PowerTransmitterPlus.ModApi` with `Version >= 1`): binds `EffectiveMaxCapacity`, `TryGetLink` (returns false when unlinked, killing the stale-distance leak), `SourceDrawMultiplier`, and the `GetTransferDebt` / `SetTransferDebt` ledger accessors (bound leniently), then calls `ClaimBillingOwnership("net.powergridplus")` exactly once. While the claim is held, PowerTransmitterPlus's native wireless debt billing (the `UsePower` debt inflation, the `GetUsedPower` cap lift, and the standalone debt ceiling) stands down and this mod's allocator is the single billing authority; the capacity advertise, receiver drain-cap lift, beam visuals, and link handling stay active on the PowerTransmitterPlus side. The claim is per-call-checked over there, so a late claim is safe; a rejected claim (another owner) leaves the read surface bound and falls back to delivery-gate containment.
2. **Legacy** (the shipped Workshop 1.8.0 line): `DistanceCostShared.SourceDrawMultiplier` (public wrapper, added after the 1.8.0 tag) or the internal `GetMultiplier` (the identical computation; the shipped build has only this), plus `MaxCapacityConfigSync.GetEffectiveMaxCapacity` and the vanilla `_linkedReceiverDistance` field. No ownership handshake exists at this tier; the `DeliveryGatePatch` + fresh-pull prefixes keep the ledgers bounded instead, exactly as before the ModApi existed.
3. **Absent**: `m = 1` and the vanilla link rating (`MaxPowerTransmission` minus the distance-delivery loss).

Exactly one Info line states the resolved tier and, on the ModApi tier, the claim outcome. The tiers agree by construction: the ModApi forwards to the same internals the legacy tier binds, so switching tiers never changes the allocator's numbers for the same world state. The source-draw multiplier `m = 1 + k * distance_km` (k host-synced, default 5) inflates the transmitter's input-side draw; delivery is never derated for distance under PowerTransmitterPlus.

### 3.8 Powered presentation policy (Stage 3)

Vanilla `PowerTick.ApplyState` derives a device's Powered flag from what it consumed this tick, and its only un-power path is gated on `Device.AllowSetPower(net)` (ApplyState is the sole caller in the game). A healthy segmenter idling at zero throughput bills a fresh pull of 0, so vanilla flips it to Powered=False: players read "device broken" on a charger whose batteries are simply full. Policy: HEALTHY = enrolled in this tick's roster, carrying no fault, and either conducting (`TotalThrough > 0`) or idle on an input network that has effective supply and no unmet rigid demand. ALLOCATE publishes the healthy set and the enrolled-seg roster by volatile reference swap (`PoweredPresentation.cs`). Two halves around vanilla's own writer: the four `AllowSetPower` postfixes (`Patches/PoweredPresentationPatches.cs`, on Transformer / AreaPowerControl / PowerTransmitter / PowerReceiver) return false for healthy devices, so ApplyState can never un-power one (no False/True double transition); the ENFORCE tail re-asserts Powered=True on healthy segmenters vanilla left dark via `SetPowerFromThread` (fires only on a real False-to-True edge; zero steady-state churn). A pair publishes health under both halves' ReferenceIds. An idle segmenter on a DARK input (night-time solar feed) is deliberately NOT healthy: vanilla un-powers it, matching the dead-input hover cue. Server-authoritative by construction: ApplyState and the tail only run in the host's atomic tick, and `SetPower` routes through the normal interactable replication; on a client the published set stays empty and the postfixes no-op.

### 3.9 Ledger adoption (Stage 3)

The private `_powerProvided` on Transformer / AreaPowerControl / PowerTransmitter / PowerReceiver is vanilla's deferred billing handshake (UsePower charges it output-side, GetUsedPower bills it, ReceivePower repays it). The game never zeroes it; at 0.2.6403 it is not serialized into saves (no SaveData member carries it, per the write-site census in `Patches/LedgerAuditPatches.cs`), so a nonzero value is runtime accumulation within the session, residue from an older-version save, or an external writer. Under fresh-pull billing it is a vestigial accumulator that drifts: a conducting wireless transmitter is drained roughly `TotalThrough` on the wireless network but repaid the full `TotalPull = TotalThrough * m + quiescent` on the source network, a structural drift of `-((m - 1) * TotalThrough + quiescent)` per conducting tick under the ownership handshake. Left alone that compounds into giant negative credits over a session (observed: -176,226 on Luna transmitter 464520), which become free energy the moment vanilla billing reads the ledger again (`min(cap, debt)` bills 0 until the credit burns off). Two counter-measures in `LedgerAdoption.cs`, both host-only inside the atomic tick:

- **World-load sweep**: on the first atomic tick after a load (armed at plugin load, re-armed by `FaultRegistryLoadPatches`), zero the ledger on every modelled segmenter, both signs, NaN included, BEFORE the first OBSERVE can serve a stale value through the vanilla `GetUsedPower` fallback. One Info line per zeroed device plus a summary.
- **Per-tick ENFORCE-tail settle**: after all ApplyState mutations, negatives clamp to zero (silent counter; that is the free-energy metric) and each OWNED ledger settles to its vanilla-equivalent standing value: Transformer := `TotalThrough` (only while exploit mitigation is on; it also makes IC10 `PowerActual` read actual current throughput), transmitter := 0 (its bill was paid in full this tick), receiver := `min(debt, TotalThrough)` (preserves the live relay cycle that drives the wireless drain and the beam visuals, shears only stuck overshoot). Writes inside a 0.01 W tolerance are skipped. The earlier `[0, 2 * TotalPull]` upper-bound rewrite is gone: under fresh-pull billing vanilla loses its self-draining `min(cap, ledger)` force, residue strands instead of draining, and healthy steady values legitimately exceeded that bound. An exact ledger audit (always-on, no config entry) replaces the earlier approximate high-water detector. Because the settle pins every owned ledger to a known value at the ENFORCE tail and nothing legitimate writes `_powerProvided` outside ApplyState (write-site census in `Patches/LedgerAuditPatches.cs`), ownership violations are detected exactly, in four classes: a tick-boundary check (the field must equal the recorded post-settle value exactly at the next tick's start), a per-mutation bracket check (Priority.First/Priority.Last wrappers around all six vanilla write sites; each mutation's BEFORE must equal the last recorded AFTER), a settle-tail identity (pre-settle field must equal boundary plus the observed shadow sum within 0.01 W), and a non-finite backstop (NaN/Infinity, repaired by the settle). Counts are exact; the boundary check emits one aggregated warning per 600 ticks while new anomalies arrive, with totals per class and the worst offender. Zero anomalies means zero log lines; any anomaly means something outside PowerGridPlus wrote a ledger it owns.

The AreaPowerControl is swept at load but deliberately NOT settled per tick: its ledger is load-bearing under this mod. Vanilla `APC.UsePower` drains the internal cell against a POSITIVE ledger one tick after the cell covers an output shortfall, and its `GetUsedPower` takes `Max(ledger, UsedPower)` so a negative never discounts a bill; clamping it would hand the cell free energy. Wireless halves route through the ModApi debt accessors when resolved, else through cached `FieldInfo` on the vanilla field.

### 3.10 Conservation checker

`ConservationChecker.cs`, gated by `Server - Diagnostics` / `Enable Conservation Check` (default on). Two invariants over the allocator's own converged grants, audited at the end of ALLOCATE, so a violation is by definition a PowerGridPlus bug and never a player problem: per network, granted inflow (rigid seg inflow + soft inflow + granted elastic + the implied generator draw, derived as the residual clamped to `[0, genSupply]` so both failure directions surface) must equal granted outflow (rigid served + storage charge + rigid pulls + soft pulls) within 0.5 W; per contributor seg, `TotalPull == TotalThrough * max(m, 1) + quiescent` whenever the seg conducts (checked one-sided against the quiescent when it does not). Warnings are throttled to once per network / per seg per 600 ticks (about 5 minutes at the 2 Hz power tick).

### 3.11 Shortfall diagnostics and the unknown-bridge census

`ShortfallDiagnostics.cs` labels every allocator network's end-of-tick RIGID state and publishes one immutable snapshot per tick by volatile swap: **Served** (0, no unmet rigid demand), **Dry** (1, unmet with every remaining feed genuinely exhausted: source-side shortage, honest darkness), **Throttled** (2, unmet with a deliberately closed valve: a locked / shed / overloaded supplier, a zero-effective-capacity `Setting = 0` firewall, or a locked elastic), and **Deadlock** (3, unmet while the allocator's own accounting says supply existed: an active supplier had headroom above 0.5 W AND its input network retained undelivered supply). Deadlock is the invisible-deadlock regression shape and must be zero on a healthy build; it is checked before the throttle rung so a closed valve on the same net cannot mask a routing failure. Diagnostics only (nothing in the mod reads it back); the ScenarioRunner `pgp-rearch-suite` census joins the snapshot via reflection, and the byte values are a cross-assembly contract.

`UnknownBridgeCensus.cs` (Stage 2b unknown-bridge lane): on the first atomic tick after a world load, every scene device that is an `ElectricalInputOutput` subclass but is neither in `SegmentingDeviceRegistry`'s known set nor described by any adapter is reported, one Info line per TYPE. Reporting is the only handling: an unknown bridge keeps its vanilla power methods unpatched and GATHER sums its `GetUsedPower` / `GetGeneratedPower` as plain rigid demand / generation on each side, the conservative fallback. The census makes the gap visible instead of silent when a third-party mod ships its own two-port power device.

### 3.12 Non-mutating cable caps and fault visuals

Cable caps are non-mutating: `CableMax.cs` returns the configured per-tier Watts caps and every reader consults it, including the patched vanilla burn check (`Patches/PowerTickPatches.cs` replaces `GetBreakableCables` with the generator-only-overflow rule; transformer-derived overflow trips OVERLOAD in the allocator instead). `Cable.MaxVoltage` in the save is never written.

Fault visuals: `BrownoutFlashBehaviour.cs` (generalised to any device with an OnOff button, attached by `Patches/TransformerFlashAttachPatches.cs`), the single-source hover lines + precedence CYCLE > VVF > OVERLOAD > SHED in `FaultHover.cs`, body hover via `Patches/FaultHoverPatches.cs` (seven `GetPassiveTooltip` override targets), button hover via `Patches/TransformerHoverErrorPatches.cs`, and the smooth countdown via `ElectricityTickCounter.SmoothSeconds` (host) / `MonotonicClock.cs`-based client mirrors.

### 3.13 Functional regions

Functional regions, each rooted at a single static class or registry:

- **Stationary battery rate limits + storage shares** (`Patches/StationaryBatteryPatches.cs`, `Patches/BatteryLogicPatches.cs`). Per-prefab absolute Watts caps (small / large / nuclear / rocket medium / auxiliary rocket) min-clamped against the allocator's per-device share caches (`SoftDemandShareCache` on charge, `SoftSupplyShareCache` on discharge) so each battery charges and discharges exactly its grant; re-implements `Battery.ReceivePower` with a charge-efficiency factor and a low-power-trickle floor. Exposes `MaxChargeSpeed` / `MaxDischargeSpeed` / `ChargeSpeed` / `DischargeSpeed` (6583-6586); the prior `ImportQuantity` / `ExportQuantity` repurpose is removed.
- **Transformer / APC presentation** (`Patches/TransformerExploitPatches.cs`, `Patches/TransformerLogicPatches.cs`, `Patches/AreaPowerControlPatches.cs`). The transformer's `GetGeneratedPower` / `GetUsedPower` serve the allocator's converged totals from `TransformerSupplyCache` (output = `TotalThrough`, input bill = `TotalPull`, replacing the lagging vanilla `_powerProvided` handshake with fresh-pull billing), which closes the free-power exploit and restores quiescent draw; `PowerActual` is exposed for IC10. The APC patches close the idle leak, bill the fresh passthrough from `ApcPassthroughCache`, apply cable caps on both sides, and expose the soft-power LogicTypes. `Patches/TransformerSettingInitPatch.cs` initialises `Setting = OutputMaximum` on new construction (load order makes saved values win for loaded worlds).
- **Wireless pair presentation** (`Patches/PowerTransmitterDrawPatches.cs`). The transmitter bills its input network the fresh pair pull (delivered times the distance multiplier plus quiescent) from `TransformerSupplyCache`, and the `DeliveryGatePatch` postfix (Priority.Last) clamps the advertised wireless delivery to the granted `TotalThrough`, so delivery follows the grant and a transfer debt cannot be seeded. Under the PowerTransmitterPlus 1.9.0+ ownership handshake this is defence in depth; against the Workshop 1.8.0 build it is the containment path.
- **Rocket umbilical support** (`Patches/RocketUmbilicalPatches.cs`). Rate caps min-clamped against the storage share caches, plus the four soft-power LogicTypes on `RocketPowerUmbilicalMale` / `RocketPowerUmbilicalFemale` (the Female exposes them through the `Device`-base declarations with an instance filter; the slot-keyed overloads force explicit `typeof(LogicType)` argument arrays on those patches).
- **Burn-reason persistence** (`BurnReasonSideCar.cs`, `Patches/BurnReasonSaveLoadPatches.cs`). Per-wreckage reasons survive save / load via the established side-car ZIP-entry pattern; mod-removal safe.
- **Voltage tiers** (`VoltageTier.cs`, `Patches/VoltageTierPatches.cs`, `Patches/CableCostPatches.cs`). The tier policy itself (`IsAllowedOnTier`, `ResolveMixedTierNetwork`, `BurnCableForMisplacedDevice`) plus the placement-preview rejections (cable-on-cable and cable-on-device) and the super-heavy cable cost runtime override.
- **Burn-reason tooltip plumbing** (`BurnReasonRegistry.cs`, `Patches/BurnReasonPatches.cs`). Each `Cable.Break()` call site registers a reason string in a `ConcurrentDictionary<Grid3, string>` keyed by the dying cable's cell. A postfix on `CableRuptured.OnRegistered` consumes the entry and attaches it to the wreckage via a `ConditionalWeakTable<Thing, string>` sidecar. A postfix on `Thing.GetPassiveTooltip` filtered to `__instance is CableRuptured` appends "Burned: \<reason\>" to the wreckage's hover tooltip.
- **Logic passthrough** (`LogicTypeRegistry.cs`, `PassthroughModeStore.cs`, `PassthroughSideCar.cs`, `Patches/LogicPassthroughPatches.cs`, `Patches/TransformerPassthroughLogicPatches.cs`, `Patches/InheritedLogicablePassthroughLogicPatches.cs`, `Patches/PassthroughSaveLoadPatches.cs`, `Patches/LogicableInitializePatch.cs`, `Patches/EnumNamePatches.cs`, `Ic10ConstantsPatcher.cs`). The writable `LogicPassthroughMode` slot (ushort 6577 from the central `Patterns/Logic/LogicTypeNumbers.cs` catalogue), plus the postfix on `CableNetwork.RefreshPowerAndDataDeviceLists` that merges the other side's device list into the local one when a bridging device's mode is 1. Five device kinds bridge: `Transformer`, `AreaPowerControl`, `Battery`, `PowerTransmitter`, and `PowerReceiver`. Four of them (`Transformer`, `Battery`, `PowerTransmitter`, `PowerReceiver`) expose the writable per-device mode and persist it across save / load via an XML entry in the save ZIP; `AreaPowerControl` has no per-device mode and bridges whenever its server toggle is on. The mode-bearing logic ports are wired on `Transformer` directly (it overrides the four `Logicable` methods) in `TransformerPassthroughLogicPatches.cs`, and on `Battery` / `PowerTransmitter` / `PowerReceiver` through a base-`Device` patch in `InheritedLogicablePassthroughLogicPatches.cs` (see section 7 for why the base type is patched). Three additions on top of the merge keep UI consumers and multiplayer clients correct: (1) a device-list refresh cascade (`Patches/CableNetworkPatches.cs` + `Patches/DishLinkPatches.cs`, marshaled to the main thread by the shared `Patterns/Threading/MainThreadDispatcher.cs`) re-fires the vanilla `OnDeviceConnectToNetwork` notification on the affected networks whenever a mode flip, a settings toggle, or a wireless dish link / unlink changes the merged set, so motherboard dropdowns and IC-housing / sensor caches rebuild without a replug (the vanilla merge mutates `DataDeviceList` silently and notifies no consumer; see `Research/GameClasses/Motherboard.md`); (2) the per-device mode and the four server toggles replicate to clients (`PassthroughModeMessage`, `PassthroughSettingsMessage`, and an `IJoinSuffixSerializer` join snapshot) so each peer computes the same merge, with `PassthroughSettingsSync.Effective*` reading the host value on a client; (3) a client-side mirror of `PowerReceiver.LinkedPowerTransmitter` (`Patches/DishLinkPatches.cs`), because vanilla replicates only the transmitter-side link reference, which would otherwise make the dish bridge one-directional on a client (see section 6).
- **Network merge determinism** (`Patches/MergeDeterminismPatches.cs`). Prefixes on `CableNetwork.Merge(List<CableNetwork>)` and `StructureNetwork.Merge(List<StructureNetwork>, out)` sort the input list by `ReferenceId` before vanilla picks `list[0]` as the survivor, so server and client always keep the same network alive after a merge. Multiplayer-correctness only; no device behaviour changes.

The shared central LogicType catalogue at `Patterns/Logic/LogicTypeNumbers.cs` is linked into the `.csproj` so the integer values come from one source of truth across every SixFive7 mod that registers a custom `LogicType`.

---

## 4. File walkthrough

Files under `Mods/PowerGridPlus/PowerGridPlus/`. Headers in Re-Volt-derived files retain the MIT notice; everything else is original Apache 2.0.

### Plugin / config

- **`Plugin.cs`** -- `BaseUnityPlugin` entry. `Awake` binds the config (`Settings.Bind(Config)`) and hooks `Prefab.OnPrefabsLoaded` so all patches are deferred until the game finishes loading prefabs. The callback sets `MOD.Networking.Required = true`, registers the LaunchPadBooster messages (passthrough mode / settings, priority, fault snapshots) and the join-suffix serializer, runs `Harmony.PatchAll()`, then `CableCostPatches.ApplyRecipeCost()`, then `Ic10ConstantsPatcher.Apply()`. Refuses to load alongside Re-Volt (`TryFindIncompatibleMod`).
- **`Settings.cs`** -- every `ConfigEntry<T>` the mod binds. Section names follow the repo convention (`Server - <Topic>`); `(Server-authoritative)` description prefix; `("Order", int)` tags for in-panel ordering. Sections: `Server - Cable Simulation`, `Server - Cable Costs`, `Server - Voltage Tiers`, `Server - Batteries`, `Server - Transformers`, `Server - Area Power Control`, `Server - Power Transmitters`, `Server - Rocket Umbilical`, `Server - Diagnostics`, `Server - Emergency Lights`. The full table is in section 9.

### Atomic tick + allocator core (original)

- **`Patches/AtomicElectricityTickPatch.cs`** -- the atomic tick driver (section 3.3): owns `ElectricityManager.ElectricityTick` via prefix-return-false and sequences the load hooks, OBSERVE, PROTECT, ALLOCATE, ENFORCE (shallow-first), the ENFORCE tail, and the vanilla DEVICE / LOGIC copies.
- **`PowerAllocator.cs`** -- ALLOCATE (section 3.4): GATHER (rigid demand + generator supply per net; contributor / elastic / soft rosters via the adapters), ORDER (Kahn topo, publishes `ShallowFirstNetworks` for ENFORCE), the DECIDE fixed point (`BackwardDesirePass` carrying rigid + soft in one walk, `DetectStructuralOverload` before shed, `ForwardSupplyAndShed` with per-round re-decided shedding and residual-funded soft grants, `DetectSupplyOverload` after), lockout commits (with the elastic-overload network retry and the Option C split-pending deferral), elastic shares, the publish tail (share caches + presentation totals + the Stage 3 snapshots + the shortfall classification), the conservation-check calls, and `SyncFaultSnapshots` (per-tick full fault-registry snapshots to clients).
- **`SegAdapters.cs`** -- the segment adapter contract (section 3.6): `ISegAdapter` / `SegSpec` / `SegAdapterKind` plus the four adapters (Transformer / WirelessPair / Apc routed, Umbilical buffered) and the shared step-up classifier.
- **`PowerTransmitterPlusInterop.cs`** -- the three-tier PowerTransmitterPlus bridge (section 3.7): ModApi with the billing-ownership claim, legacy 1.8.0 reflection, vanilla fallback; plus the `TryGetWirelessDebt` / `TrySetWirelessDebt` ledger accessors the Stage 3 sweep and clamp use.
- **`SegmentingDeviceRegistry.cs`** -- the deterministic segmenter roster (Transformer, AreaPowerControl, Battery, PowerTransmitter, PowerReceiver, the rocket umbilicals; `PowerConnection` excluded as vestigial). `EnumerateSorted` feeds GATHER; `IsSegmenter` is the rigid-sum exclusion filter.
- **Share caches** (`TransformerSupplyCache.cs`, `SoftDemandShareCache.cs`, `SoftSupplyShareCache.cs`, `ApcCellDischargeCache.cs`, `ApcPassthroughCache.cs`) -- the volatile-swapped per-tick publication surface between ALLOCATE and the ENFORCE-phase device patches (section 3.5).
- **Fault registries + detectors** (`BrownoutRegistry.cs`, `OverloadRegistry.cs`, `CycleFaultRegistry.cs`, `VariableVoltageFaultRegistry.cs`, `DeadInputRegistry.cs`, `CycleGraphBuilder.cs`, `VariableVoltageFaultDetector.cs`, `ProducerClassifier.cs`, `OffAsResetSweep.cs`, `SplitPendingRegistry.cs`) -- 120-tick (60 s) lockout state, the directed-SCC cycle walk, producer isolation, the OFF-as-reset sweep, and the in-flight cable-burn split tracking that defers new lockouts on re-partitioning networks.
- **`Patches/PowerTickPatches.cs`** -- targeted fixes on the vanilla `PowerTick` that runs in OBSERVE / ENFORCE: a per-anchor traversal-record clear in `CheckForRecursiveProviders` (vanilla misses cycles past the first anchor), the NaN/Infinity backstop on `CalculateState` (zeroes a poisoned sum and names the offending device via `DeviceOutputSanitizer`), the power-met boundary relaxation on `CacheState` (vanilla's strict `Potential > Required` would darken an exactly-served network, and the allocator reports exact throughput with no headroom), and the deterministic cable-burn rule in `GetBreakableCables` (a cable burns when the 20-tick running average of DIRECT generator power exceeds the weakest cable's `CableMax` cap; transformer / battery overflow trips OVERLOAD instead).
- **`CableMax.cs` / `CableBurnWindow.cs`** -- runtime per-tier Watts caps (never written to the save) and the 20-tick generator-overflow window.
- **`ElectricityTickCounter.cs` / `MonotonicClock.cs`** -- the shared tick counter for lockout expiry plus the smooth client-side countdown clock.
- **`DeviceOutputSanitizer.cs`** -- clamps non-finite device power outputs to 0 at the source and names each broken device once per session in the in-game console.

### Stage 3: Powered presentation + ledger adoption (original)

- **`PoweredPresentation.cs`** -- the healthy-set / roster snapshots (volatile swap from ALLOCATE) and `ReconcileEnforceTail` (assert Powered=True on idle healthy segmenters via `SetPowerFromThread`); section 3.8.
- **`Patches/PoweredPresentationPatches.cs`** -- four `AllowSetPower` postfixes (Transformer / AreaPowerControl / PowerTransmitter / PowerReceiver) that block vanilla ApplyState's un-power path for healthy segmenters.
- **`LedgerAdoption.cs`** -- the world-load `_powerProvided` zero sweep, the per-tick ENFORCE-tail settle of owned ledgers, and the exact ledger audit (tick-boundary identity, shadow sum, non-finite backstop; APC exempt from the settle); section 3.9.
- **`Patches/LedgerAuditPatches.cs`** -- Priority.First/Priority.Last observation wrappers around the six vanilla `_powerProvided` write sites, with the write-site census in the class header; section 3.9.

### Diagnostics (original)

- **`ConservationChecker.cs`** -- the per-net and per-seg conservation audit (section 3.10), config-gated, throttled warnings.
- **`ShortfallDiagnostics.cs`** -- the per-net Served / Dry / Throttled / Deadlock classification snapshot the ScenarioRunner census joins (section 3.11).
- **`UnknownBridgeCensus.cs`** -- the one-shot unmodelled-bridge census at world load (section 3.11).

### Stationary batteries (Re-Volt-derived, MIT; reworked)

- **`Patches/StationaryBatteryPatches.cs`** -- per-prefab absolute charge / discharge caps (`GetChargeCap` / `GetDischargeCap` switch on PrefabName: StructureBattery, StructureBatteryLarge, StationBatteryNuclear, StructureBatteryMedium, StructureBatterySmall; unknown prefabs pass through uncapped), each bounded by the respective cable's `CableMax` and min-clamped to the allocator share caches (charge to `SoftDemandShareCache`, discharge to `SoftSupplyShareCache`). Re-implements `Battery.ReceivePower` to multiply incoming watts by `BatteryChargeEfficiency` (with a low-power-trickle floor: incoming watts under 500 are stored at full efficiency regardless of the config). Reverse-patches `Battery.get_IsOperable`.
- **`Patches/BatteryLogicPatches.cs`** -- exposes `MaxChargeSpeed` / `MaxDischargeSpeed` (the configured caps) and `ChargeSpeed` / `DischargeSpeed` (the live allocated rates) as logic-readable values when `EnableBatteryLogicAdditions` is on.

### Transformer (Re-Volt-derived, MIT; reworked to fresh-pull billing)

- **`Patches/TransformerExploitPatches.cs`** -- prefixes on `Transformer.GetGeneratedPower` / `GetUsedPower` / `ReceivePower`. Output = the allocator's converged `TotalThrough` and input bill = `TotalPull` (both from `TransformerSupplyCache`; conservation holds, so the free-power exploit stays closed and quiescent draw is billed upstream); a shed / overload lockout forces 0 both ways so the protected subnet goes dark cleanly. `ReceivePower` still repays the vanilla `_powerProvided` ledger (which the Stage 3 clamp keeps bounded).
- **`Patches/TransformerLogicPatches.cs`** -- exposes `Transformer.PowerActual` (reads the vanilla ledger, bounded by the Stage 3 clamp; the LOGIC phase runs after the ENFORCE tail) when `EnableTransformerLogicAdditions` is on.

### Area Power Controller (Re-Volt-derived, MIT; reworked)

- **`Patches/AreaPowerControlPatches.cs`** -- closes the ~10 W APC free-power leak, bills the fresh allocator passthrough on the input side (`ApcPassthroughCache`, replacing the one-tick-lagged `_powerProvided` figure), caps charge by the input cable's remaining headroom after the APC's own passthrough (`ComputeChargeCap`, fed by a per-APC `_lastPassthrough` tracked from vanilla `UsePower`), caps output by the output cable, and exposes the soft-power LogicTypes. The earlier `UsePower` prefix from the Re-Volt port was removed 2026-05-28 (it broke the vanilla positive-ledger cell-drain handshake); vanilla `UsePower` is correct and is left alone, which is also why the Stage 3 ledger clamp exempts the APC.

### Voltage tiers (original, NEW-1 / NEW-2 / NEW-3)

- **`VoltageTier.cs`** -- the tier policy. `IsTierExempt` (`Transformer`, `AreaPowerControl`, `WirelessPower`, `PowerTransmitterOmni`), `IsGenerator` (`IPowerGenerator` + named generator classes), `IsStationaryBattery`, `IsHighDrawMachine` (built-in whitelist + `ExtraHeavyCableDevices` config). `IsAllowedOnTier(device, tier)` is the single per-device gate. (`IsAllHeavyNetwork` is a currently-uncalled leftover from the deleted probabilistic burn-skip logic.) `ResolveMixedTierNetwork(network, preferVictim)` and `BurnCableForMisplacedDevice(device, network)` perform the actual cable burns; both register a reason string with `BurnReasonRegistry` before calling `cable.Break()`.
- **`Patches/VoltageTierPatches.cs`** -- placement-preview rejections. `Cable_CanConstruct_Postfix` checks both cable-on-cable (a fresh cable adjacent to an existing cable network of a different tier) and cable-on-device (a fresh cable adjacent to an existing device that doesn't accept the cable's tier). Device placement is never cursor-blocked -- the round-3 design is reactive: device placement is allowed, the cable next to it burns on the next power-flowing tick.
- **`Patches/CableCostPatches.cs`** -- NEW-2 runtime override. The committed `GameData/cable-recipes.xml` overlay patch-replaces the vanilla `ItemCableCoilSuperHeavy` recipe with a default 2.0x cost (Time 8, Energy 800, Constantan 1.0, Electrum 1.0). `CableCostPatches.ApplyRecipeCost()` runs at plugin load; when the configured `Super-Heavy Cable Cost Multiplier` differs from the overlay's 2.0, it builds a new `WorldManager.RecipeData` and calls `ElectronicsPrinter.RecipeComparable.AddRecipe(...)` + `GenerateRecipieList()`, so the config value wins over the overlay.
- **`GameData/cable-recipes.xml`** -- the recipe overlay. The game's recipe loader patch-replaces a vanilla `<RecipeData>` when a mod ships one with a matching `<PrefabName>`; documented in `Research/GameSystems/RecipeDataLoading.md`.

### Burn-reason tooltips (original)

- **`BurnReasonRegistry.cs`** -- two stores. `_pendingByCell` is a `ConcurrentDictionary<Grid3, string>` (a reason waiting for its wreckage to register; multiple power-tick worker threads can write concurrently). `_attached` is a `ConditionalWeakTable<object, ReasonHolder>` (the reason permanently attached to a `CableRuptured` instance; GC cleans up automatically when the wreckage is destroyed). Static helpers `RegisterPending(cable, reason)`, `TryConsumePending(cell, out reason)`, `Attach(wreckage, reason)`, `GetAttached(wreckage)`.
- **`Patches/BurnReasonPatches.cs`** -- two postfixes. `CableRuptured.OnRegistered` consumes the pending reason for the wreckage's cell and attaches it via `BurnReasonRegistry.Attach`. `Thing.GetPassiveTooltip` (the universal base) filters to `__instance is CableRuptured` and appends `"<color=#ffa500>Burned:</color> <reason>"` to the `Extended` field.

### Logic passthrough (original)

- **`LogicTypeRegistry.cs`** -- declares `LogicPassthroughMode` as `LogicType` ushort 6577 (the value is read from the central `StationeersPlus.Shared.LogicTypeNumbers` constant linked into this csproj from `Patterns/Logic/LogicTypeNumbers.cs`). `CustomLogicType` record carries name + description; `All` is the registry array consumed by every integration patch (Logicable arrays, IC10 constants, EnumGetName postfixes, and Stationpedia page registration in `StationpediaPatches.cs`).
- **`PassthroughModeStore.cs`** -- the in-memory mode store. `ConcurrentDictionary<long, int>` keyed by `Thing.ReferenceId` (generalised from `Transformer` to any supported `Thing`). `GetMode(thing)` falls through to `GetDefaultMode(thing)` when no override exists. `GetDefaultMode` returns 1 for the `StructureTransformerSmall`, `StructureTransformerSmallReversed`, and `StructureRocketTransformerSmall` prefabs and for every `Battery`, `AreaPowerControl`, `PowerTransmitter`, and `PowerReceiver`; every other transformer (and anything else) defaults to 0. `SetMode` normalises any non-zero write to 1 and stores it per `Thing.ReferenceId` for every supported type, `AreaPowerControl` included.
- **`PassthroughSideCar.cs`** -- the XML side-car. Static `PendingSaveSnapshot` is captured in the `SaveHelper.Save` prefix; `WriteSideCar(zipPath, data)` rebuilds the save ZIP with an extra entry `pwrgridplus-passthrough.xml`; `ReadSideCarFromDir(tempDir)` reads it back during load from the temp-extracted save tree.
- **`Patches/TransformerPassthroughLogicPatches.cs`** -- the `Transformer`-specific logic glue (patches `Transformer` directly because it overrides the four `Logicable` methods). Prefix on `CanLogicRead` and `CanLogicWrite` returns true when `logicType == LogicPassthroughMode`. Prefix on `GetLogicValue` returns the stored mode (or default by prefab). Prefix on `SetLogicValue` is server-only (`if (!NetworkManager.IsServer) return false;`): it writes the new mode, then dirties both `InputNetwork` and `OutputNetwork`'s data device lists so the merge re-runs on the next refresh. A client tablet write routes through the host.
- **`Patches/InheritedLogicablePassthroughLogicPatches.cs`** -- the same logic glue for `Battery`, `PowerTransmitter`, and `PowerReceiver`, which inherit the four `Logicable` methods from `Device` rather than overriding them. Patches the base `Device` type with a runtime `IsBridge(__instance)` guard (`__instance is Battery || PowerTransmitter || PowerReceiver`); `SetLogicValue` is server-only and dirties the device's two networks (`Battery`: Input / Output; `PowerTransmitter`: `InputNetwork` + `LinkedReceiver?.OutputNetwork`; `PowerReceiver`: `OutputNetwork` + `LinkedPowerTransmitter?.InputNetwork`). Patching the base type rather than the subclasses is mandatory, not stylistic; see section 7.
- **`Patches/LogicPassthroughPatches.cs`** -- the actual passthrough mechanism. Prefix on `CableNetwork.RefreshPowerAndDataDeviceLists` captures the `DataDeviceListDirty` flag (so the postfix only acts when the list was actually rebuilt). Postfix walks the network's `DeviceList` and, for each bridging device, finds the "other" network and appends its `DeviceList` into the local `_dataDeviceList` (deduped via `Contains`). Five device kinds bridge, each behind its own server toggle: `Transformer` (`EnableTransformerLogicPassthrough` + `GetMode == 1`), `AreaPowerControl` (`EnableAreaPowerControlLogicPassthrough`, no per-device mode), `Battery` (`EnableBatteryLogicPassthrough` + `GetMode == 1`, bridging `InputNetwork` <-> `OutputNetwork`), `PowerTransmitter` (`EnablePowerTransmitterLogicPassthrough` + `GetMode == 1`, partner `tx.LinkedReceiver?.OutputNetwork`), and `PowerReceiver` (same toggle + `GetMode == 1`, partner `rx.LinkedPowerTransmitter?.InputNetwork`). A wired bridging device (transformer, battery, APC) sits in BOTH cable networks' `DeviceList` already, so its own logic ports are naturally visible from either side; a transmitter / receiver pair bridges across the wireless link only while linked.
- **`Patches/PassthroughSaveLoadPatches.cs`** -- save / load wiring. Three patches: `SaveHelper.Save` prefix snapshots the store on the main thread and postfix wraps the async save with a continuation that writes the side-car after the ZIP is sealed; `XmlSaveLoad.LoadWorld` postfix reads the side-car from the loose temp-extracted file; `Thing.OnFinishedLoad` postfix applies the saved mode per `Thing.ReferenceId` for `Transformer`, `Battery`, `PowerTransmitter`, and `PowerReceiver` via `PassthroughModeStore.RestoreFromSideCar` (`AreaPowerControl` is not restored, since it has no per-device mode). The on-disk schema is unchanged: the side-car is still just `(ReferenceId, Mode)` pairs, so a v0.1.0 transformer-only save loads cleanly.
- **`Patches/CableNetworkPatches.cs`** -- the `DirtyPowerAndDataDeviceLists` postfix propagates the dirty across the passthrough component AND drives the device-list refresh cascade. `ScheduleCascade` / `ScheduleCascadeForDevice` / `RefreshAllNetworks` enqueue (via the shared `MainThreadDispatcher`) a main-thread pass that, per network, diffs a `ConditionalWeakTable`-cached signature of the merged `DataDeviceList` and, when it changed, calls `OnDeviceConnectToNetwork` on every physical member (the vanilla consumer-refresh signal) -- refreshing motherboards (via the host `Computer`), IC housings, and `LogicBase` sensors. Fired from the propagation postfix (transitive far networks), the `SetLogicValue` write paths (the flipped device's component), the dish-link patches, and the settings-sync.
- **`Patches/PassthroughTopology.cs`** (extended) -- `GetBridgeNetworks` / `DirtyBridgeNetworks` (the unified per-device helper, covering Transformer / Battery / AreaPowerControl / PowerTransmitter / PowerReceiver) yield EVERY network a bridge device connects: power input, power output, its dedicated `DataCableNetwork`, and a linked dish's far power + data networks. Used by the reachability walk (gated by `IsEnabledBridge`, which replaced the old binary `GetOtherSide`), the write-path dirty, and the dish-link refresh. `IsEnabledBridge` / `AnyPassthroughEnabled` read `PassthroughSettingsSync.Effective*` instead of `Settings.Enable*.Value` directly. All-port bridging (2026-06-18) makes a reader on ANY port -- including a dedicated `Data` port (rocket transformer, large transformer, Station / Large battery, dishes) -- see across the device, not just across the two power sides.
- **`Patches/DishLinkPatches.cs`** -- postfixes on `PowerTransmitter.TryContactReceiver` (host, where the link is set) and `PowerTransmitter.ProcessUpdate` (client, where the replicated link is applied). On a net `LinkedReceiver` change it dirties + refreshes the TX-cable and old/new RX-cable networks, and (client-only, idempotent, with an MP-mutation audit comment) mirrors `rx.LinkedPowerTransmitter` so the bridge is symmetric on clients. Compatible with PowerTransmitterPlus's `TryContactReceiver` prefix-replace (our postfix runs after it) and its `WirelessPower.ProcessUpdate` auto-aim postfix (different method; we read no wire bytes).
- **`PassthroughModeMessage.cs`** -- host -> client LaunchPadBooster `INetworkMessage` replicating a per-device `(ReferenceId, mode)` change. `Process` is client-only; applies via `PassthroughModeStore.SetModeByReference`, then dirties the device's bridge networks + schedules the cascade so the client UI updates.
- **`PassthroughSettingsMessage.cs`** -- host -> client `INetworkMessage` carrying the four `Enable*LogicPassthrough` booleans.
- **`PassthroughSettingsSync.cs`** -- the effective-value layer for the four server-authoritative toggles. `Effective*` returns local config on host / single-player and the host-synced value on a client. `SettingChanged` hooks (wired in `Plugin.Awake`) broadcast + refresh-all on a host toggle; `ApplyFromHost` (live message) and `SetSyncedValues` (join) update the client. Mirrors PowerTransmitterPlus's `DistanceConfigSync`.

### UI integration plumbing (original, mirroring PowerTransmitterPlus's pattern)

- **`Ic10ConstantsPatcher.cs`** -- one-time reflection injection into `ProgrammableChip.AllConstants` (so `s d0 LogicPassthroughMode 1` compiles in an IC10 script) and `ProgrammableChip.InternalEnums` `ScriptEnum<LogicType>` / `BasicEnum<LogicType>` (so screen-rendered code highlights the name correctly). Called from `Plugin.OnPrefabsLoaded` after `Harmony.PatchAll()`. Guarded by an `_applied` flag.
- **`Patches/LogicableInitializePatch.cs`** -- postfix on `Logicable.Initialize` that appends `LogicPassthroughMode` into the static `Logicable.LogicTypes` / `LogicTypeNames` arrays plus `Logicable.LogicTypeNamesRedirects` (sort-order index) plus `EnumCollections.LogicTypes` (the tablet-UI dropdown source) plus `ScreenDropdownBase.LogicTypes` (the on-screen IC10 syntax-preview source). Guarded by an `_injected` flag; idempotent.
- **`Patches/EnumNamePatches.cs`** -- three postfixes. `Enum.GetName(typeof(LogicType), value)`, `EnumCollection<LogicType, ushort>.GetName(value)`, and `EnumCollection<LogicType, ushort>.GetNameFromValue(value)` all fall back to `LogicTypeRegistry.TryGetName` when the vanilla lookup returns null.
- **`StationpediaPatches.cs`** (project root, mirrors PowerTransmitterPlus) -- best-effort Stationpedia integration. A postfix on `Stationpedia.PopulateLogicVariables` (resolved via `AccessTools.TypeByName` + `TargetMethod()` / `Prepare()`, so it no-ops if the game refactors the method) calls `Stationpedia.Register(...)` for each `LogicTypeRegistry.All` entry, building a `StationpediaPage` keyed `LogicType<Name>` from the registry's description. Without it the per-device `LogicPassthroughMode` rows hyperlink to a page vanilla never builds: the vanilla central-page builder calls `LogicBase.GetLogicDescription`, whose default arm throws for a custom value (caught and skipped). Wrapped in try/catch; non-fatal.

### Network merge determinism (original)

- **`Patches/MergeDeterminismPatches.cs`** -- two patch classes in one file. `CableNetworkMergeDeterministicPatch` prefixes `CableNetwork.Merge(List<CableNetwork>)`; `StructureNetworkMergeDeterministicPatch` prefixes `StructureNetwork.Merge(List<StructureNetwork>, out StructureNetwork)`. Each sorts the input list by `ReferenceId` ascending (null / single-element lists are left alone) before vanilla selects `list[0]` as the surviving network. Because `ReferenceId`s are server-allocated and identical on every peer, the survivor becomes a pure function of network ids, so server and client converge on the same survivor regardless of cable-graph iteration order. `WirelessNetwork` inherits the `CableNetwork` fix; `RocketNetwork` / `RoboticArmNetwork` / `LandingPadNetwork` inherit the `StructureNetwork` fix. The in-place sort is safe because every call site passes a freshly allocated `ConnectedNetworks(...)` list. See `Research/GameClasses/CableNetwork.md` for the desync mechanism.

### Shared / linked

- **`Patterns/Logic/LogicTypeNumbers.cs`** (linked, lives at the repo root) -- the central LogicType ushort catalogue across every SixFive7 mod. PowerGridPlus's csproj has `<Compile Include="..\..\..\Patterns\Logic\LogicTypeNumbers.cs" Link="Patterns\LogicTypeNumbers.cs" />`.
- **`Patterns/Threading/MainThreadDispatcher.cs`** (linked, lives at the repo root) -- shared main-thread marshaling helper in the `StationeersPlus.Shared` namespace (a `DontDestroyOnLoad` MonoBehaviour draining a `ConcurrentQueue<Action>` in `Update`). The device-list refresh cascade can be reached from the UniTask power-worker thread but touches Unity UI, so it is enqueued here to run on the main thread. Linked (not a shared assembly), so each mod compiles its own per-assembly copy. PowerGridPlus's csproj has `<Compile Include="..\..\..\Patterns\Threading\MainThreadDispatcher.cs" Link="Patterns\MainThreadDispatcher.cs" />`. See `Research/Patterns/MainThreadDispatcher.md` (and `Patterns/Threading/README.md`).

---

## 5. Patch catalog

Every Harmony patch class, with targets, kind, gating config, and a one-line behaviour summary, grouped by subsystem. `Patches/` holds 39 files: 36 Harmony patch classes plus three helpers without Harmony attributes of their own (`CableCostPatches.cs`, the recipe-cost override called from `Plugin.OnPrefabsLoaded`; `PassthroughTopology.cs`, the shared bridge-graph helpers; `TransformerFlashAttachPatches.cs`, the `BrownoutFlashBehaviour` attach helper). The `INetworkMessage` types and the settings-sync classes live in the mod root, not `Patches/`, and are covered in section 6.

### Atomic tick + vanilla PowerTick

| File | Target | Kind | Gated by | What it does |
|---|---|---|---|---|
| `AtomicElectricityTickPatch` | `ElectricityManager.ElectricityTick` | prefix (return false) | host `GameManager.RunSimulation` | The atomic tick driver; sequences every phase in section 3.3. |
| `PowerTickPatches` | `PowerTick.CheckForRecursiveProviders` (private) | prefix (full replace) | always | Per-anchor traversal-record clear: vanilla clears the record once before the anchor loop and misses cycles past the first anchor. Belt-and-braces only; the SCC walk in PROTECT is the primary cycle handler. |
| `PowerTickPatches` | `PowerTick.CalculateState` | postfix | always | NaN / Infinity backstop: zeroes a poisoned network sum (one dark tick) and scans the network to name the offending device (source-level clamps in `DeviceOutputSanitizer` catch the classes this mod patches; this catches unknown modded classes). |
| `PowerTickPatches` | `PowerTick.CacheState` (private) | postfix | always | Power-met boundary relaxation: vanilla's strict `Potential - Required > 0` would darken an exactly-served network, and the allocator reports exact throughput with no headroom, so `Potential >= Required - Eps` now counts as met (`_powerRatio = 1`). |
| `PowerTickPatches` | `PowerTick.GetBreakableCables` (private) | prefix (full replace) | always | Deterministic cable-burn rule: a cable burns when the 20-tick running average of DIRECT generator power on the network exceeds the weakest cable's `CableMax` cap (`CableBurnWindow`; the victim sits at the top producer's output). Transformer / battery / APC overflow does NOT burn cable; the allocator trips those suppliers into OVERLOAD instead. |

### Presentation: allocator decisions onto vanilla surfaces

| File | Target | Kind | Gated by | What it does |
|---|---|---|---|---|
| `TransformerExploitPatches` | `Transformer.GetGeneratedPower` | prefix | `EnableTransformerExploitMitigation` | Output = the allocator's converged `TotalThrough` (`TransformerSupplyCache`), soft included; a shed / overload lockout forces 0 so the subnet goes dark cleanly. |
| `TransformerExploitPatches` | `Transformer.GetUsedPower` | prefix | `EnableTransformerExploitMitigation` | Input bill = the fresh `TotalPull` (= `TotalThrough * max(m,1) + quiescent`), replacing the lagging vanilla `_powerProvided` handshake; conservation holds, exploit closed. |
| `TransformerExploitPatches` | `Transformer.ReceivePower` | prefix | `EnableTransformerExploitMitigation` | Subtracts the quiescent draw and repays the vanilla ledger (which the Stage 3 clamp keeps bounded). |
| `PowerTransmitterDrawPatches` | `PowerTransmitter.GetUsedPower` | prefix | always (when a fresh value is cached) | Bills the wireless pair's fresh input pull (delivered times the distance multiplier m plus the pair quiescent) instead of the stale `_powerProvided`. |
| `PowerTransmitterDrawPatches` (DeliveryGatePatch) | `PowerTransmitter.GetGeneratedPower` | postfix, Priority.Last | always | Clamps the advertised wireless delivery to the granted `TotalThrough` so delivery follows the grant and a transfer debt cannot be seeded. The containment path under legacy PowerTransmitterPlus 1.8.0; defence in depth under the 1.9.0+ ownership handshake. |
| `AreaPowerControlPatches` | `AreaPowerControl.UsePower` | postfix | always | Tracks the per-APC downstream passthrough (`_lastPassthrough`) consumed by the charge-headroom math. Vanilla `UsePower` itself is left alone (its positive-ledger cell drain is load-bearing). |
| `AreaPowerControlPatches` | `AreaPowerControl.ReceivePower` / `GetUsedPower` | prefixes | `EnableAreaPowerControlFix` | Leak fix; input bill = fresh passthrough (`ApcPassthroughCache`) + quiescent + charge, with charge capped by the input cable's remaining headroom (`ComputeChargeCap`) and by the allocator charge share. |
| `AreaPowerControlPatches` | `AreaPowerControl.GetGeneratedPower` | postfix | `EnableAreaPowerControlFix` | Output capped by the output cable's `CableMax` and clamped to the bundled supply share (`SoftSupplyShareCache` = passthrough + cell). |
| `AreaPowerControlPatches` | `AreaPowerControl.CanLogicRead` / `GetLogicValue` | postfixes | always | Soft-power LogicTypes on the APC; `DischargeSpeed` reads the cell-only `ApcCellDischargeCache`, not the bundled share. |
| `StationaryBatteryPatches` | `Battery.GetUsedPower` | postfix | `EnableBatteryLimits` | Charge demand min-clamped to the per-prefab cap, the input cable's `CableMax`, and the allocator's `SoftDemandShareCache` grant. |
| `StationaryBatteryPatches` | `Battery.GetGeneratedPower` | postfix | `EnableBatteryLimits` | Discharge advertise min-clamped to the per-prefab cap, the output cable's `CableMax`, and the `SoftSupplyShareCache` elastic share. |
| `StationaryBatteryPatches` | `Battery.ReceivePower` | prefix | `EnableBatteryLimits` | Multiplies incoming watts by `BatteryChargeEfficiency`, with a sub-500 W trickle-charge floor. |
| `StationaryBatteryPatches` | `Battery.get_IsOperable` | reverse | always | Exposes the property for use by the patch bodies. |
| `RocketUmbilicalPatches` | Male / Female `GetUsedPower` / `GetGeneratedPower` | postfixes | `EnableRocketUmbilicalLimits` | Umbilical charge / discharge rate caps, min-clamped to the storage share caches like a battery. |
| `RocketUmbilicalPatches` | Male `CanLogicRead`; `Device.CanLogicRead` / `GetLogicValue` | postfixes | `EnableRocketUmbilicalLimits` | The four soft-power LogicTypes on both halves (the Female through the `Device`-base declarations with an instance filter). |
| `CycleFaultEnforcementPatches` | `GetGeneratedPower` + `GetUsedPower` on Transformer / Battery / APC / PowerTransmitter / PowerReceiver / umbilical Male + Female | postfixes, Priority.Last | always | Zero both terminals of a cycle-faulted segmenter so the loop dissolves; the final word over every other power patch. |
| `ProducerFaultEnforcementPatches` | producer `GetGeneratedPower` overrides | postfix (TargetMethods) | always | Zero a VARIABLE_VOLTAGE_FAULT-locked producer's generation. |
| `PoweredPresentationPatches` | `AllowSetPower` on Transformer / AreaPowerControl / PowerTransmitter / PowerReceiver | postfixes | always (the healthy set is host-published; empty on clients) | Blocks vanilla ApplyState's only un-power path for allocator-healthy segmenters (section 3.8); unhealthy devices keep exact vanilla behaviour. |

### Priority, shedding UX, hovers, lights

| File | Target | Kind | Gated by | What it does |
|---|---|---|---|---|
| `TransformerInteractWithPatches` | `Transformer.InteractWith`, `SetKnob` (prefixes), `Thing.OnFinishedLoad` (postfix) | prefixes + postfix | `EnableTransformerShedding` | The in-world knob writes Priority instead of Setting (step 10 per click, 1 with Alt) and the knob visual tracks Priority; load restore. |
| `TransformerLabellerPatches` | `Labeller.Set` / `Labeller.InputSetting` | prefixes | `EnableTransformerShedding` | The Labeller tool writes Priority. |
| `TransformerPriorityLogicPatches` | `Transformer.CanLogicRead` / `CanLogicWrite` / `GetLogicValue` / `SetLogicValue` | prefixes | always | Writable `Priority` plus read-only `Shedding` / `Overloaded` / `CycleFault` logic values (1 while the matching lockout is active). |
| `TransformerLogicPatches` | `Transformer.CanLogicRead` / `GetLogicValue` | prefixes | `EnableTransformerLogicAdditions` | `PowerActual` (reads the vanilla ledger, bounded by the Stage 3 clamp before the LOGIC phase). |
| `BatteryLogicPatches` | `Battery.CanLogicRead` / `GetLogicValue` | prefixes | `EnableBatteryLogicAdditions` | `MaxChargeSpeed` / `MaxDischargeSpeed` (configured caps) and `ChargeSpeed` / `DischargeSpeed` (live allocated rates). |
| `ProducerLogicPatches` | producer logic methods | postfixes (TargetMethods) | always | Read-only `VariableVoltageFault` on every producer with a logic port. |
| `TransformerSettingInitPatch` | `Thing.OnRegistered` | postfix | always | `Setting = OutputMaximum` on new construction (load order makes saved values win for loaded worlds). |
| `FaultHoverPatches` | seven `GetPassiveTooltip` overrides | postfix (TargetMethods) | always | Body-hover fault lines from the single-source `FaultHover` (precedence CYCLE > VVF > OVERLOAD > SHED, live countdown, dead-input and throttle cues). |
| `TransformerHoverErrorPatches` | `Thing.GetContextualName` | postfix | always | On/off-button hover error lines for faulted devices. |
| `SwitchOnOffShedPatches` | `VanillaSwitchOnOff.RefreshColorState` | prefix | always | Keeps the fault flash (orange / red pulse) from being overwritten by the vanilla button color refresh. |
| `EmergencyLightPatches` | `Prefab.LoadAll` (prefix) + `WallLightBattery.OnPowerTick` (postfix) | prefix + postfix | `EnableEmergencyLights` | Battery wall lights on the configured prefab list act as emergency backups (off on grid power, on from the cell when the grid fails; per-light Mode 1 opts out); stands down when the third-party Battery Backup Light mod is present. |

### Voltage tiers + burn reasons

| File | Target | Kind | Gated by | What it does |
|---|---|---|---|---|
| `VoltageTierPatches` | `Cable.CanConstruct` | postfix | always | Rejects cable-on-cable mismatches (different tier adjacent to an existing cable network) AND cable-on-device mismatches (existing device that doesn't accept this cable's tier) at the cursor. |
| `VoltageTierPatches` | `CableNetwork` ctors (three overloads) + `CableNetwork.Add(Cable)` | postfixes | always | Network-membership maintenance for the tier enforcer on game 0.2.6403+ (the `Cable.ConnectedCables` / `CableNetwork.OnNetworkChanged` APIs this used to lean on were removed; grid adjacency now goes through `SmallCell.Get` + `Connection.GetLocalGrid`). |
| `BurnReasonPatches` | `CableRuptured.OnRegistered`; `Structure.GetPassiveTooltip`; `Tooltip.SetValuesForInteractable` | postfixes | always | Consumes the pending burn reason for the wreckage's cell, attaches it via `BurnReasonRegistry`, and renders "Burned: (reason)" on the wreckage hover. The bare class-level `[HarmonyPatch]` marker is required for `PatchAll` to see the per-method targets. |
| `BurnReasonSaveLoadPatches` | `SaveHelper.Save` (private 4-arg worker), `XmlSaveLoad.LoadWorld`, `Thing.OnFinishedLoad` | prefix + postfixes | always | Burn reasons persist across save / load via the side-car ZIP entry. |
| `CableCostPatches` | not a Harmony patch | (called from `Plugin.OnPrefabsLoaded`) | `SuperHeavyCableCostMultiplier != 2.0` | Runtime override of the `ItemCableCoilSuperHeavy` recipe when the multiplier differs from the GameData overlay's 2.0x default. |

### World lifecycle

| File | Target | Kind | Gated by | What it does |
|---|---|---|---|---|
| `FaultRegistryLoadPatches` | `XmlSaveLoad.LoadWorld` | postfix | always | Clears every transient registry / cache / window on load and re-arms the one-shot unknown-bridge census, the Stage 3 ledger sweep, and drops the Powered-presentation and shortfall snapshots. |
| `PrioritySaveLoadPatches` | `SaveHelper.Save`, `XmlSaveLoad.LoadWorld`, `Thing.OnFinishedLoad` | prefix + postfixes | always | Per-transformer Priority persists via its side-car ZIP entry. |

### Logic passthrough + UI plumbing (unchanged by the 2026-07-02 rearchitecture)

| File | Target | Kind | Gated by | What it does |
|---|---|---|---|---|
| `LogicPassthroughPatches` | `CableNetwork.RefreshPowerAndDataDeviceLists` | prefix + postfix | per-device toggle (see below) | Merges the "other" network's `DeviceList` into the local `_dataDeviceList` for each bridging device: Transformer / Battery / PowerTransmitter / PowerReceiver when the per-device mode is 1 (toggles `EnableTransformerLogicPassthrough` / `EnableBatteryLogicPassthrough` / `EnablePowerTransmitterLogicPassthrough`), and any APC when `EnableAreaPowerControlLogicPassthrough` is on (no per-device mode). |
| `CableNetworkPatches` | `CableNetwork.DirtyPowerAndDataDeviceLists` | postfix | any passthrough toggle on | Propagates the dirty across the passthrough component (re-entrancy-guarded) AND schedules the main-thread, diff-guarded device-list refresh cascade for the transitively-reachable networks (fires `OnDeviceConnectToNetwork` on each member so motherboards / IC housings / sensors rebuild). |
| `DishLinkPatches` | `PowerTransmitter.TryContactReceiver` | postfix | always | Host: on a net `LinkedReceiver` change, dirty + refresh the TX-cable and old/new RX-cable networks. Runs after PowerTransmitterPlus's prefix-replace, reading the final link. |
| `DishLinkPatches` | `PowerTransmitter.ProcessUpdate` | postfix | always | Client: same refresh when the replicated link changes, plus the idempotent client-only mirror of `rx.LinkedPowerTransmitter` (vanilla replicates only the TX-side reference). |
| `TransformerPassthroughLogicPatches` | `Transformer.CanLogicRead` | prefix | always | Returns true for `LogicPassthroughMode`. |
| `TransformerPassthroughLogicPatches` | `Transformer.CanLogicWrite` | prefix | always | Returns true for `LogicPassthroughMode`. |
| `TransformerPassthroughLogicPatches` | `Transformer.GetLogicValue` | prefix | always | Returns `PassthroughModeStore.GetMode(__instance)` for `LogicPassthroughMode`. |
| `TransformerPassthroughLogicPatches` | `Transformer.SetLogicValue` | prefix | always (server-only) | Server-only (`NetworkManager.IsServer`); writes the mode to `PassthroughModeStore`. On change: dirties both sides (via `PassthroughTopology.DirtyBridgeNetworks`), schedules the refresh cascade, and broadcasts `PassthroughModeMessage` to clients. |
| `InheritedLogicablePassthroughLogicPatches` | `Device.CanLogicRead` | prefix | always | Returns true for `LogicPassthroughMode` when `__instance` is a Battery / PowerTransmitter / PowerReceiver (`IsBridge`). |
| `InheritedLogicablePassthroughLogicPatches` | `Device.CanLogicWrite` | prefix | always | Same gate; returns true (writable) for `LogicPassthroughMode`. |
| `InheritedLogicablePassthroughLogicPatches` | `Device.GetLogicValue` | prefix | always | Returns `PassthroughModeStore.GetMode(__instance)` for `LogicPassthroughMode`. |
| `InheritedLogicablePassthroughLogicPatches` | `Device.SetLogicValue` | prefix | always (server-only) | Server-only; writes the mode. On change: dirties the device's two networks (via the unified `PassthroughTopology.DirtyBridgeNetworks` -- Battery Input/Output; TX `InputNetwork`+`LinkedReceiver?.OutputNetwork`; RX `OutputNetwork`+`LinkedPowerTransmitter?.InputNetwork`), schedules the refresh cascade, and broadcasts `PassthroughModeMessage`. |
| `UmbilicalPassthroughPatches` | Male `CanLogicRead` (postfix); Male / Female `SetPartner` (prefix + postfix pairs) | postfix + prefix/postfix | `EnableUmbilicalLogicPassthrough` | Umbilical-pair logic bridge: the Male's own `CanLogicRead` override exposes the mode, a write to one half mirrors to the docked partner, and dock / undock refreshes the merged device lists. |
| `PassthroughSaveLoadPatches` | `SaveHelper.Save` (private worker, 4-arg overload) | prefix + postfix | always | Snapshots the mode store on the main thread; wraps the async save with a side-car write continuation. |
| `PassthroughSaveLoadPatches` | `XmlSaveLoad.LoadWorld` | postfix | always | Reads the side-car XML from the temp-extracted save tree into `PassthroughSideCar.LoadedModes`. |
| `PassthroughSaveLoadPatches` | `Thing.OnFinishedLoad` | postfix | always | Per-device restore from the loaded side-car for Transformer / Battery / PowerTransmitter / PowerReceiver. |
| `LogicableInitializePatch` | `Logicable.Initialize` | postfix | always (idempotent) | Appends the custom LogicType into `Logicable.LogicTypes` / `LogicTypeNames` / `LogicTypeNamesRedirects` / `EnumCollections.LogicTypes` / `ScreenDropdownBase.LogicTypes`. |
| `EnumNamePatches` | `Enum.GetName(typeof(LogicType), value)` | postfix | always | Returns the registered name when the vanilla lookup is null. |
| `EnumNamePatches` | `EnumCollection<LogicType, ushort>.GetName(value)` | postfix | always | Same. |
| `EnumNamePatches` | `EnumCollection<LogicType, ushort>.GetNameFromValue(value)` | postfix | always | Same. |
| `StationpediaPopulateLogicVariablesPatch` | `Stationpedia.PopulateLogicVariables` | postfix | always (best-effort, `Prepare`-gated) | Registers a central `LogicType<Name>` Stationpedia page per `LogicTypeRegistry.All` entry so the custom logic type's wiki page exists (vanilla skips it because `GetLogicDescription` throws for custom values). |
| `MergeDeterminismPatches` | `CableNetwork.Merge(List<CableNetwork>)` | prefix | always | Sorts the merge-input list by `ReferenceId` so the surviving network is deterministic across multiplayer peers. |
| `MergeDeterminismPatches` | `StructureNetwork.Merge(List<StructureNetwork>, out)` | prefix | always | Same deterministic sort for structure networks (rocket / robotic-arm / landing-pad). |

---

## 6. Multiplayer protocol

The mod sets `MOD.Networking.Required = true` (LaunchPadBooster). The connection handshake checks the mod version; clients without a matching DLL are rejected at join time.

The mod registers LaunchPadBooster `INetworkMessage` types (`PassthroughModeMessage`, `PassthroughSettingsMessage`, `PriorityMessage`, `FaultRegistrySnapshotMessage`) and an `IJoinSuffixSerializer`. The rest of its state reaches clients through vanilla state-sync:

- The power tick runs on the host; clients see device `Powered` states via the normal `Device.SetPower` replication. That includes the Stage 3 Powered presentation: the healthy set is only ever published inside the host's atomic tick, `SetPowerFromThread` routes the write through the normal interactable path, and on a client peer the `AllowSetPower` postfixes no-op (empty set), so clients simply mirror the host's replicated flag.
- Fault state reaches clients as per-tick FULL snapshots (`FaultRegistrySnapshotMessage`, one per registry kind: shed, overload, cycle, variable voltage, dead-input). A non-empty registry broadcasts its complete `(refId, remainingTicks)` map every tick; an empty registry sends exactly one empty snapshot on the non-empty-to-empty transition, then goes silent. Snapshots self-heal after packet loss, and a joining client shows the correct remaining countdown within one tick.
- Per-transformer Priority replicates via `PriorityMessage` plus the join suffix, and persists host-side in its save side-car.
- `LogicPassthroughMode` per-device mode (on `Transformer`, `Battery`, `PowerTransmitter`, `PowerReceiver`): stored host-side in `PassthroughModeStore`, persisted in the save-ZIP side-car. The `SetLogicValue` prefixes are server-only (`NetworkManager.IsServer`), so a client tablet write routes through the host. On a server-side change the host broadcasts `PassthroughModeMessage` (`(ReferenceId, mode)`); each client applies it to its local store, dirties the device's networks, and refreshes. A joining client receives the full set of explicit overrides via `IJoinSuffixSerializer`. The data-device-list merge runs per-peer, so this synced mode is what lets a client compute the same merged membership the host does (previously a client could only observe the host's membership indirectly).
- The four `Enable*LogicPassthrough` server toggles are server-authoritative and take effect live (not `RequireRestart`), but BepInEx does not sync them. `PassthroughSettingsSync` mirrors the host's values: a host toggle broadcasts `PassthroughSettingsMessage` and refreshes all networks; a joiner receives the four booleans appended to the `IJoinSuffixSerializer` suffix. `PassthroughTopology.IsEnabledBridge` / `AnyPassthroughEnabled` read `Effective*` (host value on a client, local config on host / single-player), so a client whose local config differs from the host no longer computes a divergent merge.
- Wireless dish link symmetry: vanilla replicates `PowerTransmitter.LinkedReceiver` to clients (the `WirelessPower.Receiver` delta + join) but NOT `PowerReceiver.LinkedPowerTransmitter` (its setter sets no update flag; it is assigned only inside the `RunSimulation`-gated host-only `TryContactReceiver`). So on a client the dish bridge would be one-directional, because the bridge walk (`GetBridgeNetworks(rx)`, gated by `IsEnabledBridge`) reads the null back-reference. `DishLinkPatches` mirrors the back-reference on the client (idempotent, client-gated, with an MP-mutation audit) so the merge is symmetric. PowerTransmitterPlus neither sets nor relies on this back-reference on clients, so the two mods compose; a TODO in PowerTransmitterPlus records the option for it to own the mirror. See `Research/GameClasses/PowerTransmitter.md`.
- Device-list UI refresh: the merge mutates `DataDeviceList` silently (vanilla notifies no consumer). On a mode flip, a settings toggle, a dish link change, or a device built on a far bridged network, the refresh cascade (`CableNetworkPatches` + `DishLinkPatches`, marshaled to the main thread by the shared dispatcher) re-fires `OnDeviceConnectToNetwork` so motherboard dropdowns / IC-housing / sensor caches rebuild. It runs per-peer (each peer computes its own merge), gated to whichever peer's state changed. See `Research/GameClasses/Motherboard.md`.
- The `CableNetwork` / `StructureNetwork` merge-determinism prefixes (`MergeDeterminismPatches`) exist purely for multiplayer correctness. Vanilla `Merge` keeps `list[0]` as the survivor, and server and client run the merge independently with no authoritative sync event; if their cable-graph iteration order differs, they keep different survivors and read different `CableNetwork.ReferenceId`s for the same cable indefinitely. Sorting the merge list by `ReferenceId` first makes the survivor a pure function of server-allocated ids, so every peer converges. See `Research/GameClasses/CableNetwork.md`.
- The burn-reason wreckage tooltip is computed server-side at the moment of `Cable.Break()` and persisted in the wreckage's `BurnReasonRegistry` attached state. Clients see the wreckage Thing via normal replication; the tooltip postfix runs locally on each client, reading from the client's own `BurnReasonRegistry`. **This means a joining client does not see burn reasons for wreckages that existed before the join** -- the registry's `_attached` table is populated on `CableRuptured.OnRegistered`, which only fires on the host. A late-joining client sees the wreckage but no reason. Not currently considered a bug since the visual debris itself is the more important feedback; logged as a known limitation.

Side-car XMLs persist server-only state and are part of the save ZIP. A vanilla loader (no mod) ignores unknown entries silently, so saves remain backwards-compatible if the mod is removed.

---

## 7. Pitfalls and gotchas

- **`CableNetwork.DataDeviceList.get` checks the wrong flag.** Vanilla code reads `if (PowerDeviceListDirty) RefreshPowerAndDataDeviceLists()` instead of `DataDeviceListDirty`. This means a `DirtyDataDeviceList()` call alone does not invalidate `DataDeviceList`-reads until something else (cable add / remove, power-side dirty) flips the power flag too. In practice the per-tick power-side rebuild covers this, so the bug is silent. Anyone touching this code should not rely on `DataDeviceList.get` triggering a refresh after a data-only dirty. See `Research/GameClasses/CableNetwork.md` "Field shape and accessor quirk" for the verbatim accessor code.
- **`Cable.Break()` runs on worker threads.** The Re-Volt-derived power tick runs in UniTask workers. `BurnReasonRegistry._pendingByCell` is `ConcurrentDictionary<Grid3, string>` precisely because multiple threads call `RegisterPending` concurrently. The matching `Attach` runs on the main thread in the `CableRuptured.OnRegistered` postfix (Unity Things are main-thread-only), so the registry's main-thread `ConditionalWeakTable` is fine. Adding new burn call sites needs to honour this split.
- **`Thing.GetPassiveTooltip` is a universal-base postfix.** The Burn-reason postfix patches the base method, not `CableRuptured`-specific (which does not override). Every tooltip in the game runs through this postfix; the `__instance is CableRuptured` filter is the first line. Sanity check before adding similar universal patches: is the work cheap enough to run on every hover?
- **APC patch deviation from Re-Volt 1.4.0.** Re-Volt declares the `UsePower`-bodied prefix as patching `GetUsedPower`; the parameter types only match `UsePower`. Power Grid Plus retargets at `UsePower`. If APC behaviour misbehaves in-game (battery doesn't drain to downstream, or upstream draw isn't reported), this is the first suspect; revert to the Re-Volt attribution and verify.
- **`SaveHelper.Save`'s private 4-arg overload is the only sound patch target.** The public `Save(string, CancellationToken)` overload would `AmbiguousMatchException` at `PatchAll` time without the explicit `new[] { typeof(DirectoryInfo), typeof(string), typeof(bool), typeof(CancellationToken) }` argument-type array. See `PassthroughSaveLoadPatches.cs`.
- **`ElectronicsPrinter.RecipeComparable.GenerateRecipieList` is the canonical typo.** The vanilla method is mis-spelled (`Recipie`). Do not silently fix it in calls or the override will not bind.
- **Run-once Ic10ConstantsPatcher / LogicableInitializePatch composition.** Both are guarded by static flags. If PowerTransmitterPlus also runs (it follows the same pattern), the second mod's postfix sees the first mod's already-injected array and appends on top. Order is stable as long as both mods guard with their own `_applied` flag; never `Array.Copy` over a fresh `new T[vanillaLength]`.
- **Loaded saves with pre-existing mixed-tier networks trigger a cascade.** D11 retroactive enforcement burns the boundary cable of every pre-existing mixed-tier network on the first power-flowing tick after load. On a base that mixed tiers freely under vanilla rules this is destructive. The Option-C TODO entry proposes a default-off opt-in.
- **A `[HarmonyPatch]` on an inherited method the subclass does not override throws "Undefined target method" at `PatchAll` time and silently kills the rest of the batch.** The four `Logicable` logic-port methods (`CanLogicRead` / `CanLogicWrite` / `GetLogicValue` / `SetLogicValue`) are declared on base `Device`. `Battery`, `PowerTransmitter`, and `PowerReceiver` inherit them without overriding, so `[HarmonyPatch(typeof(Battery), nameof(Battery.SetLogicValue))]` compiles (`nameof` resolves through inheritance) but fails to resolve a *declared* method at patch time. The exception bails the whole `PatchAll` batch from that point on, disabling every patch class processed after it plus the post-`PatchAll` calls in `Plugin` (`CableCostPatches.ApplyRecipeCost`, `Ic10ConstantsPatcher.Apply`); which patches survive depends on HarmonyX's class-iteration order, so the failure looks intermittent. The fix is to patch the base type that declares the method (`Device`) and add a runtime `is`-type guard (`IsBridge`) for the subclasses you care about, which is why `InheritedLogicablePassthroughLogicPatches` targets `Device` while `TransformerPassthroughLogicPatches` patches `Transformer` directly (Transformer genuinely overrides the four methods, so a base-`Device` patch would be shadowed by its override). Full writeup: `Research/Patterns/HarmonyLogicableInheritedMethodTrap.md`.
- **A granted flow with no published carrier does not exist in-game.** The allocator can decide anything it likes; unless the decision lands in the caches that `GetGeneratedPower` / `GetUsedPower` serve, on BOTH terminals of the segment, ENFORCE never sees it. The 2026-07-02 Luna deadlock was exactly this: storage-charge grants were published only for APC segments, so 50 kW of granted battery charge behind transformers advertised 0 and billed 0, a permanent fault-free fixed point. Any new flow class or seg kind must publish its totals for every kind, and the conservation checker exists to catch the next such gap.
- **Vanilla's cross-network advertise is one tick lagged, so a fully dead island cannot bootstrap itself under vanilla relaxation.** A bridge advertises `min(cap, InputNetwork.PotentialLoad)`, where `PotentialLoad` is the LAST-completed-tick mirror. On a dead source network the mirror reads 0, so nothing downstream requests, so nothing flows, so the mirror stays 0. Real case: solar farms parked at zero efficiency whose tracker ICs are powered by the same dead farm nets can never restart without the pooled allocator (which sizes supply from fresh in-tick sums instead). Do not "simplify" a capacity read to the live `PotentialLoad`; the wireless pair's static link rating exists precisely because a live read collapsed to a cross-tick zero fixed point.
- **Vanilla ApplyState un-powers zero-demand devices.** The Powered flag is derived from what a device consumed this tick, and the only un-power path is gated on `Device.AllowSetPower(net)`. Any patch set that makes a healthy device legitimately bill 0 (an idle charger under fresh-pull billing) must also own the Powered presentation or players will read the device as broken. This is why `PoweredPresentationPatches` + the ENFORCE-tail reconcile exist (section 3.8).
- **The vanilla `_powerProvided` ledger is never zeroed by the game.** Under fresh-pull billing it drifts structurally negative on wireless pairs (`-((m - 1) * TotalThrough + quiescent)` per conducting tick) and an accumulated credit becomes free energy for any code path that reads the ledger again. At 0.2.6403 the field is not serialized into saves, but older saves and external writers can still carry residue. Never bypass the Stage 3 sweep + settle when adding a new billing path; and never settle the APC's ledger (its positive value is the vanilla cell-discharge carrier, see section 3.9).
- **The wireless pair's health and lockout state is pair-wise.** The receiver never yields its own seg description, so anything keyed by seg RefId (caches, lockouts, health) anchors on the transmitter; code touching the receiver half must go through `PartnerRefId` / the roster's partner reference, and a cycle-faulted receiver must lock the pair.

---

## 8. Design decisions

The decision log spans three named phases (the 2026-05-12 first build pass, the 2026-05-13 round-2 follow-up that resolved every stubbed / deferred item, and the 2026-05-13 round-3 device-rejection -> reactive-cable-burn pivot) plus dated entries, newest first, through the 2026-07-02 power rearchitecture. Entries are append-only; supersedes are explicit.

### D1 -- D13 (2026-05-12)

- **D1**: Pure-patch mod, no new prefabs. Trims dependencies (no LibConstruct, no asset bundle), simplifies game-update maintenance.
- **D2**: Derive from Sukasa's Re-Volt (MIT) for the simulation half rather than reimplement from scratch. Attribution per section 11.
- **D3**: All config is server-authoritative; `Server - *` sections only.
- **D4**: Code name `PowerGridPlus`, display name `Power Grid Plus`.
- **D5**: NEW-3 research spike done; verdict was build-time rejection via `Cable.CanConstruct` postfix + reactive burn-on-join in `OnRegistered` postfix.
- **D6**: Three-tier voltage gating -- `normal`, `heavy`, `superHeavy` cables mutually incompatible, bridged only by transformers.
- **D7**: Super-heavy-cable cost multiplier default 2.0; NEW-1 (no burn limit) applies only to `superHeavy`.
- **D8**: Tier-by-draw, "lowest cable tier rated for peak draw / output". Stationary batteries on `heavy`; the >5 kW machines (Advanced Furnace, Arc Furnace, Carbon Sequester 45 kW/unit, Furnace, Centrifuge, Recycler, Ice Crusher, Hydraulic Pipe Bender, electric Deep Miner) on `heavy`+.
- **D9 (SUPERSEDED in round 2 by the universal cable-tier rule)**: per-prefab transformer-tier mapping (Small = `heavy<->normal`, Medium = `heavy<->heavy`, Large = `superHeavy<->heavy`).
- **D10 (SUPERSEDED in round 2 by the universal cable-tier rule)**: bidirectional transformers re-implementing `GetGeneratedPower` / `GetUsedPower` / `ReceivePower` symmetrically.
- **D11**: enforcement = build-time rejection (primary UX) + reactive burn-on-join (authoritative backstop + save migration). Burn-on-join algorithm: while a `CableNetwork` contains more than one `CableType`, pick a lowest-tier cable that is grid-adjacent to a higher-tier cable (the boundary cable) and `Break()` it. Fuses are never substituted: a tier mismatch is a topology violation, not over-current.
- **D12**: NEW-2 multiplies only the coil-crafting XML recipe; nothing touches per-cable `BuildStates[0].Tool.EntryQuantity`. Insulated cable variants count as their base `CableType`. In-game tier names stay "Normal / Heavy / Super-Heavy cable"; rejection message leans on the lore ("Cannot connect: different transmission voltage -- use a transformer").
- **D13**: all electrical generators are `heavy`-cable-only (overriding the per-output tier-by-draw rule for generators specifically). Solar Panels, the small Wind Turbine, Large Wind Turbine, Stirling Engine, Solid / Gas Fuel Generators, Turbine Generator, RTG -- all `heavy`. The build-time `Device.CanConstruct` rejection (then planned, now superseded by reactive burn per round-3 below) was the enforcement vehicle.

### Round 2 (2026-05-13): universal cable-tier rule

The user's round-2 instruction simplified the design: drop D9 / D10's per-prefab transformer tier mapping in favour of the universal rule "a `CableNetwork` must be all one tier; transformers and APCs have separate input / output networks and are therefore tier-exempt". This kept the spirit of D6 (the three tiers are separate voltages) but removed the need to distinguish transformer prefabs at all. `VoltageTier.IsTierExempt` was added; `Transformer`, `AreaPowerControl`, `WirelessPower`, and `PowerTransmitterOmni` are exempt. The "ordinary device on heavy / super-heavy = hard reject + no power if it slips through" rule was implemented at the tick level (`PowerGridTick.CalculateState_New` zeroes a device's used and generated power on the wrong tier) but the build-time hard reject was deferred (resolved in round 3).

### Round 3 (2026-05-13): build-time-device-rejection -> reactive cable-burn

The remaining round-2 gap was the build-time cursor reject for misplaced devices. On reflection the user preferred to NOT gate device placement at all and instead burn the cable next to the misplaced device, symmetric with the cable-tier rule. Done:

- `PowerGridTick.CalculateState_New` no longer zeroes a misplaced device's power. Instead it records the first misplaced device of the tick (`_misplacedDeviceForBurn`); `ApplyState_New` burns the cable adjacent to it via `VoltageTier.BurnCableForMisplacedDevice`. The device itself is never destroyed.
- Both tier-burns moved to `ApplyState_New`, gated on `powerFlow > 0`. An idle / off network destroys nothing, even if mixed-tier or with a misplaced device. As soon as actual power flows, the burn fires. Mixed-tier burns take precedence over device-tier burns (root cause first).
- The previously-removed `Cable_OnRegistered_Postfix` was confirmed unneeded. Cable registration triggers `DirtyPowerAndDataDeviceLists` -> `IsDirty = true` -> `Initialize_New` re-detects mixed-tier on the next tick. One code path covers fresh placement and loaded saves.
- `Cable_CanConstruct_Postfix` extended with a second pass over `Cable.ConnectedDevices()` so that placing a cable next to an existing device on a tier the device doesn't accept is rejected at the cursor.
- Burn-reason tooltips wired (see Architecture). Four reason categories: overload, wrong voltage (mixed-tier bridge), wrong voltage (adjacent misplaced device), and an untagged fallback.

Net result: build-time gating is cable-side only; device placement is never blocked. The "you put it on the wrong cable" feedback is the cable burning, with a tooltip on the wreckage explaining why.

### 2026-07-02: power rearchitecture (unified flow classes, segment adapters, billing ownership, ledger adoption)

Three staged changes landed together, motivated by the three-authority analysis in section 3.2 and root-caused from a live fault-free deadlock on the Luna save (two 25 kW chargers idling forever). Dedi-verified per stage (batteries charge across seven wireless links to full; conservation checker zero violations; handshake live on both sides; census empty; tick-identical charge trajectory before and after the adapter refactor).

- **Stage 1 (commit e9ef3c1f): storage charge is a first-class Soft flow.** The single-pass surplus walk, `GrantThrough`, `SoftReqTotal`, and the dead `Net.ElasticDelivered` are deleted. Soft rides the same backward/forward sweep as rigid with a proportional split (killing the surplus walk's double-count by construction) and is granted from the firm residual only; faults stay rigid-only, so excess charge desire clamps silently instead of shedding or overloading anything. The cache tail publishes `TotalThrough` / `TotalPull` for EVERY routed seg kind, which is the actual deadlock fix. `ConservationChecker` added as a permanent in-mod invariant harness (`Server - Diagnostics`). Four documented deviations from the design sketch: the quiescent draw rides the soft pull when the rigid side is idle; the backward soft gate is `IsActive` (a shed contributor's charge desire must not size its suppliers, or delivered soft strands); the weakest-cable headroom cap applies to the per-net soft pool; and the APC also gets a `TransformerSupplyCache` entry so every routed kind publishes the same surface.
- **Stage 2b (commit 2a00d674): segment adapter contract + interop switch.** Bridge descriptions move behind `ISegAdapter` / `SegSpec` (`SegAdapters.cs`), deliberately preserving the exact arithmetic of the open-coded predecessors so allocator numbers stay bit-identical across the refactor. The umbilical is formalized as Buffered (store-and-forward; the 0.2.6403 bidirectional phase-2 crossing stays outside the allocator). `PowerTransmitterPlusInterop` grew the ModApi tier with the one-shot `ClaimBillingOwnership("net.powergridplus")` handshake (PowerTransmitterPlus 1.9.0+ stands its native wireless debt billing down while the claim is held); the legacy 1.8.0 reflection chain is intact underneath, and absent-PowerTransmitterPlus falls back to the vanilla model. `UnknownBridgeCensus` reports unmodelled third-party bridges instead of silently mis-modelling them.
- **Stage 3 (uncommitted at time of writing): Powered presentation + ledger adoption.** Healthy idle segmenters present Powered=True (AllowSetPower postfix gate + ENFORCE-tail true-edge reconcile via the healthy-set snapshot); the vanilla `_powerProvided` ledger is zeroed on world load and clamped per tick to `[0, 2 x bill]` (APC exempt: its positive ledger is the vanilla cell-discharge carrier). Kills the two vanilla-semantics leaks: the "idle charger reads unpowered" diagnostic trap and the saved negative wireless credit class (the -176,226 free-energy credit on Luna transmitter 464520).
- **Behaviour changes accepted knowingly**: soft flows now traverse intermediate networks for real, so hover / IC10 network readings include charge flow and fuse / cable ratings see the real load (the `CableMax` clamps keep granted flows within tier ratings); storage behind a high-priority transformer charges before storage behind a low-priority one (soft follows the rigid priority tiers instead of the old priority-blind walk).

### 2026-06-23: data-only ports are tier-exempt (carve-out made explicit) + a load-regression fix

Two changes, both headless-verified on the dedi (ScenarioRunner `pgp-dataport-tier-diag` against `Luna_pgp_rocket`).

**Data-only ports accept any cable tier.** A device's exclusive data-only port (`NetworkType.Data`, no Power bit) must be invisible to the voltage-tier rules; only Power and power+data (`PowerAndData`) ports follow them. This was already true in practice: the game builds `CableNetwork.PowerDeviceList` from `Device.PowerCables` (power-bit cables only), so a device whose data port is on its own network is never in that network's `PowerDeviceList`, and the reactive Misplaced-burn (`DetectViolation` -> `BurnCableForMisplacedDevice`) iterates exactly that list. The cursor path likewise skipped data-only ports via `CursorAttachesToPowerPortOf` (added 2026-05-18). This session made the invariant explicit and regression-proof rather than incidental:

- `VoltageTier.ConnectionCarriesPower(Connection)` is the single predicate for "does this connection carry the Power bit"; the cursor's `CursorAttachesToPowerPortOf` now calls it.
- `VoltageTier.ReachesNetworkViaPowerPort(Device, CableNetwork)` is true iff one of the device's power cables sits on that network. `VoltageTierEnforcer.DetectViolation` now guards the Misplaced classification with it, so a device can never be tier-judged on a network it touches only through a data port, even if a future game/mod change leaked a data-only device into `PowerDeviceList`.

Verified headless: `runtimeInvariant=PASS` (realBugShape=0 over 402 live separate-data-port devices), `cursorCarveOut=PASS` (dataLeak=0 over 72 distinct data ports), across normal/heavy/superHeavy data networks; the 8 genuine power-tier would-burn networks stayed flagged (power and power+data enforcement unchanged). A separate data network may be any tier; a data cable that physically merges into a power network still follows the no-mixing rule (same tier, or bridge with a transformer), as intended. The on-screen cursor ghost color is the only client-side residue (a headless dedi cannot render the placement preview).

**csproj-migration load regression.** The 2026-06-22 SDK-style migration (`f5e67d37`) replaced the explicit `<Compile>` list with auto-glob, which pulled in `Patches/AreaPowerControlPassthroughLogicPatches.cs`, a dead file the old list deliberately excluded. Its `[HarmonyPatch(typeof(AreaPowerControl), nameof(AreaPowerControl.CanLogicWrite))]` cannot resolve (APC inherits `CanLogicWrite` from `Device`; it does not declare it), so `Harmony.PatchAll()` threw and the whole mod loaded with no patches applied. The file was superseded by `InheritedLogicablePassthroughLogicPatches` (which patches the base `Device` logic methods APC inherits), so it was deleted; the mod loads clean again (`patches applied`, no fatal). Lesson for the glob-all build: a dead `.cs` left on disk is a landmine, delete it rather than leave it excluded.

**superHeavy producers stay player error (decided 2026-06-23).** The same probe (PART-C) flagged seven superHeavy solar-collector networks (around 33 members each: solar panels, nuclear batteries, sensors) whose members would burn once the network carries power, because `IsAllowedOnTier` keeps generators, stationary batteries, and umbilicals on `heavy` exactly. Decided this is not a bug: placing a producer on a superHeavy backbone is incorrect wiring, and the heavy-only rule stands. No code change.

### 2026-06-21: rocket-family power parity (transformer tier, battery caps, umbilical heavy + logic passthrough)

Five changes resolving a friend's rocket-playtest report; the goal was to make rocket-internal power devices behave like their station equivalents and to make the power umbilical act like a real (disconnectable) wire.

- **Rocket transformer joins the tier map.** `StructureRocketTransformerSmall` was absent from `VoltageTier.GetTransformerTierMap`, so no tier-pair was enforced on it. Added it as {heavy, normal}, identical to `StructureTransformerSmall`; it now follows the same bidirectional pair rule and actively-bridging burn. (Its passthrough default-1 was already added 2026-06-19.)
- **Rocket batteries get the per-prefab rate cap.** `StationaryBatteryPatches.GetChargeCap` / `GetDischargeCap` switch on PrefabName, and the rocket batteries (`StructureBatteryMedium`, `StructureBatterySmall`) fell through to the uncapped `Infinity` default -- the only `Battery`-class devices without a rate cap. Added cases plus four `Server - Batteries` settings (`RocketBatteryMedium*` 5000 / 10000, `RocketBatterySmall*` 2500 / 5000). Everything else (heavy-cable tier, passthrough default 1, elastic shares, charge efficiency) already applied via the `Battery` class. The cap defaults are throughput estimates, tunable in-game.
- **Power umbilicals are heavy-cable-only.** `VoltageTier.IsAllowedOnTier` had no umbilical case, so the umbilical fell to the ordinary-device "normal cable only" branch and the mod burned a heavy cable wired to it -- the exact cause of the "needs to accept Heavy Cable" report. Added `IsRocketPowerUmbilical` (Male / Female) to the heavy-only branch alongside generators and stationary batteries.
- **Docked umbilical pair = a logic bridge.** Vanilla carries no logic across the umbilical coupling (a battery-buffer transfer with a null `CableNetwork`; see `Research/GameClasses/RocketPowerUmbilical.md`). Modeled the pair on the wireless dish-link: `PassthroughTopology` now treats both umbilical halves as a bridge type, gated on a non-null `_partnerUmbilical` (read by reflection -- `IUmbilical` has no public partner getter) so it bridges only while docked. `GetBridgeNetworks` yields own + partner power / data networks; a new `Server - Rocket Umbilical / Enable Umbilical Logic Passthrough` toggle (default on) joins the four existing passthrough toggles in `PassthroughSettingsSync` and the join suffix (now five booleans). Both halves expose a writable `LogicPassthroughMode` (default 1): the Female rides the base-`Device` glue (`InheritedLogicablePassthroughLogicPatches.IsBridge` extended), the Male needs a direct `CanLogicRead` patch in the new `UmbilicalPassthroughPatches` (it overrides that method). Per the "two things, one mode" requirement, a write to one half mirrors to the docked partner (`SetLogicValuePatch`). `UmbilicalPassthroughPatches` also refreshes the merged device lists on `SetPartner` (dock / undock), mirroring `DishLinkPatches`. Per-device mode persists via the existing side-car (umbilical cases added to `PassthroughSaveLoadPatches`).

Multiplayer note: the partner reference is set per-peer by geometry (`FindAndSetOtherUmbilical`), so each peer should bridge locally without a replicated back-reference; unconfirmed in MP (see `PLAYTEST.md`). If a client fails to bridge, add a client-side partner mirror like the dish back-reference mirror in `DishLinkPatches`.

### 2026-05-15: bidirectional transformer tier-pair + actively-bridging burn gate

The per-variant transformer rule lands but in two refined forms vs the original D9/D10 design:

- **Bidirectional unordered pair.** Each variant defines an unordered set `{tierA, tierB}` that the two cable ports must form together; direction does not matter. A Small is heavy/normal in either order; a Large is superHeavy/heavy in either order; a Medium is heavy/heavy. This covers both step-down (load-side wiring: heavy in, normal out) and step-up (generation-side wiring: normal in, heavy out) without distinguishing prefab-orientation variants. Reasoning: the original asymmetric rule only considered power-usage direction and didn't fit generators feeding into a heavy backbone via a Small transformer (which is a normal-side source pumping into a heavy-side network).
- **Actively-bridging burn gate.** The reactive cable burn for a transformer tier violation no longer fires whenever the local network has power flow. The stricter condition is `Transformer.OnOff == true` AND `Transformer._powerProvided > 0` (the previous tick's throughput was positive, i.e. the transformer was actually bridging power on both sides). When that condition is met AND the pair is violated, BOTH adjacent cables burn. Reasoning: transformers are expensive to build; punishing a misconfigured-but-off transformer feels wrong from a player-experience standpoint. The burn fires only when the player has actually committed to using the bad bridge.
- **Cable cursor reject scope confirmed.** Only cables are cursor-rejected, never transformers / devices. Cursor logic for the transformer-pair rule: cable's tier must be in the pair; for asymmetric variants with exactly one side wired, the new cable's tier must differ from that side's tier. Mirror of the APC rule (which requires same tier on both sides). Both-sides-wired and neither-side-wired cases handled by the cable-to-cable network-tier check upstream.

### 2026-05-14: LogicPassthroughMode + centralised LogicType catalogue

- `Transformer` gets a writable `LogicPassthroughMode` logic slot (`StationeersPlus.Shared.LogicTypeNumbers.LogicPassthroughMode`, ushort 6577). Per-Transformer override stored in `PassthroughModeStore`; defaults to 1 for `StructureTransformerSmall` + `StructureTransformerSmallReversed` + `StructureRocketTransformerSmall`, 0 for every other transformer prefab. Persists across save / load via an XML side-car in the save ZIP.
- Power Grid Plus joins the central `Patterns/Logic/` catalogue: `Patterns/Logic/LogicTypeNumbers.cs` is linked into the csproj; the integer 6577 lives there, not in `LogicTypeRegistry.cs`. The catalogue's reservation rules and the table of every SixFive7 LogicType assignment live in `Patterns/Logic/README.md`.
- Direction A vs B vs C for the Transformer-logic-transparency feature was decided: option B (per-mod patch on `CableNetwork.RefreshPowerAndDataDeviceLists`, no need to implement `ITransmitDataNetworkDevices` / `IReceiveDataNetworkDevices`). Symmetric: the bridging device acts as both transmitter and receiver against itself. The transformer's own logic ports (Setting, PowerActual) are visible from both sides because the transformer sits in BOTH networks' `DeviceList` already (one cable connection per side).

### 2026-05-18: passthrough extended to Battery / PowerTransmitter / PowerReceiver, and multiplayer merge determinism

- **Passthrough generalised from Transformer + APC to five device kinds.** `Battery`, `PowerTransmitter`, and `PowerReceiver` join the bridge (commit 33fc10b). A battery bridges its own `InputNetwork` <-> `OutputNetwork` (the two cable sides of a station battery); a linked dish pair bridges `tx.InputNetwork` <-> `rx.OutputNetwork` across the wireless link. All three default to `LogicPassthroughMode = 1`. `PassthroughModeStore` was generalised from keying on `Transformer` to keying on any `Thing.ReferenceId`, and the side-car save / load now covers all four mode-bearing types; the on-disk schema is unchanged ((`ReferenceId`, `Mode`) pairs), so a v0.1.0 transformer-only save still loads. Two new server toggles, both default on: `Server - Batteries / Enable Battery Logic Passthrough` and `Server - Power Transmitters / Enable Power Transmitter Logic Passthrough`. `AreaPowerControl` stays a flat toggle with no per-device mode.
- **Base-`Device` patch for the inherited case.** `Battery` / `PowerTransmitter` / `PowerReceiver` do not override the four `Logicable` methods, so the logic ports are wired by patching base `Device` with a runtime `IsBridge` guard (`InheritedLogicablePassthroughLogicPatches`). Attribute-patching the subclasses directly throws "Undefined target method" at `PatchAll` time and silently disables the rest of the batch; see section 7 and `Research/Patterns/HarmonyLogicableInheritedMethodTrap.md`. `Transformer` keeps its own direct patch because it does override the methods.
- **`SetLogicValue` is server-only.** Both the `Transformer` and base-`Device` `SetLogicValue` prefixes bail unless `NetworkManager.IsServer`, so a client tablet write routes through the host instead of mutating the client's local store. Verifying this did not break the tablet-routed write path is an open playtest item in `TODO.md`.
- **Deterministic network merge (commit 14946c5).** `CableNetwork.Merge` and `StructureNetwork.Merge` keep `list[0]` as the survivor, and server and client run the merge independently with no authoritative sync event, so divergent cable-graph iteration order makes the two sides keep different survivors and read different `ReferenceId`s for the same network forever. `MergeDeterminismPatches` sorts the merge-input list by `ReferenceId` first, making the survivor a pure function of server-allocated ids. Covers `WirelessNetwork` (via `CableNetwork`) and rocket / robotic-arm / landing-pad networks (via `StructureNetwork`). See `Research/GameClasses/CableNetwork.md` and `Research/Patterns/MultiplayerStateMutation.md`.

### 2026-05-21: logic passthrough made transitive (multi-hop, cycle-safe)

The 2026-05-18 merge was single-hop: each bridge unified only its two immediate networks, so a logic reader more than one bridge from its target could not see it. Repro on the Lunar save: an IC10 housing on cable network 407130 runs `sb ComputerBigScreen On` against a screen on 407119, two transformers away (407130 -> small transformer 397999 -> spine 398122 -> small transformer 401261 -> 407119); the screen was never in the housing's data device list, so the batch write never reached it.

Fix: the merge now walks the TRANSITIVE closure of the bridge graph.

- `Patches/PassthroughTopology.cs` (new) holds the shared helpers. `IsEnabledBridge(device)` is the per-device bridge test (server toggle + per-device mode), so which devices bridge is unchanged. `GetBridgeNetworks(device)` yields every network a bridge device connects (power input, power output, dedicated `DataCableNetwork`, and a linked dish's far power+data networks). `GatherReachable(start)` is a stack BFS over the bridge graph with a `HashSet<CableNetwork>` visited set: for each enabled bridge on a network it folds in ALL of that device's other networks (all-port bridging), returns every reachable network except `start`, ordered by `ReferenceId` so the merge order is identical on every peer. The visited set makes a cyclic bridge graph terminate (each network folded in once).
- `Patches/LogicPassthroughPatches.cs` postfix merges every reachable network's `DeviceList` into the local `_dataDeviceList` via `GatherReachable`, instead of only the immediate neighbours.
- `Patches/CableNetworkPatches.cs` adds the correctness half: the `DirtyPowerAndDataDeviceLists` postfix now propagates the dirty across the whole component (re-entrancy-guarded `[ThreadStatic]` flag; gated on `GameManager.RunSimulation` to skip world-load / join churn). Vanilla dirties only the network that directly changed; without propagation a far network's change leaves dependent merged lists stale. It calls `DirtyPowerAndDataDeviceLists` (both flags) because `DataDeviceList.get` checks the power flag (the section 7 quirk).

Why approach 1 (per-network BFS + propagation) over a component-precompute or a global-generation cache: the merge runs only at structural-change cadence (the dirty-gated refresh, never per tick -- see `Research/GameClasses/CableNetwork.md` "Refresh cadence"), real components are small, and this keeps the existing per-network local-mutation model with no shared cross-network state (lowest multiplayer / threading risk). If a profiler ever flags a large hot component, promote `GatherReachable`'s union into a per-component shared cache.

Verified on the dedicated server with the Lunar save (2026-05-21, game 0.2.6228.27061) via a temporary merge log: network 407130 resolved a 22-network component (the whole solar tower, bridged by mode-1 transformers / batteries / dishes) with the screen 400892 present in its merged list; 65 components computed at load with no hang (cycle-safe on a real cyclic topology), world loaded in 15s.

Trade-offs: (1) propagation is server-gated, so a client's cross-network data list can be briefly stale after a far-network change until its own next refresh -- cosmetic, server (IC10) reads stay correct, self-heals. (2) Transitivity makes every mode-1-bridged network in a connected web mutually logic-transparent, so a batch write reaches all matching devices across the whole component (the entire tower), not just the nearest -- intended, but worth knowing on heavily-bridged bases.

### 2026-05-23: cable heat dissipation rejected as unworkable

A `Server - Cable Heat Dissipation` feature was considered (idea borrowed from the [Deadly Electricity](https://steamcommunity.com/sharedfiles/filedetails/?id=3575959825) mod): each tick, convert a small fraction of a cable network's throughput into thermal energy deposited into the cable's local atmosphere, so wires get warm under load. Decompile research into Stationeers grid topology and cable placement showed the feature is unworkable in practice, and it was dropped before any code landed.

The blocking fact is the cable's coordinate resolution. A cable's `WorldGrid` resolves to a single 2 m large cell via `GridCenter()` on its `Transform.position`. There is no finer-grained atmosphere resolution; `AtmosphericsManager.AllWorldAtmospheresLookUp` is keyed by 2 m cells only. The placement system biases face-mounted cables (the body-side-bias documented for face-mounted structures in `Research/GameClasses/Cladding.md`) into the cell containing the supporting frame, so a cable visibly running along the room side of a wall still anchors to the wall's cell, not the room's.

The consequence is severe: every cable mounted on a fully-built frame -- whether buried in the cable channel or laid on a visible outer surface -- atmospherically belongs to the frame's sealed cell. That cell has no atmosphere entry at all (`GridController.CanContainAtmos` returns false; `RoomController.GetRoom` returns null; the only fallback is `PlanetaryAtmosphereSimulation.ReadOnlyGlobal`, which is the world climate and not safe to write to). The engine's own pattern for this case is `Atmosphere.CalculateThingEntropy` at decompile line 417511: `if (!CanContainAtmos) return Zero` -- no sink, no dissipation. The only configurations that would actually couple a cable to a real atmosphere are floor-laid cables in genuinely open cells and cables inside iron-frame-stage frames (build index 1, `HasPartialFrame = true`). In any normal mid-to-late-game base, near-zero cables qualify.

That makes the feature pointless. Under a "quiet drop when sealed" implementation it would silently no-op for almost every cable in play, failing the wires-get-warm intent. Under an "accumulate-and-burn when sealed" implementation it would blanket-punish the standard placement pattern of running cables inside walls and structures, which players cannot reasonably avoid. Either implementation is bad in a different way, and there is no third option the grid topology allows.

Dropped from consideration; no implementation work, no config surface, no settings group. The general-purpose heat-emission API research produced during this investigation stays useful for any future feature that emits heat from something with a real atmosphere reference (a device, a pipe, or a Thing on the atmospheric thing list); see `Research/GameSystems/HeatEmissionToAtmosphere.md`.

---

## 9. Config surface (current state)

All settings server-authoritative. This table mirrors `Settings.cs` exactly (section and key strings as bound); the README carries the user-facing copy of the same table.

| Section | Setting | Default | Effect |
|---|---|---|---|
| Server - Cable Simulation | Normal Cable Max Watts | `5000` | Watts cap for normal cable (vanilla value). 0 = unlimited. Runtime-enforced; never written to the save. |
| Server - Cable Simulation | Heavy Cable Max Watts | `100000` | Watts cap for heavy cable (vanilla value). 0 = unlimited. |
| Server - Cable Simulation | Super Heavy Cable Max Watts | `0` | Watts cap for super-heavy cable. 0 = unlimited (the backbone never burns). |
| Server - Cable Costs | Super-Heavy Cable Cost Multiplier | `2.0` | Multiplies the super-heavy cable coil recipe cost. 1.0 = vanilla. Requires restart. |
| Server - Voltage Tiers | Extra Heavy-Cable Devices | (empty) | Comma-separated PrefabName list of extra devices allowed on heavy cable (modded high-draw machines). |
| Server - Batteries | Enable Battery Limits | `true` | Charge / discharge-rate limit stationary batteries (per-prefab caps + cable-headroom cap). |
| Server - Batteries | Station Battery Charge Rate | `5000` | StructureBattery charge cap (W). |
| Server - Batteries | Station Battery Discharge Rate | `10000` | StructureBattery discharge cap (W). |
| Server - Batteries | Large Station Battery Charge Rate | `25000` | StructureBatteryLarge charge cap (W). |
| Server - Batteries | Large Station Battery Discharge Rate | `50000` | StructureBatteryLarge discharge cap (W). |
| Server - Batteries | Nuclear Battery Charge Rate | `25000` | StationBatteryNuclear (third-party MorePowerMod) charge cap (W). |
| Server - Batteries | Nuclear Battery Discharge Rate | `50000` | StationBatteryNuclear discharge cap (W). |
| Server - Batteries | Rocket Battery (Medium) Charge Rate | `5000` | StructureBatteryMedium charge cap (W). |
| Server - Batteries | Rocket Battery (Medium) Discharge Rate | `10000` | StructureBatteryMedium discharge cap (W). |
| Server - Batteries | Auxiliary Rocket Battery Charge Rate | `2500` | StructureBatterySmall charge cap (W). |
| Server - Batteries | Auxiliary Rocket Battery Discharge Rate | `5000` | StructureBatterySmall discharge cap (W). |
| Server - Batteries | Battery Charge Efficiency | `1.0` | Fraction of incoming power stored (sub-500 W trickle stored in full). |
| Server - Batteries | Enable Battery Logic Additions | `true` | Expose the max charge / discharge rates as logic values. |
| Server - Batteries | Enable Battery Logic Passthrough | `true` | Master kill-switch over the per-Battery `LogicPassthroughMode` (a battery bridges its two cable sides; default mode 1). |
| Server - Transformers | Enable Transformer Exploit Mitigation | `true` | Fresh-pull billing: transformers advertise / bill the allocator's converged totals; no free power, quiescent draw billed upstream. |
| Server - Transformers | Enable Transformer Logic Additions | `true` | Expose PowerActual as a logic value. |
| Server - Transformers | Enable Transformer Logic Passthrough | `true` | Master kill-switch over per-Transformer `LogicPassthroughMode`. |
| Server - Transformers | Enable Transformer Shedding | `true` | Priority + shedding (knob sets Priority, input-side shed lockouts, `Shedding` logic value). |
| Server - Transformers | Enable Transformer Overload Protection | `true` | Downstream-side overload lockouts (`Overloaded` logic value); off restores vanilla partial power. |
| Server - Area Power Control | Enable APC Power Fix | `true` | Close the APC power leak / idle drain; cable-headroom cap on charge; cable cap on output. |
| Server - Area Power Control | APC Battery Charge Rate | `1000` | APC cell charge cap (W), further capped by input-cable headroom after passthrough. |
| Server - Area Power Control | APC Battery Discharge Rate | `1000` | APC cell discharge cap (W), further capped by the output cable. |
| Server - Area Power Control | Enable APC Logic Passthrough | `true` | APCs are logic-transparent (no per-device mode). |
| Server - Power Transmitters | Enable Power Transmitter Logic Passthrough | `true` | Master kill-switch over the per-dish `LogicPassthroughMode` for a linked transmitter / receiver pair (bridges across the wireless link; default mode 1). |
| Server - Rocket Umbilical | Enable Rocket Umbilical Limits | `true` | Rate caps + segmenter participation + the four soft-power logic values on the umbilical pair. |
| Server - Rocket Umbilical | Rocket Umbilical Charge Rate | `10000` | Umbilical cell charge cap (W). |
| Server - Rocket Umbilical | Rocket Umbilical Discharge Rate | `10000` | Umbilical cell discharge cap (W). |
| Server - Rocket Umbilical | Enable Umbilical Logic Passthrough | `true` | Docked umbilical pair is logic-transparent; per-half mode mirrored to the partner. |
| Server - Diagnostics | Enable Conservation Check | `true` | Per-tick allocator self-audit (section 3.10): per-net inflow == outflow and per-seg pull invariant, throttled warnings on violation. |
| Server - Emergency Lights | Enable Wall Light Battery Emergency Mode | `true` | Battery wall lights act as emergency backups (off on grid power, on from the cell when the grid fails). |
| Server - Emergency Lights | Emergency Light Prefabs | `StructureWallLightBattery` | Comma-separated prefab list that gets the emergency behaviour. |

Always-on behaviour with no toggle: voltage tiers, cycle faults, producer isolation, the deterministic cable-burn rule, the unknown-bridge census, the Powered presentation policy, and the ledger adoption (sweep + clamp). Section / key strings are the entry identity in BepInEx; renames re-seed the value at next launch.

---

## 10. Pure-patch invariants

- **No new prefabs.** The mod's only added content is the `GameData/cable-recipes.xml` overlay. Every patch targets a vanilla type. Removing the DLL from a save preserves every existing thing in the world.
- **Side-car XMLs are skipped by vanilla.** `pwrgridplus-passthrough.xml` and similar entries inside the save ZIP are unknown to the vanilla loader; uninstalling the mod and saving once rebuilds the ZIP with only the five known entries. The mod's per-device `LogicPassthroughMode` overrides are forgotten silently; the affected transformers, batteries, and transmitter / receiver dishes revert to vanilla logic-opaque behaviour.
- **Multiplayer is required.** `MOD.Networking.Required = true`. Clients without the matching DLL are rejected at join time. Re-Volt itself swaps the `PowerTick` per network the same way; running both is not supported (see Appendix B for the compat table).

---

## 11. Licensing and attribution

This mod is a derivative of Re-Volt (https://github.com/sukasa/revolt), which is under the **MIT License, Copyright (c) 2025 Sukasa**. MIT permits copy, modify, trim, relicense, and ship, with one obligation: the MIT copyright + permission notice must travel with any substantial portion of the original code.

Therefore:

- Files ported substantially from Re-Volt (the transformer / battery / APC power patches; formerly also the deleted `PowerGridTick.cs` and its routing patches) carry a short header crediting Sukasa.
- The repo-root `NOTICE` names SixFive7 and the project; the repo `LICENSE` is Apache 2.0; the README ends with a `## Credits` section linking the Re-Volt repo. The `About.xml` `<Description>` mirrors the credits and the `[h2]License[/h2]` section.
- Original code (NEW-1 / NEW-2 / NEW-3, LogicPassthroughMode, the renamed glue, config) is **Apache 2.0** per the repo standard. The MIT-covered ported portions remain MIT regardless of the surrounding Apache 2.0 (dual provenance, which Apache and MIT both permit).
- Clean reimplementations of Re-Volt mechanics (as opposed to copies) carry no MIT-notice obligation; mechanics are not copyrightable. Where copied, attribution; where rewritten, courtesy.

---

## 12. Open questions

Items not yet resolved either way. Implementation backlog (decided but not yet built) lives in `TODO.md`; this section is for genuinely unresolved questions.

- **Heavy-cable device whitelist tuning.** The hardcoded high-draw machine list (`CarbonSequester`, `FurnaceBase`, `ArcFurnace`, `Centrifuge`, `Recycler`, `IceCrusher`, `HydraulicPipeBender`, `DeepMiner`) was assembled from community consensus; only the first three are verified-by-decompile to draw >5 kW. An InspectorPlus `types=[Device], fields=[UsedPower, MaxUsedPower, GetUsedPower(network)]` sweep on a high-load save is the way to verify or trim.
- **Real `MaxVoltage` values** for `heavy` and `superHeavy` cable prefabs. Recorded as an Open Question in `Research/GameClasses/Cable.md`.
- **Heavy and insulated cable coil prefab names.** Super-heavy is resolved (`ItemCableCoilSuperHeavy`); the heavy and insulated names are needed if the NEW-2 cost rule ever extends to them.
- **Cross-mod interaction with `MoreCables`' extra tiers.** That mod adds two cable types above the vanilla set; the voltage-tier rule would need to classify them (treat as `superHeavy`-equivalent, reject, or configurable). Not specified yet because no in-game testing has been done alongside MoreCables.
---

## Appendix A: Re-Volt vs Power Grid Plus feature matrix

> Historical snapshot from the 2026-05-12 port planning; the Power Grid Plus column reflects that date. The v0.2.0 rework superseded rows 1-5 and 12: the vanilla `PowerTick` now runs unmodified inside the atomic tick (no custom tick class), proportional source / load sharing became the global allocator's priority dispatch, and the probabilistic sliding-window burnout became the deterministic generator-overflow rule. Section 3 is the current architecture.

Legend:

| Icon | Re-Volt column | Icon | Power Grid Plus column |
|---|---|---|---|
| ✅ | present | ♻️ | inherited (ported from Re-Volt) |
| 🟡 | stub / partial only | 🔧 | inherited, required infrastructure ("glue") |
| ❌ | not present | 🆕 | new, original to this mod |
| | | 🚧 | planned, research-gated (go/no-go pending) |
| | | ❔ | undecided (port-time decision) |
| | | ❌ | dropped / not present |

Doc tags on the feature name: 📣 = appears on Re-Volt's Steam Workshop page; 🔬 = source-only (not described there).

| # | Feature | Re-Volt | Power Grid Plus | Notes |
|---|---|:---:|:---:|---|
| 1 | Vanilla power tick replaced (`PowerTick` -> custom) 📣 | ✅ | 🔧 ♻️ | glue |
| 2 | Proportional load sharing among all power sources 📣 | ✅ | ♻️ | |
| 3 | Proportional distribution to loads 📣 | ✅ | ♻️ | |
| 4 | Gradual, probabilistic cable burnout (not instant) 📣 | ✅ | ♻️ | heavy + superHeavy cables exempted by NEW-1 |
| 5 | Sliding-window (10-20s) overheat model 🔬 | ✅ | ♻️ | |
| 6 | Weakest fuse / weakest cable fails first 📣 | ✅ | ♻️ | |
| 7 | Fuses blow instantly and always pre-empt a cable burn 📣 | ✅ | ♻️ | |
| 8 | Breaker/cable-burn trip coordination (trip all interrupting breakers, power flows one tick) 🔬 | ✅ | ❌ | no breakers |
| 9 | Recursive/looped networks allowed; vanilla check optional 📣 | ✅ | ♻️ | config `Enable Recursive Network Limits` |
| 10 | NaN-power bugfix 🔬 | ✅ | ♻️ | |
| 11 | Power-Transmitter cable-less-network bugfix 🔬 | ✅ | ♻️ | |
| 12 | `Device.AssessPower` reimplemented for the new tick 🔬 | ✅ | 🔧 ♻️ | glue |
| 13 | Provider array / `InputOutputDevices` resync (Network Analyzer stays correct) 🔬 | ✅ | ♻️ | |
| 14 | Brownouts | 🟡 | ❌ | stub only in Re-Volt; not ported |
| 15 | Device power-class classification (Lights/Doors/Atmos/Equipment/Logic/Power/Misc) 🔬 | ✅ | ❌ | dropped with the Load Center |
| 16 | Load Center structure (mass on/off per power class) 📣 | ✅ | ❌ | |
| 17 | Load Center logic interface (per-class On read/write, per-class wattage read) 📣 | ✅ | ❌ | |
| 18 | Load Center conflict handling (2+ on one network -> all disabled) 🔬 | ✅ | ❌ | |
| 19 | All power classes default ON without a Load Center 🔬 | ✅ | ❌ | n/a -- no power-class toggling at all |
| 20 | Small Circuit Breaker (resettable, screwdriver-set trip point, probabilistic trip) 📣 | ✅ | ❌ | |
| 21 | "Reversed" mounting variants of Small/Smart breakers 🔬 | ✅ | ❌ | |
| 22 | Smart Breaker (instant trip, built-in cable-analyzer data, logic-readable) 📣 | ✅ | ❌ | |
| 23 | Breaker UI: trip-point cycling, on/off/open/close, "Test (trip)" stub, passive tooltips 🔬 | ✅ | ❌ | |
| 24 | Breaker persistence + multiplayer sync (`CircuitBreakerSaveData`, `ByteArraySync`) 🔬 | ✅ | ❌ | |
| 25 | Heavy Breaker (switchgear pseudo-network, bolted door, 4 setting buttons; recipe disabled) 📣 (listed as "future") | ✅ shipped but uncraftable | ❌ | |
| 26 | Construction kits + electronics-printer recipes (Small/Smart/Load Center; Heavy commented out) 🔬 | ✅ | ❌ | |
| 27 | Custom-prefab loader + material remap to vanilla colours 🔬 | ✅ | ❌ | |
| 28 | New devices filed under "Cable" Stationpedia category 🔬 | ✅ | ❌ | no new devices |
| 29 | Stationary battery charge/discharge-rate limiting 📣 | ✅ | ♻️ | config |
| 30 | Battery charge-efficiency option 🔬 | ✅ | ♻️ | config, default 1.0 |
| 31 | Battery logic additions (`ImportQuantity` / `ExportQuantity`) 🔬 | ✅ | ♻️ | config toggle |
| 32 | Transformer free-power exploit mitigation + quiescent draw restored 📣 | ✅ | ♻️ | config toggle |
| 33 | Transformer logic addition (`PowerActual`) 🔬 | ✅ | ♻️ | config toggle |
| 34 | APC power-discrepancy + quiescent-draw + slow-drain fix 📣 | ✅ | ♻️ | config toggle |
| 35 | `Thing.HasState` Button3 bugfix + Button4/5 state support 🔬 | ✅ | ❌ | only needed by dropped multi-button devices |
| 36 | Visual/component infrastructure (status screen, indicator changers, multi-state animator) 🔬 | ✅ | ❌ | |
| 37 | Config: `Cable burn factor` | ✅ | ♻️ | |
| 38 | Config: `Enable Recursive Network Limits` | ✅ | ♻️ | |
| 39 | Config: `Max Battery charge rate` / `Max Battery discharge rate` / `Battery Charge Efficiency` | ✅ | ♻️ | |
| 40 | Config: `Enable Battery Limits` / `Enable Battery Logic Additions` | ✅ | ♻️ | |
| 41 | Config: `Enable Transformer Exploit Mitigation` / `Enable Transformer Logic Additions` | ✅ | ♻️ | |
| 42 | Config: `Enable APC Power Fix` | ✅ | ♻️ | |
| 43 | Config: `Heavy Breaker Maximum Trip Setting` | ✅ | ❌ | no heavy breaker |
| 44 | Config: `Enable Custom Objects` (prefab content toggle) | ✅ | ❌ | no prefabs |
| 45 | Dependency: LibConstruct | ✅ Heavy Breaker switchgear network | ❌ | |
| 46 | Dependency: custom asset bundle / prefabs | ✅ | ❌ | |
| 47 | Custom save-data type registered | ✅ `CircuitBreakerSaveData` | ❌ | |
| 48 | `SetMultiplayerRequired()` | ✅ | ♻️ | |
| **NEW-1** | **Unlimited super-heavy cable (never burns)** -- `superHeavy` only; `normal` and `heavy` keep their ratings | ❌ | 🆕 | config `Enable Unlimited Super-Heavy Cables` (default on) |
| **NEW-2** | **Super-heavy cable costs more to build** | ❌ | 🆕 | config `Super-Heavy Cable Cost Multiplier`, default 2.0 (super-heavy coil costs 2x vanilla material) |
| **NEW-3** | **Three-tier voltage gating** -- `normal`/`heavy`/`superHeavy` cables mutually incompatible, bridged only by transformers; per-device tier rules; reactive cable-burn enforcement | ❌ | 🆕 | config `Enable Voltage Tiers` |
| **NEW-4** | **LogicPassthroughMode** -- writable per-device logic slot making power-network bridging devices logic-transparent (Transformer, Battery, linked PowerTransmitter / PowerReceiver; APC bridges via a flat toggle) | ❌ | 🆕 | toggles `Enable Transformer / Battery / Power Transmitter Logic Passthrough` + `Enable APC Logic Passthrough`; default mode 1 for battery / transmitter / receiver and the small transformer + reversed, 0 on other transformers; persists in save side-car |
| **NEW-5** | **Deterministic network merge** -- `CableNetwork` / `StructureNetwork` merge survivor chosen by lowest `ReferenceId` so multiplayer peers stay in sync | ❌ | 🆕 | always on; multiplayer-correctness fix, no config |

---

## Appendix B: the Stationeers power-mod landscape

What else on the Steam Workshop touches power, and how Power Grid Plus relates to each. Surveyed 2026-05-12 (game-side appid 544550) across ~30 keyword vectors. Excludes IC10 scripts / blueprints (the Workshop has dozens of power-themed scripts -- PIAC is the most polished, listed below for reference -- but they are code/blueprint UGC, not BepInEx/LaunchPad mods). "Subs" = current subscribers at survey time; rough popularity only.

Legend, "Relation to Power Grid Plus":
- 🟥 **Competes / overlaps** -- does roughly what Power Grid Plus does (a power-sim overhaul); a player picks one.
- 🟧 **Conflicts likely** -- patches the same vanilla classes (`PowerTick` / `Cable` / `Battery` / `Transformer` / `AreaPowerControl`); load-order or behavior clashes probable; Power Grid Plus must detect or document.
- 🟨 **Adjacent, mostly compatible** -- adds power *content* (generators / batteries / cables) or tweaks balance via XML/config; coexists, but Power Grid Plus's config may want to know about it (e.g. MoreCables' extra tiers).
- 🟩 **Orthogonal** -- different corner of the power space (wireless dishes, QoL paint, monitoring); no real interaction.

| Mod | ID | Author | Subs | Updated | LaunchPad? | Category | What it does | Relation to Power Grid Plus |
|---|---|---|---:|---|:---:|---|---|:---:|
| [**Re-Volt**](https://steamcommunity.com/sharedfiles/filedetails/?id=3587239682) | 3587239682 | Sukasa | ~1,510 | 2025-12-28 | yes | overhaul | The mod Power Grid Plus is derived from: rewritten power tick, proportional source/load sharing, probabilistic cable burnout, rate-limited batteries, circuit/smart/heavy breakers, Load Center, recursive networks allowed, transformer/APC fixes. | 🟥 Power Grid Plus is a trimmed-down derivative; you run one or the other. Power Grid Plus must detect Re-Volt for NEW-1 (Re-Volt swaps the `PowerTick` instance). |
| [**Deadly Electricity**](https://steamcommunity.com/sharedfiles/filedetails/?id=3575959825) | 3575959825 | Hisha | ~235 | 2025-10-29 | yes | overhaul / balance | Cable networks lose efficiency (-> heat) with size; cutting live wires sparks/injures/kills; excess generation strains/damages/ignites generators unless dumped to heaters. | 🟧 Different mechanic, but likely patches `PowerTick` / `CableNetwork` / generator classes; stacking with Power Grid Plus's rewritten tick is risky. Document as incompatible-ish. |
| **Better Power Mod** (ex-ActualSolarIrradiance) | 3234916147 | Vivien | popular | 2025-03-29 | yes | balance / generation | Fixes solar 500 W cap; buffs wind turbines, Stirling engines (20 kW), turbine generators (x10); raises battery chargers / power controllers / omni transmitters to 2,500 W; solar-position tooltip. | 🟨 Mostly generation-output buffs (likely value patches on those classes); coexists with Power Grid Plus. Combine for a "buffed + overhauled" run. |
| **PowerOverhaul** (gas-device power) | 3579306377 | Xara Loft | n/a | 2025-10-03 | yes | balance | Scales power draw of Pressure/Back-Pressure Regulator, Volume Pump, Gas Mixer (5-100%, configurable), adjusting runtime and logic-reported values. | 🟨 Edits per-device `UsedPower`; orthogonal to the tick rewrite. Coexists. Despite the name, narrow scope. |
| [**Custom Power DeepMiner**](https://steamcommunity.com/sharedfiles/filedetails/?id=3491001740) | 3491001740 | Thunder | ~25 | 2025-05-31 | yes | balance | Configurable Deep Miner power draw (1 W - 100 kW; default 5 kW vs. vanilla 500 W). | 🟨 Per-device draw tweak; coexists. |
| [**MorePowerMod**](https://steamcommunity.com/sharedfiles/filedetails/?id=3243132734) | 3243132734 | WIKUS | ~1,910 | 2025-10-28 | yes | generation / storage / wireless | Big Omni Transmitter (40 m, 5 kW, logic-controlled, wall/ceiling); Nuclear Station Battery (230.4 MW); Nuclear Wireless Battery Cell (2.304 MW). (Internal assembly name `OmniTransmitterLargeMod`.) | 🟨 New prefabs subclassing vanilla `Battery` / wireless classes; works with Power Grid Plus's tick (which is class-agnostic). The new batteries inherit Power Grid Plus's rate limits if `Enable Battery Limits` is on -- that may or may not be desired (config can't single them out). |
| [**MoreCables**](https://steamcommunity.com/sharedfiles/filedetails/?id=3555588082) | 3555588082 | spacebuilder2020 | ~150 | 2025-12-05 | yes | distribution | Adds two new cable types (super-heavy 500 kW, super-conductor 1 MW); lets all cable voltages be reconfigured. | 🟧 Directly relevant: Power Grid Plus's heavy-cable tier logic must know whether MoreCables' tiers count as "backbone". Coexistence needs an explicit decision (treat their tiers as backbone? as `normal`? configurable?). |
| [**CableTypeSwitcher**](https://steamcommunity.com/sharedfiles/filedetails/?id=3470776044) | 3470776044 | Cihla_cz | ~58 | 2025-04-26 | yes | distribution / qol | `CABLETYPESWITCH` console command: inspect a cable network, convert it between `normal` / `heavy` / `super-heavy`, split long super-heavy runs. | 🟧 Converts whole networks between tiers at runtime -- bypasses NEW-3's build-time gate. NEW-3's reactive burn-on-flow needs to fire after this too, or document the interaction. |
| [**EL Switche**](https://steamcommunity.com/sharedfiles/filedetails/?id=3287183705) | 3287183705 | Kastuk | ~295 | 2025-10-17 | yes | distribution | Logic-controlled "switch" that spawns/removes cables (normal + heavy) in place to join or split power+data networks on a click. | 🟧 Programmatically creates cables -- could create heavy cables adjacent to devices, bypassing NEW-3's build-time gate. The reactive burn-on-flow needs to cover spawned cables too. |
| [**NetworkUpgrader**](https://steamcommunity.com/sharedfiles/filedetails/?id=3656955459) | 3656955459 | Jacksonthemaster | ~320 | 2026-01-31 | yes | qol / distribution | `upgrade pipes\|cables\|chutes\|all` console command: replaces chains of 3+ straight super-heavy cables/pipes/chutes with their long variants, preserving colors. | 🟩 Mesh/length consolidation only; doesn't change tier or topology. Orthogonal. |
| [**Network Painter**](https://steamcommunity.com/sharedfiles/filedetails/?id=2876605527) | 2876605527 | Elmo | ~5,880 | 2024-12-14 | yes | qol | Spray-painting a cable repaints the whole cable (pipe/chute) network; shift = single, ctrl = checkered. | 🟩 Pure cosmetic. Orthogonal. (Note: this repo's own Spray Paint Plus is in the same space.) |
| [**Battery Backup Light**](https://steamcommunity.com/sharedfiles/filedetails/?id=3569109044) | 3569109044 | alliephante | ~1,510 | 2026-02-23 | yes | distribution / qol | Wall Light Battery becomes a true emergency light: turns on when the grid loses power, runs off its cell; Mode 1 reverts to vanilla. | 🟩 Hooks one device's `OnPowerTick`; reads network power state. Coexists with Power Grid Plus (both read the same `CableNetwork.RequiredLoad`-style state). Low risk. |
| [**"Better" Battery Light**](https://steamcommunity.com/sharedfiles/filedetails/?id=3491770665) | 3491770665 | TrippleTrip | ~23 | 2025-06-01 | yes | qol / logic | Battery wall light turns yellow below 99.5% cell (unlocked) or on when grid wattage < 100 W (locked). | 🟩 Same niche as above; orthogonal. |
| [**Omni Transmitter Settings**](https://steamcommunity.com/sharedfiles/filedetails/?id=3643534844) | 3643534844 | RogerWaters | ~155 | 2026-01-10 | yes | wireless / balance | Reconfigures the vanilla Omni Transmitter: max range, distance falloff, min-power-per-battery floor; multiplayer / dedicated aware. | 🟩 Wireless-power tuning; orthogonal to wired-power overhaul. |
| [**Better Transmitter**](https://steamcommunity.com/sharedfiles/filedetails/?id=3582023521) | 3582023521 | Viroman | ~17 | 2025-10-06 | yes | wireless / balance | Single setting to change power transmitter range (up to 10k); vanilla loss still applies. | 🟩 Orthogonal. |
| [**Power Transmitter Plus**](https://steamcommunity.com/sharedfiles/filedetails/?id=3707677512) | 3707677512 | SixFive7 | new | 2026-05-06 | yes | wireless / logic | (This repo's mod.) Visible beam + scrolling pulses on the Microwave Power Transmitter, distance-cost source-draw model, 5 logic readouts + auto-aim logic type, wall/ceiling placement. | 🟩 Sibling mod; different device. Should be combinable with Power Grid Plus -- worth a smoke test. |
| [**3.6 Megawatt Battery**](https://steamcommunity.com/sharedfiles/filedetails/?id=3004087671) | 3004087671 | walkin_here | ~265 | 2023-07-29 | XML | storage | Nuclear Battery capacity -> 3.6 MWh; charging consumes a whole Station Battery. | 🟨 XML stat edit; coexists. |
| [**EGC - End Game Content**](https://steamcommunity.com/sharedfiles/filedetails/?id=3007388410) | 3007388410 | walkin_here | ~306 | 2023-07-29 | XML | storage / wireless | XL Wireless Battery recipe + capacity -> 1170 kWh; infinite-filter recipes. | 🟨 XML; coexists. |
| [**Super XL Wireless Battery (EGC Edit)**](https://steamcommunity.com/sharedfiles/filedetails/?id=3706534108) | 3706534108 | Nitz | ~14 | 2026-04-14 | yes | storage / wireless | XL Wireless Battery -> 90 MWh; expensive recipe. | 🟨 Coexists. |
| [**BuffWirelessBatteries**](https://steamcommunity.com/sharedfiles/filedetails/?id=3355912171) | 3355912171 | Grilled Salmon | ~118 | 2024-10-27 | XML | storage / wireless | Wireless cells: small 12 kJ -> 27 kJ, large 72 kJ -> 216 kJ. | 🟨 XML stat edit; coexists. |
| [**Mod: Jigawatt Battery**](https://steamcommunity.com/sharedfiles/filedetails/?id=1785747072) | 1785747072 | Kastuk | ~360 | 2019-09-24 | XML | storage | Adds a 1.21 GW creative-scale battery; needs Processed Uranium. | 🟨 Coexists; needs Power Grid Plus's `Enable Battery Limits` consideration (a 1.21 GW battery rate-limited at 0.7%/tick is still ~8.5 MW/tick discharge -- probably fine, but note it). |
| [**RRI - Boost Da Powa [Battery Upgrades]**](https://steamcommunity.com/sharedfiles/filedetails/?id=2359999429) | 2359999429 | ZimmiMane | ~84 | 2021-01-13 | XML | storage | Upgraded battery cells (200 kJ / 600 kJ / 4 MJ) crafted from copper/gold/silver. | 🟨 Coexists. |
| [**CraftableRTG**](https://steamcommunity.com/sharedfiles/filedetails/?id=3026880031) | 3026880031 | S.W.A.TGamer | ~404 | 2024-07-11 | XML | generation | Survival craft recipe for the RTG. | 🟨 Recipe add; coexists. |
| [**Survival RTG**](https://steamcommunity.com/sharedfiles/filedetails/?id=2464235697) | 2464235697 | Aaron Tigan | ~353 | 2021-04-29 | XML | generation | RTG craftable in Electronics Printer Mk2. | 🟨 Coexists. |
| [**RTG Start Conditions and Difficulty Mod**](https://steamcommunity.com/sharedfiles/filedetails/?id=3568945039) | 3568945039 | Zky | ~138 | 2025-10-16 | XML | generation / balance | "Standard RTG Start+" (12 RTGs) + relaxed difficulties. | 🟩 Worldgen/start; orthogonal. |
| [**Nuclear Recipes**](https://steamcommunity.com/sharedfiles/filedetails/?id=3004097828) | 3004097828 | walkin_here | ~317 | 2023-07-29 | XML | generation / balance | Reworks Nuclear Battery + RTG recipes to use Processed Uranium; expensive/slow. | 🟨 Recipe rework; coexists. |
| [**[OBSOLETE] Recipe - RTG**](https://steamcommunity.com/sharedfiles/filedetails/?id=1351511005) | 1351511005 | BoNes | ~940 | 2018-04-03 | XML | generation | First-gen RTG recipe mod. Abandoned. | 🟨 Obsolete; ignore. |
| [**2x Solar Power**](https://steamcommunity.com/sharedfiles/filedetails/?id=1669621266) | 1669621266 | MutatedPixel | ~400 | 2019-03-02 | XML | balance / generation | Doubles solar output (Moon/Mars/space/Europa). | 🟨 Solar-constant edit; coexists. |
| [**Realistic Solar Constants**](https://steamcommunity.com/sharedfiles/filedetails/?id=1475742702) | 1475742702 | FreezePop | ~23 | 2018-08-12 | XML | balance | Solar irradiance set to "realistic" levels (Mars 43%, Europa 3.7%). | 🟨 Solar-constant edit; coexists (opposite direction to the above). |
| [**ArcFurnace Overhaul**](https://steamcommunity.com/sharedfiles/filedetails/?id=2564615804) | 2564615804 | SCR00G3 | ~95 | 2021-08-03 | XML | balance | Arc Furnace smelts all ingots in one tick at 10 W/unit. | 🟩 Furnace power-cost tweak; orthogonal. |
| [**Power2Resources**](https://steamcommunity.com/sharedfiles/filedetails/?id=1328890332) | 1328890332 | Dark Phoenix | ~47 | 2020-02-08 | XML | balance | Tool Manufactory makes ingots/coal at the cost of power. | 🟩 Orthogonal. |
| [**Power Trading**](https://steamcommunity.com/sharedfiles/filedetails/?id=3420179579) | 3420179579 | Wilhelm W. Walrus | ~37 | 2025-02-03 | XML | misc | Trader buys uranium / charcoal / solid fuel. | 🟩 Economy; orthogonal. |
| [**Bins Power Rebalance**](https://steamcommunity.com/sharedfiles/filedetails/?id=2392722433) | 2392722433 | Binaryclock03 | ~11 | 2021-02-12 | (old loader) | balance | Console power use 50 W -> 1 W; flagged "maybe doesn't work". | 🟨 Per-device tweak; likely broken on current game. |
| [**More Stirling Logic**](https://steamcommunity.com/sharedfiles/filedetails/?id=2883817490) | 2883817490 | silentdeth | ~13 | 2022-11-05 | (old loader) | logic | Adds atmospheric logic reads on the Stirling engine. | 🟩 Logic-surface add; orthogonal. |
| [**Super Structures Mod**](https://steamcommunity.com/sharedfiles/filedetails/?id=3159841144) | 3159841144 | Nitz | ~32 | 2024-02-13 | XML | generation (enabler) | Raises pressure/temp limits on Turbine Generator + reinforced windows / iron window / active vents. | 🟨 Lets turbine generators run harder; orthogonal to the tick. |
| [**PIAC - Power Info and Control**](https://steamcommunity.com/sharedfiles/filedetails/?id=3687433072) | 3687433072 | 3xp | ~466 | 2026-04-05 | (IC10 script, no code) | logic | IC10 power dashboard: generation/usage stats, solar tracking, generator fallback/auto mode. | 🟩 Script package; orthogonal. (The Workshop also has many smaller power IC10 scripts: Power manager 2256532974, Power Displays 2843854351, Total-Power-Control 3630943416, etc. -- not enumerated here.) |
| [**Wind Vs Gravity**](https://steamcommunity.com/sharedfiles/filedetails/?id=2936575063) | 2936575063 | Thunder | ~436 | 2026-01-14 | yes | adjacent | Wind/pressure forces scale with planetary gravity (affects wind turbines indirectly via storms). | 🟩 Atmospherics, not power. Orthogonal. |
| [**LibConstruct**](https://steamcommunity.com/sharedfiles/filedetails/?id=3505115682) | 3505115682 | tom_is_unlucky | n/a | 2026-03-15 | yes | framework | Custom-placement library (Modular Consoles etc.). Not power; Re-Volt's Workshop page does NOT actually declare a LibConstruct dependency (only `StationeersLaunchPad`). | 🟩 Not relevant -- Power Grid Plus drops the breakers, so no LibConstruct. |

Takeaways for Power Grid Plus:
- Only **Re-Volt** and **Deadly Electricity** truly compete (both rewrite the tick / cable behavior). Power Grid Plus positions itself as "Re-Volt's simulation half, no new content, plus a heavy-cable backbone tier".
- The mods Power Grid Plus must explicitly account for: **Re-Volt** (PowerTick swap -- detect for NEW-1), **MoreCables** (extra cable tiers), **CableTypeSwitcher** / **EL Switche** (programmatic cable creation/conversion -- bypass NEW-3's build-time gate; reactive burn-on-flow must cover them), and **MorePowerMod** / big-battery mods (inherit Power Grid Plus's battery rate limits if enabled -- document, can't single them out).
- Everything else is orthogonal or pure XML-stat tweaks that coexist fine.

---

## Appendix C: power-mod capability matrix (every power mod vs what it changes)

The fine-grained 48-feature breakdown lives in Appendix A (Re-Volt vs Power Grid Plus, the only two mods with that depth of overhaul). This is the wide view: every Stationeers power mod from the survey against the capability buckets that distinguish them. A literal 48-rows-by-37-mods matrix would be ~1800 mostly-empty cells; the buckets below collapse the overhaul rows (where only Re-Volt / Power Grid Plus / Deadly Electricity have anything) and expand the rows where the content mods differ. `+` = does it; `.` = doesn't; `(note)` = qualified.

Column legend:

| Col | Capability |
|---|---|
| A | Replaces the power tick / changes cable-burnout behaviour (instant -> gradual, efficiency loss, etc.) |
| B | Stationary-battery changes (charge/discharge-rate limits, charge efficiency, battery logic values) |
| C | Transformer / APC fixes (free-power exploit, quiescent draw, slow drain) |
| D | Recursive-network handling / Load Center / circuit breakers (overhaul-only structural features) |
| E | Buffs power-generation output (solar / wind / Stirling / turbine generators, or enables them to run harder) |
| F | Tweaks per-device power draw (regulators, pumps, console, deep miner, etc.) |
| G | Adjusts solar-irradiance constants |
| H | Rebalances electrical crafting recipes (RTG / nuclear battery / arc furnace / power-to-resources) |
| I | Adds a new wired generation/storage device (new battery, capacitor, etc.) |
| J | Buffs vanilla battery / cell capacity stats |
| K | New cable tier / reconfigures cable voltage ratings / converts a network's tier at runtime / logic-spawns cables |
| L | Adds a new wireless transmitter/receiver/battery device, or tunes wireless range/falloff |
| M | Power logic / monitoring additions, or power-management QoL (network paint, bulk cable upgrade, emergency lights, IC10 dashboards) |
| N | New Power-Grid-Plus-only feature (unlimited super-heavy cable / super-heavy cost / three-tier voltage gating / LogicPassthroughMode / deterministic network merge) |

| Mod | A | B | C | D | E | F | G | H | I | J | K | L | M | N |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| [Re-Volt](https://steamcommunity.com/sharedfiles/filedetails/?id=3587239682) | + | + | + | + | . | . | . | . | . | . | . | . | + | . |
| **Power Grid Plus** | + | + | + | (recursive only) | . | . | . | . | . | . | + | . | . | + |
| [Deadly Electricity](https://steamcommunity.com/sharedfiles/filedetails/?id=3575959825) | + | . | . | . | (surplus damages generators) | . | . | . | . | . | . | . | . | . |
| [Better Power Mod](https://steamcommunity.com/sharedfiles/filedetails/?id=3234916147) | . | . | . | . | + | (chargers/controllers/omni to 2.5 kW) | . | . | . | . | . | . | (solar tooltip) | . |
| [PowerOverhaul (gas devices)](https://steamcommunity.com/sharedfiles/filedetails/?id=3579306377) | . | . | . | . | . | + | . | . | . | . | . | . | . | . |
| [Custom Power DeepMiner](https://steamcommunity.com/sharedfiles/filedetails/?id=3491001740) | . | . | . | . | . | + | . | . | . | . | . | . | . | . |
| [Bins Power Rebalance](https://steamcommunity.com/sharedfiles/filedetails/?id=2392722433) | . | . | . | . | . | + (console 1 W) | . | . | . | . | . | . | . | . |
| [ArcFurnace Overhaul](https://steamcommunity.com/sharedfiles/filedetails/?id=2564615804) | . | . | . | . | . | + (arc furnace) | . | + | . | . | . | . | . | . |
| [Power2Resources](https://steamcommunity.com/sharedfiles/filedetails/?id=1328890332) | . | . | . | . | . | . | . | + (power -> ingots) | . | . | . | . | . | . |
| [Super Structures Mod](https://steamcommunity.com/sharedfiles/filedetails/?id=3159841144) | . | . | . | . | + (turbine gen limits) | . | . | . | . | . | . | . | . | . |
| [MorePowerMod](https://steamcommunity.com/sharedfiles/filedetails/?id=3243132734) | . | . | . | . | . | . | . | . | + (nuclear batteries) | . | . | + (big omni transmitter + nuclear wireless cell) | . | . |
| [MoreCables](https://steamcommunity.com/sharedfiles/filedetails/?id=3555588082) | . | . | . | . | . | . | . | . | . | . | + (super-heavy + super-conductor tiers; voltage reconfig) | . | . | . |
| [CableTypeSwitcher](https://steamcommunity.com/sharedfiles/filedetails/?id=3470776044) | . | . | . | . | . | . | . | . | . | . | + (convert network tier at runtime) | . | + (QoL) | . |
| [EL Switche](https://steamcommunity.com/sharedfiles/filedetails/?id=3287183705) | . | . | . | . | . | . | . | . | . | . | + (logic-spawns/removes cables) | . | + (logic device) | . |
| [NetworkUpgrader](https://steamcommunity.com/sharedfiles/filedetails/?id=3656955459) | . | . | . | . | . | . | . | . | . | . | (consolidates straight runs) | . | + (QoL command) | . |
| [Network Painter](https://steamcommunity.com/sharedfiles/filedetails/?id=2876605527) | . | . | . | . | . | . | . | . | . | . | . | . | + (network-wide cable paint) | . |
| [Battery Backup Light](https://steamcommunity.com/sharedfiles/filedetails/?id=3569109044) | . | . | . | . | . | . | . | . | . | . | . | . | + (emergency-light behaviour) | . |
| ["Better" Battery Light](https://steamcommunity.com/sharedfiles/filedetails/?id=3491770665) | . | . | . | . | . | . | . | . | . | . | . | . | + (battery-light indicator) | . |
| [More Stirling Logic](https://steamcommunity.com/sharedfiles/filedetails/?id=2883817490) | . | . | . | . | . | . | . | . | . | . | . | . | + (Stirling logic reads) | . |
| [Omni Transmitter Settings](https://steamcommunity.com/sharedfiles/filedetails/?id=3643534844) | . | . | . | . | . | . | . | . | . | . | . | + (omni transmitter range/falloff) | . | . |
| [Better Transmitter](https://steamcommunity.com/sharedfiles/filedetails/?id=3582023521) | . | . | . | . | . | . | . | . | . | . | . | + (transmitter range) | . | . |
| [Power Transmitter Plus](https://steamcommunity.com/sharedfiles/filedetails/?id=3707677512) (SixFive7) | . | . | . | . | . | . | . | . | . | . | . | + (microwave transmitter beam / distance-cost model / wall+ceiling) | + (5 logic readouts + auto-aim) | . |
| [3.6 Megawatt Battery](https://steamcommunity.com/sharedfiles/filedetails/?id=3004087671) | . | . | . | . | . | . | . | . | . | + (nuclear battery 3.6 MWh) | . | . | . | . |
| [EGC - End Game Content](https://steamcommunity.com/sharedfiles/filedetails/?id=3007388410) | . | . | . | . | . | . | . | + (XL wireless battery recipe) | . | + (XL wireless battery 1170 kWh) | . | . | . | . |
| [Super XL Wireless Battery (EGC Edit)](https://steamcommunity.com/sharedfiles/filedetails/?id=3706534108) | . | . | . | . | . | . | . | . | . | + (XL wireless 90 MWh) | . | . | . | . |
| [BuffWirelessBatteries](https://steamcommunity.com/sharedfiles/filedetails/?id=3355912171) | . | . | . | . | . | . | . | . | . | + (wireless cells 2.25-3x) | . | . | . | . |
| [Mod: Jigawatt Battery](https://steamcommunity.com/sharedfiles/filedetails/?id=1785747072) | . | . | . | . | . | . | . | + (recipe) | + (1.21 GW battery) | . | . | . | . | . |
| [RRI - Boost Da Powa](https://steamcommunity.com/sharedfiles/filedetails/?id=2359999429) | . | . | . | . | . | . | . | + (recipes) | + (upgraded battery cells) | . | . | . | . | . |
| [CraftableRTG](https://steamcommunity.com/sharedfiles/filedetails/?id=3026880031) | . | . | . | . | . | . | . | + (RTG recipe) | . | . | . | . | . | . |
| [Survival RTG](https://steamcommunity.com/sharedfiles/filedetails/?id=2464235697) | . | . | . | . | . | . | . | + (RTG recipe) | . | . | . | . | . | . |
| [Recipe - RTG (obsolete)](https://steamcommunity.com/sharedfiles/filedetails/?id=1351511005) | . | . | . | . | . | . | . | + (RTG recipe; abandoned) | . | . | . | . | . | . |
| [RTG Start Conditions and Difficulty Mod](https://steamcommunity.com/sharedfiles/filedetails/?id=3568945039) | . | . | . | . | . | . | . | . | . | . | . | . | (start config: 12 RTGs) | . |
| [Nuclear Recipes](https://steamcommunity.com/sharedfiles/filedetails/?id=3004097828) | . | . | . | . | . | . | . | + (nuclear battery + RTG recipes) | . | . | . | . | . | . |
| [2x Solar Power](https://steamcommunity.com/sharedfiles/filedetails/?id=1669621266) | . | . | . | . | . | . | + (2x) | . | . | . | . | . | . | . |
| [Realistic Solar Constants](https://steamcommunity.com/sharedfiles/filedetails/?id=1475742702) | . | . | . | . | . | . | + (realistic, weaker) | . | . | . | . | . | . | . |
| [PIAC - Power Info and Control](https://steamcommunity.com/sharedfiles/filedetails/?id=3687433072) | . | . | . | . | . | . | . | . | . | . | . | . | + (IC10 power dashboard) | . |
| [Power Trading](https://steamcommunity.com/sharedfiles/filedetails/?id=3420179579) | . | . | . | . | . | . | . | . | . | . | . | . | (trader buys fuel) | . |

Reading guide:
- Columns A-D are the "power-system overhaul" space. Only Re-Volt populates all four; Power Grid Plus populates A/B/C and the recursive-network bit of D (no Load Center, no breakers); Deadly Electricity populates A. Nothing else touches this space, which is exactly why Re-Volt and Deadly Electricity are the only true competitors.
- Columns E-H are "balance / per-device tuning", mostly XML or value-patch mods that coexist with everything.
- Columns I-L are "new power content". The ones that matter for Power Grid Plus: MoreCables (col K, adds super-heavy / super-conductor tiers the three-tier gating must classify), CableTypeSwitcher and EL Switche (col K, create/convert cables at runtime, bypassing the build-time gate).
- Column M is logic / monitoring / QoL, all orthogonal.
- Column N is the Power-Grid-Plus-only features.
- For the per-feature detail of the A-D space (the 48 Re-Volt features), see Appendix A. For each mod's full feature bullets and the conflict / compat assessment, see Appendix B.
