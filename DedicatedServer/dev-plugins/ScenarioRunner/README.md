# ScenarioRunner

Developer tooling. A scenario-driven runtime probe for the Stationeers dedicated server. Loads as a BepInEx plugin via StationeersLaunchPad; each scenario is a small read-and-log or reflection-driven probe that fires from a Harmony postfix on a simulation tick. Lives next to the dedi launcher at `DedicatedServer/dev-plugins/ScenarioRunner/`; ships nowhere else.

## Why this exists

Some questions about how a mod actually behaves on a live save are easier to answer with a runtime probe than with an InspectorPlus snapshot:

- "Does this patch site fire?" -> have a scenario call the patched method directly with synthetic inputs and log before / after.
- "What state does this collection settle to over N ticks?" -> have a scenario log it every N ticks and diff first vs last offline.
- "If I flip this config, what changes in the simulation?" -> run twice across two `-Start` cycles with the config flipped between them.

`tools/save-edit/` is the offline counterpart: it manipulates persisted save state. `ScenarioRunner` observes the running simulation. The two compose: edit the save offline to set up a controlled scenario, then load it with ScenarioRunner active to observe behaviour at known tick offsets. Full overview in `DedicatedServer/CLAUDE.md` under *Manipulating world state without a client*.

## Build and deploy

```powershell
dotnet build DedicatedServer/dev-plugins/ScenarioRunner/ScenarioRunner.sln -c Release
DedicatedServer/dedicated-server.ps1 -Lock -Purpose "ScenarioRunner scenario run"
DedicatedServer/dedicated-server.ps1 -DeployMods -As <id> -Mod ScenarioRunner -Configuration Release
```

The `-DeployMods -Mod ScenarioRunner` resolves under `DedicatedServer/dev-plugins/` (the launcher searches `Mods/`, then `Plans/`, then `DedicatedServer/dev-plugins/`). Detecting a dev-plugins target, the launcher mirrors the DLL and `About/` folder into `DedicatedServer/data/mods/Local_ScenarioRunner/` (the StationeersLaunchPad load path) and appends a matching `<Local Enabled="true">` entry to `DedicatedServer/install/modconfig.xml` if one is not already present. Re-running `-DeployMods` is idempotent on the modconfig side. No manual file copy or hand-edit is required.

Dev-plugin deploys deliberately do NOT write to `DedicatedServer/install/BepInEx/plugins/ScenarioRunner/`. With the same DLL in both `install/BepInEx/plugins/` and `data/mods/Local_<X>/`, BepInEx Chainloader and StationeersLaunchPad each load it, the plugin's `Awake` fires twice, every Harmony prefix is registered twice, and side-effecting patches double. We hit this exact trap during PGP battery-efficiency verification and got delta=10000 instead of 5000. The launcher additionally removes any stale `install/BepInEx/plugins/<X>/<X>.dll` left over from a pre-mirror layout, so a repo that was previously deployed the other way self-heals on the next `-DeployMods`.

## Configuration

Settings appear in `DedicatedServer/install/BepInEx/config/net.scenariorunner.cfg` after first launch.

| Key | Default | What it does |
|---|---|---|
| `Probe / Scenario` | `""` | Scenario id. Empty disables the probe. |
| `Probe / Delay Ticks` | `5` | Wait this many simulation ticks after world load before the scenario starts. |
| `Probe / Log Inventory On First Tick` | `true` | One-line dump of `Battery / Transformer / AreaPowerControl / CableNetwork / CableFuse` counts on the first scenario tick, plus a per-concrete-type Battery breakdown. Runs regardless of which Scenario is selected. |

## Pump

A Harmony postfix on `ElectricityManager.ElectricityTick` (`DedicatedServer/dev-plugins/ScenarioRunner/ScenarioRunner/SimTickPump.cs`) calls `Dispatcher.OnSimTick()` each simulation tick the world is running. `OnSimTick` deduplicates by `Time.frameCount` so multiple pump sources converge to one scenario call per simulation frame. To add a second pump (for example, on an atmospheric tick), add another `[HarmonyPatch]` class that targets that method and calls `Dispatcher.OnSimTick()` from its postfix.

ElectricityTick is the chosen pump because (a) it is a public static method on a manager class, (b) it is the same pump InspectorPlus uses for its request poller, and (c) it fires whenever `GameManager.RunSimulation` is true. On a headless dedicated server `MonoBehaviour.Update` does not reliably fire after world load, so an Update-based pump cannot replace this. See `Research/Patterns/ThingEnumerationOffMainThread.md` for the threading constraints on what the postfix can safely read.

## Scenarios

### General (work without any specific mod)

| Id | What it does |
|---|---|
| `""` (default) | Probe is dormant. Plugin still loads; no scenario fires. |
| `inventory` | Counts of every power entity in the loaded scene, plus a per-concrete-type Battery breakdown (so subclasses like `StationBatteryNuclear` show separately). Fired once on the first scenario tick. |
| `battery-charge-snapshot` | Every five simulation ticks, log `PowerStored`, `PowerMaximum`, `OnOff`, `Mode` for every `Battery`. Diff offline to compute rate / efficiency deltas over a window. |
| `sun-noon` | Pins the world sun at zenith (noon) and freezes it for the whole session. On its first tick it sets `OrbitalSimulation.TimeScale = 0`, scans `SimulationTimeSeconds` for the maximum `WorldSunVector.y` via the worker-safe private `SetAllBodies(double)` (never `SetSimulationTime`, which writes a Unity transform off-thread), lands the sun there, and re-arms `TimeScale = 0` every tick. Both `SimulationTimeSeconds` and `TimeScale` network-sync, so a connected client sees the same frozen noon. Logs a `sun-noon SET ...` line with the resulting sun vector / elevation. General scenario (no mod required). Reflection-only against `OrbitalSimulation`. On the dedi the sim must be ticking, so use `Force Unpause Without Client` (InspectorPlus) or connect a client. See `Research/GameClasses/OrbitalSimulation.md`. |
| `power-prefab-dump` | One-shot. Iterates `Prefab.AllPrefabs` and emits a structured `power-prefab-dump | PrefabName=... | Type=... | UsedPower=... | OverridesGetUsedPower=... | ImplementsIPowerGenerator=... | IsBattery=... | IsTransformer=... | IsWireless=... | IsAPC=... | DLC=...` line per power-relevant prefab (any of: non-zero UsedPower, IPowerGenerator, Battery, Transformer, WirelessPower, AreaPowerControl, or a class that overrides `GetUsedPower(CableNetwork)`). Includes START / END markers with emit count. Use it to build a classification list of every base-game powered device. Run on a fresh `-New` world; the prefab registry is populated at `Prefab.OnPrefabsLoaded` so no save is needed. |
| `paintable-prefab-dump` | One-shot. Answers "which structures are spray-paintable". Reads each prefab's serialized `Thing.PaintableMaterial` field (a non-null value IS the paintability signal: `Thing.IsPaintable` returns true iff `PaintableMaterial != null` for non-mask types, and the Stationpedia "Paintable: Yes/No" line reads the same field). Emits a `paintable | <PrefabName> type=... PaintableMaterialSet=true/false renderMode=... customMaterials=...` line for a fixed list of the steel-/iron-frame variants plus the composite walls (a known-paintable control), then a `paintable SUMMARY structures=N paintable=P notPaintable=U` line across every `Structure` prefab. `renderMode` (`Standard` vs `Batched`) flags whether per-instance recolor is even possible (`Structure.SetCustomColor` throws on `Batched`). Null-checked with `ReferenceEquals` (no Unity `==` operator) so it is managed-state only and worker-safe. Run on a fresh `-New` world; the prefab registry is populated at `Prefab.OnPrefabsLoaded` so no save is needed. |
| `merge-long-variant-num4` | One-shot. Reproduces, on the live binary, the negative merge-cost delta (`num4`) that `MultiMergeConstructor.Construct` computes when a merge resolves onto an existing LONG (`StraightAsymmetric`) variant. For every `MultiConstructor` kit with a `StraightAsymmetric` entry, it runs the game's own `GetConnectionType()` + `BuildStates[0].Tool.EntryQuantity` over the kit's `Constructables` to reproduce the `index`/`num2` resolution and compute `num4 = EQ(merged Straight result) - EQ(existing StraightAsymmetric)`. Logs each kit's runtime base connection type (settles "is the 1-cell base `Straight` or the C# default `Exhaustive` at runtime") and whether `StackedGeneCollections` is null (the `OnUseItem` `RemoveRange` is gated on it; null on a kit means the negative `num4` silently duplicates on this build rather than throwing). Vanilla: long-variant kits show `num4 = -2`. With NetworkPuristPlus active the `StraightAsymmetric` entries are stripped at prefab-load, so the merge-onto-long precondition is unreachable. Managed-state only (worker-safe); no spawning. Run on a fresh `-New` world. See `Research/GameClasses/MultiMergeConstructor.md`. |

### PowerGridPlus-specific (require `net.powergridplus` loaded)

All of these gracefully no-op and log a warning if PowerGridPlus is not loaded.

| Id | What it does |
|---|---|
| `pgp-transformer-conservation` | Every five ticks, log `Setting`, `UsedPower`, `InputNetwork.CurrentLoad`, `OutputNetwork.CurrentLoad` per Transformer. Verifies `Input draw ~= Output throughput + UsedPower` (PGP's `TransformerExploitPatches`). |
| `pgp-battery-efficiency-probe` | One-shot. Picks the first OnOff Battery with headroom and an `InputNetwork`, then calls `Battery.ReceivePower(net, 5000)` and `Battery.ReceivePower(net, 200)` directly, logging the `PowerStored` delta after each. Run twice across two `-Start` cycles (`BatteryChargeEfficiency = 1.0` vs `0.5`) to verify the efficiency clamp and the sub-500 W trickle floor. |
| `pgp-apc-idle-probe` | Every five ticks, log each `AreaPowerControl`'s attached `Battery.PowerStored`. Diff first vs last; idle APCs should hold constant (PGP's `AreaPowerControlPatches`). |
| `pgp-cable-burn-probe` | Two parts. Periodic: list every `CableNetwork` with `RequiredLoad` or `CurrentLoad > 5 kW`. One-shot at tick `Delay + 25`: reflect-invoke `PowerGridPlus.Power.PowerGridTick.TestBurnCable(10000, 10000)` against every network's `PowerTick` and tally `wouldBurn` counts by tier. Verifies PGP's burn-decision formula in isolation. |
| `pgp-tooltip-filter-probe` | One-shot. Sweeps `OcclusionManager.AllThings`, calls `thing.GetPassiveTooltip(null)` on each, inspects `PassiveTooltip.Extended` for the orange `Burned:` marker that `BurnReasonPatches.Thing_GetPassiveTooltip_Postfix` appends. Verifies the postfix's `__instance is CableRuptured` filter holds: any non-CableRuptured Thing with a `Burned:` line is a filter bug. Reports total / failed / CableRupturedSeen / CableRupturedWithBurned / OtherWithBurned plus up to five offender samples when `OtherWithBurned > 0`. Threading: calls run on a UniTask worker (overrides that touch Unity APIs may throw and get counted as failed; the aim is broad coverage, not 100 %). |
| `pgp-stationpedia-page-probe` | One-shot. End-to-end headless verification of the LogicType wiki-page navigation chain. Five checks: (1) `Stationpedia._linkIdLookup["LogicTypeLogicPassthroughMode"]` exists with the expected `Key` / `Title` / `Text` (the page registered by `StationpediaPopulateLogicVariablesPatch`). (2) ROUND-TRIP: page `Text` byte-equal to `LogicTypeRegistry.LogicPassthroughMode.Description`. (3) REACH-SCAN: full scan of all 2330 pages for the link target string (expect 0 hits, confirming chips are UI-dynamic, not baked into device-page Text). (4) ENUM: `EnumCollections.LogicTypes.Names` / `ValuesAsInts` arrays contain `LogicPassthroughMode` with the right value (the tablet-UI dropdown source extended by `LogicableInitializePatch`). (5) ENUM-NAME: `Enum.GetName(typeof(LogicType), <value>)` returns `LogicPassthroughMode` so vanilla chip rendering emits `<link="LogicType"+name>` matching our key (the path patched by `EnumNamePatches`). The description-footer half of PGP's Stationpedia work is covered separately by `pgp-rate-cap-probe`, not duplicated here. Managed-state only; worker-safe. |
| `pgp-rocket-parity-probe` | One-shot. Headless pre-verification of the five rocket-family PowerGridPlus changes plus a battery-capacity reverse-engineering dump. Five blocks (each line prefixed `rocket-parity ...`): (A) CAPACITY DUMP reads the public `PowerMaximum` field off the battery prefabs (`StructureBatteryMedium`/`Small`/`Battery`/`Large`, `StationBatteryNuclear`) and the rocket power-umbilical prefabs from `Prefab.AllPrefabs` (the reverse-engineering deliverable). (B) ROCKET TRANSFORMER TIER invokes `VoltageTier.GetTransformerTierMap("StructureRocketTransformerSmall")` (expect `(heavy, normal)`). (C) UMBILICAL HEAVY-ONLY invokes `VoltageTier.IsAllowedOnTier(umbilical, normal/heavy)` on a live umbilical (expect `normal=False heavy=True`). (D) ROCKET BATTERY CAPS invokes `StationaryBatteryPatches.GetChargeCap`/`GetDischargeCap` on live `StructureBatteryMedium`/`Small` instances (expect finite ~5000/10000 and ~2500/5000, not Infinity). (E) UMBILICAL LOGIC BRIDGE forces `PassthroughModeStore` mode 1 on both halves of each docked male/female pair, then runs `PassthroughTopology.GatherReachable` on the male `InputNetwork` and female `OutputNetwork` and checks each partner network is reachable. Blocks C/D/E skip with a warning if the save lacks the relevant instances. Managed-state reflection only; worker-safe. |
| `pgp-umbilical-passthrough-probe` | One-shot. Headless verification of the rocket-umbilical logic-passthrough feature (the "two halves share one mode and bridge each other's networks" feature). Finds a docked male/female pair from `OcclusionManager.AllThings` and runs: (P1) DEFAULT MODE -- clears any stored mode and confirms `GetMode` falls through to `GetDefaultMode == 1` for both halves. (P2) REAL WRITE + MIRROR -- invokes the real `Device.SetLogicValue(LogicPassthroughMode, 0/1)` on one half (driving PGP's `SetLogicValuePatch` + partner mirror) and confirms BOTH halves read back the same value; verifies zero clients first so `SendAll` is a no-op and the synchronous mirror is worker-safe. (P3) MERGED DataDeviceList -- with mode 1 + docked, reads the female `OutputNetwork.DataDeviceList` and the male `InputNetwork.DataDeviceList` and confirms a far-side witness device shows up in the near side's merged list (what a motherboard dropdown / IC10 sees, via `LogicPassthroughPatches`). (P4) UNDOCK -- nulls `_partnerUmbilical` on both halves and confirms `IsEnabledBridge` -> false and `GatherReachable` drops the partner net; restores and re-confirms. (P5) MASTER TOGGLE -- forces `Settings.EnableUmbilicalLogicPassthrough` + the synced `PassthroughSettingsSync.EffectiveUmbilical` false and confirms `IsEnabledBridge` -> false even docked + mode 1; restores. (P6) MP join-suffix round-trip focused on the umbilical surface (per-device mode entry + the 5th/umbilical boolean survive serialize -> deserialize in field order). (P7) standalone `PassthroughModeMessage` serialize -> deserialize (DeviceId + Mode survive). Restores all mutated state at the end. Mutates the live save's umbilical mode + global toggle transiently; run on a save copy. |
| `pgp-umbilical-saveload-set` / `pgp-umbilical-saveload-verify` | Two-phase save/load persistence test for the umbilical mode side-car (run across two `-Start` cycles, like `pgp-priority-shedding-persist-probe`). `-set` sets a docked umbilical's mode to 0 (non-default; default is 1) on both halves so a save captures it via `PassthroughSaveLoadPatches`. After a save + reload, `-verify` confirms the mode restored to 0 (the umbilical cases in `ThingOnFinishedLoadPassthroughPatch` + side-car read fired), reports whether `PassthroughSideCar.LoadedModes` was populated, then restores the mode to the default 1. Caveat: batch-mode `-Save` via stdin can no-op; use `-Stop -SaveAs` or AutoSave to persist between phases. |
| `pgp-priority-shedding-probe` | One-shot. 12-phase end-to-end verification of the Transformer Priority + Shedding feature against a real save. Validates `PriorityStore` (default 100, version-bumping writes), `TransformerAllocator` (strict-priority allocation, per-network cache, InvalidateAll escape hatch), `BrownoutRegistry` (ShortfallTolerance=2, LockoutDurationTicks=20, lockout precedence over priority change, NoteSupplyOk reset), `TransformerPriorityLogicPatches` (CanLogicRead/Write surface, GetLogicValue rewires for Setting/Ratio/Maximum, SetLogicValue redirect Setting -> Priority), `PriorityMessage` (server-side short-circuit), `PrioritySideCar` (in-memory snapshot + on-disk Write -> Read round-trip), `ShedSettingsSync` (Effective matches ConfigEntry), `EnableTransformerShedding` master-toggle OFF fallback (allocator reverts to vanilla Setting, footer flips to "Behaviour: vanilla"), and the Stationpedia footer presence on all 4 transformer prefab names with a negative case (ItemWrench). Each phase emits structured `[ScenarioRunner] PSP <phase> <PASS|FAIL>` lines plus a final `PSP END pass=N fail=N total=N`. Mutates `PriorityStore` and `BrownoutRegistry` extensively; run on a SAVE COPY, not on a save you want to keep clean. The probe restores most state at end, but a save after this scenario carries the synthetic priorities the probe wrote. |
| `pgp-priority-shedding-persist-probe` | One-shot follow-up to `pgp-priority-shedding-probe`. Verifies the side-car restore path through the game's load pipeline: after a save (`-Save -Name <X>`) post-probe and a reload (`-Stop`, `-Start -Load <X>`), counts non-default-priority transformers. PASS iff at least 2 non-default priorities survived load. Confirms `ThingOnFinishedLoadPriorityPatch` + `XmlSaveLoadLoadWorldPrioritySideCarPatch` + `SaveHelperSavePrioritySideCarPatch` Harmony hooks fire end-to-end. Caveat: on a batch-mode dedicated server `-Save` via stdin can no-op (see `DedicatedServer/CLAUDE.md`); use `-Stop -SaveAs` or wait for AutoSave instead. |
| `pgp-priority-shedding-knob-probe` | One-shot. Headless verification of the screwdriver knob path (PLAYTEST C-a + C-b). Reads `NeedleFullScale` (expects 200), `PriorityStepNormal` (expects 10), `PriorityStepSmall` (expects 1) from `TransformerInteractWithPatches` via reflection. Then nulls out the patch's resolved `SetKnobMethod` (so its main-thread-only Unity transform writes are skipped from the worker-thread tick) and directly invokes `InteractWithPatch` against a real Button1 and Button2 `Interactable` lifted off a live Transformer, exercising six sub-cases: Button2 no-Alt (+10), Button2 Alt (+1), Button1 no-Alt (-10), Button1 Alt (-1), clamping at 0 from priority 5, and `doAction=false` no-write. Confirms knob-driven priority step semantics against the same code path the game's `Transformer.InteractWith` would run. The visual needle rotation is not directly observable headless. |
| `pgp-priority-shedding-flash-probe` | One-shot. Headless verification of the on/off button shed flash attach (PLAYTEST C-c). Reads `BrownoutFlashBehaviour.FlashColor` and asserts it sits in the orange band (R=1, G in [0.4, 0.7], B=0); reads `FlashHz` and expects 2 Hz. Walks every transformer, calls `GetComponent(BrownoutFlashBehaviour)` via reflection, verifies the component attached on every transformer (the `TransformerFlashAttachPatches.Postfix` on `Thing.OnRegistered`), and reads the private `_renderers` array to confirm `DiscoverRenderers` resolved at least one `MeshRenderer` per transformer (the on/off button target). The actual emission animation under shed state is not directly observable headless. |
| `pgp-priority-shedding-hover-probe` | One-shot. Headless verification of the shed hover error (PLAYTEST C-d). For a sample transformer: clears `BrownoutRegistry`, calls `Thing.GetPassiveTooltip(null)` and asserts the returned tooltip `Extended` contains no "Shedding" line. Then forces a shed via 2x `NoteShortfall` on consecutive ticks and confirms `IsShedding=true`, re-calls `GetPassiveTooltip`, and verifies `Extended` now contains `Shedding (Priority` + `#ffa500` + `insufficient upstream supply`. Finally, clears shed state again and confirms the tooltip drops the line. Exercises `TransformerHoverErrorPatches.Thing_GetPassiveTooltip_Postfix` end-to-end including the live `PriorityStore` priority interpolation. |
| `pgp-priority-shedding-labeller-probe` | One-shot. Headless verification of the labeller-panel pre-fill redirect (PLAYTEST C-e). Directly invokes `TransformerLabellerPatches.Set_Prefix` and `InputSetting_Prefix` with synthetic `(ISetable, ref LogicType)` arguments across four cases: (1) Transformer + Setting -> swap to Priority, (2) Transformer + Setting via InputSetting_Prefix -> swap to Priority, (3) Transformer + non-Setting (Mode) -> no swap, (4) non-Transformer (Battery) + Setting -> no swap. Verifies the prefix logic without needing to spawn a Labeller item or open a real input window. |
| `pgp-priority-shedding-mp-probe` | One-shot. Headless verification of multiplayer code paths (PLAYTEST C-f). (P1) Tests `BrownoutRegistry.SetClientShedding` / `ClientIsShedding` state machine round-trip. (P2) Tests `ShedStateMessage.Process(0)` on the host: expects short-circuit because `NetworkManager.IsServer=true`; the client-shed dict must not be mutated. (P3) Re-tests `PriorityMessage.Process(0)` host short-circuit (already covered by PSP P8). (P4) Locates the live `Plugin` instance via `BepInEx.Bootstrap.Chainloader.PluginInfos["net.powergridplus"].Instance`, then performs a full `SerializeJoinSuffix -> DeserializeJoinSuffix` round-trip through a `MemoryStream`-backed `RocketBinaryWriter` / `RocketBinaryReader`: seeds three synthetic priorities, serializes, mutates the priorities to 999, deserializes, verifies the originals restored. Cross-process delivery cannot be tested from a single dedi but the message handler + serializer / deserializer + state machine ARE verified end-to-end. |
| `pgp-priority-shedding-saveload-probe` | One-shot. Headless verification of the save/load Harmony hook attachment (PLAYTEST C-g). Uses `Harmony.GetPatchInfo` via reflection to confirm `PowerGridPlus`-owned patches are attached on: (P1) `SaveHelper.Save(DirectoryInfo, string, bool, CancellationToken)`, (P2) `XmlSaveLoad.LoadWorld`, (P3) `Thing.OnFinishedLoad`. (P4) Reads `PrioritySideCar.LoadedPriorities` and reports its count: non-null means the load-postfix fired during initial world load (proof the hook is reaching production code paths). On a fresh `-New` world the side-car is absent so LoadedPriorities is null; this is reported as NOTE rather than FAIL. The on-disk Write / Read round-trip itself is verified by PSP P10. |
| `pgp-priority-shedding-topology-probe` | One-shot. Headless verification of the global allocator's topology independence (regression check for the 418422/418423 cascade fix). (P1) For every parallel transformer pair (same input network AND same output network), assigns 500 / 100 priorities and verifies the higher-priority transformer's allocation is >= the lower's. (P2) For every output network with positive `RequiredLoad`, sums the allocations across all feeders and verifies the total does NOT exceed `RequiredLoad + 1 W` (demand cap). (P3) For every On transformer with downstream `RequiredLoad < 1 kW` and sufficient upstream `PotentialLoad`, verifies its allocation is bounded by the downstream demand (the demand-driven `desired` fix prevents greedy `OutputMaximum` claims that cascade-shed). (P4) If transformers 418422 or 418423 exist, runs the specific cascade case end-to-end and verifies neither sheds when demand is below the per-transformer cap. |
| `pgp-priority-shedding-all` | One-shot aggregator. Runs every probe above (knob, flash, hover, labeller, mp, saveload, topology) plus the original `pgp-priority-shedding-probe` and `pgp-priority-shedding-network-breakdown` in sequence on the first scenario tick. Convenience for capturing every PASS/FAIL line in one `-Start` cycle. |
| `pgp-shed-multilevel` | One-shot. Verifies the allocator still sheds in PRIORITY order on a multi-level transformer chain. Observes peak downstream demand, picks a transformer-fed fanout input net with >=2 demanding consumers, assigns distinct descending priorities, throttles the feeder to ~half the demanding sum, drains all batteries/APC cells, then traces shed state per consumer. Self-verdicts `SHEDML VERDICT: PASS` when a shed fired, the highest-priority drawer survived, and there is no priority inversion. Run under both Legacy and Sweep (`Enable Sweep Allocator` off / on); the shed set should match, proving shedding is unchanged by the Sweep rework. Mutates `Setting` and drains stores; run on a save copy. |
| `pgp-rearch-suite` | Phased PASS/FAIL regression for the unified rigid + soft flow classes (the storage-charge-behind-a-seg deadlock fix). Phase A (first tick): one-shot segmenter-roster inventory by concrete type (reflection on `SegmentingDeviceRegistry.EnumerateSorted`; device counts as fallback) plus `PowerStored` baselines for the two Luna target batteries 511274 / 511135 (absent targets log a notice and the verdict becomes SKIP). Phase B (ticks 1-120): logs both batteries' `PowerStored` + 10-tick delta every 10 ticks, and a per-tick census (v2) of vanilla supply shortfalls (`RequiredLoad > PotentialLoad + 0.5`), logged as `shortfallNets=<deadlockCount> (dry= throttled= faulted= offscope= served=)`: faulted = shed/overload/cycle/VVF registry hit on a member (checked first; a net both faulted and classified keeps this bucket), the rest bucketed via PowerGridPlus's `ShortfallDiagnostics` per-net classification snapshot (reflection join; absent from the snapshot = offscope; classifier unreachable = every fault-free shortfall counts as deadlock and a notice logs once). Only the deadlock bucket (the allocator's own accounting says supply existed yet the net stayed unmet) is the regression shape; dry / throttled / served / faulted are honest darkness. Phase C (~tick 130): a shortfall detail line with each bucket's window max, then `[RearchSuite] VERDICT charge=<PASS/FAIL/SKIP> shortfalls=<max deadlock seen>`; charge PASS = `PowerStored` rose on both batteries and the combined rise exceeds 100 kJ. Run on the Luna save with `sun-noon` lighting assumptions in mind (solar must be producing). Read-only; worker-safe. |
| `pgp-2cycle-freeze` | One-shot. Verifies the Sweep allocator's re-decidable shed/overload loop fixes the Legacy "60-second freeze" (the shed<->overload 2-cycle). Picks a transformer-fed fanout F, designates the largest-downstream consumer Y (throttled to ~0.85x its downstream so it OVERLOADS while still drawing nearly its full demand from F) plus a smaller sibling X (forced to lowest priority), then throttles F's feeders so F covers Y and every other consumer fully but only ~40% of X, making X the sole contended victim. When Y trips on overload it frees its F draw, which now covers X. Traces `BrownoutRegistry.IsShedding(X)` and `OverloadRegistry.IsOverloaded(Y)`. Under Legacy: `LEGACY BUG REPRODUCED` (Y overloaded AND X frozen-shed despite the freed budget). Under Sweep: `SWEEP PASS` (Y overloaded, X never shed). The A/B across the toggle is the proof. Mutates `Setting` + priorities and drains stores; run on a save copy. |

| `pgp-dataport-tier-diag` | One-shot PASS/FAIL regression test for "exclusive data-only ports accept any cable tier and never burn". PART-A: census of every prefab with a dedicated `Data`-only connector (the universe; 207 in the captured vanilla+mod set). PART-B: per live separate-data-port device, compares its data network to its power network(s) and asserts the runtime invariant -- a device whose data port is on its OWN network (`dataDistinct=True`) is never in that network's `PowerDeviceList`, so the tier rule (`IsAllowedOnTier` over `PowerDeviceList`) and the reactive burn can never reach it; `realBugShape` counts violations (must be 0). PART-C: per data network, which `PowerDeviceList` members are not allowed on the net's own tier (a power-port concern, `IsAllowedOnTier` direct; a data-only-port device must never appear). PART-D: reflects PGP's `VoltageTierPatches.CursorAttachesToPowerPortOf(Cable, Device)` on a cable drawn from each device's separate data network and asserts it returns false (the cursor never applies the device's tier rule to a data placement); a power cable on the same device asserts true (sanity). Emits `DPT VERDICT runtimeInvariant=PASS/FAIL cursorCarveOut=PASS/FAIL`. Managed-state reflection only; worker-safe. Run on a populated save (e.g. `Luna_pgp_rocket`) for live coverage across normal/heavy/superHeavy data networks. |

| `pgp-dataport-cursor-proxy` | Headless proxy for the placement-cursor ghost COLOR on a data-only port. The color is `Cable.CanConstruct().CanConstruct` (true green / false red) and PowerGridPlus's only contribution is `Cable_CanConstruct_Postfix`. `ConnectedCables` (the postfix's cable-to-cable pass) reads `Transform.position` and is main-thread only, so this does NOT invoke the full postfix on the worker (it would throw and be swallowed to a false green); it computes the color from the postfix's worker-safe predicates: the real `CursorAttachesToPowerPortOf` (a data port returns false, so the device's tier rule is skipped for ANY cursor tier) plus topology for the cable-to-cable pass (a placed cable on a separate single-tier network has no different-tier power-merge by construction). Reports the ghost color per data-only port (expect all green), the carve-out wins (device,tier pairs the device REJECTS on a power port but ACCEPTS on its data port), a power-port CONTRAST (a wrong tier on a power port is still red, so the greens are not vacuous), and a VERDICT. Run on a populated save (e.g. `Luna_pgp_rocket`). |

### PowerTransmitterPlus-specific (require `net.powertransmitterplus` loaded)

All of these gracefully no-op and log a warning if PowerTransmitterPlus is not loaded.

| Id | What it does |
|---|---|
| `ptp-autoaim-cache-probe` | One-shot. Finds every transmitter with a non-zero cached auto-aim target (`AutoAimState.GetCachedTarget(tx)` via reflection; loaded from the save's auto-aim side-car), then for each: clears the dish's `AutoAimUpdateFlag` (0x2000) bit, writes the current `RotatableBehaviour.TargetHorizontal` value back (same value, no slew change), and re-reads both the cache and the flag. The Harmony postfix `RotatableTargetHorizontalResetPatch` (PTP commit `14946c5`) fires regardless of value change, passes its `NetworkManager.IsServer` gate on the dedi, and calls `AutoAimState.ClearCache` which clears the cache box to 0 and raises `dish.NetworkUpdateFlags \|= 0x2000`. PASS iff every probed dish ends with cache 0 AND the flag bit set. Verifies PTP TODO #1 (SP manual-override clears the cache) and the host-side half of TODO #3 (override propagates via the per-tick payload). The client-side receive cannot be verified server-side. |
| `ptp-long-distance-link-probe` | One-shot. Enumerates linked TX-RX pairs in the scene, reads `PowerTransmitter._linkedReceiverDistance` (set by `LinkPatch` on every successful link probe) via reflection, reports distance distribution. PASS iff at least one linked pair is at >= 150 m, which is evidence that the joint mutual-aim solver (v1.7.1) and the widened SphereCast link probe still establish links at the user-tested range. Observational, not proactive: the post-load auto-aim re-solve pass (`AutoAimSaveLoadPatches`) re-runs the joint solver against every cached pair after `Thing.OnFinishedLoad`, so a link being present at scenario tick time means it survived both initial deserialisation AND the solver's fixed-point iteration. Verifies PTP TODO #4. |
| `ptp-beam-predicate-probe` | One-shot. For every `PowerTransmitter` in the scene, calls `BeamVisibility.ShouldShow(tx)` via reflection and cross-checks against an independent classification by link state and `OnOff` (managed fields, safe from the worker thread). PASS iff `ShouldShow` returned false for every unlinked TX, every linked-but-tx-off TX, and every linked-but-rx-off TX. The aim-validity gate (7 degree forward-antiparallel) is a Unity-transform read and is not independently computed from the worker, so the linked + both-on case (`ShouldShow=true` ratio) is reported as informational only; a misaimed pair can legitimately produce `ShouldShow=false` there. Verifies the v1.7.3 predicate correctness server-side against a real save. |
| `ptp-standalone-suite` | Phased PASS/FAIL regression for the v1.9.0 STANDALONE fixes (PowerTransmitterPlus billing its own transfer debt on the vanilla allocator; run with PowerGridPlus stashed). Composes `sun-noon` at the top of every pump like `pgp-rearch-suite`. Phase A (t=0): resolves `PowerTransmitterPlus.ModApi` via reflection once (`Version`, `EffectiveMaxCapacity`, `TryGetLink`, `SourceDrawMultiplier`, `GetTransferDebt`, `BillingOwner`); a loaded pre-v1.9.0 build without ModApi logs a warning and emits `VERDICT api=ABSENT` with everything else SKIP. Enumerates every linked TX-RX pair (refIds, display name, distance, source-draw multiplier, debt ceiling = `(cap>0?cap:5000) * max(mult,1) * 4` mirroring the advertise prefix) and picks the largest-multiplier pair as TOGGLE TARGET; zero pairs -> verdict SKIP. Phase A ends with the one-shot SOLAR BOOTSTRAP: Luna_rearch's source grids are solar-only farms parked off-sun (pitch -75 deg, 0 W under the frozen zenith, tracker ICs on the same dead nets), so the suite force-aims every `SolarPanel` at the zenith by writing `RotatableBehaviour.TargetVertical = 0.5` (physical pitch 0 = straight up; a managed double whose move task defers Unity work to the main thread, same thread-safety class as TrySetOnOff; Horizontal is irrelevant at pitch 0) and logs the re-aim count; the heads LERP over ~18 ticks. Bootstrap window (t 1..60): per tick checks liveness = any transmitter's `InputNetwork.PotentialLoad > 0`, logging `bootstrap t=<n> livePairs=<k> maxSrcPotential=<W> maxPanelEff=<e>` every 10 ticks (efficiency sampled across up to 8 re-aimed panels so a failed aim is diagnosable from the log); on liveness at t=L the measurement clock m = t - L starts; if still dead at t=60 it emits `SOURCE DEAD: no transmitter input net came alive within 60 ticks; environment invalid` and the verdict `api=OK pairs=<n> debt=SKIP flow5k=NOTSEEN toggle=SKIP`. Phase B (m 1..240): per tick samples each transmitter's transfer debt (the TX half carries the ceiling-governed source-billing debt) and the wireless network `CurrentLoad` (delivered watts); tracks per-pair max debt plus whether any flow exceeded 5000 W (evidence the receiver drain-cap lift makes >5 kW billable); one compact line per pair every 10 ticks. Toggle sub-test on the target: OnOff=false at m=80, true at m=120 (TrySetOnOff, the PtCampaign mechanism); asserts (a) debt frozen while off (outcome always logged explicitly, PASS or FAIL), (b) flow resumed within 40 ticks of re-enable with debt inside the same per-pair bound as the global debt assertion, and the ledger settled by window end (debt <= `max(lastFlow * multiplier * 1.5, 1000 W)`, the steady one-tick billing lag with headroom) or still strictly decreasing (a large re-enable lump draining = converging), (c) no single-tick debt increase above `maxPairFlow * multiplier * 1.05 + 1 W` (no free lump / no double bill; one tick may book at most one tick's `delivered * multiplier`). Phase C (m>=250): exactly one grep-able line `[PtpSuite] VERDICT api=OK\|ABSENT pairs=<n> debt=PASS\|FAIL\|SKIP flow5k=SEEN\|NOTSEEN toggle=PASS\|FAIL\|SKIP` (debt PASS = every pair's max debt <= `ceiling + maxPairFlow * multiplier * 1.02 + 1 W`; the ceiling is a pause gate evaluated PRE-delivery, so exactly one burst bill of overshoot is by design, and on unlimited-cap servers bursts are bounded by source `PotentialLoad`, not by any constant), preceded by one per-pair debt summary line (maxDebt vs computed bound, logged on pass and fail), one detail line per failed assertion, and a one-line toggle sub-result summary; debt/toggle read SKIP on api=ABSENT, zero pairs, or SOURCE DEAD. Safe with PowerGridPlus ALSO loaded (info line only; with PGP owning billing the debt stays near zero, so the suite is only meaningful standalone). Mutates the target dish's OnOff transiently and every solar panel's vertical aim target (persists in the save); run on the Luna_rearch save. |
| `ptp-all` | One-shot. Runs `ptp-autoaim-cache-probe`, `ptp-long-distance-link-probe`, and `ptp-beam-predicate-probe` in sequence on the first scenario tick. Convenience for capturing all three results in one `-Start` cycle. |

## Adding a new scenario

Edit `ScenarioRunner/Dispatcher.cs`:

1. Add a `case` to the `Tick` switch matching the scenario id.
2. Add a `Scenario_*` method.
3. If the scenario depends on a specific mod's assembly, gate it with `RequireModAssembly(assemblyName, scenarioId)`. Use reflection (`GetModAssembly`) to reach mod-internal types and methods; no build-time dependency on other mods.
4. Document the id in this README under the right section.
5. Rebuild and redeploy. The next `-Start` picks up the new scenario.

Naming: mod-specific scenarios get a mod-tag prefix. `pgp-` for PowerGridPlus today. Adding scenarios for SprayPaintPlus / EquipmentPlus / etc would use `spp-` / `eqp-` / etc.

## Output

Every scenario emits structured `[ScenarioRunner] ...` lines to the BepInEx log at `DedicatedServer/install/BepInEx/LogOutput.log`. NOT `DedicatedServer/data/server.log`: that file carries Unity / game logs, not BepInEx plugin output. Grep:

```powershell
Select-String -Path DedicatedServer/install/BepInEx/LogOutput.log -Pattern '\[ScenarioRunner\]'
```

## Limitations

- **Reads and logs only.** Some scenarios call existing patched methods (`Battery.ReceivePower`, `PowerGridTick.TestBurnCable`) for side-effect probing, but the plugin does not spawn new Things in the world. For that, use `tools/save-edit/` to write the world state offline before `-Start`.
- **Server-side only.** Reads `OcclusionManager.AllThings` and `CableNetwork.AllCableNetworks` from the simulation thread. A connected client reads from its own simulation; clients see effects of the scenario (e.g. cable burns, battery state changes) via normal state-sync but do not receive ScenarioRunner log lines.
- **No client-UI assertions.** Cursor previews, wreckage tooltips, settings-panel rendering, IC10 chip reads -- all require a connected client.
