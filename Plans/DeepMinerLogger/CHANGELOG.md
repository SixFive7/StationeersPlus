# Changelog

Full version history for Deep Miner Logger. The newest entry also appears in `About.xml` `<ChangeLog>`.

## v0.1.1: Fixed chamber gas-ratio reads and CombustionEnergy read
- Fixed chamber gas-ratio reads (use the GasMixture.GetGasTypeRatio method).
- Fixed CombustionEnergy read (it is a field, not a property).
- Dropped the rVolatiles column.

## v0.1.0: Initial build
- Initial build of diagnostic CSV telemetry for the Combustion Deep Miner.
