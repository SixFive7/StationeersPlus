# Research Index

This index lists every page in `Research/`, grouped by category. Use Ctrl+F to find a class, subsystem, pattern, protocol, or workflow by name; follow the link for the full page. Tags narrow by subject (for example, `power`, `harmony`, `save-format`); the canonical tag vocabulary lives in `CLAUDE.md`.

## GameClasses

| Page | Summary | Tags |
|---|---|---|
| [AdvancedTablet](./GameClasses/AdvancedTablet.md) | Vanilla game class representing the player's cartridge-driven tablet. Inherits from `Item` (via `DynamicThing`). Holds a cached `CartridgeSlots` list built once at Awake. | equipment, ic10, slots |
| [AllowedRotations](./GameClasses/AllowedRotations.md) | Vanilla `[Flags]` enum at `Assets.Scripts.Objects.AllowedRotations` (`None=0, Wall=1, Ceiling=2, Floor=4, Vertical=6, All=7`). Controls which surface classes a `Structure` may be placed against. Set per-prefab in the Unity inspector; the C# default is `All`. Two consumers: `InventoryManager.UpdatePlacement` (auto-corrects cursor face) and `Structure.Rotate` (Floor falls through to Y-axis only). No subclass overrides exist; per-prefab restrictions live in `sharedassets0.assets`. | prefab, transforms |
| [AreaPowerControl](./GameClasses/AreaPowerControl.md) | Vanilla `Assets.Scripts.Objects.Electrical.AreaPowerControl : ElectricalInputOutput`. The APC. Bridges `InputNetwork` and `OutputNetwork`; the four power-tick methods are side-keyed (`GetUsedPower`/`ReceivePower` are InputNetwork-only; `GetGeneratedPower`/`UsePower` are OutputNetwork-only). Battery drain happens inside `UsePower(OutputNetwork, powerUsed)` (the only path that decrements `Battery.PowerStored` on the output side), driven by `PowerTick.ApplyState` -> `Providers[].ApplyPower` -> `Device.UsePower`. Re-Volt 1.4.0's `UsePowerPatch` is misattributed to `GetUsedPower` but takes a `powerUsed` parameter; PGP retargets it at `UsePower` to match the signature. | power, prefab |
| [Battery (station-mounted)](./GameClasses/Battery.md) | Vanilla `Assets.Scripts.Objects.Electrical.Battery`. Wall / floor station battery base class (what mods like MorePowerMod's `StationBatteryNuclear` inherit). `OnPowerTick` sets `_batteryState` from `PowerStored / PowerMaximum` thresholds (0.999 / 0.75 / 0.5 / 0.25 / 0.1 / 0); ladder is identical to `BatteryCell`. The on-prefab segment bar is driven per-Mode by `RefreshAnimState` -> `SetRenderersState(numberToEnable, displayModeMaterials[Mode])`: 1..5 segments lit at VeryLow..Full, 0 at Empty, and a `FlashingDisplay` UniTask toggling at 2 Hz at Critical. Material colors live in the prefab `displayModeMaterials[]`, not in code. | power, prefab |
| [Cable](./GameClasses/Cable.md) | Vanilla placed power-cable structure `Assets.Scripts.Objects.Electrical.Cable : SmallSingleGrid`. Three tiers ship in the base game: `Cable.Type { normal, heavy, superHeavy }`. `MaxVoltage` (per-prefab serialized; `5000f` is the C# default) is the rupture threshold in watts. `CableType` is enforced only for visual merging / grid occupancy (`_IsCollision`, `CanReplace`, `WillMergeWhenPlaced`); `IsConnected`/`ConnectedCables`/`CableNetwork.Merge` ignore it, so heavy and normal cables that touch end up on one electrical `CableNetwork`. Cable open ends are `NetworkType.Power`/`PowerAndData`; there is no per-tier `NetworkType`. `OnRegistered`/`DeserializeSave`/`DeserializeOnJoin` re-merge networks with no tier check. `Break()` self-marshals off-thread, spawns `CableRuptured`, `OnServer.Destroy`s the cable. | power, prefab |
| [CameraController](./GameClasses/CameraController.md) | Vanilla game class driving the main player camera. Holds the CameraFilterPack MonoBehaviour components for existing vision effects. | ui |
| [Cartridge](./GameClasses/Cartridge.md) | Vanilla game class representing a cartridge item slotted into an `AdvancedTablet`. Drives the per-frame screen update and the scroll behavior for the currently-displayed cartridge. | equipment, ic10 |
| [Cell](./GameClasses/Cell.md) | Vanilla game class representing a single voxel cell in the world grid. Used by `Room.Grids` and the room-neighbor scan. | terrain |
| [ChatCanvas](./GameClasses/ChatCanvas.md) | Vanilla game class attached to each `Human` entity. Hosts the floating chat bubble UI rendered above player heads. | chat, ui |
| [ChatMessage](./GameClasses/ChatMessage.md) | Vanilla game class at `Assets.Scripts.Networking.ChatMessage`. The wire-format message carrying in-world chat. Indexed at 80 in the `MessageFactory`. | chat, network |
| [ChatStatusMessage](./GameClasses/ChatStatusMessage.md) | Vanilla game class at `Assets.Scripts.Networking.ChatStatusMessage`. Indexed at 81 in the `MessageFactory`. Carries the "is typing" indicator. | chat, network |
| [ColorSwatch](./GameClasses/ColorSwatch.md) | Vanilla game class at `Assets.Scripts.Objects.ColorSwatch` (a serializable C# class, one entry per color in `GameManager.CustomColors`). Holds `Normal` and optional `Emissive` materials plus `Light` color, `StringKey`, and a lazy `Index`. `Emissive` is null on some swatches. `Thing.SetCustomColor(int, bool emissive)` selects between Normal and Emissive and writes `_EmissionColor` on every `ThingRenderer`; the flag is transient, not stored, not synced. Only `ChemLight` and `RoadFlare` call with `emissive: true` in vanilla. | prefab |
| [CombustionDeepMiner](./GameClasses/CombustionDeepMiner.md) | Vanilla game class at `Assets.Scripts.Objects.Pipes.CombustionDeepMiner : DeepMiner, IInternalCombustion`. Internal-combustion drill with two player-facing levers: `Throttle` (linear fuel intake, 0.01 mol/tick at 100%) and `CombustionLimiter` (quadratic burn fraction, peaks at 75%). Tick pipeline burns then accelerates RPM then vents then admits. Fails at Stress=100. Full constants table, fuel-chemistry comparison, and logic-variable matrix. | logic, ic10 |
| [ConfigCartridge](./GameClasses/ConfigCartridge.md) | Vanilla game class for the Configuration Cartridge tablet cartridge. Builds a per-tick readout of the currently-scanned device's logic values. | equipment, ic10 |
| [Constructor](./GameClasses/Constructor.md) | Vanilla `Constructor : Stackable, IConstructionKit`. Single-prefab kit-item placement form. `OnUsePrimary(Vector3, Quaternion, ulong, bool)` -> `Construct` -> `SpawnConstruct(CreateStructureInstance)` which forks: simulation owner calls `Thing.Create<Structure> + SetStructureData`; remote client sends `ConstructionCreationMessage`. Authoring mode bypasses item-cost only, NOT placement gates. | prefab, network |
| [CreateStructureInstance](./GameClasses/CreateStructureInstance.md) | Plain managed class at `Assets.Scripts.Networking.CreateStructureInstance`. The placement-intent payload carrying `(Prefab, LocalGrid, LocalRotation, OwnerClientId, CustomColor)`. `WorldRotation` is a getter aliased to `LocalRotation`; `WorldPosition` is computed from `LocalGrid` via `GridController`. Used by Constructor / MultiConstructor and as the source for `ConstructionCreationMessage`. | prefab, network |
| [CursorManager](./GameClasses/CursorManager.md) | Vanilla game class at `Assets.Scripts.CursorManager`. Singleton that raycasts the player's cursor each tick and caches the hovered Thing. Static accessors: `CursorThing` (returns `Thing` or null), `CursorTerrain`, `CursorHit` (RaycastHit), `CursorHitMask`. Null on empty space, terrain, paused game, or blocking UI. The accessor mod code uses for "what is the local player looking at right now?" without doing its own raycast. | ui |
| [ElectricalInputOutput](./GameClasses/ElectricalInputOutput.md) | Vanilla two-port power-device base `Assets.Scripts.Objects.Electrical.ElectricalInputOutput : Device`. The shared base of every device that bridges two distinct cable networks (`Transformer`, `Battery`, `AreaPowerControl`, the `WirelessPower` dish family). Declares `InputNetwork` / `OutputNetwork` (resolved in `CheckConnections` from the serialized `InputConnection` / `OutputConnection` cables), `IsPowerProvider => true` and `IsPowerInputOutput => true` (base `Device` is false for both), and `IsOperable` false when `InputNetwork == OutputNetwork` (a bridge must join two DISTINCT networks; this is what errors a self-shorted transformer/APC). Forwards three load getters to its networks (`PotentialLoad => InputNetwork.PotentialLoad`, `CurrentLoad => OutputNetwork.CurrentLoad`, `AvailablePower => PotentialLoad`). `IsProviderToDevice` is a recursive input-side cycle walk bounded by `Device.MaxProviderRecursionIterations` (512) that drives `PowerTick.CheckForRecursiveProviders`'s loop-burn. Submergeable (sparks + eventual break when powered-on and underwater). Canonical home for facts previously scattered across Transformer / Battery / Device. | power, network |
| [ElectricityManager](./GameClasses/ElectricityManager.md) | Vanilla power-simulation driver `Assets.Scripts.Networks.ElectricityManager : ThreadedManager`. `ElectricityTick()` (host-only, `GameManager.RunSimulation`-gated, on the UniTask ThreadPool worker) runs one whole power tick in three fixed phases: `AllCableNetworks.ForEach(OnPowerTick)` (per-network supply/demand/brownout/burn, see PowerTick) -> `AllPoweredThings.ForEach(OnPowerTick)` (per-device post-power hook) -> `CircuitHolders.Execute()` (every IC10 chip runs last). Both collections are `DensePool`s; `DensePool.ForEach` walks `_activeList` in SLOT order with no sort and no double-buffer, so cross-network power propagation order is pool-slot order (construction/destruction history), NOT topological. KEY architectural fact: a multi-hop bridge chain (transformer/battery cascade, microwave TX/RX) does not settle in one tick and the per-tick lag is order-DEPENDENT, changing when a pooled member is destroyed/replaced. Clients receive each network's three load floats via `SerialiseDeltaState` and never run the tick. | power, threading, network |
| [Elevator](./GameClasses/Elevator.md) | Vanilla elevator class family: three placeable `Thing` classes (`ElevatorShaft`, `ElevatorLevel : ElevatorShaft`, `ElevatorCarrage : DynamicThing`; note the in-code typo `Carrage`) plus the `ElevatorShaftNetwork` coordinator. `Network.Shafts` mixes shafts and levels; `Network.Carrage` holds the single cabin. Paintability is driven by the prefab-level `Thing.PaintableMaterial` field, with no C# override needed; all with-cable / without-cable visual variants paint uniformly. Both `ElevatorShaft` and `ElevatorCarrage` expose a `ShaftNetwork` back-reference, so a one-hop walk covers every paint target. | prefab, network |
| [Entity](./GameClasses/Entity.md) | Vanilla game class at `Assets.Scripts.Objects.Entities.Entity`. Base class for living in-world actors (Humans and animals). Drives state transitions between `Alive` and `Unconscious` and renders stun post-processing on the local camera. | entity, damage |
| [GameManager](./GameClasses/GameManager.md) | Vanilla top-level game-manager singleton. Holds global state referenced by many subsystems. | ui |
| [Grid3](./GameClasses/Grid3.md) | Vanilla game class representing the world grid used for cell/room lookups. | terrain, ui |
| [HelpLinkHandler](./GameClasses/HelpLinkHandler.md) | Vanilla game class at `HelpLinkHandler : UserInterfaceBase, IPointerClickHandler, ...` (line 221638). The TMP link-click handler used across Stationpedia surfaces. Its `LateUpdate` references `WorldManager.IsGamePaused`, tying the component to scene state. | stationpedia, ui |
| [Human](./GameClasses/Human.md) | Vanilla game class representing the player-controlled character entity. Lives under `Assets.Scripts.Objects.Entities` in the Entity hierarchy. | entity |
| [ILifeSuspender](./GameClasses/ILifeSuspender.md) | Vanilla game interface at `Assets.Scripts.Objects.Electrical.ILifeSuspender`. Marks devices that suspend a player's life tick (sleeper, cryo tube, bed). Drives `Entity.IsSleeping`. | entity, damage |
| [ISprayer](./GameClasses/ISprayer.md) | Vanilla game interface at `Assets.Scripts.Objects.Items.ISprayer`. Marker for paint tools (`SprayCan`, `SprayGun`). Hosts the static `DoSpray` helper that is the convergence point where every paint application reaches `OnServer.SetCustomColor`; the concrete `sprayer` type is still visible there, so `DoSpray` is the correct hook point for mods that need to distinguish the gun from the bare can. | prefab |
| [InventoryManager](./GameClasses/InventoryManager.md) | Vanilla game class routing per-frame primary-use clicks against held items. The correct hook point for intercepting click-based item interactions, because neither `SensorLenses` nor `AdvancedTablet` set `AllowSelfUse = true`. | equipment, slots |
| [InventoryWindow](./GameClasses/InventoryWindow.md) | Vanilla game class rendering the inventory window for a held multi-slot item (tablet, lenses). Instantiates `SlotDisplayButton` widgets only once, at `SetSlots` time, based on each slot's `IsInteractable` at that instant. | ui, slots |
| [MultiConstructor](./GameClasses/MultiConstructor.md) | Vanilla `MultiConstructor : Stackable, IConstructionKit, IConstructionStarter, IReferencable, IEvaluable`. Multi-variant kit form (rail-kit with 8 pieces, etc.). Holds `List<Structure> Constructables` and `LastSelectedIndex`. `Construct(Grid3, Quaternion, optionIndex, ...)` -> `Constructor.SpawnConstruct`. Asymmetry vs Constructor: omits the client-side `ConstructionCreationMessage` branch (caller dispatches via `OnServer.UseMultiConstructor`). | prefab, network |
| [MultiMergeConstructor](./GameClasses/MultiMergeConstructor.md) | Vanilla `MultiMergeConstructor : MultiConstructor`. The pipe/chute/Super-Heavy-cable kit form. `Constructables` includes the 1-cell straight piece plus the multi-cell "long" variants (`StructurePipeStraight3/5/10`, `StructureInsulatedPipeStraight3/5/10`, `StructurePipeLiquidStraight3/5/10`, `StructureInsulatedPipeLiquidStraight3/5/10`, `StructureChuteStraight3/5/10`, `StructureCableSuperHeavyStraight3/5/10`). With `ToolExit` in the off hand, `Construct` merge-absorbs an existing `Piping`/`Cable` run into the bigger variant (charging only the item delta) via `IGridMergeable.CanReplace` / `WillMergeWhenPlaced`; without it, places the selected variant as-is. Length is baked into the prefab's collider/mesh footprint (`GridBounds.GetLocalSmallGrid` -> `Grid3[]`), not into the DLL. Includes a "remove the long pieces" mod lever list. | prefab, network |
| [ItemKitLinearRail](./GameClasses/ItemKitLinearRail.md) | Vanilla `MultiConstructor` kit item that builds the 8 robotic-arm rail track pieces (straight, corner, inner-corner, outer-corner, straight-stop, corner-stop, screw-left, screw-right). Wraps `Assets.Scripts.Objects.MultiConstructor.Constructables` as a serialized `List<Structure>` populated on the prefab. | prefab |
| [LanderCapsule](./GameClasses/LanderCapsule.md) | Vanilla game class at `Assets.Scripts.Objects.LanderCapsule`. The drop pod that delivers players from orbit. Inherits from `DynamicThing`. Entirely independent of the death/respawn system. | worldgen, entity |
| [LogicMirror](./GameClasses/LogicMirror.md) | Vanilla `Assets.Scripts.Objects.Electrical.LogicMirror : LogicInputBase : LogicUnitBase`. The "Logic Mirror" device: a transparent proxy (pointer/alias) for one other readable device on its own data network. All four logic methods delegate to a `CurrentDevice` target (`GetLogicValue`/`SetLogicValue` gated on the mirror's own `Powered`; `CanLogicRead`/`CanLogicWrite` are not), so to an IC10 or Batch reader the mirror looks exactly like the target for every `LogicType`. Target set only by screwdriver (`Button1` -> `Logicable.GetNextReadable` over `InputNetwork1DevicesSorted`, AltKey reverses); stored/synced as a bare `ReferenceId` resolved lazily in `OnFinishedLoad` / `ProcessUpdate` (custom `NetworkUpdateFlags` bit `1024`, save field `LogicMirrorSaveData.CurrentDeviceId`). `CheckOperable` errors unless the target is on the input data network. Full verbatim class. | logic, ic10, network, save-load |
| [LogicUnitBase](./GameClasses/LogicUnitBase.md) | Vanilla `Assets.Scripts.Objects.Electrical.LogicUnitBase : SmallDevice, ISetable, ILogicable, ...`. Base class for the small logic-unit family (logic gates, math, memory, batch reader/writer, plus the Keypad workshop mod's `Keypad`). Holds exactly two `CableNetwork` references, `_inputNetwork1` and `_outputNetwork1`, one per physical connector socket (`Input1Index = 0`, `OutputIndex = 1`). The two networks are kept independent: the device READS from input-network devices and WRITES to output-network devices, but does NOT bridge power or data across the two sockets. `Setting` is the single `[ByteArraySync]` `double` the device stores and serialises (under `LogicBaseSaveData.Setting`, `NetworkUpdateFlags |= 256`). | logic, power, network |
| [NetworkChannel](./GameClasses/NetworkChannel.md) | Vanilla game enum used by the networking layer to tag outgoing messages with delivery semantics. Chat messages ride `GeneralTraffic` (reliable). | network |
| [OnServer](./GameClasses/OnServer.md) | Vanilla static facade for server-side mutation entry points. Callers funnel gameplay actions (paint, damage, attack) through its methods so the server remains the authoritative simulator. | network |
| [PowerReceiver](./GameClasses/PowerReceiver.md) | Vanilla `Assets.Scripts.Objects.Electrical.PowerReceiver`, receiver half of the dish-to-dish wireless pair (subclass of `WirelessPower`). `GetGeneratedPower(OutputNetwork)` returns `WirelessInputNetwork.PotentialLoad` (the transmitter's deliverable re-emitted onto the receiver's cable network); `GetUsedPower(InputNetwork)` returns `Min(MaxPowerTransmission + UsedPower, _powerProvided)`. Carries no rating of its own; PowerTransmitterPlus's distance model lives entirely on the transmitter side, so the receiver is a faithful pass-through. | power, network |
| [PowerTick](./GameClasses/PowerTick.md) | Per-`CableNetwork` power-tick worker `Assets.Scripts.Networks.PowerTick`, held in `CableNetwork.PowerTick`. `CableNetwork.OnPowerTick` calls `Initialise -> CalculateState -> ApplyState` on it once per tick (on the UniTask ThreadPool worker). Vanilla cable burn is instantaneous and per-cable: `GetBreakableCables` flags any cable with `MaxVoltage < _actual` (`_actual = min(Potential, Required)`), `BreakSingleCable` picks one at random and `Break()`s it (a fuse blows first if any `PowerBreak < _actual`). This is the class Re-Volt subclasses as `RevoltTick : PowerTick`. Reconciles the class-attribution imprecision on [CableNetwork](./GameClasses/CableNetwork.md) (those `ConsumePower`/`CalculateState` bodies are `PowerTick` members). | power, threading |
| [PowerTransmitter](./GameClasses/PowerTransmitter.md) | Vanilla game class at `Assets.Scripts.Objects.Electrical.PowerTransmitter`. Transmitter half of the dish-to-dish wireless power pair. Paired at runtime with a `PowerReceiver` via raycast and drives a private `_powerProvided` debt accumulator across two cable-network ticks. | power, logic, transforms |
| [PowerTransmitterOmni](./GameClasses/PowerTransmitterOmni.md) | Vanilla omnidirectional wireless charger `Assets.Scripts.Objects.Electrical.PowerTransmitterOmni : Electrical`. NOT a `WirelessPower` subclass (sibling branch under `Device`): single-network consumer, no servo, no `WirelessNetwork`, no `_powerProvided`. Phase-2 `OnPowerTick` runs `LocateBatteries` (nearest working omni claims each eligible `WirelessBattery` within `_maxDistance` 15) + `CalculateUsedPower` (`Min(UsedPower + SUM Min(cell.PowerDelta, 300), 2000)`, read back one tick stale by `GetUsedPower`); delivery equal-splits inside `ReceivePower` while budget > 0.1 W; the 75-to-25 percent distance derate lives in `WirelessBattery.AddPowerWireless`. | power |
| [PowerTransmitterVisualiser](./GameClasses/PowerTransmitterVisualiser.md) | Vanilla `MonoBehaviour` in the global namespace that drives the line-renderer beam material on `WirelessPower` dishes (`Activate`, `Deactivate`, `SetDirection`, `SetIntensity`, plus the private `SetMaterialPropertiesForIntensity`). `SetIntensity` is the only method that handles thread routing: from a ThreadPool worker it enqueues a closure on `UnityMainThreadDispatcher`. The closure is not safe across scene unload; pending entries fire on the next main-thread frame after world-scene teardown and throw `NullReferenceException` from `_lineRenderer.material` on the destroyed Renderer. Cosmetic only (logged, no crash, no functional consequence); vanilla bug. | power, threading, unity |
| [RoboticArmDock](./GameClasses/RoboticArmDock.md) | Vanilla `RoboticArmDock` and its four subclasses (atmos, cargo, collector, hydroponics). IC10 logic surface (`Setting`, `Idle`, `Extended`, `PositionX`, plus cargo's `TargetSlotIndex` / `TargetPrefabHash`), the arm-hand slot model (`Slots[0]` is the arm's hand), `RoboticArmDockCargo`'s `IProxySlot` index 255, and the full `LogicSlotType` enum. Includes the canonical "is the arm holding anything" IC10 read. | logic, ic10, slots, prefab |
| [RoboticArmRail](./GameClasses/RoboticArmRail.md) | Vanilla family of rail pieces traversed by `RoboticArm` dock assemblies. All subclasses derive from `Structure -> SmallGrid -> RoboticArmRailBase` (or `-> Device -> SmallDevice -> RoboticArmRailDeviceBase` for docks/bypass) and live on the 0.5-unit small grid, not the world grid. 8 rail-only prefabs, 5 dock kit prefabs, 1 junction class. Connection topology runs through `RoboticArmNetwork.RailList`. | prefab, network, transforms |
| [RocketPowerUmbilical](./GameClasses/RocketPowerUmbilical.md) | Vanilla rocket power umbilical pair, since 0.2.6403 built on an abstract `RocketPowerUmbilical : ElectricalInputOutput, IUmbilical` base (owns the cell, the single partner reference, `TransferProgress`) with `RocketPowerUmbilicalMale` / `RocketPowerUmbilicalFemale` halves; the "Item Umbilical (Power)" / "Umbilical Socket (Power)" / "Umbilical Socket Angle (Power)" devices. NOT dumb wires: each half is an internal cell (10000f code default) and BOTH halves push across the gap via direct `partner.ReceivePower(null, n)` (no CableNetwork): Male cell -> Female each tick when its `CanTransfer` gate passes, and Female pulls from `RocketNetwork.Batteries` and pushes to the Male per completed `TransferProgress` cycle. Each half charges from its `InputNetwork` and discharges to its `OutputNetwork` like a `Battery`. Connector count / cable-type acceptance is prefab data. | power, prefab |
| [Room](./GameClasses/Room.md) | Vanilla game class representing an enclosed interior volume. Holds the list of interior cells forming the sealed space. | terrain |
| [RotatableBehaviour](./GameClasses/RotatableBehaviour.md) | Vanilla servo class that drives dish rotation on `WirelessPower` subclasses and panel aim on `SolarPanel`. `TargetHorizontal` / `TargetVertical` are the only worker-thread-safe aim writers (managed double stores; `DoMoveTask` self-marshals to the main thread) and ride the existing flag-256 delta-state that clients slew to. The slew gates on `IsBroken` / `CanRotate()` only, never on power state. | transforms, network, threading |
| [SayCommand](./GameClasses/SayCommand.md) | Vanilla game class at `Util.Commands.SayCommand`. Implements the `say` console command and is the canonical demonstration of the server-side chat-send pattern. | chat, network |
| [SensorLenses](./GameClasses/SensorLenses.md) | Vanilla game class representing the sensor-lens visor equipment item. Inherits from `Item` (via `DynamicThing`). Vanilla ships with 2 Sensor Processing Unit (SPU) slots but only one active `Sensor` property. | equipment, slots |
| [SolarPanel](./GameClasses/SolarPanel.md) | Catalogs every solar-tracking device family in vanilla: power-generating `SolarPanel` (10 fixed-mount prefab keys plus the `PortableSolar` handheld), heat-radiating `RadiatorRotatable` and its sole subclass `LargeExtendableRadiator` (the second `ISolarRadiator` implementer, registered in the same `Networks.SolarRadiators` pool, with double-faced sun reading and a `SolarRadiationModulator` that down-rates radiation 24x while sun-pointed), and the solar `DaylightSensor` (`ILightActivated` sensor exposing `SolarAngle`, `Horizontal`, `Vertical`, `SolarIrradiance` for IC10 trackers). Includes the `SolarControl` motherboard, the exact `PowerGenerated()` formula with `1.4` / `1.6` storm scalar (altitude-aware weather terms since 0.2.6403), the per-arm `SolarPanelArm` five-raycast `GenerationEfficiency` model (averaged across arms; eclipse / terrain-occlusion early-outs; derived 60-degree off-axis zero-output cutoff), the vertical ratio's three scales (physical pitch `Lerp(-75, +75)` vs logic `Lerp(15, 165)` vs raw ratio), the main-thread-only setters vs worker-safe `RotatableBehaviour` targets split, the solar-only island bootstrap corollary, and the LogicType surface for each device family. | power, logic, ic10, prefab |
| [SprayCan](./GameClasses/SprayCan.md) | Vanilla game class representing a spray-paint can consumable. The game ships one `SprayCan` prefab per color. | prefab |
| [SprayGun](./GameClasses/SprayGun.md) | Vanilla game class at `Assets.Scripts.Objects.Items.SprayGun : Tool, ISprayer, IUsedAmount, IUsed`. Paints targets by scaling `quantity` by gun efficiency and delegating to the loaded `SprayCan.OnUseItem`. Reaches `OnServer.SetCustomColor` via the shared `ISprayer.DoSpray` convergence; concrete tool type is only distinguishable upstream of that call. | prefab, equipment |
| [Stackable](./GameClasses/Stackable.md) | Vanilla `Assets.Scripts.Objects.Items.Stackable : Item, IQuantity, ITradable, IEvaluable, IReferencable, IMergeable`. Base class for quantity-bearing held items (ores, sheets, `Plant`, the construction kits). `OnUseItem(float quantity, ...)` is the crash site for `ArgumentOutOfRangeException ("count")` from `List.RemoveRange`: its guard checks the *index* (`StackedGeneCollections.Count - (int)quantity >= 0`) but not the *count* (`(int)quantity`), so a negative `quantity` always passes the guard and calls `RemoveRange(Count + n, -n)`. The negative quantity is produced by `MultiMergeConstructor.Construct`'s merge cost delta `num4 = EntryQuantity(result) - EntryQuantity(existing)` when the merge result is cheaper than the absorbed piece. Base-game logic gap (decompile + IL confirmed). | prefab, slots |
| [Stationpedia](./GameClasses/Stationpedia.md) | Vanilla game class at `Assets.Scripts.UI.Stationpedia`. Singleton controller of the in-game Stationpedia window. Exposes `Register`, `SetPage`, `OnPageChanged`, and owns the row prefab inventory used by page rendering. | stationpedia, ui |
| [StationpediaPage](./GameClasses/StationpediaPage.md) | Vanilla game class at `StationpediaPage` (line 233507 in game decompile). Data model for one Stationpedia entry: the record holding `Key`, `Title`, `Text`, parsed `Description`, category fields, and the insert-list collections consumed by `UniversalPage`. | stationpedia, ui |
| [Structure](./GameClasses/Structure.md) | Vanilla game class for player-built, fixed-position game objects. Subclass of `Thing`. Covers walls, frames, pipes, cables, and devices. | prefab |
| [Thing](./GameClasses/Thing.md) | Vanilla game class at `Assets.Scripts.Objects.Thing`. The base class of every in-world game object. All prefab types derive from `Thing` either as `DynamicThing` (non-fixed-position) or `Structure` (player-built, fixed). | prefab, slots |
| [ThingRenderer](./GameClasses/ThingRenderer.md) | Vanilla game class at `Assets.Scripts.Objects.ThingRenderer`. Wrapper around a Unity `Renderer` that unifies material access across the `MeshRenderer` and `DrawData` paths. Each Thing carries a `List<ThingRenderer>` in `Thing.Renderers`. Exposes `Materials`, `sharedMaterials`, and `GetMaterial()` accessors returning Unity `Material` references; used by `Thing.SetCustomColor` to iterate and set `_EmissionColor` per-renderer. | prefab, unity |
| [Transformer](./GameClasses/Transformer.md) | Vanilla `Assets.Scripts.Objects.Electrical.Transformer : ElectricalInputOutput, ISetable, ILogicable`. The only step-down / network-bridging device in vanilla besides APCs. There is exactly one `Transformer` class (no `TransformerLarge` / `TransformerSmall` subclasses; a smaller transformer prefab is the same class with a different serialized `OutputMaximum`). `Setting` clamped to `[0, OutputMaximum]` (`10000f` C# default); logic `Setting`/`Maximum`/`Ratio`. `ElectricalInputOutput` resolves `InputNetwork`/`OutputNetwork` from serialized `InputConnection`/`OutputConnection` (`ConnectionRole.Input`/`Output`) in `CheckConnections()`; `IsOperable` false when `InputNetwork == OutputNetwork`. | power, logic |
| [UniversalPage](./GameClasses/UniversalPage.md) | Vanilla game class at `UniversalPage : UserInterfaceBase` (line 233792). Whole-page renderer that hosts the 19 `StationpediaCategory` fields driven by the `Populate*Inserts` methods. | stationpedia, ui |
| [Wall](./GameClasses/Wall.md) | Vanilla game class for wall structures. Inherits from `LargeStructure`. Wall-painting flow must honor inheritance ordering when dispatching. | prefab |
| [WeatherEvent](./GameClasses/WeatherEvent.md) | Vanilla data class `Assets.Scripts.WeatherEvent : DataCollection` (line 52768). XML-loaded weather definitions held one-at-a-time on `WeatherManager.CurrentWeatherEvent`. Documents the class schema, the eight shipped events from `StreamingAssets/Data/weather.xml` with verbatim SolarRatio / DamageMultiplier / Duration / TemperatureOffset values (atmospheric storms attenuate sun 5-30 percent and damage panels; `Rain` / `Snow` attenuate to 50 percent and do no damage; `SolarStorm` and `VulcanSolarStorm` MULTIPLY sun by 4x and do NO damage), and the `OrbitalSimulation.SolarIrradiance = SolarConstant / distanceAu^2` formula with a per-body irradiance table (SolarConstant default 1367 is never overridden in vanilla `celestialbodies.xml`; irradiance varies only with orbital distance). Also covers the WeatherManager statics, save data, and the directional-light flicker block on solar storms. | power, prefab |
| [WindTurbineGenerator](./GameClasses/WindTurbineGenerator.md) | Vanilla producer at `WindTurbineGenerator : Device, IPowerGenerator` (line 146848); `LargeWindTurbineGenerator` (146360) subclasses it without overriding `GetGeneratedPower`, so one patch covers both. `GetGeneratedPower` recomputes `CalculateGenerationRate()` on every call from `WindStrength` (a static rewritten every frame by `UpdateWind()` at the tail of `GameManager.Update`, simplex noise over `NetworkTime.time`), main-thread weather state, and world atmosphere pressure, so two reads inside one electricity tick can disagree; the only producer family besides solar whose output is not tick-stable. Carries the verbatim generation formula and wind-state code. | power, threading |
| [WirelessPower](./GameClasses/WirelessPower.md) | Vanilla base at `Assets.Scripts.Objects.Electrical.WirelessPower : ElectricalInputOutput, IRotatable`. Servo / dish-aim machinery shared by `PowerTransmitter` and `PowerReceiver` (its only subclasses; `PowerTransmitterOmni` is unrelated, see its page). `Vertical` and `Horizontal` setters write `localRotation` (frame-invariant). `MaximumVertical = 180`, `MaximumHorizontal = 360` (degrees); H/V are normalized [0..1] ratios. NetworkUpdateFlags 256 (per-tick H/V half-precision) and 512 (visualizer). Late-join ships full-precision doubles. `OnRegistered` resets to (H=0, V=1) on fresh placement, skipped on save-load. | power, transforms, network |

## GameSystems

| Page | Summary | Tags |
|---|---|---|
| [AtmosphericThingTickDispatch](./GameSystems/AtmosphericThingTickDispatch.md) | How the game dispatches `Thing.OnAtmosphericTick()` each simulation tick: the `AtmosphericThings` DensePool (line 439561), the single `ThingAtmosphereTickAction` delegate call site (line 439619, the vanilla frame a stale override crashes from), `Register`/`Deregister` admission, the `Thing` -> `Device` override chain, and three vanilla-only levers to intercept or filter one thing type's atmospheric tick. The hook point Force Field Door Mod Fix uses. | harmony, threading |
| [CameraFilterPack](./GameSystems/CameraFilterPack.md) | Stationeers ships with the third-party CameraFilterPack library embedded as a set of shader-based Unity MonoBehaviours. 274 effects are available; each is added to the main camera with `AddComponent<>()`, parameters are set on the component, and the effect runs automatically. This page catalogs the library and the best-fit effects for gameplay use such as gas exposure, unconsciousness, EMP, and vision changes. | ui, entity |
| [ChatBroadcast](./GameSystems/ChatBroadcast.md) | Stationeers chat is not a separate protocol; player chat messages flow through the game's native network message system as `ChatMessage` instances. The four-step flow from client input to every client's console and chat bubble is documented below. | chat, network |
| [ChemistryGasPhaseData](./GameSystems/ChemistryGasPhaseData.md) | Every gas in Stationeers shares one vapor-pressure curve `P = A * T^B`, parameterised per-gas by two coefficients plus a freezing-pressure floor and a critical pressure. Records verbatim `EvaporationCoefficientA` / `B` tables for all 16 substance families (including the March-2026 additions: Hydrogen, Hydrazine, Helium, Sodium Chloride, Silanol, Hydrochloric Acid, Ozone, Alcohol), the `Mole.FreezingTemperature` and `Mole.MaxLiquidTemperature` switches, and a computed freeze / max-liquid table in K and C. Water and PollutedWater short-circuit to hardcoded 273.15 / 276.15 K. Helium has `A = B = 0` and never has a liquid phase. | save-format |
| [CreativeModeAndDifficulty](./GameSystems/CreativeModeAndDifficulty.md) | How Stationeers represents Creative Mode. The `GameMode : byte` enum has only `Survival`/`Creative` (no Sandbox); `WorldManager.GameMode` is a plain public field (line 58749) derived from `DifficultySetting.Current.Creative` via the `SetCurrent` -> `SetCreativeMode` bridge (line 55251). The save stores only `<DifficultySetting Id="..." />` (a child of `<WorldData>`); GameMode is reconstructed on load (`XmlSaveLoad.LoadWorld` line 251396), so an offline edit to `Id="Creative"` persists and reaches a joining client via the `SerializeOnJoin` handshake. The stock `Creative` preset (in `difficultySettings.xml`) zeroes all consumption rates AND sets `Creative=true`; the `Creative` bool itself only gates spawn menu / teleport / lava immunity / creative commands, while zero-consumption is the rate fields. The only console command is `difficulty <name>` (host-only, no live sync); there is no `creative`/`sandbox`/`cheat` command. A worker-thread runtime flip of `WorldManager.Instance.GameMode` is safe (bare field write) but for full parity also needs `DifficultySetting._current` repointed. | save-edit, save-load, network |
| [ConveyorBeltCutContent](./GameSystems/ConveyorBeltCutContent.md) | The `StructureConveyor*` "Flexi-belt" conveyor prefabs (`StructureConveyorStraight` / `StraightLong` / `StraightShort` / `Corner` / `Riser`, plus `ItemKitConveyor` and the `DynamicCrateConveyorSupplies` crate) are cut content from very early Stationeers, the predecessor of the chute network. Prefabs and string/Stationpedia entries still ship registered, but no conveyor-specific C# class exists: an exhaustive `grep -i conveyor` of Assembly-CSharp finds only leftovers (`LoreFactions.Recurso`, a "Conveyors" audio bucket, `CrateType.ConveyorSupplies` -> 2x `ItemKitConveyor`, the vestigial `DynamicThing.OnConveyor` bool, and the misleadingly-named `DeviceImportExport.ImportConveyorPosition` chute transforms). Spawnable via the creative prefab spawner; "building" one errors because the construction wiring is gone. For mods (Network Purist Plus TODO), removing it is a "hide a vestige" task, not a "neuter a working kit/merge" task. | prefab, dead-end |
| [DamageState](./GameSystems/DamageState.md) | Every `Thing` carries a `DamageState` with up to nine channels (Burn, Brute, Oxygen, Hydration, Radiation, Starvation, Toxic, Stun, Decay). This page catalogs the channels, the class hierarchy, the `HealAll` behavior at each layer, the writable float fields, the save-edit disambiguation gotcha, and a real-save observed damage distribution. | damage, save-edit, save-format |
| [DevicePowerDraw](./GameSystems/DevicePowerDraw.md) | Which vanilla devices exceed the 5 kW normal-cable rating. Per-device `UsedPower` is prefab serialized data (C# default `10f`), not in the decompile -- only a few `GetUsedPower` overrides and constants are: `CarbonSequester.POWER_PER_UNIT_CARBON = 45000f` (45 kW per unit carbon), `AdvancedFurnace.GetUsedPower` returns up to 3x `UsedPower` at full settings, `ArcFurnace.GetUsedPower` is `UsedPower + recipe.Energy`/tick. So multiple endgame machines draw far more than 5 kW by design; a "all devices on the 5 kW network" rule needs a carve-out (the cable feeding a 45 kW device must itself be >= 45 kW rated; a transformer doesn't change that). `Transformer.OutputMaximum` default `10000f`. | power |
| [Explosions](./GameSystems/Explosions.md) | How explosives detonate and why a vanilla Demolition / Mining Charge shatters glass and KOs players but leaves frames/walls/machines standing. One `ItemExplosive` class behind every explosive prefab (`CanRemoteDetonator` discriminates the timed Mining Charge from the remote Demolition Charge); `ExplosionRadius=5.3`, `ExplosionForce=2000`. `Explosion.Explode(force, pos, radius, maxDamage, mineTerrain)` runs server-only, sweeps expanding `OverlapSphere` shells, applies `Brute`+`Stun` with linear falloff from `thing.Position`. The load-bearing limiter is the total-damage budget `maxDamage = min(force*100, MINING_CHARGE_MAX_DAMAGE=2000)`: the first colliders soak it and the loop bails, so sturdy structures never get meaningful damage. No explosion-immunity flag exists; structures take Brute normally but never enough. Includes the mod-lever list for a bigger/destructive charge. | damage |
| [IC10DeviceAddressing](./GameSystems/IC10DeviceAddressing.md) | The three IC10 device-operand forms (pin `d0..d5`, alias, ReferenceId via `$hex` / register), which opcodes accept which forms (`l`/`s`/`ls`/`lr`/`get`/`put` accept all three; `ld`/`sd`/`getd`/`putd` are ReferenceId-only), and the resolution path through `CircuitHousing.GetLogicableFromId` that confines ReferenceId addressing to devices on the IC10 housing's data network. Documents the missing null guard in `_LD_Operation` and the 32-bit narrowing of the id. | ic10, logic |
| [IC10ExecutionTick](./GameSystems/IC10ExecutionTick.md) | IC10 execution is driven by the `GameManager` 500 ms / 2 Hz game tick, not the 20 Hz atmospheric tick. The call chain `GameManager -> ElectricityManager.ElectricityTick -> CircuitHolders.Execute -> CircuitHousing.Execute -> ProgrammableChip.Execute(128)` runs up to 128 instructions per chip per game tick, gated by `OnOff`, `Powered`, `GameManager.RunSimulation`, and a clean compilation. Documents the verbatim call sites and yield semantics. | ic10, logic, power |
| [IC10SyntaxHighlighting](./GameSystems/IC10SyntaxHighlighting.md) | The in-game screen (computer, laptop, wall-mounted screen) syntax-highlighting pipeline for IC10 source code. All rendered IC10 code flows through a single `Localization.ParseScript()` call; `ProgrammableChip.InternalEnums` snapshots `Enum.GetValues(typeof(LogicType))` at static-init time, so runtime-added LogicType values render red as "invalid" unless their private `_types` / `_names` arrays are extended via reflection. | ic10, logic, ui |
| [LightSources](./GameSystems/LightSources.md) | Catalog of every code-recognised light-emitting Thing in vanilla (`ILight` -> `WallLight` and its `WallLightBattery` / `FlashingLight` / `GrowLight` subclasses for structure lights; `IWearableLight` -> `Flashlight` / `Headlamp` / `Helmet` / `GasMask`; `PortableLight`; `StackableLight` -> `ChemLight` / `RoadFlare`). Covers the `ThingLight` wrapper (occlusion cullingMask gate, `EffectLight`-tagged exclusion, forced `shadowBias = 0.1`), the three power models (cable watts via prefab `Device.UsedPower`; per-tick `BatteryCell` drain; finite 180 s lifetime), `WallLight`'s binary on/off + occlusion/shadow-distance + smart-rotate + paint-recolours-the-`Light` behaviour, `WallLightBattery.BatteryChargeRate = 200f`, `Helmet` 5/tick and `GasMask` 50/atmos-tick drains, `RoadFlare`'s code-set range (10 m / 25 m parachute) + flicker + atmosphere ignition vs `ChemLight`'s none, and `LightManager` being the sunlight-occlusion manager (no `IlluminationManager` exists). Per-prefab light spread (Spot/Point, range, spotAngle, intensity, default color) is `sharedassets0.assets` data, not in the decompile. | power, unity, prefab |
| [Localization](./GameSystems/Localization.md) | How the game resolves display strings for prefabs, LogicTypes, and slots: the `Localization.LanguageFolder.LoadAll` path, the `english.xml` / `english_help.xml` resources, the legacy `ThingTemplate` placeholder that is now dead code, the vanilla transmitter/receiver body text (same string for both), and the `StringKey` / `StringHash` pair that drives `Slot.DisplayName`. | ui, stationpedia |
| [LogicType](./GameSystems/LogicType.md) | `LogicType` is a `ushort` enum with 350 vanilla values (0-349) that names every logic readable and writable in the game. Extending it at runtime requires writing into four independent registries, all snapshotted from `Enum.GetValues(typeof(LogicType))` at class load. Missing any registry produces silent breakage in at least one UI surface. | logic, ic10, ui |
| [LogicValueFormatting](./GameSystems/LogicValueFormatting.md) | How the game converts a `double` logic value into a display string. Two distinct patterns: display-only surfaces (`ConfigCartridge.ReadLogicText`, `LogicValueDisplay.Refresh`) use `Math.Round(value, 3, MidpointRounding.AwayFromZero)`; input dialogs and tooltips use `ExtensionMethods.ToStringExact()` (`"0." + new string('#', 339)`). All paths use `CultureInfo.CurrentCulture`. The game is not uniform across surfaces. | logic, ui, equipment |
| [ModDeduplication](./GameSystems/ModDeduplication.md) | When the same mod is enabled from two sources (Workshop, local, Repo), StationeersLaunchPad's `ModSet.TryGetExisting` dedupes on Workshop handle then ModID then reference. `ModList.DisableDuplicates` picks the winner by a priority table (default Repo > Local > Workshop) and sets `Enabled = false` on the loser with a `disabled in favor of` log line. Folder name is not a dedup key; a `ModID` mismatch between a Workshop and local copy defeats dedup entirely and both plugins load. | launchpad, packaging |
| [ModLoadSequence](./GameSystems/ModLoadSequence.md) | When `Prefab.OnPrefabsLoaded` and `OnAllModsLoaded` fire relative to `Stationpedia.Regenerate`, and why Unity API calls from inside `OnAllModsLoaded` are safe without a main-thread dispatch. | launchpad, unity, threading |
| [ModMetadataLimits](./GameSystems/ModMetadataLimits.md) | The 8000-character cap on About.xml `<ChangeLog>` and `<Description>` is a StationeersLaunchPad `PublishItemValidator` publish-time check, not a base-game or Steam parse-time limit. Over-limit publishes are rejected (not truncated). The base-game `ModAbout` class has no ChangeLog field; `<ChangeLog>` is a StationeersLaunchPad `ModAboutEx` element routed to the Steam change note via Facepunch.Steamworks `.WithChangeLog(...)`. Also caps `Name` 128 and `thumb.png` 1 MB. | launchpad |
| [NetworkRoles](./GameSystems/NetworkRoles.md) | How `NetworkManager.IsActive`, `IsServer`, and `IsClient` combine across single-player, multiplayer host, multiplayer client, and dedicated server modes, and why the `IsActive && !IsServer` guard is the correct remote-client check. | network |
| [NetworkUpdateFlags](./GameSystems/NetworkUpdateFlags.md) | The `Thing.NetworkUpdateFlags` 16-bit bitmask and how vanilla serialization uses the low 12 bits, leaving a small band of free bits that mods can piggyback on to add custom per-thing sync data. | network, save-load |
| [Occlusion](./GameSystems/Occlusion.md) | Distance-based renderer culling. `OcclusionManager.CheckAllOcclusion` iterates `GridController.AllStructuresPool` and `AllDynamicThings`; per-Thing `SetOcclusion` flips `IsOccluded` based on `CurrentCameraDistanceSquared` versus `GetRenderMaxDistanceSquared` (default 100 m base distance times a multiplier of 0.8 / 1.0 / 1.5 / 2.0 / 2.5 by `RenderDistance` setting). `OnStopRender` only hides `ThingRenderer.Visible`. Colliders are toggled solely on the `DynamicThing` path via `PhysicsOnRender -> SetColliders`; `Structure` subclasses keep colliders enabled at any distance. Implication: `Physics.Raycast` against a distant `Structure` (e.g. `PowerReceiver.DishTarget`) hits regardless of render distance setting. | unity, transforms |
| [PlacementOrientation](./GameSystems/PlacementOrientation.md) | End-to-end placement pipeline: player input -> `InventoryManager.PlacementMode` -> `UpdatePlacement` (the AllowedRotations gate) -> `Constructor.OnUsePrimary` -> `Constructor.SpawnConstruct` -> `Thing.Create + SetStructureData` (simulation) or `ConstructionCreationMessage` (remote client). Quaternion is lossless through every channel (multiplayer placement, late-join, per-tick `WriteTransform`, save). `CanConstruct` does NOT consult `AllowedRotations`; the orientation gate exists only in the cursor. Documents the three fields `(ThingTransform.rotation, RegisteredRotation, Direction)` and how they reconcile. Also covers the unconstrained cosmetic *roll* of straight small-grid pieces: `Cable.ConnectionType = Exhaustive` (24 orientations), connectivity is roll-blind for `IsStraight` pieces, super-heavy cables visibly glitch (red band, no dynamic re-skin) while pipes/chutes do not, and ZoopMod's hard-coded `SmartRotate.Rot{X,Z}` rotation seeds mixed-roll runs. | prefab, transforms, network |
| [PowerProducerLogicReadability](./GameSystems/PowerProducerLogicReadability.md) | Which power-producer classes declare a device-specific `CanLogicRead` / `GetLogicValue` override (Solar, both wind turbines, the small wall turbine, gas / solid / stirling generators, exposing readable logic such as `PowerGeneration`) versus those that only inherit the universal base types (RTG, a rocket-internal with no normal data port; PowerConnector). Having no on/off button does NOT imply no logic surface. | power, logic |
| [PowerProducerOnOffState](./GameSystems/PowerProducerOnOffState.md) | Which power-producer classes report `Thing.OnOff == true`, i.e. carry an `Interactable` with `Action == InteractableType.OnOff` (sets `HasOnOffState`, also gates `LogicType.On` writeability). The three fuel generators (gas / solid / stirling) have it; the gas generator is decompile-proven via a `base.InteractOnOff` deref, solid and stirling are strongly inferred. Solar, both winds, the small wall turbine, the RTG, and the PowerConnector dock are buttonless (`OnOff == false` permanently), inferred from absence since `Interactables` is serialized prefab data the decompile cannot read directly. | power, logic, prefab |
| [PrefabSourceAttribution](./GameSystems/PrefabSourceAttribution.md) | How to tell, at runtime, which mod contributed each `Thing` in `Prefab.AllPrefabs`. The game stores NO provenance on `Thing` (only `PrefabName` / `PrefabHash`), and `Prefab.LoadAll` clones a flat `WorldManager.SourcePrefabs` into `AllPrefabs` with no mod identity in scope, so there is no single hook that sees prefab + mod together. Mod identity survives only in the per-loader source registries (`LaunchPadBooster.Mod.AllMods[].Prefabs`, StationeersLaunchPad `ModLoader.LoadedMods[].Prefabs`). Verdict: single-pass per-mod attribution IS feasible with no restarts by joining `AllPrefabs` to those registries by `PrefabHash`, with "vanilla" as the residual bucket; a `Prefab.RegisterExisting` Harmony hook using `ModLoader.TryGetExecutingMod` catches direct-injection mods. Load ordering (`LoadAll` line 58987 before `LoadDataFiles` line 59036), `CustomThingData` via `ThingImporter.RegisterThing` (line 116838), and the existing flat DebugPackage dumps are documented. | prefab, launchpad |
| [PowerTickThreading](./GameSystems/PowerTickThreading.md) | The game's power-tick simulation runs on a UniTask ThreadPool worker. Any Harmony patch on `PowerTick`-adjacent methods inherits that thread. Unity API calls from those threads hard-crash the player; a `MainThreadDispatcher` is required to bridge writes back to the main thread. | power, threading |
| [RenderingPipelineAndGlow](./GameSystems/RenderingPipelineAndGlow.md) | Stationeers uses Unity's built-in render pipeline on Unity 2021.2 LTS with UltimateBloom enabled by default (toggled via `CameraController.SetBloom`). Emissive pixels bloom into visible glow; without bloom emission renders self-lit but flat. `Thing.SetCustomColor(index, emissive: true)` is the vanilla glow API (swaps to `ColorSwatch.Emissive` when non-null and writes `_EmissionColor` on every `ThingRenderer`), but the flag is transient and not synced, so mods must re-apply after every `SetCustomColor` entry. Compares three glow techniques (vanilla API, per-instance `_EmissionColor` write, additive shader swap) with trade-offs. | unity |
| [RepairMechanics](./GameSystems/RepairMechanics.md) | Vanilla repair paths: duct tape, Welding Torch / Arc Welder build-state revert, what cannot be repaired, and the resource-cost-to-time-cost ratio. | damage |
| [RespawnFlow](./GameSystems/RespawnFlow.md) | The full Stationeers respawn path creates a brand-new `Human` rather than reviving the existing body. Important context for any mod that wants to manipulate "entering a capsule" (time-skip, knockout) without triggering respawn semantics. | entity |
| [RocketConstructionEligibility](./GameSystems/RocketConstructionEligibility.md) | What makes a structure "rocket family" and gates inside-vs-outside-rocket placement, by data not by name. Two interfaces (`IRocketComponent` marker, `IRocketInternals : IRocketComponent`) plus two members (`InternalCellType`, a `[Flags] RocketInternalCellType` with `None=0/Pipes/Cables/Devices/Chutes/Umbilical/Engine/CargoBay/CableConnector`; and `bool StrictlyInternal`). Membership classifier (verbatim at `Prefab.RegisterExisting` L304008 and `Stationpedia.AddRocketInfo` L231724): `prefab is IRocketComponent rc && !(rc is IRocketInternals { InternalCellType: None })` -> populates `IRocketComponent.AllRocketPrefabs` + the "Constructable In Rockets" panel. Outside-rocket build gate (`SmallGrid.CanConstruct` L294100): `this is IRocketInternals { StrictlyInternal: not false } && !(smallCell?.Owner is RocketNetwork)` -> `CannotPlaceOutsideRocket`. Serialized-vs-code is MIXED: `Battery` / `Transformer` read both values from `[SerializeField]` fields (so `StructureBattery`/`Small`/`Medium` are one class, indistinguishable in code); `Pipe`/`Tank`/`Cable`/`DirectHeatExchanger` serialize the cell-type only; most `Rocket*` classes hardcode both. Read per-prefab via `(thing as IRocketInternals)?.InternalCellType` / `.StrictlyInternal`. Full ~38-class table + sanity check. | prefab, transforms |
| [SaveDataRegistration](./GameSystems/SaveDataRegistration.md) | How a mod registers its custom save-data types with the game's `XmlSaveLoad.ExtraTypes` list, and why a dual-registration (LaunchPadBooster `AddSaveDataType` plus direct `ExtraTypes` injection) is required to tolerate the load-order race. | save-load, launchpad |
| [SaveZipExtension](./GameSystems/SaveZipExtension.md) | Feasibility of writing side-car files (e.g., `sprayplus-glow.xml`) into the save ZIP alongside `world.xml`. Read path preserves unknown entries; write path rebuilds from scratch and strips them. Requires Harmony patch on `XmlSaveLoad.SaveWorld` to inject before ZIP seal, plus `Thing.OnFinishedLoad` postfix to re-apply state. No sanctioned LaunchPad hook exists. | save-load, save-format, launchpad |
| [StationpediaAscendedInternals](./GameSystems/StationpediaAscendedInternals.md) | Reverse-engineered internals of the third-party Stationpedia Ascended (SPA) mod, as they pertain to an integrating mod: SPA's Harmony patch list, what SPA deliberately does NOT patch, the `ChangeDisplay` postfix flow, microwave-device coverage, the tooltip resolution chain, the `SearchPatches` state and hide-policy, SPA's own link-handler inventory, and the 100ms tooltip polling coroutine. | stationpedia, spa, ui |
| [StationpediaMarkup](./GameSystems/StationpediaMarkup.md) | Full replacement table for Stationpedia's `{TAG:value}` markup tokens. `ParsePage` expands these to TextMeshPro rich-text spans (`<link>`, `<color>`, `<size>`, `<indent>`). TMP tags pass through unchanged; bare identifiers do not auto-link. | stationpedia, ui |
| [StationpediaPageRendering](./GameSystems/StationpediaPageRendering.md) | End-to-end lifecycle of a Stationpedia page render: the `Stationpedia.Regenerate` 15-step sequence that builds every page from prefab data at load time, the `UniversalPage.ChangeDisplay` 6-step flow that populates the on-screen UI when a page is opened, and the two `Populate*` methods that iterate `EnumCollections.LogicTypes` to emit per-LogicType rows. | stationpedia, ui |
| [StationpediaSearch](./GameSystems/StationpediaSearch.md) | The Stationpedia search pipeline: from the search field's `onValueChanged` callback through `DoSearch`'s async UniTask into `StationpediaPage.IsRegexMatch`. This page documents the trigger chain, the async state-machine split (and why a Postfix on `DoSearch` cannot filter results), and the `IsRegexMatch` single-caller fact that makes it the correct filter target. | stationpedia, ui |
| [StructurePlacementValidation](./GameSystems/StructurePlacementValidation.md) | How the build-cursor decides whether a structure may be placed. `InventoryManager`'s placement-preview loop calls `ConstructionCursor.CanConstruct()` every frame (and `IGridMergeable.CanReplace` for cables/pipes), gates the left-click on the result, and writes a red `ErrorMessage` into the cursor tooltip on failure. `CanConstructInfo.InvalidPlacement(string)` / `.ValidPlacement`. `Cable.CanConstruct` / `Device.CanConstruct` already override the base, so they are the natural Harmony-postfix hook for build-time rejection with a player-visible message. The gate is client-side preview only: `Constructor.SpawnConstruct` on the server does NOT re-run `CanConstruct`. | prefab |
| [StunStateMachine](./GameSystems/StunStateMachine.md) | Consciousness in Stationeers is not a standalone stat. It is driven entirely by the stun damage channel on the Brain organ's `DamageState`. This page covers the transitions, the proxy chain from `Entity.DamageState.Stun` down to `OrganBrain.DamageState._stunDamage`, the sleeper mechanism, the full inventory of stun-damage sources, the natural-decay rule, the sleep-metabolism modifiers, and the public-member visibility reference used to drive stun programmatically. | entity, damage |
| [TerrainOctree](./GameSystems/TerrainOctree.md) | The dual-octree system underlying Stationeers terrain: a read-only procedurally-generated `ReadOnlyOctree` baseline paired with a mutable `VoxelOctree` delta. The game writes only the delta to `terrain.dat`; unmodified regions fall through to the readonly tree at read time. Understanding this fallthrough is the key to implementing selective-reset tooling without needing the base terrain values on hand. | terrain, save-format, save-edit |
| [ThirdPartyModIdentities](./GameSystems/ThirdPartyModIdentities.md) | Reference table for third-party mod identity constants (Plugin GUID, Harmony ID, Workshop ID, Version) that our mods consult when using `[HarmonyAfter("...")]`, `Chainloader.PluginInfos.ContainsKey(...)`, or simple "is that other mod loaded" checks. Also a survey of the existing Stationeers damage-adjacent mod ecosystem. | launchpad |
| [UnregisteredSaveDataBehavior](./GameSystems/UnregisteredSaveDataBehavior.md) | When a save file contains a `ThingSaveData`-derived type whose class is NOT in `XmlSaveLoad.ExtraTypes` (the "mod removed but its save data still in file" case), `XmlSerializer` throws `InvalidOperationException` on the unknown `xsi:type`. `XmlSerialization.Deserialize` catches and returns `null`; `XmlSaveLoad.LoadWorld` throws `NullReferenceException("Failed to load the world.xml: ...")`. The entire save fails to load; there is no per-Thing fallback, substitution, or silent skip. | save-load, launchpad |
| [WorldStateAPIs](./GameSystems/WorldStateAPIs.md) | Quick-reference index of public world-state APIs: atmospheres (`AtmosphericsManager.AllAtmospheres`), pipe and cable networks, logic value read/write on `Device`, reusable vanilla LogicType values for generic fields (On, Power, Mode, etc.), and the game-state guards that mods use to gate server-only work. | logic, network, terrain |

## Patterns

| Page | Summary | Tags |
|---|---|---|
| [AboutXmlWorkshopHandleParse](./Patterns/AboutXmlWorkshopHandleParse.md) | StationeersLaunchPad's generated `XmlSerializer` parses `<WorkshopHandle>` as `UInt64`; an empty element throws `XmlConvert.ToUInt64("")` at deserialize time and the mod is renamed `[Invalid About.xml] <ModID>` in Player.log. BepInEx still loads the plugin (separate code path), so `LogOutput.log` looks fine. Diagnostic tell: plugin logs look healthy but LaunchPad-side name is wrong. | launchpad, dead-end |
| [AccessToolsRecipes](./Patterns/AccessToolsRecipes.md) | Two mechanisms for reading/writing a private field of a game class from a Harmony patch: cached `FieldInfo` via `AccessTools.Field(...)`, or the Harmony parameter-naming convention (four underscores + field name). Pick by call frequency and patch context. | harmony |
| [AsyncEnumerator472](./Patterns/AsyncEnumerator472.md) | Stationeers targets .NET Framework 4.7.2 (per the Unity 2021.2 toolchain). `await foreach` and `IAsyncEnumerable.ToBlockingEnumerable()` are not available. Draining an `IAsyncEnumerable<T>` requires manual use of `GetAsyncEnumerator()` + `MoveNextAsync()`. | threading |
| [AsyncHarmonyTrap](./Patterns/AsyncHarmonyTrap.md) | Patching an `async` method with a Harmony Postfix does not intercept the method's work: the compiler's state-machine rewrite leaves only a kick-off stub under the original name, and a Postfix on that stub fires when the awaitable is returned, before any of the asynchronous body has run. The fix is to patch a synchronous helper that the async body calls per unit of work. | harmony, threading |
| [BestEffortIntegration](./Patterns/BestEffortIntegration.md) | When a mod integrates with another mod whose APIs are unstable or whose presence is optional (Stationpedia, Stationeers Logic Extended, Stationpedia Ascended), use imperative reflection with `TypeByName` fallback + `Prepare()` gating + try/catch around each reflection call. Missing targets degrade gracefully to "feature disabled," never to a crash. | harmony |
| [BinaryStreamSafety](./Patterns/BinaryStreamSafety.md) | Custom postfixes that append fields to the per-tick update stream or the join snapshot MUST NOT wrap their read/write calls in `try`/`catch`. Catching a read/write exception leaves the underlying `RocketBinaryReader`/`RocketBinaryWriter` at the wrong position, silently corrupting every subsequent field for that object. | network |
| [ClientDisconnectedPrefix](./Patterns/ClientDisconnectedPrefix.md) | Patches that clean up per-client state when a player disconnects must run as a Harmony Prefix on `NetworkServer.ClientDisconnected`. A Postfix cannot read the `Client` record because vanilla's own `RemoveClient` call destroys it before returning. | network, harmony |
| [ConditionalWeakTableCache](./Patterns/ConditionalWeakTableCache.md) | `ConditionalWeakTable<TKey, TValue>` stores a per-instance value keyed weakly on `TKey`. When the key is GC'd, the entry is removed automatically; no manual cleanup code. Ideal for "one piece of state per Thing instance" mod caches whose lifetime should match the underlying instance. | unity |
| [ConflictDetection](./Patterns/ConflictDetection.md) | BepInEx's `BepInIncompatibility` attribute covers conflicts detectable at plugin-load time, but StationeersLaunchPad loads mods progressively, so a conflicting assembly may not exist yet when the current plugin's `Awake()` runs. The documented pattern is a deferred scan of `AppDomain.CurrentDomain.GetAssemblies()` after StationeersLaunchPad finishes loading. | launchpad, harmony |
| [CustomLogicValueInjection](./Patterns/CustomLogicValueInjection.md) | Pattern for adding a new readable logic value to a vanilla device (for example, exposing a private field as an IC10-readable LogicType). Uses a Prefix on `CanLogicRead` (sets `__result = true`, returns false to skip original) and a Prefix on `GetLogicValue` that reads the private field via the Harmony parameter-naming convention. | logic, harmony |
| [FileSystemWatcherMainThread](./Patterns/FileSystemWatcherMainThread.md) | `System.IO.FileSystemWatcher` events fire on a .NET thread-pool thread. Any Unity API call from the callback crashes the native player. File-share races let the event fire while the writer still holds the file open. Both bite anything using `FileSystemWatcher` inside a Unity plugin. | threading, unity |
| [Float16Quantization](./Patterns/Float16Quantization.md) | Stationeers network serialization quantizes floats to half-precision. Values that are not exactly representable in `float16` round to the nearest representable value, which often differs from the user's input in visible ways. `0.2` becomes `0.2002...`, which prints as `0.202` in the UI. | network |
| [HarmonyBaseCallDetourMultiFire](./Patterns/HarmonyBaseCallDetourMultiFire.md) | Harmony detours the compiled method body, and a C# `base.Method()` call is a non-virtual call into that body, so patches installed on multiple levels of one override chain each fire once per top-level call. No exception is thrown; the symptom is silent duplication (a postfix's effect lands once per patched level, observed on the `GetPassiveTooltip` chain). Mitigation: outermost-only depth guard (prefix increments a counter, postfix acts at depth 1, finalizer decrements on all exits including throws). | harmony |
| [HarmonyFieldOrProperty](./Patterns/HarmonyFieldOrProperty.md) | When reflecting against a game member whose declaration (field vs property) is unclear or might change between game versions, use `Traverse.Create(target).Field("Name").GetValue<T>()` with a property fallback. The decompile may read a field one version and a property the next; accepting either keeps the patch resilient. | harmony |
| [HarmonyInheritedMethods](./Patterns/HarmonyInheritedMethods.md) | The most-cited Harmony pitfall in the repo. `[HarmonyPatch(typeof(Subclass), "InheritedMethod")]` fails at `PatchAll` time when the named method is inherited without override. Six independent findings confirm the rule across three mods; a counter-example clarifies when the pitfall does not apply. | harmony |
| [HarmonyPatchOrdering](./Patterns/HarmonyPatchOrdering.md) | HarmonyX 2.9.0 (BepInEx 5.4.23.5) patch pipeline semantics, read from the 0Harmony decompile: patches sort by priority descending (`First` = 800, `Normal` = 400, `Last` = 0) with ties broken by insertion order; every prefix runs and a bool prefix only ANDs into `__runOriginal` (one skip branch after the whole prefix block); postfixes always run even when the original is skipped; `__state` is one shared local per declaring patch class per patched method. A `Priority.First` prefix + `Priority.Last` postfix pair from one class brackets the entire remaining pipeline. | harmony |
| [HarmonyPatchTypes](./Patterns/HarmonyPatchTypes.md) | Quick reference for the four Harmony patch kinds, their intended use, and the implicit parameters Harmony provides. Split out from the RepairPrototype plugin template. | harmony |
| [HarmonyPrefixReturnBool](./Patterns/HarmonyPrefixReturnBool.md) | A Harmony `[HarmonyPrefix]` method returning `void` cannot block the original method. If the prefix advances mod-owned state while the vanilla method also runs, the two states desync. The fix: declare the prefix as `bool`-returning, and `return false` when the mod wants vanilla skipped. | harmony |
| [ILRepackPerModCopy](./Patterns/ILRepackPerModCopy.md) | When a shared library is ILRepack'd into each consuming mod's assembly, every mod loads its OWN copy with OWN static state and OWN Harmony patches. Harmony handles multiple postfixes on the same target method by running them in sequence; each mod sees only its own registrations. This constrains how a shared library can coordinate across consumers, but produces predictable, additive behavior with no mod-ordering surprises. | harmony, packaging |
| [InGameConsoleOutput](./Patterns/InGameConsoleOutput.md) | Three log surfaces in a modded Stationeers (`BepInEx\LogOutput.log`, Unity `Player.log`, the in-game `~` console) carry different content. The in-game console (`ConsoleWindow`) shows ONLY `ConsoleWindow.Print*` / command output -- not `UnityEngine.Debug.Log`, not BepInEx `Logger.Log*`. To make a message a player sees while playing, call `ConsoleWindow.PrintAction(string, bool aged=false)` / `PrintError(string, bool suppressStacktrace=false)` (resolve `ConsoleWindow` by reflection; namespace ambiguous). Includes the recommended three-channel `PlayerLog` helper. | ui |
| [LogicableInitializeAppend](./Patterns/LogicableInitializeAppend.md) | `Logicable` exposes the per-device LogicType list as a set of parallel arrays the configuration-tablet UI uses to build its dropdowns: `LogicType[]`, `string[]` (display names), and a redirect index for binary-search lookup. Appending a custom LogicType requires extending all three arrays consistently. | logic, harmony |
| [LogicTypeRegistration](./Patterns/LogicTypeRegistration.md) | Recipe for registering a custom `LogicType` value at runtime. Custom values must land in five independent static collections (`Logicable.LogicTypes`, `EnumCollections.LogicTypes`, `ScreenDropdownBase.LogicTypes`, `ProgrammableChip.AllConstants`, `ProgrammableChip.InternalEnums` ScriptEnum / BasicEnum entries) plus three Harmony name-lookup fallback postfixes (`Enum.GetName`, `EnumCollection<LogicType, ushort>.GetName` / `GetNameFromValue`). Split across three files in this repo: `LogicableInitializePatch` (sites 1-3, Harmony postfix on `Logicable.Initialize`), `Ic10ConstantsPatcher` (sites 4-5, called at plugin Awake), `EnumNamePatches` (the three fallbacks). The ScreenDropdownBase appended tail survives `Awake` because the population loop is bounded by the vanilla enum's fixed member count. | logic, harmony, ic10 |
| [MainThreadDispatcher](./Patterns/MainThreadDispatcher.md) | `ConcurrentQueue<Action>` drained in `Update()` on a `DontDestroyOnLoad` MonoBehaviour. Required whenever a callback can fire off the Unity main thread and the callback needs to touch a Unity API. Appears verbatim as a helper class in multiple mods in this repo. | threading, unity |
| [PeriodicProcessing](./Patterns/PeriodicProcessing.md) | When a mod needs to do work every N seconds (auto-repair ticks, background scans, status polling), three mechanisms are available. Pick by coupling and frequency: patch-based for per-thing loops, plugin Update() for low-frequency global work, coroutine for simple fixed-interval polls. | |
| [PooledSpanEnumeration](./Patterns/PooledSpanEnumeration.md) | Stationeers exposes world collections through `PooledSpan<T>` rentals backed by `ArrayPool`. The rented array can be larger than the actual count, leaving trailing nulls the enumeration must filter. Skipping the null check crashes the first time the pool hands out an oversized buffer. | |
| [PrefabCloning](./Patterns/PrefabCloning.md) | Full recipe for cloning a vanilla prefab, registering it with the game's prefab system, wiring multi-constructor kits, and preserving multiplayer/save-load compatibility. Lifted verbatim from RepairPrototype's reverse-engineering of the Mirrored Devices mod. | prefab, harmony, save-load |
| [ProtectedMethodPublicAPIBypass](./Patterns/ProtectedMethodPublicAPIBypass.md) | When a mod's patch needs to call a protected method on a game class (for example, `Structure.GetRoom()`), composing the same result out of already-public APIs is usually cleaner than reflection. Faster, safer across game-version updates, and self-documenting. | |
| [SaveDataIsinstInheritance](./Patterns/SaveDataIsinstInheritance.md) | When extending a Thing's save-data payload by registering a custom type, inherit the vanilla save-data class. Vanilla's `DeserializeSave` often does an `isinst` check against its own type; a sibling class (no inheritance) fails the check and vanilla skips its own fields entirely. Inherit, and vanilla's check passes while the custom subclass adds fields the mod cares about. | save-load |
| [SaveLoadOrdering](./Patterns/SaveLoadOrdering.md) | When a parent Thing's saved state references child Things that have not yet been placed into its slots, `DeserializeSave` cannot restore that state immediately. The child-placement pass fires `OnChildEnterInventory` per child, which can clobber values set earlier. The pattern: stash the saved value in a dictionary during `DeserializeSave`, then apply it from `OnFinishedLoad`, which runs after every child has been placed. | save-load, harmony |
| [ServerAuthoritativeSimulation](./Patterns/ServerAuthoritativeSimulation.md) | Stationeers runs simulation on the server. Clients receive synced state and display it; no simulation runs client-side. Simulation-tweaking patches only meaningfully execute on the server; clients run the patches but the underlying vanilla work does not happen there. Server-authoritative mods that don't add new prefabs or assets can be server-only installs. | network |
| [SlotInsertionBlock](./Patterns/SlotInsertionBlock.md) | How to block a specific `(Thing, Slot)` combination via Harmony. Server-authoritative block goes on `Thing.CanEnter(Slot)` postfix (returns `CanEnterResult.Fail(...)`); UI affordance block goes on `Slot.AllowMove(...)` prefix. Legacy saves with already-loaded contents require a separate eject pass on `Thing.OnFinishedLoad`. | slots, harmony |
| [StationeersLaunchPadSettingsGrouping](./Patterns/StationeersLaunchPadSettingsGrouping.md) | StationeersLaunchPad groups mod settings in its in-game GUI by the BepInEx `ConfigDefinition.Section` string (first arg to `Config.Bind`). Categories are sorted alphabetically and cannot be nested; entries inside a group sort by an optional `Order` tag then by key. No custom attribute or StationeersLaunchPad-specific API exists for grouping; the section string is the only knob. | launchpad, ui |
| [SinglePlayerNetworkRole](./Patterns/SinglePlayerNetworkRole.md) | Single-player runs with `NetworkRole.None`: all three of `NetworkManager.IsActive`, `NetworkManager.IsServer`, and `NetworkManager.IsClient` are false. Guards written as "skip remote clients" using `!IsServer` accidentally also skip single-player, breaking mod features for solo players. | network |
| [SlotTypeIconLookup](./Patterns/SlotTypeIconLookup.md) | Vanilla's `Slot.PopulateSlotTypeSprites` loads sprite assets from `Resources/UI/SlotTypes` whose filenames follow a `sloticon-<lowercase-enum-name>` convention. Missing assets (e.g. `sloticon-sensorprocessingunit`) produce empty slot hint icons. A Harmony Postfix on `PopulateSlotTypeSprites` arrives too late because the method runs at `InventoryManager.ManagerAwake`, before any BepInEx plugin has its patches installed. The fix populates `Slot._slotTypeLookup` directly during plugin load. | ui, slots |
| [StationeersModdingGotchas](./Patterns/StationeersModdingGotchas.md) | Small list of recurring pitfalls that don't have their own page. Add new entries as they surface; promote to a dedicated page once an entry has three or more distinct expressions. | |
| [StationeersNamespaces](./Patterns/StationeersNamespaces.md) | Reference table of game types whose namespace is easy to guess wrong. All verified against game version 0.2.6228.27061. Compile errors of the form `using <namespace>;` followed by "type not found" usually trace back to one of these. | |
| [SubcomponentGrafting](./Patterns/SubcomponentGrafting.md) | When a mod needs a specific child component from a vanilla prefab (an on/off button, a collider, a sub-mesh) but does NOT want the entire prefab cloned, graft just the child GameObject into the mod's prefab with `GameObject.Instantiate(srcChild, targetTransform)`. Wire up any cross-references by hand after the instantiate. | prefab |
| [TextMeshProLineMetrics](./Patterns/TextMeshProLineMetrics.md) | How the game's TextMeshPro build computes the vertical line advance and how a rich-text `<line-height>` tag drives it, verified from the shipped `Unity.TextMeshPro.dll`. Natural advance = `faceInfo.lineHeight * (fontSize / pointSize * scale)` plus the `lineSpacing` / `paragraphSpacing` term; a driven break advances by exactly the tag value plus that term; the em scale uses the COMPONENT font size, so `<size>` tags do not perturb it. Includes the formula for matching one component's line spacing inside another. | ui, unity |
| [UniTaskDelaySignatureDrift](./Patterns/UniTaskDelaySignatureDrift.md) | The 2026-03-26 game update (0.2.6228.27061) added a trailing optional `bool cancelImmediately = false` to every `UniTask.Delay` / `DelayFrame` overload. Adding an optional parameter is source-compatible but binary-incompatible: mods built before the update have the old four-param `Delay(int, bool, PlayerLoopTiming, CancellationToken)` frozen in their IL and throw `MissingMethodException` at runtime (surfacing at `AsyncUniTaskVoidMethodBuilder.Start` for async bodies). Root cause of the KeypadMod "Method not found: UniTask.Delay" crash. Patchable additively without cloning: HarmonyX reads bodies via Cecil, so the broken state-machine call can be IL-redirected to a current-signature shim, or skipped via a prefix-reimplement on the clean kickoff method. | threading, harmony, launchpad |
| [UnityFakeNull](./Patterns/UnityFakeNull.md) | `obj == null` returns `true` on a destroyed `UnityEngine.Object` even when the managed C# wrapper is still alive. Reflection walks that dereference fields on such wrappers crash with `MissingReferenceException`. Required reading for any code that walks arbitrary object graphs (serializers, diagnostic dumpers). | unity |
| [UnityMaterialPerInstance](./Patterns/UnityMaterialPerInstance.md) | Unity's `Renderer.material` getter clones the shared material on first access and caches the clone on the renderer. Reading the getter ONCE yields a per-instance material the mod can mutate without affecting other renderers. The mod MUST `Destroy(instanceMaterial)` in `OnDestroy` or the clone leaks. | unity |
| [WorldSpacePreviewRendering](./Patterns/WorldSpacePreviewRendering.md) | What rendering primitives a mod can reuse to draw a world-space preview volume (placement ghost, selection box, blast sphere, "what's affected" highlight). The game has no general "draw a translucent volume" or "outline this Thing" API. Catalogs: the build ghost (pre-instantiated clones with materials swapped for `CursorManager.CursorShader`, toggled via `SetActive`), the hover highlight (one stretched box scaled to `Thing.GetSelection()` bounds, not a per-mesh outline), GL.LINES wireframes via `Shader.Find("Hidden/Internal-Colored")` in `OnRenderObject()` (the `Wireframe` recipe), and BlueprintMod's precedent (`CreateBoxMarkers` cube primitives + `Unlit/Color`, `MaterialPropertyBlock` `_Color` tint on ghost clones, `BlueprintWireBatcher`). Plus per-frame hooks and an approach ranking. | unity, ui, transforms |

## Protocols

| Page | Summary | Tags |
|---|---|---|
| [AtmosphereSaveData](./Protocols/AtmosphereSaveData.md) | Per-pipe-network gas mix stored inside world.xml. Each entry is keyed by `<NetworkReferenceId>`; every gas species is an individual float XML element. Mods editing atmospheres offline can zero out unwanted species; relative to a dominant gas, small changes have negligible thermal impact because energy scales with total moles. | save-format, save-edit |
| [ConstructionCreationMessage](./Protocols/ConstructionCreationMessage.md) | Vanilla `ProcessedMessage<ConstructionCreationMessage>` at `Assets.Scripts.Networking.ConstructionCreationMessage`. Client-to-server placement request when the client is not the simulation owner. Wire format: `(int PrefabHash, Vector3 WorldPosition, Quaternion WorldRotation, Quaternion LocalRotation, ulong OwnerClientId, Grid3 LocalGrid, int CustomColorIndex)`. Both rotations are full 16-byte `WriteQuaternion`; lossless. Server `Process(hostId)` calls `OnServer.Create<Structure> + SetStructureData` with the received Quaternions verbatim. | network, prefab |
| [EquipmentPlusNetworking](./Protocols/EquipmentPlusNetworking.md) | EquipmentPlus extends SensorLenses with an active-sensor reference that must sync across the multiplayer boundary. Rather than shipping a full custom wire-format, the mod piggybacks on vanilla's `NetworkUpdateFlags` delta stream by claiming an unused flag bit (0x4000) and riding the existing `BuildUpdate` / `ProcessUpdate` / `SerializeOnJoin` / `DeserializeOnJoin` plumbing. One small custom LaunchPadBooster message (`SetActiveSensorMessage`) triggers the host-side write. | network, launchpad |
| [GameMessageFactory](./Protocols/GameMessageFactory.md) | Reference page for vanilla Stationeers network messages that mods ride directly rather than defining custom LaunchPadBooster messages. Covers the `SetLogicFromClient` routing rules, the `Interactable.State` -> `Mode` sync channel, and the reliability caveat around `hostId` on server-side message dispatch. | network |
| [LaunchPadBoosterNetworking](./Protocols/LaunchPadBoosterNetworking.md) | LaunchPadBooster's networking layer (`LaunchPadBooster.Networking`) exposes the mod-to-mod wire-format primitives that every StationeersPlus mod uses to send custom messages between host and clients. It wraps vanilla Stationeers networking with automatic compression, multi-packet splitting, and a mod-version handshake. | network, launchpad |
| [PowerTransmitterPlusNetworking](./Protocols/PowerTransmitterPlusNetworking.md) | PowerTransmitterPlus ships two server-authoritative config sync protocols on top of LaunchPadBooster. The host pushes its authoritative config values (`DistanceCostFactor k`, beam visual settings) to all clients on every join and every `SettingChanged` event; clients use the synced values for display-only math while the simulation runs server-side. | network, launchpad |
| [SPADeviceDatabase](./Protocols/SPADeviceDatabase.md) | Wire-format reference for Stationpedia Ascended's shipped JSON description database and its runtime `DeviceDatabase` dictionary. Mods that augment SPA tooltips or add new device pages need the exact schema to produce valid entries and to understand which lookup paths SPA consults at runtime. | save-format |
| [SaveFileStructure](./Protocols/SaveFileStructure.md) | Top-level file layout of a Stationeers save ZIP. The archive is a standard ZIP; each entry is plaintext XML, binary voxel data, or a PNG. Sizes in the example column are from an observed mid-sized save and are indicative, not exact. | save-format, save-edit |
| [SprayPaintPlusNetworking](./Protocols/SprayPaintPlusNetworking.md) | SprayPaintPlus ships two custom LaunchPadBooster `INetworkMessage` types (client-to-server) to carry per-can color state and per-player modifier state, plus two server-authoritative sync flows that layer on top of vanilla `Consumable` update ticks and `AttackWithMessage` paths. | network, launchpad |
| [TerrainChunkChecksums](./Protocols/TerrainChunkChecksums.md) | Stationeers stores one rolling XOR-multiply checksum per base-terrain octree in world.xml. On load, each stored value is compared to the corresponding `ReadonlyVoxelOctree.CheckSum`. Any mismatch triggers an offer to regenerate ALL terrain (all-or-nothing, no per-chunk reset). | save-format, save-edit |
| [TerrainDat](./Protocols/TerrainDat.md) | Byte-level layout of a Stationeers `terrain.dat` base-terrain file. One file per base-terrain octree (world-size dependent, see reference table below). The file stores an `INT32 LE MaxDepth` header followed by a DFS pre-order walk of the octree and then per-vein delta data. The file is NOT internally compressed; the outer save ZIP provides compression. | save-format, save-edit |
| [ThingColorMessage](./Protocols/ThingColorMessage.md) | Vanilla `ProcessedMessage<ThingColorMessage>` carrying `(long ThingId, int ColorIndex)` from server to clients. Sent from `OnServer.SetCustomColor`; received via `Thing.Find<Thing>(ThingId).SetCustomColor(ColorIndex)`. No slack in the 12-byte payload; `Thing.SetCustomColor` silently clamps invalid indices via `GameManager.IsValidColor`, which rules out piggybacking extra bits inside `ColorIndex`. | network, prefab |
| [WorldXml](./Protocols/WorldXml.md) | `world.xml` is the primary save payload: every Thing, atmosphere, network, and player lives here. This page documents individual subsections as they are investigated. | save-format, save-edit |

## Workflows

| Page | Summary | Tags |
|---|---|---|
| [BCSIImplementationRoadmap](./Workflows/BCSIImplementationRoadmap.md) | Seven-step build plan for BCSI v1 (Building Maintenance Inspector damage tax + passive auto-repair). Reach for this recipe when starting a new auto-repair-style device mod that clones a vanilla prefab, adds logic channels, runs a scan loop, posts chat reports, and ships with BepInEx config. | damage, power, logic |
| [CameraEffectsRuntime](./Workflows/CameraEffectsRuntime.md) | Add or remove a `CameraFilterPack` shader effect on the main camera at runtime, and override the stun-driven effects that `Entity.OnCameraUpdate` resets every frame. Reach for this recipe when a mod wants to flash a distortion or color shift during a scripted event. | ui, harmony, entity |
| [HealAllDamagedThings](./Workflows/HealAllDamagedThings.md) | Iterate every `Thing` currently alive in the scene and call `DamageState.HealAll()` on each damaged, repairable instance. Reach for this recipe when a mod needs a bulk repair pass, a time-skip cover operation, or a "service technician" event that resets wear / decay / burn / brute damage across the world. | damage, save-edit |
| [InspectorPlusUsage](./Workflows/InspectorPlusUsage.md) | How to capture live runtime state from a running Stationeers session using the InspectorPlus plugin. Reach for this recipe whenever you need to verify a hypothesis about field or property values at a specific moment instead of adding one-off logging. | unity, threading, harmony |
| [KnockPlayerUnconscious](./Workflows/KnockPlayerUnconscious.md) | Transition a player to `EntityState.Unconscious` (and back) without using the sleeper. Reach for this recipe when a mod needs to induce unconsciousness directly from code, to cover the player during a time-skip, or to drive a custom sleep-like mechanic. | entity, damage |
| [ModProjectSetup](./Workflows/ModProjectSetup.md) | Framework stack, assembly references, plugin template, Config.Bind usage, and Harmony patch types used by every Stationeers mod in this monorepo. Reach for this page when scaffolding a new mod or when a contributor asks "what does the baseline look like." | harmony, launchpad, packaging |
| [ResetTerrainLive](./Workflows/ResetTerrainLive.md) | Runtime approach for resetting modified voxels back to base terrain during a live multiplayer session. Reach for this recipe when a mod wants to wrap a selective terrain reset inside a lore-based event (reclamation event, visual effects, chat narration) rather than shutting the server down and editing the save offline. | terrain, worldgen, network |
| [ResetTerrainOffline](./Workflows/ResetTerrainOffline.md) | Run the `terrain_reset.py` script against a closed `.save` archive to collapse every modified voxel outside a computed "keep" bounding box back to base terrain. Reach for this recipe when a save has accumulated so many minor terrain deltas that performance or file size has degraded, but the player wants to preserve the built-up area around their base. | terrain, save-edit, save-format, python |
| [SPADumpKeys](./Workflows/SPADumpKeys.md) | Use Stationpedia Ascended's built-in `/spda_dumpkeys` console command to list every registered Stationpedia page during implementation or debugging of a Stationpedia-adjacent mod. Reach for this recipe when you need to confirm a custom page is registered, verify hidden keys, or look for key collisions between mods. | stationpedia, spa |
| [StationeersLaunchPadModLoading](./Workflows/StationeersLaunchPadModLoading.md) | How StationeersLaunchPad turns an enabled mod into loaded assemblies, prefabs, and a fired `OnLoaded(ContentHandler)` entrypoint, and where mod identity is retained. Covers `ModInfo` (config phase), `LoadedMod` (runtime phase, holds `Prefabs` + `ContentHandler`), `ModLoader.LoadedMods` / `AssemblyToMod`, the three load phases (Assemblies / Assets / Entrypoints), the absence of any current-mod static (identity via loop variable, async state-machine `this`, or `ModLoader.TryGetExecutingMod` stack walk), and the LaunchPadBooster `Mod.AllMods` + `PrefabPatch` (`Prefab.LoadAll` prefix) prefab route. Since StationeersLaunchPad 0.4.0: plugins it loads NEVER appear in `Chainloader.PluginInfos`; the live `BaseUnityPlugin` instance lives in `ModLoader.LoadedMods[].Entrypoints` (`BepInExEntrypoint.Instance` via raw `AddComponent`). Companion to PrefabSourceAttribution. | launchpad, prefab |
| [TTSDeadEnd](./Workflows/TTSDeadEnd.md) | Preserved investigation of every path for adding text-to-speech to a Stationeers mod. Each viable path has a disqualifying problem; combined, they make TTS too fragile to ship as a dependable feature. Reach for this page when a future contributor is about to re-investigate "should we add TTS to the chat bot," so they do not repeat the same three-day walk. | dead-end, llm |
| [TimeSkipWorldManipulation](./Workflows/TimeSkipWorldManipulation.md) | Catalog of the systems a mod can mutate to fake elapsed time while a cover window (lander capsule, fake loading screen, ragdoll pause) hides the transition. Reach for this recipe when designing a "skip ahead" event: the systems table names the APIs, the APIs reference lists the non-obvious camera / helmet / input / loading / ragdoll / timescale handles you pair with them. | timeskip, worldgen, entity |
| [TriggerLanderCapsule](./Workflows/TriggerLanderCapsule.md) | Spawn a `LanderCapsule` under a living player, move the player into it, and trigger the descent animation without going through the death / respawn flow. Reach for this recipe when a mod wants a cinematic "the player just arrived" effect, or a time-skip cover window that locks the player's camera inside a capsule while world state mutates. | worldgen, entity, timeskip |

## Unsorted

No entries at migration. Populated by the misfit-log protocol documented in `CLAUDE.md`.
